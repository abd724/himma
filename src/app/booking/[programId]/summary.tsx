import { BookingSummaryScreen } from '@/features/booking/booking-summary-screen';

/** Booking summary — review, edit actions, checkout contract (docs/21 §8). */
export default function BookingSummaryRoute() {
  return <BookingSummaryScreen />;
}
