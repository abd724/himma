import { BookingSelectionScreen } from '@/features/booking/booking-selection-screen';

/** Booking step 1 — session/plan selection (docs/21 §3, §5). */
export default function BookingSelectionRoute() {
  return <BookingSelectionScreen />;
}
