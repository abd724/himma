import { CheckoutScreen } from '@/features/booking/checkout-screen';

/** Checkout — order review, price, policy display, inert CTA (docs/22 §4). */
export default function CheckoutRoute() {
  return <CheckoutScreen />;
}
