import { ProgramDetailsScreen } from '@/features/details/program-details-screen';

/** HMA-015 — root-level detail push; dock hidden structurally (docs/20 §2.1). */
export default function ProgramDetailsRoute() {
  return <ProgramDetailsScreen />;
}
