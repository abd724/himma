import { ProviderStorefrontScreen } from '@/features/details/provider-storefront-screen';

/** HMA-014 — root-level detail push; dock hidden structurally (docs/20 §2.1). */
export default function ProviderStorefrontRoute() {
  return <ProviderStorefrontScreen />;
}
