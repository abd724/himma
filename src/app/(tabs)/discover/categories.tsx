import { AllCategoriesScreen } from '@/features/catalogue/all-categories-screen';

/** HMA-011 — All Categories (docs/15 §4.1). */
export default function AllCategories() {
  return <AllCategoriesScreen />;
}
