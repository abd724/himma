import { ActivityTypeScreen } from '@/features/catalogue/activity-type-screen';

/** HMA-013 — Activity-type page (docs/15 §4.1). */
export default function ActivityType() {
  return <ActivityTypeScreen />;
}
