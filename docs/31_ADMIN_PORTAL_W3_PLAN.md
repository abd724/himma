# 31 — Internal Admin Portal & Provider Verification (W3) — Workstream Plan

**Status: W3-0 OWNER-APPROVED and CLOSED at `98c57ab`. Owner decisions D-W3-1 (separate PRIVATE evidence document storage — PostgreSQL-authoritative metadata, private object storage, no public URLs, server-controlled short-lived access; detail in W3-4) and D-W3-2 (three-layer rejection feedback: internal notes / machine reason code / provider-safe message; internal notes never auto-exposed; detail in W3-3/5/8) are APPROVED and binding. The W3-1 final owner decision "Admin baseline access" is APPROVED and binding: ordinary Admin Portal access = authenticated identity + live LoginSession + MFA assurance + ≥1 active PostgreSQL admin role, WITHOUT a continuously recent MFA factor; recent-factor step-up is a separate action-level mechanism (`adminStepUp`) whose high-risk action set stays deferred under D-W3-5. D-W3-3…6 remain deferred per §10. W3-1 is OWNER-APPROVED and CLOSED (`b5654b3` + the baseline/step-up correction `48537e8`). W3-2 CLOSED at `06b9b14`; W3-3 CLOSED at `c46c5b9`; W3-4 CLOSED at `a0ee435` (`d347cd4`+correction); W3-5 CLOSED at `4651fa1`; **W3-6 OWNER-APPROVED and CLOSED at `ba168d8` (2026-08-20)** — final verification at closure: backend Jest 831/831 (66 suites) · admin Jest 93/93 (11 suites) · admin contract 9/9 · portal contract 69/69 · admin Playwright 22 passed / 23 by-design skips · builds green · db:verify 11/0 · no migration. The production verification review path remains INOPERABLE by construction; D-S3-3's flag stays as defense-in-depth. **W3-7 OWNER-APPROVED and CLOSED at `4759521` (2026-08-20)** — final verification at closure: backend Jest 832/832 (66 suites) · admin Jest 118/118 (13 suites) · admin contract 13/13 · portal contract 69/69 · admin Playwright 25 passed / 29 by-design skips · db:verify 11/0 · no migration · backend/admin typecheck + lint clean · admin production build green. **W3-8 OWNER-APPROVED and CLOSED at `07135a9` (2026-08-20)** — final verification at closure: backend Jest 836/836 (66 suites) · backend tsc/ESLint clean · db:verify 12/0 · migration `0012_listing_moderation_feedback.sql` up/down/up proven · codegen drift-free · Provider Portal Jest 762/762 (58 suites) · portal contract 70/70 · portal Playwright 208 passed / 20 skipped · portal build/tsc/lint green · admin Jest 118/118 (13 suites) · admin contract 13/13 · admin Playwright 25 passed / 29 by-design skips · admin build/tsc/lint green. Preserved at closure: the provider evidence-upload UX remains DEFERRED for explicit owner assignment; revision-rejection provider feedback remains OUTSIDE W3-8; D-W3-3/D-W3-4/D-W3-5/D-W3-6 remain unresolved; D-S3-3 and the content-safety invariant remain unchanged; production verification/review remains inoperable until its real policy/content-safety dependencies exist. W3-9 (closeout/hardening) is IMPLEMENTED — see the W3-9 record at the end of this document. W3 is complete pending the owner's closeout review.**

Authority: docs/23 (§6.1, §9, §15–16), docs/24, docs/25, docs/26 (identity/admin roles), docs/27 (Slice-3 provider organizations incl. D-S3-3), docs/28 (Slice-4 moderation/taxonomy), docs/29 (Provider Portal, W2 closures), and the repository itself. W2-12 — Real Provider Portal API Integration — is CLOSED at `2ba9a2e`. This plan reconciles what the backend already provides, what W3 must add, and the bounded order to build it in. It contains **no application code** and creates **no routes or schema**.

---

## 1. Objective

The Internal Admin Portal is the Himma **staff-facing** web surface (docs/23 §9), distinct from the customer app and the Provider Portal. W3's first production priority is **provider onboarding and verification**: a provider must not become operational (`live`) merely by filling in its profile — Himma staff review the business and its evidence, decide, and separately activate. That capability is the binding precondition the backend already encodes: **D-S3-3** makes `in_review → verified` and `verified → live` **fail closed in production** until an authoritative verification-evidence capability exists and reports ready (`organization-admin.ts`; forcing the flag refuses startup, and blocked attempts are audited). W3 exists to build that capability and the staff surface around it.

Three readiness levels stay separate (docs/23 §15–16): **engineering implementation complete** ≠ **provider-outreach/onboarding ready** ≠ **public marketplace launch ready**. W3 primarily advances the second.

## 2. Current-state matrix (repository-reconciled)

### 2.1 Already implemented (backend, code-complete and tested)

| Capability | Where | Notes |
|---|---|---|
| Admin identity & authorization | Slice 2 (`admin_role_assignment`, `resolveAdminRoles`, `authPolicy: 'admin'`) | Live session + MFA assurance + ≥1 ACTIVE PostgreSQL admin role; route handlers check the specific role; Cognito claims grant nothing. Dual-control role grant/approve/deny/revoke with D4 exclusivity and finance-approver rules (DB triggers final authority). |
| Admin role registry | `ADMIN_ROLES = operations · support · finance · access_admin · auditor` | Exactly five internal roles; no superadmin. `operations` owns org lifecycle, moderation, taxonomy. |
| Admin role HTTP surface | `/admin/role-assignments` (list/detail/revoke) · `/admin/role-requests` (+ approve/deny) | AD-17 backend exists end-to-end. |
| Organization lifecycle (admin) | `organization-admin.ts` + `/admin/organizations` routes | Create-with-founding-invitation; transitions `start-review` (submitted→in_review), `verify` (in_review→verified), `reject` (in_review→rejected), **`go-live` (verified→live — a SEPARATE admin action)**, `suspend`, `reinstate`, `offboard`. CAS `expectedVersion`; optional machine `reasonCode` (audit-only); S3-1 state trigger is final authority; every transition audited + outboxed. |
| D-S3-3 production gate | `verificationEvidenceCapabilityReady` | verify/go-live REFUSE in production until the evidence capability ships; refusals audited. |
| Program moderation | `moderation.ts` + `/admin/listings` routes | Queue (keyset), moderation view (full ProgramDetailView + open revision), `review/start`, `review/approve` (rests at `approved` — approval ≠ publication, D-S4-2), `review/request-changes` (+ audit-only reasonCode). Operations-only, audited + outboxed. |
| ProgramRevision moderation | same module | Revision queue, `review/start`, `approve` (applies the payload atomically), `reject`. The moderation view exposes a **`RevisionChangeSetView`** (the exact protected-field/option change set) — reviewers CAN see what they are approving; **no W3 backend gap here**. |
| Taxonomy administration | `taxonomy-admin.ts` + `/admin/taxonomy` routes | Full admin view + create/patch for areas, categories, activity types, collections (activate/deactivate, labels, sort, synonyms). Operations-only, audited. |
| Audit + outbox conventions | `appendAuditEvent` / `appendOutboxEvent` | Used by every sensitive admin mutation above; `admin-event-integrity` tests lock the pattern. |
| Provider-side onboarding submission | Slice 3 + W2-12B | `draft|rejected → submitted` from the Provider Portal; org view renders exact verification state. |

### 2.2 Partially implemented

| Capability | Present | Missing |
|---|---|---|
| Provider verification | The full org state machine, admin transitions, audit | The actual **VerificationCase/evidence review capability** the D-S3-3 gate demands: nothing records WHAT was reviewed or the evidence it rested on. |
| Rejection feedback | Machine `reasonCode` on reject/request-changes (audit-only) | Any **provider-safe explanation** (the Provider Portal's carried Class-B gap) — no storage, no contract, no read. |
| Admin work queues | Moderation + revision queues; org transitions | **No `GET /admin/organizations` list/detail read exists at all** (the only org admin routes are POST create + POST transitions) — the AD-02/AD-04 provider queue/directory/detail reads are missing. |
| Audit review | Events are written | No audit **read/explorer** route (AD-18). |

### 2.3 Missing entirely (W3 must add)

VerificationCase domain (case per review round, states, reviewer ownership, CAS) · evidence/document records + private binary storage · evidence review decisions with internal notes vs provider-safe output · ~~admin organization list/search/detail reads~~ (landed in W3-2) · provider-safe rejection contract + Provider Portal read · taxonomy request workflow (provider-side "request a new Activity Type") · admin audit explorer read · **the entire Admin Portal frontend** · the D-W3-5 high-risk action-set ruling (the `adminStepUp` policy itself exists since the W3-1 final correction; the future action set stays deferred).

### 2.4 Explicitly out of W3's first phase (deferred per docs/23 §9, future workstreams)

Customer lookup (AD-07), booking operations (AD-08), payments/reconciliation (AD-09), refunds (AD-10), credits (AD-11), payouts (AD-12), support cases (AD-13), content/media moderation beyond listings (AD-14), notifications (AD-15), platform configuration (AD-16). These depend on domains (bookings/payments) that do not exist yet.

## 3. Verification domain design (W3 core)

Canonical vocabulary stays docs/23 §6.1 / Slice-3: `draft → submitted → in_review → verified → live`, `in_review → rejected (→ submitted on resubmit)`. W3 adds the evidence capability UNDER that machine — it does not change it.

**VerificationCase** (one per review round): `organizationId`, state (`open → in_review → decided`, mirroring the org edge it governs), `openedAt`, assigned reviewer (nullable), decision (`verified` | `rejected`), decision actor/time, **internal reviewer notes** (staff-only, never serialized to providers), machine `reasonCode`, **provider-safe message** (see §6), `version` (CAS). A case opens when an organization reaches `submitted` (created lazily on first admin touch or eagerly via outbox — decided in W3-3 from repo conventions). History is append-per-round: a resubmission opens a NEW case; prior cases remain immutable audit history.

**VerificationEvidence** records: per-case rows carrying evidence kind (from a **policy-configurable checklist**, §5), uploader (provider or staff), storage reference, content metadata (filename, content type, size, hash), review status per item (`pending → accepted | rejected`), and reviewer per-item note. Domain infrastructure is fixed; **which documents Himma requires per provider type is policy/configuration**, not schema — a `verification_requirement` config (seeded, admin-editable later) keyed by org kind, so the legal checklist can change without migrations.

**Decision workflow** (§12 of the task): operations reviewer (PostgreSQL role, fresh per transaction) → case in `in_review` with the org in `in_review` → evidence reviewed item-by-item → decision `verify` or `reject` with CAS on both the case and the organization (one transaction: case decided + `transitionOrganization` edge + audit + outbox) → the D-S3-3 readiness input flips to REAL truth: `verify` requires the governing case to report ready (all mandatory checklist items accepted). Nothing in the frontend can bypass this — the service composes the existing state trigger.

**Go-live** stays exactly canon: `verified → live` remains a **separate deliberate operations action** (`POST /admin/organizations/:id/go-live`), already implemented and audited — activation is NOT automatic after verification. This is settled by D-S3-3 + §6.1 + the shipped route; it is not an open owner question.

## 4. Evidence storage (the media/binary dependency)

No binary storage exists anywhere (ProgramMedia is metadata-only). Two architectures were weighed:

**Option A — one shared binary-storage foundation now, consumed later by public media.** Pro: one infrastructure. Con: verification documents are PRIVATE, sensitive business/identity papers; marketplace media is public CDN content. A shared first implementation invites access-tier mistakes at the worst possible boundary, and couples W3 to public-media requirements (image processing, CDN) it does not have.

**Option B (recommended) — a deliberately separate private document store for verification evidence.** S3-compatible object storage, private bucket/prefix, server-mediated access only: uploads via short-lived pre-signed PUT (or server-proxied upload) recorded against the case; retrieval ONLY through authorized routes issuing short-lived signed GETs — provider principals may access solely their own organization's evidence via the provider surface; admin access requires the operations role; **no public URL ever exists**; keys are unguessable and never derived from org names. Object metadata + hash stored in PostgreSQL; deletion/retention policy recorded (owner policy input, not schema). The later public-media milestone MAY reuse the low-level client/config layer, but the access tier, bucket, and lifecycle stay separate by design. → Owner decision **D-W3-1** (§10) with this recommendation.

## 5. Provider-facing rejection/feedback architecture

Three strictly separated layers (task §13): (1) **internal reviewer notes** — staff-only, never leave the admin surface; (2) **machine `reasonCode`** — canonical vocabulary, audit + analytics + Portal branching; (3) **provider-safe message** — reviewer-authored (or template-derived) actionable text stored ON the decision (case or moderation action) explicitly marked provider-visible. The smallest proper contract: the existing provider org/listing reads gain a provider-safe `latestDecision { reasonCode, providerMessage, decidedAt }` projection (organization verification first; listing `changes_requested` second, closing the W2 carried gap). Internal notes live in different columns and are structurally absent from every provider serializer — test-locked, mirroring the existing capability-shaped projection pattern.

## 6. Admin authentication & authorization

Reuse the approved identity architecture wholesale (docs/26; W2-12A code): browser → Cognito (USER_PASSWORD_AUTH + SOFTWARE_TOKEN_MFA, public client) → `POST /auth/session` → bearer + §14.E cookie/CSRF continuity → per-request liveness. **Authority is PostgreSQL admin roles only** (`authPolicy: 'admin'` = the BASELINE: live session + MFA assurance + ≥1 active role, deliberately WITHOUT a factor-recency requirement per the W3-1 final owner decision "Admin baseline access"; handlers check the specific role; deny-by-default; Cognito groups grant nothing). Admin and provider authority are disjoint by construction — the same person may hold both, but each surface resolves its own. The admin portal is a **separate origin** in `PORTAL_ALLOWED_ORIGINS` terms (its own allowlist entry/config). The W2-12A live auth runtime, transport, continuity channel, and fixture/live/unconfigured mode discipline are reused as a pattern (and where clean, as shared code) — with `/provider/me` replaced by an admin bootstrap (`GET /admin/me` — a small W3 addition returning the caller's active admin roles; today roles are only implicit in `request.principal`).

**Step-up:** the `adminStepUp` policy EXISTS since the W3-1 final correction (`admin` baseline + the Slice-2 recent-factor window, exactly as `providerStepUp` composes on `provider`). Recent-factor step-up is an ACTION-LEVEL mechanism, never a shell-bootstrap gate: `GET /admin/me` sits on the baseline, while every pre-existing sensitive admin route (role administration, organization lifecycle, moderation, taxonomy) kept its pre-split recent-factor strength by moving verbatim onto `adminStepUp` — the split weakened nothing. The final high-risk action SET remains a recommendation under **D-W3-5** (go-live, suspend/offboard, role approve/deny, taxonomy deactivation); dual-control on role grants already exceeds step-up and stays as is.

## 7. Frontend architecture

A new **`admin/`** package, sibling to `portal/`, mirroring its proven shape: Vite + React + TypeScript, typed port contracts, **deterministic fixture mode** for Jest/Playwright/walkthroughs, **live mode** over the centralized authenticated transport, **unconfigured fail-closed** default; provisional Himma tokens from docs/07 (staff-appropriate, light, non-childish); docs/23 §8.4 web-quality contract (not docs/12 native rules). **No provider/customer UI-component sharing beyond the neutral token layer** — coupling staff tooling to provider screens would harm both; small primitives may be duplicated deliberately. Contract tests run against the real `buildApp` + real PostgreSQL with the fake Cognito boundary, exactly like `portal/test-contract`.

Minimum W3 surfaces (docs/23 §9.1 subset): **AD-01** sign-in/MFA/role display · **AD-02** work queue (verifications awaiting review; listings submitted; open revisions — real queues only, no speculative analytics) · **AD-03** verification case/evidence review + decision · **AD-04** provider directory/detail (needs the new admin org reads) · **AD-05** listing review queue/detail (existing contracts) · AD-05b revision review (existing contracts incl. change-set) · **AD-06** taxonomy management (existing contracts) · **AD-17** roles administration (existing contracts) · **AD-18** audit explorer (needs a read route; late slice). Provider detail (task §11) composes real truth only: organization/verification state, storefront profile, branches, staff summary (counts/roles — no credentials), catalogue status counts, verification case + evidence, decision/audit history where permitted; no secrets or authentication data.

## 8. Reuse commitments (no rebuilds)

- **Program moderation (AD-05):** consume the existing queue/view/decision routes verbatim. Approval RESTS at `approved`; providers publish (D-S4-2). No new domain.
- **Revision moderation:** consume the existing queue + `RevisionChangeSetView` + start/approve/reject. Sufficient reviewer information exists; no fabricated diffs.
- **Taxonomy (AD-06):** consume existing admin CRUD. Provider "request a new Activity Type" (W2 carried gap): recommended as a lightweight request record (provider submits label + context via a small provider route; operations sees it in the AD-02 queue; outcome = create the taxonomy row via the EXISTING admin route, or decline with a provider-safe message). Providers never mutate global taxonomy. In/out of W3 scope = **D-W3-4**.
- **Audit:** every new W3 mutation uses `appendAuditEvent`/`appendOutboxEvent` in-transaction (verification decisions, evidence acceptance/rejection, go-live — already audited, provider-safe message publication, taxonomy requests). AD-18 is a paginated, role-gated read over the existing table (auditor + operations), added late (W3-9), never frontend logs.

## 9. Security requirements (binding for every W3 slice)

Deny-by-default `admin` policy on every route; PostgreSQL-authoritative roles resolved fresh per transaction; **no universal superadmin** (five roles, specific-role checks); MFA baseline; `adminStepUp` for the D-W3-5 set; dual control retained on role grants; cross-role tests (support/finance/auditor refused from operations surfaces and vice versa); no provider/customer authority leakage in either direction (contract-proven per slice); verification documents privately stored, server-mediated, signed short-lived retrieval, no public URLs, access limited to the operations role and the owning organization's provider principals; internal notes structurally absent from provider serializers; audit on every sensitive action; fixture/live isolation identical to the portal discipline.

## 10. Owner decisions

**Required before W3-3 (backend verification domain):**
- **D-W3-1 — Evidence storage architecture.** Recommendation: Option B (§4) — separate private document store, server-mediated signed access; low-level client reusable later, access tier never shared with public media.
- **D-W3-2 — Provider-facing rejection content.** Recommendation: §5's three-layer model; reviewer-authored provider-safe message required on reject, optional on request-changes; internal notes never exposed.

**Deferrable (needed before the slice named, not before W3-1):**
- **D-W3-3 — First-launch mandatory evidence checklist per provider type** (policy config; needed before REAL provider onboarding, not before the infrastructure; a placeholder checklist serves dev/test).
- **D-W3-4 — Taxonomy request workflow in W3?** Recommendation: yes, as the lightweight request record above (W3-7); the truthful Support fallback remains meanwhile.
- **D-W3-5 — Admin step-up action set.** Recommendation: go-live, suspend, offboard, role approve/deny, taxonomy deactivate (before W3-5).
- **D-W3-6 — Evidence retention/deletion policy** (operational/legal input; before outreach, not before implementation).

Settled by existing canon (NOT open): go-live is a separate admin action after verification (D-S3-3, §6.1, shipped `go-live` route) · exactly five admin roles · approval ≠ publication · reviewers see revision change-sets.

## 11. W3 decomposition (bounded, sequential; each ends with owner STOP)

| Slice | Scope | Depends on |
|---|---|---|
| **W3-0** | This plan. | — |
| **W3-1** | Admin Portal foundation: `admin/` package, shell/navigation, fixture+live+unconfigured composition, sign-in/MFA/continuity over existing identity routes, NEW `GET /admin/me` bootstrap, role display, deny-by-default routing. | Plan approval |
| **W3-2** | Provider directory/queue/detail: NEW admin organization read routes (list/search/filter by verification state + detail composition), AD-02 queue (verifications + existing moderation queues) and AD-04 directory over them; existing lifecycle actions (suspend/reinstate/offboard) wired with truthful states; verify/go-live visible but production-gated (D-S3-3 honest). | W3-1 |
| **W3-3** | Verification domain backend: VerificationCase + evidence records + requirement config (schema + migrations), case lifecycle services, decision transaction composing `transitionOrganization`, D-S3-3 readiness wired to REAL case truth, audit/outbox, contract tests. | D-W3-1/2 approved |
| **W3-4** | Evidence file infrastructure: private document store per D-W3-1, provider upload surface (Provider Portal onboarding gains real evidence upload), admin retrieval via signed access, privacy proofs. | W3-3 |
| **W3-5** | Verification review/decision integration: AD-03 case UX end-to-end (checklist, evidence viewing, per-item review, decide with CAS + step-up), go-live enabled by real readiness; end-to-end contract journey submitted→case→evidence→verify→go-live→live storefront public. | W3-3/4 |
| **W3-6** | Catalogue + revision moderation integration (AD-05): existing contracts, change-set review UX, approve/request-changes; composition proofs with the Provider Portal truth. | W3-1 |
| **W3-7** | Taxonomy administration (AD-06) + the D-W3-4 request workflow. | W3-1 (W3-7b needs D-W3-4) |
| **W3-8** | Provider-facing correction/reason integration: the §5 contract on org verification + listing changes-requested; Provider Portal reads and renders it (closing the W2 carried gap). | D-W3-2; W3-3/5 |
| **W3-9** | Closeout/hardening: AD-17 roles UX over existing routes, AD-18 audit explorer (NEW read route), admin step-up set (D-W3-5), cross-role/cross-org security regression, W3 closeout audit (W2-12D pattern). | all prior |

W3-6/W3-7 are parallel-eligible after W3-1 but remain sequential owner-approved tasks. Every slice: RED→GREEN backend tests where backend changes; real-PostgreSQL contract tests through the real authenticated transport; fixture Jest/Playwright for the admin surface; `db:verify`; certification per docs/25.

## 12. Provider-outreach gate (honest, current)

Before Himma can onboard/outreach to real providers: ✅ W2 Provider Portal core (CLOSED `2ba9a2e`) · ⬜ W3 minimum admin/verification capability (W3-1…W3-5 + W3-8) · ⬜ real Cognito environment + §14.E′ smokes (operational) · ⬜ evidence checklist policy (D-W3-3) + retention (D-W3-6) · ⬜ real mail delivery for invitations/notifications (operational) · ⬜ external design-partner validation round (docs/23 §8.6) with findings dispositioned · ⬜ hosting/deployment of backend + portals with production config (docs/23 §10/§18 scope) · ⬜ legal/compliance content where docs/23 §15 requires it for provider-facing operation. Public marketplace launch additionally requires the future booking/payment workstreams and the §15 gates — far beyond W3, and deliberately not claimed here.

## 13. Non-goals of W3 (recorded ownership)

Bookings/schedules/capacity/payments/finance/support-case domains (future workstreams, docs/23 §16) · public marketplace media binaries/CDN (future media milestone; may reuse §4's low-level layer, never its access tier) · bulk import engine (separate milestone) · customer-facing surfaces · Provider Portal visual polish · analytics/CRM.

---

## W3 implementation record

**W3-1 · Admin Portal foundation, authentication & shell (2026-08-17 — awaiting owner review).** The independent **`admin/`** package (Vite + React 19 + TypeScript strict; own tsconfig/ESLint/Jest/jest-axe/Playwright/build; Himma theme tokens shared; ZERO provider/customer domain component sharing) implements the W3-1 scope: **backend** — the ONE bounded addition, `GET /admin/me` under the existing `admin` policy (live session + MFA + recent factor + ≥1 ACTIVE PostgreSQL role; registration inherits the B2-6C production readiness gate), returning exactly `{user{id, displayName}, roles, capabilities}` where capabilities is the NEW centralized `admin-capabilities.ts` projection of already-enforced service authority (operations → providers.operate/catalogue.moderate/taxonomy.manage · access_admin → roles.administer/roles.view · auditor → roles.view/audit.read · support/finance → truthfully empty this phase; no wildcard exists) — 10 backend tests on real PostgreSQL lock the denial matrix (customer-only/provider-only/claims-suggesting-admin forbidden; single-factor mfaRequired; revoked-final-role and revoked-LoginSession exits; D4 exclusivity honored; DTO exactness with no secret material). **Frontend** — the W2-12A auth architecture reused as deliberately contained copies (Cognito boundary, API client, §14.E continuity bootstrap, memory-only tokens, step-up seams; a shared package was rejected to avoid destabilizing the certified portal — recorded duplication), with an admin-shaped session machine whose `stepUpRequired` state honors the admin policy's RECENT-factor demand (re-verify TOTP → re-resolve access; never a bypass); fixture/live/unconfigured composition with the same fail-closed discipline (production defaults unconfigured; live is explicit and never falls back to fixtures — composition/storage/fixture-isolation sweeps test-locked); deterministic fixture identities for every role shape incl. multi-role and no-role; the responsive console shell (desktop sidebar · mobile drawer with focus-in/Escape/focus-return · skip link · identity/roles display · sign out) with **capability-aware navigation and per-route guards derived ONLY from the `/admin/me` projection**, truthful W3-slice placeholders (no fake queues/cases anywhere in any mode), and a truthful dashboard foundation (no fabricated metrics). **Verification:** admin tsc/ESLint clean · **admin Jest 41/41 across 5 suites** (access flows, denial matrix, step-up flow, session machine, live-port mapping incl. fail-closed DTO + noAccess/stepUpRequired outcomes, composition security, jest-axe over five representative states) · **admin Playwright 11 passed / 4 by-design skips** at 1440/1024/390 (evidence `artifacts/admin-w3-1/`) · admin production build green/fail-closed · **backend Jest 755/755 across 60 suites** (route-inventory snapshot deliberately updated with the one new route) · `db:verify` 10/0, no migration · Provider Portal UNTOUCHED (zero file changes — no recertification required). **Operational dependencies unchanged:** no real Cognito pool (live smoke pending); the B2-6C production admin gate stays fail-closed until its capability report is truthfully ready. **Next: W3-2 — real admin provider directory/queue/organization detail reads** — do NOT start without owner approval.

**W3-1 final correction · Admin baseline access vs recent-factor step-up (2026-08-19 — awaiting owner review).** W3-1 was CONDITIONALLY APPROVED at `b5654b3` with one semantics issue: `GET /admin/me` inherited the recent-factor demand, making step-up effectively global to the Admin Portal. The owner decision **"Admin baseline access"** is now recorded and implemented: ordinary admin access = authenticated identity + live `LoginSession` + MFA assurance + ≥1 active PostgreSQL admin role — NOT a continuously recent MFA factor. **Backend:** the route-policy registry gained `adminStepUp` (= `admin` baseline + the Slice-2 recent-factor window, mirroring `provider`/`providerStepUp` exactly); the pipeline's admin block now enforces the baseline (roles → enrollment → an MFA-verified factor, recency-free) and composes recency ONLY on `adminStepUp`; **all 38 pre-existing admin route registrations (role administration, organization lifecycle, moderation, taxonomy) moved verbatim onto `adminStepUp` — retained strength, nothing weakened — while `GET /admin/me` alone sits on the baseline** (route-inventory snapshot deliberately updated; the three policy-lock suites now pin the split shape). Real-PostgreSQL proofs (admin-me suite, now 13 tests): Case A fresh factor → 200; **Case B the SAME admin with a stale factor (auth_time aged past the 300 s window) → STILL 200**; Case C no MFA assurance → `mfaRequired`; Case D no active role → `forbidden`; and the non-weakening probe — the very same stale bearer that bootstraps `/admin/me` is refused `stepUpRequired` on both a sensitive read (`GET /admin/role-assignments`) and a sensitive mutation (`POST /admin/role-requests`), while a fresh factor still passes; a stale factor never rescues a revoked role or session. **Admin frontend:** ordinary bootstrap flows auth/session → MFA baseline → `/admin/me` → shell; the `stepUpRequired` state/screen and the TOTP/recovery re-verification seam are RETAINED as the generic action-level mechanism for D-W3-5 (plus the degenerate no-MFA-factor session) and are no longer an ordinary bootstrap condition; the fixture `stale@himma.demo` now bootstraps straight to the shell (proving the decision), and a new `demandStepUp()` fixture control keeps the seam exercisable end-to-end (screen → fresh code → authoritative re-resolution — never a bypass). D-W3-5 (which future actions demand step-up) remains DEFERRED — nothing new was wired to it. **Verification:** backend tsc/ESLint clean · backend Jest **758/758 across 60 suites** · `db:verify` 10/0 (no migration) · admin tsc/ESLint clean · **admin Jest 42/42 across 5 suites** · **admin Playwright 12 passed / 6 by-design skips** (new desktop stale-factor-shell evidence in `artifacts/admin-w3-1/`) · admin production build green · Provider Portal untouched (shared auth source unmodified — copies, not imports).

**W3-2 · Real Admin Provider Directory, Review Queue & Organization Detail Reads (2026-08-19 — awaiting owner review).** W3-1 CLOSED (`b5654b3` + `48537e8`). A READ-ONLY operations slice: **backend** — the first-class internal read model `organization-admin-read.ts` + two new routes on the `admin` BASELINE policy (ordinary internal reads never demand step-up; every lifecycle mutation stays untouched on `adminStepUp`), with the `operations` role required fresh per transaction (docs/31 §6 canon: provider operational data is operations-only; read-only-role expansion is a future decision, deliberately NOT made here). `GET /admin/organizations`: ONE list contract serving BOTH the directory and the review queue — authorization → authoritative predicates over the complete set → deterministic (created_at, id) order → keyset cursor → bounded summary projection `{organizationId, displayName, tradeName, verificationState, reviewState, storefront{published, publiclyVisible}, activeBranchCount, createdAt, updatedAt}`. Search (`q` ≤160): case-insensitive literal substring (wildcards escaped, whitespace collapsed, blank = no predicate) over display/trade/legal names (legal is searchable management data, never shown in the summary row). Filters: `state` (the exact 8-value ck_organization_state vocabulary; invalid → 422) and `needsReview=true` — **the documented queue predicate: `submitted` (awaiting_review), `in_review` (in_review), `verified` (awaiting_go_live) need HIMMA action; draft/rejected wait on the provider, live/suspended/offboarded are settled** — pinned per state in tests; no timers/SLA/risk heuristics exist. `GET /admin/organizations/:organizationId`: deliberate bounded section queries (org+profile join, branches, ACTIVE team joined to the canonical `customer_account.display_name` — the safe internal display identity; users without an account row project null, never invented — and a single listing-state GROUP BY aggregate in the exact docs/24 §5.3 vocabulary with honest zeros). Deliberately NOT projected: user ids/emails (PII-lean; multi-identity ambiguity), media refs (no delivery path), commercial_terms_ref, invitations, and any secret/session material (leak-swept). Authorization precedes existence — unauthorized identities get the same `forbidden` for real and fabricated ids (no enumeration oracle); provider staff, customers, and admin-looking Cognito claims are all refused; support/finance/access_admin/auditor are refused per canon. Efficiency: the list is ONE query with a correlated branch-count subselect (constant-statement-count proven); detail queries never scale with rows. **Backend Jest 778/778 across 61 suites** (20 new; route snapshot +4 baseline GET/HEAD lines; the admin-routes policy lock now pins the per-method baseline set). **Admin frontend** — the Providers placeholder became the real directory: URL-held search (debounced, race-free local input)/state filter/view, the Review-queue view as `needsReview=true` over the SAME contract (no duplicate query logic; wording is "review queue", never "verification cases"), a dense internal table that restacks into labelled rows under 720 px (no horizontal overflow), keyset "Load more" via useInfiniteQuery (keys include q/state/view — filter changes reset pagination; no client-side filtering anywhere), truthful loading/empty/filtered-empty/error+retry/forbidden states with NO fixture fallback in live failure, and `/providers/:organizationId` — Overview / Verification (current STATE only + the truthful evidence-capability note; **NO verify/reject/go-live/suspend controls exist anywhere** despite the backend routes existing) / Branches / Team / Catalogue sections. Fixture mode: 12 deterministic fictional organizations covering every state behind the same port seam with server-mirrored semantics + a `setProvidersOutage` control; live port validates every row/detail fail-closed; unconfigured stays unavailable; composition/isolation/no-web-storage sweeps extended. **Admin Jest 62/62 across 7 suites · NEW admin contract suite (real buildApp + PostgreSQL + fake Cognito, reusing the portal harness): 2/2 — the full journey (authenticate → /admin/me → directory → search/queue → detail) and the auditor refusal journey · admin Playwright 16 passed / 11 by-design skips (evidence `artifacts/admin-w3-2/`, stable ×3) · admin build green · portal contract 69/69 re-proven · `db:verify` 10/0, NO migration.** Carried gaps: VerificationCase/evidence still absent (W3-3/4/5; D-S3-3 unchanged), lifecycle actions deliberately unexposed until then, provider-safe rejection content W3-8, W2 provider-side member display identity gap unchanged (the INTERNAL projection now exists). **Next: W3-3 — VerificationCase domain & evidence metadata foundation** — do NOT start without owner approval.

**W3-3 · VerificationCase domain & evidence metadata foundation (2026-08-19 — awaiting owner review).** W3-2 CLOSED (`06b9b14`). A BACKEND DOMAIN FOUNDATION slice — no HTTP routes, no Admin/Provider frontend changes, no binary storage. **Migration `0011_verification_domain`** (deterministic; rollback/reapply proven; `db:verify` 11/0; kysely codegen regenerated): four tables. **`verification_case`** — ONE Himma review ROUND per organization; the organization keeps the canonical lifecycle (no competing state machine). Case states, the smallest set: `open` (collecting; readiness is COMPUTED, deliberately not a state) → `in_review` → `decided`, with `superseded` closing an active round without a decision; transitions trigger-enforced, terminal rounds immutable, identity/snapshot columns frozen. Round identity: `UNIQUE (organization_id, round)`; **at most one ACTIVE round per org is a partial unique index**, not an application promise; `UNIQUE (id, organization_id)` makes children org-bound by construction. **`verification_case_requirement`** — the IMMUTABLE per-round policy snapshot (UPDATE trigger-refused): stable key, label/description, required flag, sort, with the case recording `policy_version`; later policy drift never rewrites history (test-pinned). Requirement policy itself is the injected **`VerificationRequirementPolicyProvider`** seam — D-W3-3 stays deferred, NO launch checklist exists in schema/domain, tests use fictional keys (`business_document`, `operating_license`), and **policy absence FAILS CLOSED: no provider, an empty checklist, or duplicate keys refuse to open a case, and an empty snapshot evaluates `policyUnavailable`, never "satisfied"**. Multiplicity ruling (§17): one current document per requirement; multi-document needs are separate keys. **`verification_evidence`** — PRIVATE metadata only (D-W3-1): filename/declared content type at registration (`pending_upload` intent), and write-once trusted storage facts (byte size, sha256, opaque `storage_ref` — schema-CHECKed non-URL; no url/uri/link column and no bytea exists, test-swept). **`stored` is reachable ONLY through `finalizeEvidenceStorage`, a trusted boundary with no HTTP route** (W3-4's storage flow is its only future caller; a browser can never claim `stored: true` — schema CHECK + trigger + service all refuse). Replacement = same-transaction supersession preserving history (partial unique index: one non-superseded row per requirement); nothing deletes (D-W3-6 deferred; himma_app holds NO DELETE grant on any verification table, and requirements/decisions have no UPDATE grant). **Readiness** (`evaluateCaseReadiness`): computed structured semantics — `ready` / `missingRequirements[{key,label}]` / `policyUnavailable` — over required snapshot rows × current `stored` evidence; optional requirements never block; **readiness ≠ approval and never transitions the organization**. **`verification_decision`** — append-only (trigger + grants), one per round (`UNIQUE(case_id)`), outcomes aligned with the org machine (`approved`/`rejected`), carrying the D-W3-2 three layers in NAMED columns: `reason_code` (machine, mandatory for rejection — schema CHECK), `provider_safe_message` (the only reviewer text ever intended for providers; service-mandatory for rejection), `internal_note` (staff-only). **Read seams (§34/§35): `getInternalVerificationCaseView`** (operations-gated; readiness + evidence status + full decision incl. internal note; never storage refs/digests/binaries) is structurally distinct from **`getProviderSafeVerificationSummary`**, which cannot select internal notes or reviewer identity (test: serialized output lacks both). Services (`verification-case.ts`): open/startReview/supersede/registerEvidence/finalize/decide — all operations-role-gated fresh per transaction (finalize audits as `system`), CAS `expectedVersion` throughout, audit + outbox per repo conventions (case-opened and decision-recorded outbox events carry ids/machine layers only — no labels, filenames, or reviewer text). **D-S3-3 regression (hard criterion): with a fully READY case present, production `verify` and `go-live` still return `verificationEvidenceUnavailable`** — the gate is untouched, as are provider submit/resubmit and every certified admin edge (zero route/snapshot changes; full backend suite green). **Backend Jest 801/801 across 63 suites** (23 new) · lint/tsc clean · migration up/down/up proven · Admin + Provider portals untouched. Deferred still: D-W3-3 checklist, D-W3-5 step-up set, D-W3-6 retention. **Next: W3-4 — private verification evidence binary storage** — do NOT start without owner approval.

**W3-4 · Private verification evidence binary storage (2026-08-19 — awaiting owner review).** W3-3 CLOSED (`c46c5b9`). The real private storage boundary over the W3-3 trusted seam — NO migration (metadata schema was already complete). **Binding W3-4 architecture decisions:** (1) **server-proxied I/O only** — every upload and download streams through authorized Himma routes; **NO presigned or public URL exists anywhere** (requests are per-call authorized; nothing reusable can leak; presigned access, if scale ever demands it, is a future deliberate change); (2) the storage boundary is a **port** (`VerificationEvidenceObjectStore`) with two implementations: the deterministic in-memory fake (dev/test only, failure-injectable) and an **S3-compatible SigV4 driver** (AWS S3/MinIO/R2; node:crypto + fetch, no new dependency; server-side encryption via the store's production convention — AES256 default or configured KMS key; private-by-bucket-policy, the driver never sets an ACL; https mandatory; wire format unit-proven against a deterministic in-process endpoint, **real-bucket smoke = recorded operational dependency**, the Cognito posture); (3) **keys are internal, opaque, tenant-isolated, never filename-derived**: `verification/{organizationId}/{caseId}/{evidenceId}`; (4) upload boundaries are TECHNICAL configuration, not business policy (default 20 MiB; pdf/jpeg/png/webp) — D-W3-3 still owns what documents are required; (5) filename is display metadata only (schema-bounded; Content-Disposition-sanitized on download); (6) **abandoned/incomplete uploads**: rows stay `pending_upload` (never ready, never downloadable), orphaned objects are inert and unreachable, nothing deletes (D-W3-6 deferred); retry legally overwrites only its own key; (7) **malware scanning is explicitly deferred** with a fail-safe position: documents are reachable only by the owning organization's owner and operations admins, no customer-facing trust decision rests on them while D-S3-3 keeps production verification closed, and scanning is recorded as a go-live-gate operational dependency for the review workflow. **Composition:** `identity.verificationEvidenceStorage` config — absent (the default everywhere incl. production) → **the entire evidence surface does not exist (fail-closed 404)**, proven. **Authorization:** provider surface on the `provider` policy with the NEW owner-only capability **`verification.evidence.manage`** (activated per the documented capability pattern; widening is a future owner decision; grants no admin authority) — routes `POST …/verification/evidence` (intent registration via the shared W3-3 core, org-pinned), `PUT …/evidence/:id/content` (raw declared-type body; backend computes SHA-256/size from the actual bytes, writes the private object, HEAD-verifies, then calls the no-HTTP `finalizeEvidenceStorage` under CAS — **no client value ever becomes a trusted fact**, and no finalize-shaped route exists), `GET …/evidence/:id/content` (own org's CURRENT stored document only); admin surface `GET /admin/verification/evidence/:id/content` on the `admin` baseline + operations role (superseded history retrievable for review; every access audited `org.verification_evidence_accessed`). Cross-org anything is a not-found shape at every step. New typed outcomes: `invalidEvidenceMetadata`/`evidenceStateConflict`/`storageUnavailable`. **Proven (16 new tests):** full lifecycle → `stored` → download; pending never ready; forged fields stripped and digests computed from real bytes; org_manager refused; cross-org register/upload/download refusals; auditor/customer admin-download refusals; declared-type allowlist + request-type match + non-buffer + oversize (413) refusals; supersession history (superseded un-revivable, provider-invisible, ops-retrievable); put-failure AND post-write/pre-finalize failure both leave recoverable `pending_upload` with no false readiness; unconfigured surface absent; policy declarations pinned (config-gated surface, hence absent from the default inventory snapshot — the sessionContinuity precedent); no binary/URL in PostgreSQL; storage refs/digests absent from responses, headers, audit and outbox. **Backend Jest 816/816 across 65 suites · portal contract 69/69 · admin contract 2/2 · db:verify 11/0 (no migration) · lint/tsc clean · D-S3-3 regression untouched and green.** Frontends untouched (provider upload UX and the admin review UI belong to W3-5/W3-8 slices). **Next: W3-5 — verification review/decision integration** — do NOT start without owner approval.

**W3-4 final correction · Internal retrieval fails closed until content safety exists (2026-08-19 — awaiting owner review).** Owner review found the scanning deferral not fully fail-safe: provider-controlled bytes could already reach an operations reviewer's workstation. The smallest fail-closed boundary now exists, with no port/driver/key/lifecycle/schema redesign: `verificationEvidenceStorage.contentSafetyReady` is a CAPABILITY REPORT defaulting FALSE — **in the live/default composition an authorized operations admin's download is refused with the distinct typed `verificationEvidenceSafetyUnavailable` (503) BEFORE any object-store read** (spy-proven), never disguised as storage or authorization failure and leaking no refs/digests/keys/credentials. Authorization still runs FIRST (unauthorized admins and cross-org callers see exactly the pre-correction forbidden/not-found shapes and learn nothing about safety or storage state); the provider owner's retrieval of their OWN organization's document is deliberately outside the gate; permitted downloads carry `Content-Disposition: attachment` + `X-Content-Type-Options: nosniff` (asserted). There is NO operator bypass: this build contains no scanning capability, so a production start claiming `contentSafetyReady: true` refuses loudly (the D-S3-3 pattern); dev/test compositions set it true to exercise retrieval deterministically. **Content scanning/safety is a HARD prerequisite for enabling live Admin evidence review — and the binding W3-5 handoff invariant: W3-5 may design and integrate the review/decision workflow, but must NOT make the production review path operable, and must NOT replace D-S3-3 with any allow-path permitting real decisions, while evidence content safety is unavailable.** D-W3-3/D-W3-5/D-W3-6 untouched. Backend Jest 818/818 (65 suites) · portal contract 69/69 · admin contract 2/2 · db:verify 11/0 · lint/tsc clean.

**W3-5 · Verification review/decision integration (2026-08-19 — awaiting owner review).** W3-4 CLOSED (`d347cd4`+`a0ee435`). The ONE authoritative path — case → readiness → review ownership → decision → canonical organization transition — with W3-3 as domain authority and the organization aggregate as lifecycle authority; no second state machine, no second transaction path. **Backend:** the internal cores were extracted (`transitionOrganizationInTrx`, `recordDecisionForLockedCaseInTrx` — wrappers keep the certified standalone semantics byte-for-byte) and composed in `verification-review.ts`: `beginVerificationReview` (case open→in_review + the org's `start_review` edge in ONE transaction when the org is still submitted) and `decideVerification` (org verify/reject edge FIRST, decision insert SECOND, one transaction — divergence structurally impossible; proven by a sabotaged-insert rollback test and a concurrent-direct-rejection test). **The W3-4 content-safety invariant is mandatory:** BOTH operations refuse with typed `verificationEvidenceSafetyUnavailable` while the capability is unavailable — the only production-representable state (no operator flag was added; the W3-4 startup refusals stand) — so **the production review path cannot complete a real evidence-based decision**, and the D-S3-3 flag remains untouched as defense-in-depth on the standalone verify/go-live edges (which the composed path never needs: in dev/test the gate passes trivially, in production the safety gate refuses first). **Readiness is necessary, never sufficient:** approval requires `ready` (refusing with the structured `verificationCaseNotReady {missing[]}` — the programIncomplete pattern) AND an explicit reviewer decision; a merely-ready case transitions nothing; rejection is legal on unready evidence and drives the org's `reject` edge with the mandatory machine + provider-safe layers. HTTP: `GET /admin/organizations/:id/verification` (the reviewer workspace read — org state, round history, latest case with readiness/evidence/decision incl. internal note, plus internal `contentSafetyReady`/`policyConfigured` truth) on the `admin` BASELINE; `POST …/verification/cases`, `…/cases/:caseId/review`, `…/cases/:caseId/decision` on **`adminStepUp` — the safer existing boundary, because D-W3-5 remains an owner decision** (report: the final ruling on which of open-round/start-review/decide/go-live must keep recent-factor step-up is owner-pending; nothing was decided here). New typed outcomes: `verificationPolicyUnavailable` (503, D-W3-3 unconfigured — opening rounds fails closed), `verificationCaseConflict` (409), `invalidVerificationDecision` (422). **Admin frontend (AD-03 per plan §11):** the provider-detail Verification section became the real case workspace — round history, requirement/evidence checklist with authorized downloads (via a new memory-only binary transport leg), truthful readiness, Open-round/Start-review/Approve/Reject (three-layer form with the internal note labeled staff-only), Go-live for verified orgs, the content-safety banner disabling review when unavailable, the D-W3-5 step-up seam handling action-level `stepUpRequired` through TOTP re-verification, and typed refusal messages for every distinct condition; the Verification nav area now lands on the review queue; fixture mode shares ONE mutable dataset between directory and verification ports so decisions surface in the queue (deterministic journeys incl. approve→verified→go-live→live and the three-layer rejection); the fixture `setContentSafetyReady(false)` control models the live fail-close. **Provider-safe boundary unchanged:** no provider route was added (W3-8 owns exposure); the structural W3-3 seam re-proven post-decision (no internal note, reviewer identity, storage material). **Verification:** backend Jest **830/830 across 66 suites** (12 new; snapshot +5 routes; the admin baseline policy lock extended) · admin Jest **74/74 across 9 suites** (panel journeys, live-port distinct-refusal mapping, sweeps extended) · **admin contract 5/5** (the REAL journey: authenticate → workspace → open → evidence via the real W3-3/W3-4 services → review → approve → verified → go-live → public storefront; the structured unready refusal; the auditor refusal) · portal contract 69/69 (harness extended additively) · admin Playwright **19 passed / 17 by-design skips** (evidence `artifacts/admin-w3-5/`) · builds green · db:verify 11/0, NO migration · provider submit/resubmit and every certified admin edge unchanged. Provider Portal untouched (no shared source modified — no recertification). Deferred still: **D-W3-3** (the real launch checklist — production round-opening fails closed until configured), **D-W3-5** (step-up action set — all new mutations parked on `adminStepUp`), **D-W3-6** (retention), the content-safety capability, and the real-bucket smoke. **Next: W3-6 — catalogue + revision moderation integration** — do NOT start without owner approval.

**W3-6 · Catalogue + revision moderation integration (2026-08-20 — **FULLY APPROVED and CLOSED at `ba168d8` (2026-08-20)**; final verification counts at closure: backend Jest 831/831 across 66 suites · admin Jest 93/93 across 11 suites · admin contract 9/9 · portal contract 69/69 · admin Playwright 22 passed / 23 by-design skips · builds green · db:verify 11/0 · no migration).** W3-5 CLOSED (`4651fa1`). The §1 reconciliation found the CERTIFIED S4-2 admin moderation surface already complete (`GET /admin/listings[/:programId]`, `GET /admin/revisions`, and the six named decision routes over `reviewProgram`/`startRevisionReview`/`approveRevision`/`rejectRevision` with the reviewer change-set) — so W3-6 invents nothing: **the ONLY backend change is the W3-1 policy split applied by the owning slice: the three moderation READS moved to the `admin` baseline; every decision mutation keeps `adminStepUp`** (D-W3-5 still owner-pending — the final ruling on which moderation decisions retain recent-factor step-up is reported, not decided), proven by a new stale-factor split test (stale ops admin reads queues/detail, is `stepUpRequired`-refused on decisions) and the per-method policy lock; NO new routes, NO service changes, NO migration, snapshot deliberately updated (6 lines). **Admin frontend (AD-05):** `/moderation` (server-filtered listing review queue with provider identity), `/revisions` (the distinct ProgramRevision queue), and the `/moderation/:programId` workspace — authoritative listing truth (state, provider + verification state, eligibility, description, prices, branches), the **current-vs-proposed change-set table** when a revision is open (only fields the revision proposes; never confusable with listing truth), and the certified decisions only: listing start-review/approve/request-changes and revision start/apply/reject with the certified machine `reasonCode` slug (**no three-layer feedback was invented here** — that model belongs to verification; richer provider-facing listing feedback stays the W3-8 gap). **The two mutation models never blur:** no Admin control edits provider catalogue fields — revision approval applies the provider's own submitted change-set through the S4 service, atomically, with the listing remaining published throughout (test- and e2e-pinned); **approval rests at `approved` and no publish control exists anywhere** (D-S4-2). Typed distinct refusal messages, the action-level step-up seam, and capability guarding (`catalogue.moderate`; access_admin/auditor refused, ports never called) throughout; fixture mode ships deterministic fictional listings/revisions mirroring the S4 semantics. **Verification:** backend Jest **831/831 across 66 suites** (stale-split test added; provider submit/resubmit, S4 catalogue/moderation, public catalogue/search, and all W3-3/4/5 verification suites green) · admin Jest **93/93 across 11 suites** · **admin contract 9/9** — incl. the REAL journeys: provider-service-created listing → queue → start → approve (rests at `approved`, one canonical outbox event); request-changes with reasonCode → certified provider RESUBMISSION unchanged; provider protected edit → revision queue → change-set → apply mutating the published listing atomically; access_admin refused · portal contract 69/69 · admin Playwright **22 passed / 23 by-design skips** (evidence `artifacts/admin-w3-6/`) · builds green · db:verify 11/0, NO migration · Provider Portal untouched (no shared source — no recertification). Deferred still: D-W3-3, D-W3-5, D-W3-6, content safety, real-bucket smoke. **Next: W3-7 — taxonomy administration** — do NOT start without owner approval.

**W3-7 · Taxonomy administration (AD-06) (2026-08-20 — **FULLY APPROVED and CLOSED at `4759521` (2026-08-20)**; final verification counts at closure: backend Jest 832/832 across 66 suites · admin Jest 118/118 across 13 suites · admin contract 13/13 · portal contract 69/69 · admin Playwright 25 passed / 29 by-design skips · db:verify 11/0 · no migration · backend/admin typecheck + lint clean · admin production build green).** W3-6 CLOSED (`ba168d8`). The §3 reconciliation found the CERTIFIED S4 taxonomy administration surface already complete — `GET /admin/taxonomy` (the full administration view over the four distinct entities: area · category · activity_type · collection, inactive rows included) plus the eight named create/patch routes over `taxonomy-admin.ts` (operations-only fresh per transaction; slug-immutable by construction — no PATCH schema carries a slug and the 0007 trigger is final authority; deactivate-only retirement with NO delete route and no DELETE grant; CAS `expectedVersion` on every patch; activity types parented to ONE category for life with ghost parents typed `invalidTaxonomy`; ordering = the certified `sortHint` data only; category/type label edits refresh search documents in the same transaction; `taxonomy.*_changed` audit+outbox with ids-only payloads) — so **W3-7 invents nothing: the ONLY backend change is the W3-1 policy split applied by the owning slice — the administration READ moved to the `admin` baseline; all eight mutations keep `adminStepUp`** (D-W3-5 still owner-pending; no NEW actions were added to the parked inventory — the eight taxonomy mutations were already on the safer boundary and stay there; taxonomy deactivation remains in the D-W3-5 recommendation), proven by a new stale-factor split test (a stale ops admin reads the view, is `stepUpRequired`-refused on create AND patch with zero state change) and the per-method policy locks; NO new routes, NO service changes, NO migration, NO codegen drift (schema untouched), snapshot deliberately updated (2 lines). **W3-7b (the D-W3-4 provider taxonomy-request workflow) is NOT built: D-W3-4 remains an owner decision and the owner's W3-7 instruction excludes provider taxonomy-management functionality — the truthful Support fallback stands.** **Admin frontend (AD-06):** `/taxonomy` — the four resource types as DISTINCT sections (never a flattened universal record): tables incl. inactive/state badges and slug identity, per-entity creation (slug chosen once, permanently), editing of exactly the certified mutable fields (labels, city, sortHint, synonyms; collections: title/subtitle/audience/featured/seasonal/lifecycle state) with the slug displayed as explicitly immutable and the activity-type parent pinned for life, activate/deactivate (collections archive), CAS stale refusals that refresh to authoritative truth without partial change, distinct typed refusal messages (`slugConflict`/`invalidTaxonomy`/`invalidInput` never disguised as outages), the action-level step-up seam, and capability guarding (`taxonomy.manage`; access_admin/auditor refused, ports never called). **No unsupported operation exists anywhere: no delete, no reorder beyond sortHint, no slug/identifier change, no re-parenting — in the API, the UI, or fixture mode** (the port surface is test-pinned to exactly the nine certified operations; fixture data returns snapshots, mirrors CAS/conflict/ghost-parent semantics, and honours the read-baseline/mutation-step-up split). **Consumer stability proven end-to-end in the admin contract suite (real buildApp + PostgreSQL):** the live port reads the REAL D-S4-3 seed (11+ categories); deactivating an activity type through the live admin transport leaves an EXISTING provider listing's reference intact and truthfully represented (`activityType.active: false` via the certified provider read), REFUSES the type for a NEW provider listing through the S4 server-side validation (never frontend filtering), and drops the row from the public `/catalogue/activity-types` read while the admin view keeps administering it; audit/outbox exactly once per mutation through the canonical path (ids-only payloads, refused mutations emit nothing). **Verification:** backend Jest **832/832 across 66 suites** (1 new; provider submit/resubmit, S4 catalogue/moderation, public catalogue/search, W3-3/4/5 verification, and W3-6 moderation suites all green) · backend tsc/ESLint clean · db:verify 11/0, NO migration · **admin Jest 118/118 across 13 suites** (25 new across 2 new suites) · **admin contract 13/13** (4 new: the seeded-truth management journey; the deactivation consumer-safety journey; audit/outbox exactness; the access_admin refusal) · portal contract 69/69 re-proven · **admin Playwright 25 passed / 29 by-design skips** (evidence `artifacts/admin-w3-7/`; the W3-1 shell journey now lands on the real taxonomy workspace) · admin build green · Provider Portal source untouched (no recertification). Deferred still: D-W3-3, D-W3-4 (taxonomy requests), D-W3-5, D-W3-6, content safety, real-bucket smoke. **Next: W3-8 — provider-facing correction/reason integration — do NOT start without owner approval.**

**W3-8 · Provider-facing correction/reason integration (2026-08-20 — **FULLY APPROVED and CLOSED at `07135a9` (2026-08-20)**; final verification counts at closure: backend Jest 836/836 across 66 suites · backend tsc/ESLint clean · db:verify 12/0 · migration `0012_listing_moderation_feedback.sql` up/down/up proven · codegen drift-free · portal Jest 762/762 across 58 suites · portal contract 70/70 · portal Playwright 208 passed / 20 skipped · portal build/tsc/lint green · admin Jest 118/118 across 13 suites · admin contract 13/13 · admin Playwright 25 passed / 29 by-design skips · admin build/tsc/lint green; preserved at closure: provider evidence-upload UX deferred for explicit owner assignment · revision-rejection feedback outside W3-8 · D-W3-3/4/5/6 unresolved · D-S3-3 + content-safety invariant unchanged · production verification/review inoperable until its real dependencies exist).** W3-7 CLOSED (`4759521`). The §3 reconciliation pinned the scope to the exact §11 row — **the §5 provider-safe `latestDecision { reasonCode, providerMessage, decidedAt }` contract on org verification + listing changes-requested, with the Provider Portal reading and rendering it (closing the W2 carried Class-B gap)** — and reconciled the deferral note in the W3-4/W3-5 records against the plan: the **provider evidence-upload UX is NOT part of W3-8** (the §11 row does not assign it; it was the W3-4 plan-row deliverable deferred at W3-4's closure and remains a RECORDED DEFERRED item for owner assignment — naturally gated on unresolved D-W3-3, since a requirements/upload UX without the real checklist would run only against fictional policies). Revision-rejection feedback likewise stays outside §5's smallest contract (machine reasonCode, audit-only — recorded). **Org verification exposure (backend): no new route and no new storage** — the certified `GET /provider/organizations/:id` view (org.read) gains a `verification` projection composed from the W3-3 STRUCTURAL seam `getProviderSafeVerificationSummary` inside the same read transaction; internal reviewer notes and reviewer identity remain UNSELECTABLE through the seam, so the provider serializer cannot leak them (test: the serialized org view after a real W3-5 rejection carries the two provider-safe layers and neither the internal note nor the reviewer id); null when no round has ever existed, `latestDecision: null` while a round is undecided, and the historical decision stays truthfully visible after the certified resubmission. **Listing changes-requested exposure (backend): the ONE genuinely missing piece (§2.2 "no storage, no contract, no read") — migration `0012_listing_moderation_feedback`** (up/down/up proven; db:verify 12/0; codegen regenerated): an append-only row per `request_changes` decision carrying ONLY the two provider-visible layers (`reason_code` slug-checked, `provider_safe_message` ≤2000 like `verification_decision`; **no internal-note column exists in this domain, so the table is provider-safe BY CONSTRUCTION**; UPDATE trigger-refused, no UPDATE/DELETE grant — retention stays D-W3-6), written by `reviewProgram` atomically with the state edge through the canonical service; the admin `request-changes` route alone gains the optional `providerMessage` input (D-W3-2: optional on request-changes; start/approve declare no such field and the service only records it on request_changes), and **the provider-safe message never enters audit/outbox payloads** (test-swept; the machine reasonCode stays in the certified outbox payload unchanged). The shared `ProgramDetailView` (provider detail + admin moderation view, additive) gains `latestDecision`; refused mutations write nothing; feedback history survives resubmission truthfully. **Provider Portal:** the onboarding hub's rejected stage renders the REAL feedback ("What needs to change": the reviewer-authored provider message — with the truthful Support fallback only when no message exists — plus the machine reference and decision date), and the listing workspace's changes-requested state renders "What Himma asked to change" the same way (the W2-9 "no invented reviewer feedback" pins were deliberately updated: the contract now EXISTS and is real); contracts/live ports validate the new projections fail-closed (malformed → the whole read refuses; absent → null, never invented), fixture mode mirrors the real shapes (a rejected org and a changes-requested listing now carry deterministic feedback), and no new provider mutation, route, or capability exists — reads ride the existing org.read/catalogue.read authority and org scoping. **Admin Portal (shared-contract change, recertified):** the AD-05 workspace's request-changes action gains the optional "Message to the provider" text (explicitly labeled provider-visible); the live port sends it on the request-changes leg ONLY (dropped on start/approve exactly like the certified schema refuses it). **Untouched by construction: D-S3-3, the W3-4/W3-5 content-safety invariant (production review/decision remains inoperable; the contract journey drives a decision only under the test composition's `contentSafetyReady: true`), evidence upload/storage boundaries, verification domain semantics, moderation decisions/lifecycle, taxonomy, and all parked owner decisions (D-W3-3/4/5/6 — none decided).** **Verification:** backend Jest **836/836 across 66 suites** (4 new; W3-3/4/5 verification, W3-6 moderation, W3-7 taxonomy, S4 catalogue/public/search all green) · backend tsc/ESLint clean · **db:verify 12/0 — migration `0012` (up/down/up proven, codegen drift-free)** · portal Jest **762/762 across 58 suites** · **portal contract 70/70** (new REAL journey: submitted org → certified W3-3/W3-5 rejection with all three layers → org view/onboarding snapshot carry exactly the provider-safe truth → resubmission; the listing journey now proves the closed gap end-to-end) · portal tsc/ESLint clean · portal build green · **portal Playwright 208 passed / 20 skipped** · admin Jest **118/118 across 13 suites** · **admin contract 13/13** (request-changes journey extended with the provider message through the live admin transport) · admin tsc/ESLint clean · admin build green · **admin Playwright 25 passed / 29 by-design skips**. Deferred still: D-W3-3, D-W3-4, D-W3-5, D-W3-6, provider evidence-upload UX (recorded above), content safety, real-bucket smoke. **Next: W3-9 — closeout/hardening — do NOT start without owner approval.**

**W3-9 · Closeout/hardening (2026-08-20 — this commit, awaiting owner review — the FINAL W3 slice).** W3-8 CLOSED (`07135a9`). The §3 reconciliation found the AD-17 backend complete (the B2-5 dual-control role surface, end-to-end) and exactly ONE genuinely missing backend piece: the AD-18 read route. **Backend:** (1) **NEW `GET /admin/audit-events`** (docs/31 §8) — a paginated keyset read over the EXISTING 0001 `audit_event` table (its indexes already exist — NO migration, NO schema change, NO codegen change): baseline `admin` policy with the SERVICE gating `auditor` + `operations` fresh per transaction (per §8's explicit assignment; the capability projection truthfully gains `audit.read` for operations, mirroring the enforced authority — the certified projection contract), exact-match filters (entityType/entityId/actorId/action), DESC keyset pagination, and a deliberately BOUNDED projection: digests, principal context, and request ids are never selected (repository-enforced), and reading emits nothing (viewing is not an action); (2) **the W3-1 read/mutation split applied by the owning slice to the two role-administration READS** (`GET /admin/role-assignments[/:assignmentId]` → baseline; the service's access_admin|auditor gate unchanged) while all four role MUTATIONS keep `adminStepUp` + dual control (the MFA recency probes retargeted to a mutation, with the baseline read additionally pinned); (3) **the consolidated cross-role/cross-surface security regression suite** — every internal role probed against every baseline read surface (exactly the designated roles admit; refusals are `forbidden`, proving assurance ≠ authorization), single-factor `mfaRequired` everywhere, ONE stale operations bearer reading every operations surface while `stepUpRequired`-refused on a mutation of EVERY W3 mutation family with zero state change, provider↔admin disjointness in both directions, cross-org fail-closure, and admin-claiming Cognito claims worth nothing. **Admin frontend:** **AD-17 `/access`** — assignments with dual-control identities and state filter, the five canonical roles only (no superadmin is expressible anywhere), request/approve/deny/revoke with CAS, the DISTINCT dual-control refusal surfaced verbatim, the finance-capable split rendered truthfully (finance/access_admin rest at `requested`; other roles activate on request — the certified B2-5 truth, mirrored exactly in fixtures), auditors read-only with a truthful explanation (ports never asked to mutate), and the step-up seam; **AD-18 `/audit`** — the read-only bounded trail with server-side filters and keyset Load more; **NO pending placeholder areas remain — every navigation entry is a real connected surface (test-pinned)**. **D-W3-5 REPORTED, NOT RESOLVED (W3-9 is the plan's decision point; no route left its safer certified boundary):** the complete parked `adminStepUp` inventory is **29 mutations** — role administration ×4 (request · approve · deny · revoke; approve/deny additionally dual-controlled, already exceeding step-up) · organization lifecycle ×8 (create · start-review · verify · reject · go-live · suspend · reinstate · offboard) · verification review ×3 (open round · start review · decision) · catalogue moderation ×6 (listing start/approve/request-changes · revision start/apply/reject) · taxonomy ×8 (create/patch × four entities). Every ordinary internal READ already rides the baseline. Categorization for the owner's ruling: **genuinely high-risk (recommend KEEPING `adminStepUp` — extends §10's recommendation):** go-live · suspend · offboard · org verify/reject · verification decision · revision apply/reject · listing approve/request-changes · role approve/deny/revoke · taxonomy deactivation/collection-state changes; **ordinary administrative mutations (the owner MAY relax to the baseline):** org create · org start-review · verification open-round/start-review · listing/revision start-review · taxonomy creation and label/sort/synonym edits · role request creation (finance-capable activation stays dual-controlled regardless). **The decision required: which of the 29 keep the recent-factor demand — until the ruling, ALL 29 stay on `adminStepUp`.** **W3 closeout audit (W2-12D pattern):** every W3 invariant re-proven in one pass — backend Jest **845/845 across 67 suites** (W3-3/4/5 verification, W3-6 moderation, W3-7 taxonomy, W3-8 provider-safe feedback, D-S3-3 and the content-safety fail-close, provider submit/resubmit, S4 catalogue/public/search all green; audit/outbox canonical and exactly-once through domain mutations; no new sensitive data in any API/outbox/audit payload — the audit projection is digest/context/request-id-free by construction). Carried OPERATIONAL dependencies (unchanged, all recorded): real Cognito pool + §14.E′ smokes · content safety/scanning (HARD prerequisite for live evidence review) · the real-bucket storage smoke · real mail delivery · hosting/deployment · D-W3-3 launch checklist · D-W3-6 retention. Carried FRONTEND gaps (unchanged): provider evidence-upload UX (deferred for explicit owner assignment) · revision-rejection provider feedback · the W2 provider-side member display identity gap. Production posture unchanged: admin routes fail closed in an unready production build; production verification remains inoperable by construction. **Open owner decisions after W3-9: D-W3-3 · D-W3-4 · D-W3-5 (reported above, awaiting the ruling) · D-W3-6 — none resolved here.** **Verification:** backend Jest **845/845 across 67 suites** (9 new; 1 new suite) · backend tsc/ESLint clean · db:verify 12/0, NO migration · snapshot deliberately updated (the new read + the role-read split) · **admin Jest 138/138 across 15 suites** (20 new across 2 new suites) · **admin contract 15/15 across 5 suites** (2 new: the REAL dual-control lifecycle through the live transport with the database's own dualControlViolation + the audit explorer reading the real trail of those very actions; the §8 authority matrix) · **admin Playwright 29 passed / 37 by-design skips** (evidence `artifacts/admin-w3-9/`) · admin build/tsc/lint green · portal contract **70/70** re-proven (Provider Portal source untouched). **W3 is COMPLETE pending the owner's closeout review. No W3-10 exists in the plan; the next workstreams (bookings/payments domains, docs/23 §16) are separate owner-approved plans.**
