# 35 — W4 Backend Slice 6: Fulfillment, Entitlements & Attendance — Workstream Plan (S6-0)

**Status: S6-0 OWNER-APPROVED and CLOSED at `3b7d770` (2026-08-26). The full corrected design is preserved through its lineage: initial plan `17dfd88` → correction round 1 `861f7bc` (Corrections 1–10: full commercial-spine target generalization) → correction round 2 `d866799` (Corrections A–D: entitlement-bound reservation trail; zero-price acquisition non-payment boundary) → final lock-order correction `3b7d770` (entitlement commitment created under the Entitlement lock; canonical lock hierarchy). ALL D-S6 rulings in this document are BINDING. No migration, backend code, portal code, app code, or docs/24 change existed at S6-0 closure — docs/24 Amendment A4 and migration `0017` are owned by S6-1. S6-1 (Commercial Target, Fulfillment Revision, Entitlement Foundation & Acquisition) is AUTHORIZED; S6-2, S6-3, W2-13, RI-4, and RI-5 do NOT start.** Accepted at round 2 and NOT reopened here: generalized `price_quote.commercial_shape` · Booking-vs-EntitlementPurchase PaymentIntent targeting · target-neutral D-W5-7 economics · the EntitlementPurchase state-machine direction · the late-success invariant · finite reservation commitment semantics · derived `used/remaining/reservedUpcoming/availableToReserve` · the credential security design · the recurring-schedule immutable snapshot · D-S6-2/3/4 · the two-migration/three-slice decomposition. Owner rulings recorded at the review: **D-S6-2 RESOLVED** (supersede/drop the empty `package_entitlement` in `0017`; never authoritative; must not coexist as a second entitlement truth; down-migration may restore the empty legacy structure) · **D-S6-3 RESOLVED** (additive `membership` price kind approved as commercial/product vocabulary ONLY; finite/unlimited, uses, validity, reservation and walk-in behavior live in the fulfillment configuration, never in `price_kind`) · **D-S6-4 RESOLVED** (V1 entitlement validity starts at successful acquisition confirmation — `30 days` runs from confirmation; fixed-date products keep the immutable configured end date; first-use activation deferred; later product edits never rewrite purchased validity) · **D-S6-1 DIRECTION RESOLVED** (exactly one commercial target — capacity Booking or unit-less EntitlementPurchase; no second payment stack) **with the required correction: the target generalization must be complete from quote → purchase/payment → trusted financial result → fulfillment, not only at `payment_intent` — §4–§5 of this revision are that complete specification.** This plan still discharges the D-RI-1 obligation: the full spine evolution is specified here and STOPPED before any migration; owner approval of this corrected plan authorizes S6-1 only.

---

## 1. Roadmap/workstream naming — reconciled, confirmed

Verified against docs/23, docs/24, docs/32, docs/33, docs/34, and HANDOFF: docs/23 §3 defines workstreams W1–W7 and contains **no numbered W4 slice list** (its W4 narrative is "identity → catalogue → booking/capacity → payments with W5 → payouts/refunds"); attendance appears in docs/23 only as W2 portal scope (PP-11) and §7 role authority. docs/24 §13's *specification* slice numbering (where 6 = "Sessions & availability") was superseded by the **executed** convention — docs/26/27/28/32 = Backend Slices 2–5, with docs/24 §13's slices 5–7 folded into executed Slice 5. The executed convention therefore makes this domain **W4 Backend Slice 6 (S6) — Fulfillment, Entitlements & Attendance**, exactly as docs/34 §5 derived ("no new workstream name invented"), and docs/34 §5 already commits the plan document by name: **S6-0 authors `docs/35`** — this file. S6 activates under docs/23 §16 with the docs/20–22 planning pattern; every implementation slice keeps implement → certify → commit → **STOP** → owner review.

## 2. Authority baseline — what S6 consumes, and what stands in its way

**Reused unchanged (frozen/certified):** the Slice-5 capacity/booking authority (units `session`/`camp_week`/`enrolment_cohort` with counter invariants and shared transition trigger; `capacity_hold`; `booking` with `ck_booking_one_unit`; `confirmFreeBooking`/`confirmPaidBooking`/`confirmCore`; global lock order idempotency-key → unit → hold → booking) · the certified W5 payment core (`startPaidCheckout`, `payment_intent`/`payment_attempt`/`payment_transaction`/`gateway_event`, webhook ingress + `ingestGatewayDelivery`, `processTrustedPaymentResults`/`runPaymentResultSaga`, ledger-shape compensation, `sweepLapsedPaidCheckouts`, `customerPaymentStatus`) · D-W5-7 commission economics (`organization_commission_term`, `payment_intent_economics`, `computeCommissionSplit` round-half-up integer fils) · the generic `idempotency_key` infrastructure (`runIdempotent`, scope-unique `(principal_ref, endpoint_scope, idempotency_key)`, snapshot replay) · audit/outbox discipline (`appendAuditEvent`, `appendOutboxEvent` typed `Trx`-only, advisory-lock per-aggregate sequencing, `inbox_event` consumer dedup, ids-and-machine-facts-only payloads) · `recurring_schedule` (purely temporal, never owns inventory) · provider capability registry + `staff_membership` role/branch-scope model · D-10 trial redemption.

**Absent (S6 creates):** any purchase aggregate for unit-less sales; any entitlement domain with real semantics; any credential/check-in/redemption object; any attendance authority (docs/24 §3.5 `AttendanceRecord` is specified but nothing exists and its shape needs amendment — §18); any calendar read.

**Structural locks that gate S6 (each amended only by the owning-slice pattern, at its owning S6 slice):** `program-schema.test.ts` forbidden-table lock (`attendance_record`, any `*redemption*` besides `trial_redemption`) and catalogue-column hygiene lock · `booking-customer-routes.test.ts` exact customer-route lock + URL regex ban · reserved-capability locks (`attendance.manage`, `roster.view`) · `booking-capacity-schema.test.ts` exact-12-S5-1-tables and append-only-grant assertions (names `package_entitlement`) · `src/db/migrations.ts` expected-object manifest (`db:verify`). The W5 locks that must remain UNAMENDED in meaning: `confirmPaidBooking` HTTP-unreachability source locks and the no-`confirm-paid|payment-success|capture|refund|commission`-URL lock — S6 only EXTENDS the same ban to `confirmPaidEntitlementPurchase`.

## 3. Binding V1 product model and the pricing/fulfillment separation (D-S6-3 RESOLVED)

Owner-confirmed V1 forms: **(A)** single scheduled session — certified Booking; S6 adds check-in fulfillment where a product wants it. **(B)** finite multi-use package — purchase once, no upfront occurrence selection, `purchased → used → remaining`. **(C)** limited-use membership (8 visits / 30 days). **(D)** unlimited time-based pass — a semantic mode, never a fake counter. **(E)** recurring scheduled program — occurrences belong to the calendar; attendance only on actual check-in. **(F)** automatic recurring billing — NOT V1 (manual re-purchase; docs/24 §14.B5 untouched).

**Vocabulary (per D-S6-3):** `program_price_option.kind` gains exactly ONE additive value, **`membership`** (commercial/product vocabulary only — revises docs/09 §21.14 as D-RI-2 anticipated). Form B stays `package` (certified `sessions_count` = the finite total, publicly served); forms A/E stay `dropIn`/`free`/`monthly`/`term` on the Booking spine. **All fulfillment semantics live in the fulfillment configuration, never in a price enum** — and, per Correction 1/9, that configuration is **immutable-revisioned** so quotes and entitlements can bind to exact terms:

**`price_option_fulfillment_revision`** (the fulfillment-terms authority; required for kinds `package`/`membership`, forbidden otherwise): `id` · `price_option_id` + composite FK `(price_option_id, program_id) → program_price_option (id, program_id)` · `program_id`, `organization_id` (org-composite FKs) · `revision_no` (`UNIQUE (price_option_id, revision_no)`) · `usage_kind ∈ {finite, unlimited}` (CHECK: `package` ⇒ `finite`) · `uses_total integer` (CHECK: NOT NULL iff finite AND kind=`membership`; for `package` the certified `sessions_count` IS the total — no duplicate column, service-enforced equality at revision creation) · `validity_kind ∈ {daysFromConfirmation, fixedEndDate, none}` · `validity_days` / `validity_end_date` (paired CHECKs; CHECK: `unlimited` ⇒ validity_kind ≠ `none`) · `reservation_required boolean NOT NULL` · `walk_in_allowed boolean NOT NULL` (CHECK: at least one true; both true legal) · `branch_id?` (org-composite FK; NULL = all branches) · `state ∈ {active, superseded}` + partial unique one-active-per-option · `created_at`. **Rows are immutable** (the `organization_commission_term` pattern exactly: term columns frozen by trigger; only `active → superseded`; no DELETE; a provider "edit" supersedes and inserts the next revision). `uq_fulfillment_revision_id_option UNIQUE (id, price_option_id)` is the composite-FK target for quotes. Child **`price_option_fulfillment_schedule_term`** (`revision_id`, `weekday`, `start_time`, `end_time`; append-only): for schedule-bound membership products this snapshots the PROMISED pattern terms at revision creation (copied VALUES from the program's `recurring_schedule` rows — Correction 9: never a reference to the mutable table). Neither table is a catalogue-lock-scoped surface; the catalogue column-hygiene lock stays intact.

## 4. The generalized commercial spine — quote invariant first (Correction 1)

**Required quote invariant (owner-stated, adopted verbatim):** a `price_quote` describes exactly one commercial fulfillment shape. New column **`commercial_shape text NOT NULL DEFAULT 'capacityPurchase' CHECK (commercial_shape IN ('capacityPurchase','entitlementAcquisition','entitlementReservation'))`**, and `ck_price_quote_one_unit` is REPLACED by:

**`ck_price_quote_shape`** —
- `capacityPurchase` ⇒ `num_nonnulls(session_id, camp_week_id, cohort_id) = 1` AND `option_kind IN ('dropIn','monthly','term','camp','free')` AND `fulfillment_revision_id IS NULL` AND `entitlement_id IS NULL` — today's shape, byte-identical semantics;
- `entitlementAcquisition` ⇒ `num_nonnulls(…) = 0` AND `option_kind IN ('package','membership')` AND **`fulfillment_revision_id IS NOT NULL`** AND `entitlement_id IS NULL` — the unit-less product quote ("10 massages for AED 800" has no Session/CampWeek/Cohort and gets none: no fake capacity, ever);
- `entitlementReservation` ⇒ `num_nonnulls(…) = 1` AND `option_kind IN ('package','membership')` AND `total_fils = 0` AND `fulfillment_revision_id IS NULL` AND **`entitlement_id IS NOT NULL`** — the S6-3 zero-priced occurrence reservation is BOUND TO THE EXACT PURCHASED ENTITLEMENT (Correction A): Ahmed reserving Wednesday 18:00 against `E1` produces a quote anchored to `E1`, never a generic zero-price quote that merely happens to describe Pilates.

New columns/FKs: `fulfillment_revision_id uuid` + composite FK `(fulfillment_revision_id, price_option_id) → price_option_fulfillment_revision (id, price_option_id)` — an acquisition quote is structurally bound to the exact Program, price option/Offer, participant, provider organization (all existing NOT NULL columns), AND the immutable fulfillment terms in force when the customer was quoted; a provider edit between quote and confirmation supersedes the revision but NEVER changes what this quote sells. **`entitlement_id uuid NULL`** + five-column composite FK **`(entitlement_id, account_id, participant_id, organization_id, program_id) → entitlement (id, account_id, participant_id, organization_id, program_id)`** (target unique `uq_entitlement_reservation_lineage` on `entitlement`) — a reservation quote's account ownership, beneficiary participant, provider organization, and Program lineage are provably the ENTITLEMENT'S at the row level; a foreign or mismatched entitlement id cannot produce a quote. **Reservation eligibility derives from the entitlement's purchased immutable revision (Correction A1):** the reservation branch of `requestQuote` reads terms through `entitlement.fulfillment_revision_id` — the purchased snapshot (schedule terms, branch limitation, reservation permission), NEVER the provider's currently-active product configuration; a provider who moves the public product from Mon/Wed to Tue/Thu does not silently mutate an existing customer's entitlement (an intentional change to active entitlements is the explicit future disruption policy, §12). `ck_price_quote_option_kind` gains `membership`. **No `EntitlementPurchase` id exists on the quote — the Purchase does not exist at quote time.**

**Reservation quote authorization (Correction A2 — at quote creation, all server-side):** authenticated account owns the beneficiary participant (certified check) · the entitlement belongs to that participant/account/org/program (structural via the five-column FK; cross-account/cross-participant entitlement ids are NOT-FOUND-shaped) · the entitlement exists ⇒ its purchase is confirmed; effective validity currently permits reservation · the purchased snapshot allows reservation (`reservation_required` true; walk-in-only products do not reserve) · the requested occurrence is permitted by the purchased terms (branch limitation; snapshotted schedule pattern where schedule-bound; Program lineage) · a finite entitlement is not exhausted, with `availableToReserve` as a PROJECTION at quote time — the atomic authority is reservation CONFIRMATION (§7), because another device may commit the final available use after the quote is issued.

**Existing S5 rows valid with zero rewrites:** every existing row takes the default `capacityPurchase`, has exactly one unit, has a NULL revision, and carries a capacity option kind (no `package` quote was ever creatable — no purchasable package path existed), so both new CHECKs pass without touching a row.

**`requestQuote` dispatch:** by the price option's kind. Capacity kinds → the certified path unchanged (unit required and validated, session overrides, D-6 TTL). Entitlement kinds → any supplied unit is refused (`unitNotApplicable` typed outcome); an ACTIVE fulfillment revision is required — absent ⇒ fail-closed `fulfillmentUnavailable` (the `commissionTermsUnavailable`/`policyUnavailable` precedent); participant-ownership and age eligibility run identically (program-level rules; there is no unit to override them); money stays server-authored identically (base line from the option's `amount_fils`, offer application, deferred total = Σ lines); TTL and append-only immutability unchanged.

**Cross-trail impossibility (both directions, structural):**
- *An entitlement quote can never attach to a capacity Booking:* new uniques `uq_price_quote_id_session/(camp_week)/(cohort) UNIQUE (id, <unit>_id)` become targets for THREE additive composite FKs on `capacity_hold` — `fk_capacity_hold_quote_session FOREIGN KEY (quote_id, session_id) REFERENCES price_quote (id, session_id)` (and camp-week/cohort twins; the certified `fk_booking_hold_*` MATCH SIMPLE null-skip pattern). Consequence: a hold's unit must EQUAL its quote's unit (an additive strengthening of what `claimHold` already enforced in service code), and a zero-unit `entitlementAcquisition` quote can never be held — and `booking.hold_id NOT NULL` means never booked. Existing hold rows satisfy the FKs (the service always enforced unit=quote-unit; the `0017` migration asserts it).
- *A capacity quote can never attach to an EntitlementPurchase:* `entitlement_purchase.quote_id` carries `uq_entitlement_purchase_quote UNIQUE (quote_id)` (one purchase per quote, forever — retries rejoin by idempotency snapshot) and a BEFORE INSERT trigger **`enforce_entitlement_purchase_shape()`** (the `enforce_payment_intent_amount` precedent): the referenced quote must have `commercial_shape = 'entitlementAcquisition'` and matching `account_id`/`participant_id`/`organization_id`/`program_id`/`price_option_id`/`offer_id` — a capacity or reservation quote is refused at the row level.
- *A reservation quote* feeds only the certified hold→Booking flow (its shape permits holds), is refused by the purchase trigger, and can never gain a PaymentIntent (`enforce_payment_intent_amount` refuses zero totals).

**Commercial-target continuity — the two trails, no crossing:**
`capacityPurchase quote → CapacityHold → Booking → PaymentIntent(target=Booking) → W5 trust/ledger → confirmPaidBooking` (unchanged), and
`entitlementAcquisition quote → EntitlementPurchase → PaymentIntent(target=EntitlementPurchase) → the SAME W5 trust/ledger machinery → confirmPaidEntitlementPurchase → Entitlement`.

## 5. EntitlementPurchase, the PaymentIntent target invariant, and the full W5 target audit (Corrections 2–4)

### 5.1 `EntitlementPurchase` (`entitlement_purchase`)

`id` · `account_id` · `participant_id` + composite FK `(participant_id, account_id) → participant` · `organization_id` · `program_id` (org-composite FK) · `price_option_id` (program-composite FK) · `offer_id?` · `quote_id NOT NULL → price_quote (id)` (+ `uq_entitlement_purchase_quote`, + shape trigger — §4) · `state` · `reference_code` write-once UNIQUE (own non-Booking prefix) · `expires_at timestamptz NOT NULL` (**commercial checkout-abandonment metadata ONLY — a Purchase holds no inventory and `expires_at` implies no capacity ownership**; recommended default 30 min, service-config) · `confirmed_at?` · `created_at/updated_at/version` · `uq_entitlement_purchase_id_account_quote UNIQUE (id, account_id, quote_id)` (the intent composite-FK target).

**Exact state machine (Correction 3 — trigger-enforced, identity-frozen; canonical vocabulary reused, Booking states NOT copied blindly — there is no hold, no unit, no no-show here):**
- **Creation states (Correction B refinement):** a NONZERO acquisition inserts the Purchase as `pending_payment` beside its intent; a ZERO-PRICE acquisition inserts Purchase + Entitlement atomically AS `confirmed` (with `confirmed_at`/`reference_code`; a confirmation-facts CHECK — the `ck_booking_confirmation_facts` pattern — makes a `confirmed` row without them impossible). **No externally observable zero-price `pending_payment` state exists**, not even transiently on the wire;
- `pending_payment` — paid acquisition initiated;
- `pending_payment → confirmed` — acquired; the entitlement row is created in the same transaction (the trusted saga is the ONLY caller — the free boundary never passes through `pending_payment`, §5.5);
- `pending_payment → payment_failed` — the gateway definitively failed the attempt series (saga authority); `payment_failed → pending_payment` re-entry for a retried checkout (W5-5 replay semantics);
- `pending_payment | payment_failed → expired` — abandoned: the wind-down sweep terminalized a purchase whose intent had **no capture posting**; no financial success occurred, nothing is owed;
- `pending_payment | payment_failed → compensated` — genuine captured money could NOT legally produce the entitlement; the same-amount `reversal` posted (§5.4); NO entitlement exists, NO settleable commission or provider payable;
- terminals `confirmed`, `expired`, `compensated` frozen.

### 5.2 The exact PaymentIntent commercial-target invariant (D-S6-1 structure)

Migration `0017`: `payment_intent.booking_id` and `payment_intent.hold_id` drop NOT NULL (columns, plain FKs, and the 4-column composite `fk_payment_intent_booking` retained verbatim); new nullable `purchase_id uuid` + `fk_payment_intent_purchase FOREIGN KEY (purchase_id, account_id, quote_id) REFERENCES entitlement_purchase (id, account_id, quote_id)`; **`ck_payment_intent_one_target CHECK (num_nonnulls(booking_id, purchase_id) = 1)`**; **`ck_payment_intent_target_shape CHECK ((booking_id IS NULL) = (hold_id IS NULL))`** (a Booking target always carries its hold; a Purchase target never does); mirrored partial uniques `uq_payment_intent_live_purchase ON (purchase_id) WHERE state IN ('created','in_progress')` and `uq_payment_intent_succeeded_purchase ON (purchase_id) WHERE state = 'succeeded'`. Every existing row satisfies both CHECKs with zero rewrites; every Booking-path constraint, trigger, and behavior is byte-compatible.

### 5.3 Complete target-dependent W5 audit (Correction 2 — every place reconciled)

| W5 surface | Today | Target-neutral evolution |
|---|---|---|
| `payment_intent` | Booking-bound composite FK | §5.2 — one-target CHECK + per-target composite FKs; Booking trail untouched |
| `payment_intent_economics` | table target-agnostic; org truth via Booking | table UNCHANGED; `enforce_economics_org_binding()` gains the purchase branch — **required invariant: an intent and its economics snapshot can never belong to one organization while its Booking/Purchase target belongs to another** — enforced by the trigger resolving org through the intent's ACTUAL target (`booking.organization_id` | `entitlement_purchase.organization_id`), with the commission-term composite FK `(commission_term_id, organization_id)` continuing to pin the term to that same org; D-W5-7 economics/rounding/fail-closed rules unweakened |
| quote binding | 4-col composite FK shares `quote_id` with Booking | purchase trail: 3-col composite FK shares `quote_id` with the Purchase; both trails prove intent.quote ≡ target.quote structurally |
| account binding | composite FK shares `account_id` | same on both trails (the composite FKs above) |
| idempotency scopes | `booking.initiate`/`booking.confirm.free`/`booking.confirm.paid` | untouched; new sibling scopes `entitlement.purchase.initiate` / `entitlement.confirm.free` / `entitlement.confirm.paid`; provider-side keys already target-neutral (`cs:<accountId>:<key>`, `himma:checkout:<intentId>:<seq>`, `saga:<intentId>`) |
| one-live / one-succeeded intent | partial uniques on `booking_id` | retained; mirrored on `purchase_id` (§5.2) |
| checkout orchestration | `startPaidCheckout` assumes Booking+hold | generalizes over a resolved commercial target: Booking branch byte-identical (hold-validity gate incl. D-W5-5); Purchase branch gates = purchase `pending_payment`/re-enterable + quote shape/TTL + active-window check; identical fail-closed `commissionTermsUnavailable`, identical economics snapshot in the same transaction |
| payment-status reads | `customerPaymentStatus` resolves Booking→intent | projection logic (intent state + ledger shape) is already target-neutral; "confirmed" dispatches on the target's state; purchase-status route resolves Purchase→intent; D-RI-5 vocabulary/copy unchanged |
| webhook attempt resolution | `gateway_ref`/`himmaIntentRef` → attempt → intent | zero change — never Booking-aware |
| W5-4 saga dispatch | `loadContext` reads `intent.booking_id/hold_id` | reads the target discriminant: Booking target → the certified path invoking **`confirmPaidBooking` unchanged** (target dispatch lives OUTSIDE it); Purchase target → `confirmPaidEntitlementPurchase` (§5.4); money corroboration via `inspectPayment` identical |
| compensation obligation detection | capture posting + no confirmed Booking + no reversal | restated target-neutral: capture posting + target not `confirmed` + no reversal ⇒ compensation required; reversal path identical; purchase additionally CASes to its explicit `compensated` terminal when the reversal posts |
| wind-down sweep | `sweepLapsedPaidCheckouts` joins `capacity_hold` | Booking branch unchanged; purchase branch analogue selects capture-less live intents whose purchase window lapsed → attempts errored, intent `expired`, purchase `expired`; **a captured intent is never swept** (capture-posting guard), preserving §5.5 |
| reconciliation / stuck-state | `reconcileLedgerAgainstProvider` (ledger-based); `findStuckPaymentStates` | reconciliation already target-neutral; stuck-state diagnostics extended to join both targets (a stuck purchase-intent is as visible as a stuck booking-intent) |
| audit/outbox | intent events on `payment_intent` aggregate; booking events on `booking` | intent aggregate unchanged and target-neutral; purchase lifecycle events on the `entitlement_purchase` aggregate (§15); no payload ever assumes a Booking |
| structural/source locks | `confirmPaidBooking` never imported by `http/`; URL regex ban | both bans retained verbatim and EXTENDED to `confirmPaidEntitlementPurchase`; route/capability/table locks amended only at their owning S6 slice |

### 5.4 `confirmPaidEntitlementPurchase` — the entitlement success seam, and the exact V1 late-success rule (Correction 4)

Invoked ONLY by the W5-4 saga (never imported by any `http/` module; same source-lock family as `confirmPaidBooking`); `runIdempotent` scope `entitlement.confirm.paid`, `principalRef 'system:payments'`. **Binding rule: payment success never grants permission to violate an entitlement-domain invariant.** At trusted financial success (verified signed evidence + provider inspection corroborating captured amount — the certified steps, unchanged), under the purchase row lock:

**CONFIRM** iff ALL hold: (1) the purchase exists and is in `pending_payment`/`payment_failed` (an already-`confirmed` purchase replays its snapshot — converged, no duplicate entitlement: `entitlement.purchase_id UNIQUE`); (2) reciprocal identity — `intent.purchase_id/quote_id/account_id` match the purchase row (composite FK + runtime `reciprocalMismatch` check, the `confirmPaidBooking` discipline); (3) the participant target is still valid under canonical acquisition rules — the participant row is `active` and still belongs to the purchasing account; (4) the quote/revision snapshot is intact (structurally guaranteed by immutability; fail-closed if violated). A purchase whose `expires_at` has passed but which is still non-terminal CONFIRMS on genuine capture — there is no scarce-inventory race, and the window is abandonment metadata (§5.1), not capacity ownership.

**COMPENSATE** (same-amount `reversal` through the existing machinery; purchase → `compensated`; no entitlement; no settleable commission) iff any certified invariant refuses fulfillment: the purchase is already TERMINAL (`expired` — swept before capture; terminals are never resurrected, the W5 discipline) · the participant is archived or no longer belongs to the account · reciprocal/commercial-target mismatch or duplicate-target anomaly · any entitlement-domain invariant failure at creation. **Deliberately NOT in the refusal set (no invented semantics):** program/option/provider state changes after quote — no canonical rule treats them as blocking already-started purchases, and the immutable revision means later edits cannot alter what was sold; a future provider-revocation/disruption policy would be its own owner-ruled feature.

### 5.5 Zero-price entitlement acquisition — the non-payment boundary (Correction B), and commission

A provider may genuinely offer a free entitlement product (e.g., a free introductory 3-use package). Its acquisition NEVER creates payment state — no PaymentIntent, no Stripe Checkout, no payment transaction, no economics row, no observable `pending_payment` — merely because paid acquisition uses `EntitlementPurchase`. The zero-price analogue of the certified S5 rule is **`confirmFreeEntitlementPurchase`** (`runIdempotent` scope `entitlement.confirm.free`, customer principal), which in ONE transaction: validates the fresh authoritative `entitlementAcquisition` quote (shape guard + TTL + `total_fils = 0` — a nonzero quote is refused `notFreeQuote`) · validates participant/account ownership and participant activeness · validates the quote's immutable fulfillment revision · creates exactly one `EntitlementPurchase` inserted AS `confirmed` (§5.1) · creates exactly one `Entitlement` with validity snapshotted from this acquisition confirmation (D-S6-4) · emits the audit/outbox set exactly once · replays its snapshot on any same-key retry. The certified zero-total trigger independently refuses any PaymentIntent for such a quote — **zero money never enters the payment stack (D-W5 behavior preserved), and no commission/settlement event arises from nonexistent money**. D-10 `freeTrial` is not repurposed.

**Idempotency + quote single-use (Corrections B1/B2):** a same-key storm on free acquisition yields exactly one Purchase, one Entitlement, one audit/outbox effect set; an ambiguous-transport retry with the same key returns the same snapshot (proof 19). One `entitlementAcquisition` quote can never yield multiple confirmed Purchases/Entitlements through double-click, concurrent requests, free-path retry, paid-path retry, or a free/paid cross-path attempt: `uq_entitlement_purchase_quote UNIQUE (quote_id)` is the structural authority (both the free boundary and paid initiation INSERT the purchase row referencing the quote — the second path, whichever it is, collides and is refused with a typed outcome or replays its own idempotent snapshot; proof 20).

**Commission (unchanged D-W5-7):** the generalized `startPaidCheckout` snapshots economics once at PAID initiation — AED 1,000 at the 1200-bps launch term ⇒ 120/880, immutable; **a package/membership earns commission exactly once, when sold**; redemption/attendance touches no payment table and emits no commission event; payouts remain future work.

## 6. Entitlement — participant-owned, revision-snapshotted, finite/unlimited explicit (D-S6-4 applied)

**`entitlement`** (append-only: `forbid_mutation` + SELECT/INSERT-only grants; created complete at acquisition confirmation): `id` · `purchase_id UNIQUE NOT NULL → entitlement_purchase (id)` · `account_id` · `participant_id` (+ composite FK — structural participant ownership: Ahmed's 10 swimming sessions are Ahmed's; cross-account and cross-participant substitution impossible by FK shape; no transferable/family package in V1) · `organization_id` · `program_id`, `price_option_id` · **`fulfillment_revision_id NOT NULL`** + composite FK to the IMMUTABLE revision (§3) — the terms authority; because revisions are append-only frozen rows, referencing one IS a snapshot (no mutable row is ever the historical authority) · computed validity instants **`valid_from timestamptz NOT NULL`** (= acquisition `confirmed_at`, D-S6-4) and **`valid_until timestamptz`** (`daysFromConfirmation` ⇒ `confirmed_at + days`; `fixedEndDate` ⇒ the immutable configured date; NULL only for the explicitly-configured finite no-expiry case) · denormalized-for-derivation `usage_kind` + `uses_total` (copied from the immutable revision/`sessions_count`; CHECK-consistent) · `branch_id?` · `created_at`. Later provider edits create NEW revisions for FUTURE sales; purchased validity and terms are never rewritten.

**Finite vs unlimited are explicit modes.** Finite consumption truth is append-only attendance (§10): `used = COUNT(attendance_record WHERE entitlement_id = …)`; no mutable used-counter exists as authority; derived counters may appear in projections but history is always reconstructable. Unlimited: NO remaining value of any kind; attendance still records every visit. Effective status DERIVED: `active` / `expired` (past `valid_until`) / `exhausted` (finite, fully used); the final-use redemption emits `entitlement.exhausted` in-transaction (an authoritative transition); no sweep is needed for correctness.

## 7. Reservation against finite entitlements — commitment semantics (Correction 5)

`reservation_required` / `walk_in_allowed` stay independent product booleans (§3). **Walk-in use:** credential straight against the entitlement (§9). **Reservation-required use:** the customer reserves a future eligible occurrence via the CERTIFIED capacity flow — an `entitlementReservation` quote (§4: unit-bound, zero-total, ENTITLEMENT-bound) → hold → Booking — plus the S6 subtype **`entitlement_reservation (booking_id PK → booking, entitlement_id → entitlement, organization_id org-composite, created_at)`** created in the same confirming transaction. **No second capacity system exists** — Session scarcity stays entirely S5's.

**`confirmEntitlementReservation` — the dedicated reservation confirmation authority (Correction A3).** An `entitlementReservation` quote does NOT fall through `confirmFreeBooking` as if it were a provider's free activity. The command is a distinct internal domain boundary (`runIdempotent` scope `entitlement.reservation.confirm`, customer principal) whose authority REQUIRES the Entitlement as a first-class input; it reuses certified S5 primitives internally (hold validation, `confirmCore` counter/consumption mechanics) and in ONE transaction establishes: quote still valid, `entitlementReservation`-shaped, and quote.entitlement_id ≡ the commanded entitlement · account/participant ownership (certified checks + the §4 five-column FK) · the exact entitlement effectively active and reservable under its PURCHASED snapshot · **`availableToReserve > 0` under the entitlement lock** (the atomic authority — quote-time projections grant nothing) · capacity-unit availability via the live hold (certified truth) · the capacity Booking + the `entitlement_reservation` subtype row · audit/outbox exactly once. **No PaymentIntent. No Stripe. No attendance. No usage consumption.**

**Canonical lock hierarchy (round-3 FINAL — stated in terms of actual contested ROW locks; `entitlement_reservation` is NOT a lock-order stage):**

> **idempotency key → capacity unit → hold → booking → credential → entitlement**

Every command takes an order-compatible subset of that hierarchy; MUTATIONS are ordered separately, after the relevant locks:

- **Reservation creation:** locks `key → unit FOR UPDATE → hold → booking → entitlement FOR UPDATE`; **then, with the Entitlement lock HELD: recompute the authoritative finite commitment state, verify `availableToReserve > 0`, and only then INSERT the `entitlement_reservation` commitment**, then audit/outbox, then commit. The persistent commitment row is NEVER inserted before the Entitlement lock is acquired — correctness never depends on transaction-snapshot or self-row visibility of provisional commitment rows. The Booking/capacity mutations earlier in the same transaction are provisional and roll back atomically when the Entitlement has no reservable use.
- **Walk-in redemption:** locks `key → credential FOR UPDATE → entitlement FOR UPDATE`; then the attendance INSERT + credential consumption CAS.
- **Reserved-use redemption:** the Booking row needs NO lock — check-in mutates only the credential and inserts attendance (the Booking's state is untouched in V1), so the reservation/Booking/occurrence rows are VALIDATED BY READ under the already-serialized credential + entitlement locks; the sequence stays `key → credential → entitlement` and no path anywhere takes `credential → booking`, so no reverse-Booking-lock hazard exists.
- **Release / occurrence conclusion:** the derived-commitment model needs no Entitlement lock (statuses derive from committed truth, §7 lifecycle); hold release/expiry and unit sweeps keep their certified S5 lock orders and never touch entitlement locks. Any FUTURE release mutation that genuinely needs both rows must take `booking → entitlement`, never the reverse.

**Isolation requirement (binding on S6-3 implementation):** under the project's READ COMMITTED model, the commitment-count query MUST execute after the Entitlement row lock has actually been acquired, as a subsequent statement with a fresh statement snapshot — a transaction that waited on the Entitlement lock thereby observes the prior holder's committed reservation; the design must never fold the availability read into a pre-wait stale snapshot.

**Final-credit two-transaction proof (pinned as a real PostgreSQL multi-connection proof in S6-3 — proof 11):** entitlement `totalUses = 1`; T1 and T2 concurrently reserve two DIFFERENT Sessions. Both independently acquire their different capacity-unit/hold/booking locks. At the shared Entitlement: T1 acquires the Entitlement lock, recomputes `used + reservedUpcoming` fresh, sees one reservable use, inserts its reservation, commits; T2 then acquires the same lock, performs a FRESH authoritative commitment query after lock acquisition, observes T1's committed reservation, computes `availableToReserve = 0`, refuses (`entitlementFullyCommitted`), and rolls its provisional Booking/capacity mutations back atomically. Result: exactly one reservation exists and exactly one capacity Booking commits — never by depending on seeing an uncommitted reservation. Proof 21 exercises the full reserve/release/check-in/expiry interleaving matrix against this hierarchy.

**Structural non-crossing (Correction A4 — no zero-price escape hatch around commitment accounting):** `confirmFreeBooking` gains a shape guard refusing any quote with `commercial_shape ≠ 'capacityPurchase'` (bounded owning-slice amendment; every historical quote IS `capacityPurchase`, so certified behavior is unchanged) — an `entitlementReservation` quote submitted there is refused (proof 15) · `confirmEntitlementReservation` refuses any quote with `commercial_shape ≠ 'entitlementReservation'` — a normal zero-price capacity quote cannot enter it (proof 14) · an acquisition quote can never become a reservation and vice versa (`ck_price_quote_shape` — shapes are insert-frozen on the append-only quote) · a reservation quote can never create an `EntitlementPurchase` (the §4 purchase shape trigger) and never a PaymentIntent (zero-total trigger) · one reservation binds exactly one Entitlement and one Booking/occurrence (`entitlement_reservation.booking_id` PK + single `entitlement_id` + the Booking's certified exactly-one-unit) · knowing another customer's Entitlement id conveys NO authority (five-column ownership FK + not-found shaping; proofs 17–18).

**Commitment vocabulary (owner-stated, adopted):** `used` = successfully redeemed attendance (append-only count). `reserved/committed` = a finite entitlement unit temporarily allocated to a future scheduled use, not yet consumed. `remaining` (historical) = `uses_total − used`. `availableToReserve` = `uses_total − used − activeFiniteReservations`. **A reserved credit is never mislabeled attended.**

**Reservation lifecycle — stored truth vs derived status (Correction C; no mutable duplicated status column exists):** STORED truth is exactly: the append-only `entitlement_reservation` row · the Booking's certified state · the occurrence's times on its unit row · append-only `attendance_record`. From those four, the lifecycle statuses are DERIVED deterministically — **`reservedUpcoming`** (Booking `confirmed` ∧ occurrence not concluded ∧ no attendance) · **`fulfilled`** (attendance record exists) · **`released`** (Booking `cancelled_by_customer`/`cancelled_by_provider`, where such authority exists) · **`concludedNoShow`** (Booking `confirmed` ∧ occurrence concluded ∧ no attendance — consumes nothing in V1). `availableToReserve` therefore never depends on frontend interpretation: **`activeFiniteReservations`** = COUNT of reservations in `reservedUpcoming`. Consequences, each an owner-required invariant: check-in against a reservation atomically converts commitment → consumption (the single attendance INSERT flips the row from the reserved-count into the used-count — atomic by construction); cancellation of a reservation Booking (any future cancellation authority) restores availability derivationally; **a no-show does not consume the use in V1** — a concluded occurrence with no attendance leaves `used` unchanged and drops out of the commitment count (availability restored derivationally; no-show release *policy/penalties* stay deferred with the owner, and the schema forces no consumption).

**Concurrency (owner example: 1 Pilates use remaining must not be reservable Mon+Wed+Fri):** reservation creation computes `availableToReserve` under the **entitlement row lock** and refuses at ≤ 0 (`entitlementFullyCommitted` typed outcome); two sessions racing the final available unit → exactly one reservation succeeds (proof 11, §17); the structural backstop invariant is *active commitments + used can never exceed `uses_total`*. **Lock ordering:** governed by the canonical round-3 hierarchy below — the entitlement lock is always last, the commitment insert happens under it, and no S6 transaction ever takes a unit/hold/booking/credential lock after an entitlement lock (no new deadlock or oversell path).

## 8. Customer-facing "remaining" vocabulary (Correction 6)

The customer projection exposes the four distinct truths, never one collapsed number: for a pack of 5 with 1 attended and 2 future reservations — **`used` = 1 · `remaining` = 4 · `reservedUpcoming` = 2 · `availableToReserve` = 2**. The simple entitlement card may display "4 remaining"; booking/reservation UX consumes `availableToReserve` as the authoritative reservable availability. All four are derived (§6/§7); unlimited products expose none of them (validity + active/expired only).

## 9. Redemption credential — target model and server authority (Corrections 7–8)

**Canonical targets (exactly one, structural CHECK `num_nonnulls(entitlement_id, booking_id) = 1`), with unambiguous consumption authority:**
- **Single Booking** (no reservation subtype): `booking_id` target (+`session_id` occurrence) — check-in against that exact booked occurrence; consumes nothing finite;
- **Reserved entitlement use:** `booking_id` target where the Booking IS an `entitlement_reservation` — redemption resolves the reservation row and consumes the committed unit (attendance records BOTH `booking_id` and `entitlement_id`); a Booking either has the subtype row or not, so the consumption authority is never ambiguous;
- **Walk-in entitlement:** `entitlement_id` target — direct consumption where no reservation is required.

**Server authority (Correction 8 — the short numeric string is NEVER the underlying authority):** the canonical authority is a **high-entropy opaque credential token** (≥128-bit server random), returned once at issuance and stored only as `token_digest` (SHA-256) — the future QR payload encodes this opaque token, so QR arrives with zero domain redesign. The **numeric code is a short-lived human-enterable ALIAS**: stored only as `alias_digest`, scoped to the issuing organization among LIVE credentials (partial unique `(organization_id, alias_digest) WHERE state='live'`); provider lookup = caller's org + alias digest over live rows only — **no global enumeration surface exists**; the raw token and raw alias are never logged and never appear in audit/outbox payloads (credential UUID only). **Alias length — analyzed, not assumed:** the guessing surface is authenticated provider staff scoped to their own org's live credentials; success probability per attempt ≤ liveCredentialsInOrg / 10^N; V1 default **8 digits** (displayed 4-4; config) with service-layer attempt bounding per staff principal + organization (typed lockout outcome + audit past the threshold) — 6 digits was rejected as insufficient margin, and the analysis, limits, and length knob are recorded rather than "probably enough". No repository constraint forces an owner ruling here.

**Row:** `id` · `token_digest` · `alias_digest` · target columns above · `session_id?` · `account_id`, `participant_id` (composite FK) · `organization_id`, `branch_id?` · `issued_at` · `expires_at` (**TTL 10 min default**, server-config — owner-ratified) · `state ∈ {live, used, superseded, expired}` · `used_at?`, `redeemed_by_staff_membership_id?` (org-composite FK) · `created_at/version`; at-most-one-live-per-target partial uniques. **Lifecycle (binding):** issuing consumes nothing; expiry consumes nothing; regenerate freely after expiry; successful use is permanently terminal. **Regeneration = supersede-not-replay** (one live credential per target at any instant; screenshots of old codes die on regeneration). **Generation windows:** scheduled targets −60/+60 min around start (config; never days early); walk-in targets while the entitlement is effectively active, the participant eligible, and `availableToReserve`-independent `remaining > 0` where finite.

## 10. Atomic redemption + the attendance canonical model

**`attendance_record`** (append-only: `forbid_mutation` + SELECT/INSERT-only grants; NEVER a mutable counter as truth): `id` · `organization_id`, `branch_id?` · `participant_id`, `account_id` (composite FK) · `entitlement_id?` / `booking_id?` (≥ 1; both for reservation check-ins — the retained fulfillment-target history Correction 7 requires) · `session_id?` · **`credential_id NOT NULL UNIQUE → redemption_credential (id)`** (structural single-use) · `validated_by_staff_membership_id` (org-composite FK) · `occurred_at` · `source ∈ {numericCode, qr}` (V1 reaches only `numericCode`) · `created_at`.

**Atomic redemption transaction:** resolve live credential by org + alias/token digest → lock credential `FOR UPDATE` → verify live/unexpired/org/branch scope/staff capability → resolve the consumption authority (§9 target rules) → lock the entitlement `FOR UPDATE` where consumption applies → verify effective validity + `remaining > 0` where finite (the COUNT runs as a fresh statement AFTER the entitlement lock is held — the §7 READ COMMITTED isolation requirement) → verify scheduled window where session-bound → INSERT `attendance_record` → CAS credential `live → used` → audit + outbox. Two staff, one credential → row lock serializes, exactly one success (proof 1). Two credentials racing the final finite use → entitlement lock serializes, remaining never < 0 (proof 2). Lock order per §7. **Corrections/fraud posture:** V1 ships no correction workflow; append-only design keeps audit-safe reversal records possible later; docs/24 §3.5's `expected/no_show/excused` marking is deferred (§18); no-show consumption deferred (§7).

## 11. `package_entitlement` — superseded and dropped (D-S6-2 RESOLVED)

Verified truth: booking-subtype PK, `sessions_total/used` with bounds CHECK, valueless `expiry_policy`, append-only trigger + INSERT-only grants (so `sessions_used` is un-incrementable by two independent locks), **zero service references, provably empty**. Inadequacies: booking-anchored (the exact coupling D-RI-1 forbids) · mutable used-counter as sole truth · no participant/org binding, validity, reservation, or unlimited semantics · valueless expiry text. **Owner-ruled disposition: superseded/dropped in `0017`** — it has never been authoritative and must not coexist as a second entitlement truth; no data movement (empty); down-migration restores the empty legacy structure; `db:verify` manifest, the S5-1 twelve-table lock, and append-only-grant assertions amended by the owning-slice pattern; docs/24's row amended to record the supersession (§18).

## 12. Recurring membership and the unified calendar (derived read; immutable purchased schedule — Correction 9)

**Entitlement validity ≠ calendar occurrences**, and per Correction 9 a schedule-bound membership's PROMISED pattern is **immutably identifiable for the purchased entitlement**: the entitlement's immutable fulfillment revision carries the snapshotted `price_option_fulfillment_schedule_term` rows (§3 — pattern TERMS copied at revision creation, chosen over a revision-id into the mutable `recurring_schedule` table, which has no revision mechanism). The customer's purchased calendar derives from that snapshot ∩ validity; later provider edits to `recurring_schedule` change only future revisions/sales and can never silently rewrite an existing customer's purchased calendar. Where the provider generates real Sessions matching the pattern, those real occurrences (and the customer's actual reservations) dominate the display. **If schedule changes are ever intended to affect ACTIVE memberships, that is an explicit future disruption/change policy (owner-ruled, the D-5 family) — never accidental mutability.**

**Calendar authority:** a derived customer READ (`GET /customer/calendar?from&to`, S6-3) combining confirmed Session Bookings · CampWeek spans · Enrolment/Cohort pattern expansion (`enrolment_cohort_schedule → recurring_schedule` ∩ cohort range, minus exceptions — the Booking-spine trail keeps reading its certified sources) · membership occurrences from the entitlement's immutable schedule snapshot ∩ validity · actual reservations (they are Bookings). **A purchased unscheduled 10-massage package contributes zero calendar dates.** No occurrence/calendar table exists; RI-5 is pure frontend consumption. (docs/09 §13 calendar EXPORT remains W5-integration scope.)

## 13. Customer API plan (S6-1/S6-3; cross-account ids not-found-shaped)

S6-1: entitlement-product quote via the §4 dispatch (reconciled at S6-1: widen `POST /customer/quotes` or add a sibling route — whichever amends the route lock most cleanly) · `POST /customer/entitlement-purchases/confirm-free` · `POST /customer/entitlement-purchases/initiate` (identifiers + idempotencyKey → `{purchaseId, redirectUrl, expiresAt}`) · `GET /customer/entitlement-purchases/:purchaseId/payment` (the generalized D-RI-5 projection). S6-3: `GET /customer/entitlements(/:id)` — "My Passes & Memberships" with the §8 four-truth vocabulary (finite) or validity-only (unlimited), next reserved use, check-in eligibility, attendance history where the product calls for it; NEVER commission/provider-share/ledger/policy internals, and never any credential secret after use · credential issue/supersede routes (`POST /customer/entitlements/:id/credential`, `POST /customer/bookings/:id/credential` — returns the alias + opaque token + `expiresAt` once; regeneration per §9; issuing consumes nothing) · reservation flow (the §7 certified path) · redemption observation by re-read (the payment-status-polling precedent; never optimistic) · `GET /customer/calendar`. Customers can never generate credentials for another participant/account, an unrelated provider's entitlement, an expired/exhausted entitlement, or outside a scheduled window.

## 14. Provider API plan + capability activation (the W2-13 contract)

Routine check-in is Provider Portal authority (Admin has no redemption surface). **Preview + confirm, both server-authoritative:** `POST {ORG}/check-in/preview` (alias in; pure read; consumes nothing; returns participant first name, product context, the §8 truths, eligibility verdict) → `POST {ORG}/check-in/redeem` (alias + the credential id preview returned; the server re-validates EVERYTHING atomically per §10 — a client-side preview is never redemption authority; the live credential + TTL is the short-lived server proof). Both endpoints are attempt-bounded per §9. Supporting reads: the certified `bookings.view` roster; bounded attendance/entitlement context reads at S6-2. **Smallest capability activation:** `attendance.manage` for `owner` / `org_manager` / `branch_manager` (branch-scoped) / `front_desk` (branch-scoped), per the registry's own future-activation map; `coach` only with assigned-session scoping (`session.instructor_staff_id`) for session-bound check-ins, no walk-in authority; **`roster.view` stays RESERVED**; `sessions.manage` untouched. Reserved-capability locks amended at S6-2.

## 15. Audit/outbox events (canonical mutations only; replay-safe)

Aggregates `entitlement_purchase` / `entitlement` / `redemption_credential`: `entitlement.purchase.created` / `.confirmed` / `.expired` / `.failed` / `.compensated` · `entitlement.created` · `entitlement.exhausted` (final-use redemption, in-transaction) · `credential.issued` / `credential.superseded` · `attendance.redeemed`. NOT evented: credential expiry and entitlement expiry (time-derived; no transition executes), reads, countdowns. Payloads: ids and machine facts only — never the raw alias or token, never QR material, never PII, never commission figures; same-transaction `appendOutboxEvent(Trx)` with advisory-lock sequencing; idempotent replays emit nothing twice.

## 16. Security/fraud model

Screenshot/shared code → 10-min TTL + supersede-on-regenerate + permanent single-use. Brute force/enumeration → §9 in full: opaque token authority, hashed alias scoped to org among live rows only, attempt bounding + lockout + audit, 8-digit default from analysis, no global lookup. Cross-org redemption → org-scoped resolution; foreign codes not-found-shaped. Branch bypass → staff `branch_scope` AND entitlement/credential `branch_id` enforced in-transaction. Generating for others → participant/account composite FKs + not-found shaping. Same-code reuse → `used` terminal + `attendance_record.credential_id UNIQUE`. Concurrent double redemption → row-lock serialization. Expired replay → effective-expiry under lock. Client-tampered balances → all four §8 truths server-derived from append-only records; no client value accepted. Fake QR → no V1 QR ingestion; the future path validates the opaque token digest (rendering ≠ authority). Over-reservation → §7 commitment invariant under the entitlement lock.

## 17. Concurrency proof plan (real PostgreSQL, RED→GREEN, the S5-2 harness pattern)

| # | Proof | Slice |
|---|---|---|
| 1 | Two simultaneous redemptions of one credential → exactly one attendance/`used` | S6-2 |
| 2 | Two credentials racing the final finite use → one success; remaining never < 0 | S6-2 |
| 3 | Regenerate vs redeem race → never two live, never two effects | S6-2 |
| 4 | Expiry vs redeem boundary → lapsed refused under lock; no consumption | S6-2 |
| 5 | Branch A vs branch B validation → out-of-scope staff and out-of-branch entitlement refused | S6-2 |
| 6 | Entitlement validity boundary → at/after `valid_until` refused; effective truth | S6-2 |
| 7 | Purchase idempotency storm → same key = one purchase/intent; no duplicate entitlements | S6-1 |
| 8 | Paid success vs compensation → capture-then-unconfirmable = reversal + no entitlement + no settleable commission; capture-then-confirm = exactly one entitlement | S6-1 |
| 9 | Repeated webhook/saga delivery → converged single confirmation/economics/outbox | S6-1 |
| 10 | Projections ≡ append-only truth (all four §8 counts) under concurrent load | S6-2/S6-3 |
| 11 | Final-credit two-transaction proof (§7, verbatim): `totalUses = 1`, T1/T2 reserve DIFFERENT Sessions concurrently → T2's post-lock fresh recount observes T1's committed reservation, refuses, and rolls back its provisional Booking/capacity mutations — exactly one reservation and one Booking commit (real PostgreSQL, two connections) | S6-3 |
| 12 | Reservation check-in converts commitment → consumption atomically (never double-counted) | S6-3 |
| 13 | Late-capture vs wind-down sweep race → a captured intent is never swept; a swept purchase compensates, never confirms | S6-1 |
| 14 | Normal zero-price capacity quote submitted to `confirmEntitlementReservation` → refused (shape guard) | S6-3 |
| 15 | `entitlementReservation` quote submitted to `confirmFreeBooking` → refused (shape guard) | S6-3 |
| 16 | Entitlement E1's reservation quote cannot be confirmed using E2 → refused (quote↔entitlement reciprocal identity) | S6-3 |
| 17 | Account/participant A cannot quote or reserve using account B's entitlement → not-found-shaped at BOTH quote and confirmation | S6-3 |
| 18 | Provider product/schedule edit after purchase does NOT alter the entitlement's reservation-eligibility snapshot | S6-3 |
| 19 | Same-key zero-price package acquisition ×20 → exactly one Purchase + one Entitlement + one audit/outbox effect set; ambiguous-transport same-key retry returns the same result | S6-1 |
| 20 | One acquisition quote can never confirm once free AND once paid — nor yield two Purchases/Entitlements under double-click, concurrent, or cross-path races (`uq_entitlement_purchase_quote`) | S6-1 |
| 21 | Lock-order matrix: reserve vs release vs check-in vs expiry interleavings → no deadlock, no opposing acquisition order, no oversell | S6-3 |

(Owner round-2 verification addition #1 — "1 finite use remaining, two concurrent reservation commands → exactly one succeeds" — is proof 11.) Plus the full certified battery re-running green at every slice; W5 Booking-path suites never weakened.

## 18. docs/24 amendment plan (Amendment A4 — applied at S6-1 after approval, not in this commit)

**(a)** §3.4: add `EntitlementPurchase` (§5.1) beside Booking; record `PackageEntitlement` SUPERSEDED by `Entitlement` (§6). **(b)** §4.1/§4.2: `PriceQuote` gains `commercial_shape` + `fulfillment_revision_id` + the entitlement-bound reservation reference (`entitlement_id`) per the §4 invariant; `PaymentIntent` gains the commercial-target abstraction (§5.2 verbatim). **(c)** §2.2/§2.6: `membership` kind (commercial vocabulary only) + `PriceOptionFulfillmentRevision` (+ schedule-term snapshot child) — §14.B7 revised to shipped-additively. **(d)** §3.5: `AttendanceRecord` redefined to the §10 append-only redemption-anchored shape; `expected/no_show/excused` marking deferred. **(e)** new canonical `RedemptionCredential` (§9) and `EntitlementReservation` (§7). **(f)** derived-calendar note (§12). docs/09 §21.14/§21.7 annotated as revised/resolved on approval.

## 19. Explicit scope exclusions (S6 must not absorb)

Automatic recurring Stripe billing · provider payouts/Connect · general customer refunds · cancellation policy engine + self-service cancellation · no-show penalties/debits + reservation-release policy timing (schema forces no consumption; policy deferred with the owner) · provider disruption/schedule-change policy for active memberships (§12) · manual attendance-correction workflow · transferable/family packages · loyalty/rewards · promo codes · sponsored placement · Customer App implementation (RI-4/RI-5) · Provider Portal implementation (W2-13) · Admin UI · W6 job-scheduler infrastructure (sweeps stay invocation-driven). Production payment gates (docs/23 §19, D-W5-6, VAT, commission administration) carried untouched; `productionChargingPossible` remains structurally false.

## 20. Implementation decomposition (each slice: implement → certify → commit → STOP → owner review)

| Slice | Scope (bounded, coherent) | Migration | Unblocks |
|---|---|---|---|
| **S6-0** | THIS PLAN (corrected) + §22 rulings | — | S6-1 |
| **S6-1** | **Complete commercial-target + entitlement foundation**: `0017` (§21 — the full §4/§5 spine in one atomic migration); fulfillment revisions; `requestQuote` dispatch; `EntitlementPurchase` domain + state machine; generalized `startPaidCheckout`/saga dispatch + `confirmPaidEntitlementPurchase` + `confirmFreeEntitlementPurchase`; purchase wind-down; generalized payment-status read + stuck-state diagnostics; customer purchase routes; docs/24 Amendment A4; proofs 7–9, 13, 19–20 | `0017` | S6-2 |
| **S6-2** | **Credential + redemption/attendance authority + provider surface**: `0018` (credential, attendance, reservation-subtype structure); issue/supersede/expire; atomic redemption; preview/redeem + attendance reads; capability activation; attempt bounding; proofs 1–6, 10 | `0018` | W2-13 |
| **S6-3** | **Reservation flow + customer fulfillment reads + calendar + closeout**: `confirmEntitlementReservation` over the certified capacity flow + §7 commitment authority + the `confirmFreeBooking` shape guard; entitlement list/detail (§8 vocabulary); credential routes; `GET /customer/calendar`; proofs 10–12, 14–18, 21; cross-surface security regression + closeout audit (S5-6/W5-6 pattern) | — | RI-4 · RI-5 |

Two migrations, not three (Correction 10): `0017` owns the complete commercial-target + entitlement foundation atomically; `0018` owns redemption/attendance/reservation structure with its authority slice; S6-3 ships no migration. W2-13 and RI-4 remain separately owner-gated.

## 21. Expected migrations (exact, none executed at S6-0)

Exact ownership (Correction D — two migrations preserved; S6-3 stays migration-free; no third migration for convenience):

| Object | Migration |
|---|---|
| `program_price_option.kind` +`membership`; `price_option_fulfillment_revision` + `price_option_fulfillment_schedule_term` | `0017` |
| `price_quote.commercial_shape` (DEFAULT `'capacityPurchase'`) | `0017` |
| `price_quote.fulfillment_revision_id` + composite FK | `0017` |
| `price_quote.entitlement_id` (nullable) + the five-column ownership FK to `entitlement` (+ `uq_entitlement_reservation_lineage`) — landing in `0017` because `entitlement` itself is created there, keeping the COMPLETE `ck_price_quote_shape` invariant atomic in one migration | `0017` |
| `ck_price_quote_shape` (replacing `ck_price_quote_one_unit`) + `ck_price_quote_option_kind` +`membership` + `uq_price_quote_id_session/camp_week/cohort` + the three additive `capacity_hold` quote-unit composite FKs | `0017` |
| `entitlement_purchase` (shape trigger, state-machine trigger incl. the §5.1 confirmation-facts CHECK, uniques incl. `uq_entitlement_purchase_quote`) | `0017` |
| `entitlement` (append-only) | `0017` |
| `payment_intent` target evolution (§5.2) + `enforce_economics_org_binding` purchase branch | `0017` |
| DROP `package_entitlement` (D-S6-2) | `0017` |
| `entitlement_reservation` | `0018` |
| `redemption_credential` | `0018` |
| `attendance_record` | `0018` |

Migration names: **`0017_commercial_target_and_entitlement_foundation`** (S6-1) · **`0018_redemption_attendance_reservation`** (S6-2). Each proven up/down/up, codegen drift-free, `db:verify` manifest extended; lock amendments only in their owning slice. `booking`, the three capacity units, and all S5 counters/triggers: **zero changes** (`capacity_hold` gains only additive FKs whose truth the service already enforced). The `entitlementReservation` shape exists structurally from `0017` but no service can create one until the S6-3 flow activates.

## 22. Owner decision state at this STOP

**RESOLVED at the S6-0 review round (recorded in the status header, binding):** D-S6-2 (drop `package_entitlement` in `0017`) · D-S6-3 (`membership` = commercial vocabulary only; fulfillment semantics in the fulfillment configuration) · D-S6-4 (validity from acquisition confirmation; first-use activation deferred; edits never rewrite purchased validity) · D-S6-1 direction (one commercial target; no second payment stack), with the exact structure delivered by Corrections 1–4 in §4–§5.

**ACCEPTED at review round 2 (`861f7bc`) and not reopened:** the §4 shape generalization, PaymentIntent targeting, target-neutral economics, the purchase state-machine direction, the late-success invariant, commitment semantics, the four-truth vocabulary, credential security, the schedule snapshot, D-S6-2/3/4, and the two-migration/three-slice decomposition.

**Remaining genuine owner decision: approval of this round-3 revision** — the §7 canonical lock hierarchy with commitment-under-Entitlement-lock and the READ COMMITTED isolation requirement (round 3), plus the two round-2 seams: (a) the entitlement-BOUND reservation trail (§4 `entitlement_id` + five-column ownership FK + purchased-snapshot eligibility + `confirmEntitlementReservation` + the §7 lock order and non-crossing guards, incl. the bounded `confirmFreeBooking` shape guard), and (b) the zero-price acquisition boundary (§5.5 `confirmFreeEntitlementPurchase`, the §5.1 insert-as-`confirmed` rule, quote single-use). No other unresolved product decision remains; deferred-with-owner items are recorded in §19 (no-show/reservation-release policy timing; disruption policy for schedule changes affecting active memberships).

**Ratified defaults (owner-directed or adopted-and-recorded):** credential TTL 10 min · scheduled window −60/+60 min · walk-in generation while valid · numeric-first/QR-ready with the §9 opaque-token authority and 8-digit attempt-bounded alias · provider-staff validation per §14 · derived calendar · D-W5-7 commission-once · no automatic recurring billing · no-show consumption deferred · participant ownership · supersede-not-replay regeneration · purchase abandonment window 30 min (config) · preview + confirm provider flow · attendance only-via-credential in V1 · coach activation only with assigned-session scoping; `roster.view` reserved.

## 23. Definition of done — met by this corrected planning task (S6-0)

Everything from the first S6-0 pass and round-1 Corrections 1–10, PLUS the round-2 Corrections A–D integrated: the entitlement-bound `entitlementReservation` quote (§4 — `entitlement_id`, ownership FK, purchased-snapshot eligibility, quote authorization), the dedicated `confirmEntitlementReservation` authority with its exact lock order and structural non-crossing (§7), the explicit stored-vs-derived reservation lifecycle (§7), the zero-price acquisition boundary with the refined creation-state rule and quote single-use (§5.1/§5.5), the per-migration ownership map (§21), and proofs 14–21 (§17). And from round 1: the generalized quote invariant with exact columns/CHECKs/FKs, zero-rewrite backward compatibility, dispatch, and bidirectional cross-trail impossibility (§4) · the complete target-dependent W5 audit incl. target-neutral D-W5-7 org binding (§5.3) · the exact EntitlementPurchase state machine (§5.1) · the authoritative late-success confirmation/refusal set (§5.4) · reservation commitment semantics with the four-truth vocabulary and lock ordering (§7–§8) · the unambiguous credential target model and opaque-token/alias security architecture (§9) · the immutable purchased-schedule snapshot rule (§12) · the revised two-migration/three-slice decomposition (§20–§21) · D-S6-2/3/4 recorded RESOLVED · **no code, no schema, no frontend change**. Next step after owner approval: **S6-1 — and not before. RI-4 and W2-13 do not start.**

---

## S6 implementation record

**S6-1 · Commercial Target, Fulfillment Revision, Entitlement Foundation & Acquisition — OWNER-APPROVED / CLOSED at `0f44f98` (2026-08-26; base implementation `fe3c026`; rollback-safety correction `0f44f98`; owner-authorized at the S6-0 closure `3b7d770`; S6-0 closure bookkeeping `bff9c9c`). Closure verification (recorded at approval): migration head `0017_commercial_target_and_entitlement_foundation` · backend Jest 1164/1164 across 94 suites · backend tsc/ESLint clean · `db:verify` 17/0 · Customer App Jest 475/475 across 33 suites · Playwright 6/6 · Provider Portal Jest 762/762 · Admin Jest 138/138 · all W5 production gates unchanged. S6-2 (Redemption Credential, Provider Validation & Attendance Authority) is AUTHORIZED; S6-3, W2-13, RI-4, RI-5 do NOT start.** Final verification: backend Jest **1163/1163 across 94 suites** (all 1124 certified + 39 new; the full W4/S5 concurrency and W5 payment batteries re-ran green; locks amended only by the owning-slice pattern) · backend tsc/ESLint clean · `db:verify` **17/0** (head `0017_commercial_target_and_entitlement_foundation`) · migration **up/down/up proven** twice (fresh-database CLI pass + the staged in-suite compatibility proof over LIVE certified rows) · codegen drift-free (regenerated `src/db/generated/db.ts`) · Customer App Jest **475/475 across 33 suites** + tsc/lint clean (zero app changes) · Playwright **6/6** (all RI journeys on the migrated stack) · Provider Portal Jest **762/762** · Admin Jest **138/138** · clean tree. Implementation record follows.

**Pre-implementation reconciliation (owner item 1 — the exact constraints `0017` alters):** `program_price_option`: `ck_program_price_option_kind` (+`membership`) and `ck_program_price_option_amount_positive` (entitlement kinds may be genuinely zero — a free introductory product's real price; capacity kinds unchanged) · `price_quote`: `ck_price_quote_one_unit` REPLACED by `ck_price_quote_shape`; `ck_price_quote_option_kind` widened; +`commercial_shape` (DEFAULT `capacityPurchase`), +`fulfillment_revision_id` (composite FK to the revision), +`entitlement_id` (five-column ownership FK to `entitlement`); +`uq_price_quote_id_session/camp_week/cohort` · `capacity_hold`: three ADDITIVE quote↔unit composite FKs (`fk_capacity_hold_quote_*` — the certified fk_booking_hold_* null-skip pattern; the service had always enforced the equality, so live rows validated at migration time) · `payment_intent`: `booking_id`/`hold_id` DROP NOT NULL (FKs verbatim), +`purchase_id` + 3-column composite FK, `ck_payment_intent_one_target`, `ck_payment_intent_target_shape`, mirrored `uq_payment_intent_live/succeeded_purchase`, `enforce_payment_intent_transition` re-created NULL-safe (+purchase_id frozen) · `enforce_economics_org_binding` re-created target-neutral (Booking branch byte-equivalent) · `package_entitlement`: DROPPED behind an is-empty guard (D-S6-2; down restores the legacy empty structure).

**Migration `0017` (one atomic file):** `price_option_fulfillment_revision` (immutable revisioned terms — the commission-term supersede pattern: term columns trigger-frozen, `active → superseded` only, one-ACTIVE partial unique, per-kind shape trigger: package ⇒ finite with `sessions_count` as the total, finite membership ⇒ `uses_total`; unlimited must expire; reservation-or-walk-in ≥ 1) + `price_option_fulfillment_schedule_term` (the promised-pattern VALUE snapshot, append-only) · the full §4 quote-shape invariant (all three shapes structural from `0017`; `entitlementReservation` is STRUCTURE ONLY — proofs pin that no service/http source can utter it until S6-3) · `entitlement_purchase` (quote-unique, shape trigger proving quote identity + `entitlementAcquisition` shape, exact §5.1 state machine trigger with insert states limited to `pending_payment | confirmed`, confirmation-facts CHECK, reference write-once, terminals frozen, no DELETE) · `entitlement` (append-only; `uq_entitlement_purchase` one-grant-per-purchase; 7-column grant-identity composite FK; `uq_entitlement_reservation_lineage` for S6-3; finite ⇔ `uses_total` CHECK — unlimited stores NO count; `valid_until > valid_from`; created-only-for-confirmed-purchase trigger) · the §5.2 PaymentIntent target evolution · grants (revisions supersede via UPDATE; purchase state-machine via UPDATE; entitlement + schedule terms INSERT-only; no DELETE anywhere).

**Domain services (backend only):** `modules/entitlement/services/entitlement-quote.ts` — `requestEntitlementQuote` (unit-less certified branch: published program, participant ownership + program-level age gate at quote time, entitlement-kind option, ACTIVE revision required else fail-closed `fulfillmentUnavailable`, server-authored money = the option amount, D-6 TTL, customer-safe terms VALUES on the wire — never revision ids; V1 applies no Offers to entitlement products — D-10 not repurposed, discounts deferred). `entitlement-shared.ts` — the D-S6-4 validity computation (`valid_from` = acquisition confirmation; `daysFromConfirmation` → +days; `fixedEndDate` → end-of-date Asia/Dubai; `none` → finite-only no expiry) + `grantEntitlement` (the single grant authority both paths compose) + `HMP-` reference codes. `entitlement-acquisition.ts` — `confirmFreeEntitlementPurchase` (ONE idempotent transaction: fresh zero-total acquisition quote → Purchase inserted AS `confirmed` + its one Entitlement; audit `entitlement.purchase.created`+`.confirmed` + outbox + `entitlement.created` exactly once; NO PaymentIntent/transaction/economics/provider call — zero money never enters the payment stack; `uq_entitlement_purchase_quote` catches concurrent different-key winners) and `confirmPaidEntitlementPurchase` (the trusted seam — saga-only, HTTP-unreachable, `runIdempotent` scope `entitlement.confirm.paid` under `system:payments`; purchase row FOR UPDATE = the entitlement-domain lock; reciprocal intent↔purchase identity; the §5.4 rule implemented literally: still-pending past-`expires_at` purchases CONFIRM on genuine capture, TERMINAL purchases / archived-or-detached participants / lapsed fixed-date products REFUSE typed → the saga compensates; the W5 settlement seam joins the SAME transaction) + the own-purchase read.

**One payment architecture (owner item 15 — the docs/35 §5.3 audit implemented):** `checkout-orchestration.ts` generalized, not duplicated — the T3/NET/T4 attempt/provider-session/outcome pipeline extracted into ONE shared `runGatewaySessionLeg` used verbatim by both trails; `startPaidCheckout` (Booking) behavior byte-compatible (full W5 battery green); new `startPaidEntitlementCheckout` = capability + D-W5-7 gates → T1′ ensure-Purchase (`entitlement.purchase.initiate` idempotency; `payment_failed` re-enters; different-key live checkout arbitrated by the one-live-intent partial unique) → T2′ intent ensure (purchase target, TTL = the purchase abandonment window, economics snapshot in the same transaction) → the shared leg. `payment-saga.ts` — target dispatch OUTSIDE the seams: `confirmPaidBooking` unchanged; purchase branch → `confirmPaidEntitlementPurchase`; compensation reuses the identical ledger machinery, with the reversal-posting transaction additionally CASing the purchase → `compensated` (+audit/outbox; a swept `expired` purchase keeps its terminal state — ledger shape stays the customer-status authority). `sweepLapsedPaidCheckouts` — booking branch unchanged; purchase branch winds down ONLY capture-less lapsed purchase checkouts (`purchaseExpired` failure code; purchase → `expired` + events) — a captured intent is NEVER swept (the §5.4 guard). `customer-payment-read.ts` — `customerEntitlementPurchasePaymentStatus`: the SAME converged vocabulary (`awaitingPayment` +`purchaseExpiresAt` / `processing` / `confirmed` +`HMP-` reference / `expired` / `compensationPending` / `compensated`) — never a fake Booking id, never economics. `findStuckPaymentStates` + `liveIntentsOnConcludedPurchases` (both targets diagnosed); ledger reconciliation was already target-neutral.

**Customer routes (5, all `authenticatedCustomer`; route lock amended):** `POST /customer/entitlement-purchases/quote` · `POST /customer/entitlement-purchases/confirm-free` · `POST /customer/entitlement-purchases/initiate` (fail-closed without a composed provider + server-authored URLs; response = purchaseId/redirectUrl/expiresAt ONLY) · `GET /customer/entitlement-purchases/:purchaseId(/payment)`. The customer authors NOTHING commercial: no price/currency/uses/validity/commission/revision field exists on any wire (additionalProperties:false + Ajv stripping, proven with smuggled-field tests); cross-account ids are not-found-shaped.

**docs/24 Amendment A4 applied (this commit):** §2.6 `membership` (commercial only) + `PriceOptionFulfillmentRevision`; §3.4 `EntitlementPurchase` + `Entitlement` + PackageEntitlement SUPERSEDED; §4.1 PriceQuote shapes + the PaymentIntent commercial target; the §5.1 purchase lifecycle; §14.B7 partially resolved. S6-2 credential/attendance is documented only as reserved/future.

**New proofs (39 tests across 3 suites, real PostgreSQL):** `entitlement-schema.test.ts` (20) — the §31 matrix: shape CHECKs all three ways, acquisition-quote-unholdable + hold-unit≡quote-unit FKs, reservation ownership FK + zero-total + purchase-trigger refusal (structure only), quote→purchase identity/uniqueness, exact state machine incl. terminal freeze + insert-state bounds, one-grant-per-purchase + 7-column identity FK + confirmed-only creation, finite⇔uses_total (no sentinel), revision per-kind shape/immutability/supersede/one-active, one-target + target-shape + live/succeeded-per-purchase, zero-total-never-intents, target-neutral economics (correct purchase-org accepted; foreign org refused), package_entitlement absent, S6-2/S6-3 tables absent, and the `'entitlementReservation'`-source lock. `entitlement-acquisition.test.ts` (17) — free storm ×20 (ONE purchase + ONE entitlement + ONE audit/outbox set + ZERO payment artifacts + provider counters untouched; proof 19), free/paid cross guards + cross-path exclusion (proof 20) incl. different-key concurrency, paid storm ×20 (ONE purchase/intent/economics/provider-session; D-W5-7 at 1200 bps on AED 1,000 = 12000/88000 fils asserted; proof 7), trusted success → saga → ONE entitlement + repeated-delivery convergence (proof 9), unlimited = NULL count + fixed-date validity instant asserted, finite membership 30-days-from-confirmation asserted, LATE success on still-pending past-window purchase CONFIRMS, swept-then-slipped-completion COMPENSATES (expired stays terminal, reversal posted, no entitlement, intent never `succeeded` → no settleable economics; proofs 8+13), captured-never-swept guard, participant-archived compensation, D-RI-5-vocabulary status projection (awaiting → processing → confirmed; compensation never success), full HTTP journey + smuggled-field inexpressibility + cross-account/unauthenticated shaping, and free-path failure injection at both named points (nothing persists; the key replays unpoisoned). `entitlement-migration-compat.test.ts` (1, staged) — 0016-head seeded with LIVE certified rows through the real services → `0017` up (zero rewrites; quotes stay `capacityPurchase`; intents keep Booking targets; NOTHING rewritten into EntitlementPurchase) → certified paths exercised on the migrated schema → down (legacy schema + empty package_entitlement restored, certified rows intact, NOT NULLs back) → up again. Paid-confirm atomicity rides the certified saga failure-injection battery (the same settlement seam; W5-4 suite green) and the storms (economics 1:1 with intents).

**Lock amendments (owning-slice pattern, each annotated in place):** customer route inventory (+5) · `confirmPaidBooking` source ban extended to scan `modules/entitlement/http` AND to ban `confirmPaidEntitlementPurchase` everywhere · payment_intent column set (+`purchase_id`) · S5-1 table lock 12→11 with an explicit package_entitlement-absent assertion · price-option kind lock (`membership` legal; zero-amount entitlement rule added) · economics trigger message (`commercial target`) · http-policy route snapshot regenerated · `db:verify` manifest +17 objects. UNCHANGED and re-proven: `productionChargingPossible` literal false, deterministic-provider production impossibility, no `confirm-paid|payment-success|capture|refund|commission` URL, webhook trust boundary, D-10, every S5 capacity counter/trigger.

**S6-1 owner-review correction (rollback hardening — this commit; `fe3c026` otherwise accepted; no domain/API/frontend change):** the `0017` DOWN migration now begins with an explicit FAIL-CLOSED preflight — before any destructive DDL it refuses the downgrade whenever S6-1-native durable state exists that the legacy `0016` schema cannot represent: any `entitlement_purchase` row · any `entitlement` row · any `payment_intent.purchase_id IS NOT NULL` · any `price_quote.commercial_shape <> 'capacityPurchase'` · any fulfillment revision or schedule-term snapshot · any 0016-incompatible price option (`membership` kind, or a zero-amount entitlement option) — with a clear error naming the counts; production commercial data is never deleted or transformed automatically. Atomicity: node-pg-migrate runs each migration in ONE transaction (the repository convention, `src/db/migrations.ts`), and the guard is the FIRST down statement — safety never rests on a later NOT NULL/FK failure. Proofs (both in `entitlement-migration-compat.test.ts`): (1) legacy-compatible downgrade — `0016` seeded with certified Booking/W5 rows → up → down SUCCEEDS, rows survive, exact legacy schema restored → up; (2) S6-native refusal — genuine state through the REAL domain path (confirmed free Purchase + Entitlement via `requestEntitlementQuote`/`confirmFreeEntitlementPurchase`, plus a purchase-target intent via a real paid initiation) → down REFUSED by the preflight message, `db:verify` still 17/0 with zero problems, every S6-1/certified count byte-identical before/after, Purchase (`confirmed`) + Entitlement (uses_total 3) + the in-progress purchase intent queryable and unchanged. Editing the pre-approval `0017` file required rebuilding the local dev/scratch databases (applied-migration checksum immutability); the fresh-database CLI up/down/up re-proved clean. Post-correction verification: backend Jest **1164/1164 across 94 suites** · tsc/ESLint clean · `db:verify` **17/0** · Playwright **6/6** re-run on the rebuilt dev stack; app/portal/admin untouched — their recorded S6-1 results stand per convention.

**Recorded S6-1 gaps/deferrals (truthful, not blockers):** provider fulfillment-revision administration has no route (W2-13 owns the editor; tests/dev create revisions through the fixture boundary; dev-seed carries no revisions yet, so no entitlement product is purchasable in the dev app until RI-4 wiring — the Customer App is deliberately untouched this slice) · Offers on entitlement products deferred (quote refuses none — the field simply does not exist on the acquisition wire) · S6-2 redemption/attendance and S6-3 reservation/calendar remain structurally absent and lock-pinned · all standing production gates carried (D-W5-6 real Stripe certification, live activation, VAT ruling, commission-term administration, W6 runners, Cognito/production config). **Next: owner review — S6-2, S6-3, W2-13, RI-4, RI-5 do NOT start.**

**S6-2 · Redemption Credential, Provider Validation & Attendance Authority — OWNER-APPROVED / CLOSED at `d69e1ab` (2026-08-26; S6-1 closure bookkeeping `f40c80e`; base implementation `3638cc1`; issuance-concurrency correction/final `d69e1ab`; owner-authorized at the S6-1 closure `0f44f98`). Closure verification (recorded at approval): migration head `0018_redemption_attendance_reservation` · backend Jest 1197/1197 across 96 suites · backend tsc/ESLint clean · `db:verify` 18/0 · Customer App Jest 475/475 · Playwright 6/6 · Provider Portal Jest 762/762 · Admin Jest 138/138 · all production payment gates unchanged · clean tree. W2-13 (Provider Fulfillment Configuration, Check-In & Attendance UX) is AUTHORIZED; S6-3, RI-4, RI-5 do NOT start.** Final verification: backend Jest **1195/1195 across 96 suites** (all 1164 certified + 31 new; the complete S5/W4 concurrency, W5 payment, and S6-1 acquisition batteries re-ran green) · backend tsc/ESLint clean · `db:verify` **18/0** (head `0018_redemption_attendance_reservation`) · migration up/down/up proven on a fresh database + BOTH fail-closed data-bearing downgrade refusals proven (`0017` with S6-1 data; `0018` with S6-2 data — the approved rollback-safety precedent) · codegen drift-free · Customer App Jest **475/475 across 33 suites** + tsc/lint clean (zero app changes) · Playwright **6/6** · Provider Portal Jest **762/762** · Admin Jest **138/138** · clean tree. Implementation record follows.

**Pre-implementation reconciliation (owner item 1, reported):** `attendance.manage`/`roster.view` were RESERVED in `provider-capabilities.ts` with the future-activation map carrying `attendance.manage` only under coach + front_desk (the S6-0 §14 ruling adds owner/org_manager/branch_manager); roles = owner · org_manager · branch_manager · listings_editor · coach · front_desk · finance with `branch_scope_kind all|branches` resolved to `OrgScope.branchScope: 'all' | branchId[]` over ACTIVE assigned branches (`branchInScope`). Booking unit forms: **Session IS the canonical single occurrence** (`start_at`/`end_at`/`branch_id`); **CampWeek (a date span) and EnrolmentCohort (an unmaterialized pattern) have NO canonical attendance-occurrence identity today** → their credential issuance is FAIL-CLOSED (`occurrenceUnsupported`, proven) and occurrence-scoped attendance is explicit S6-3 completion work — no fake occurrences were invented, and the schema stays compatible (nullable `session_id`; the session-booking attendance unique is deliberately PARTIAL). Rate limiting: the identity `RateLimiterStore` is an in-memory, PRODUCTION-REFUSING abstraction (no approved distributed store exists) → it cannot carry a production brute-force guarantee, so S6-2 ships the smallest DURABLE mechanism (below).

**Migration `0018_redemption_attendance_reservation` (one atomic file):** two additive certified-table FK targets (`uq_booking_id_account_participant`, `uq_entitlement_id_account_participant`) · **`entitlement_reservation`** (structure only: booking-subtype PK; ownership pinned by the booking 3-column + entitlement 5-column composite FKs; org/program-lineage shape trigger; append-only; S6-3 operates it) · **`redemption_credential`** (digest-only secrets: unique `token_digest` + org-scoped `alias_digest`; exactly-one-target CHECK + booking⇔occurrence pairing CHECK; live-only creation + target-org/branch/occurrence shape trigger; lifecycle `live → used | superseded | expired` with identity/terminal freeze + used-facts pairing; one-LIVE-per-target partial uniques + live-alias-per-org unique; no DELETE) · **`attendance_record`** (append-only; **`credential_id` UNIQUE = the structural single-use backstop**; consumed-credential + credential↔attendance identity + reservation-entitlement agreement + FINITE-FLOOR shape trigger; one attendance per session-backed Booking occurrence via the deliberately PARTIAL unique — owner item 21) · **`redemption_lookup_attempt`** (durable fixed-window failure counters per provider principal + organization — cross-instance by construction) · grants (attendance/reservation INSERT-only; no DELETE anywhere). **Down migration fails closed** (the `0017` precedent): any credential/attendance/reservation row refuses the downgrade before destructive DDL (attempt windows are operational counters, like idempotency rows, and do not block).

**Credential security/issuance (owner items 5–12, the recorded §7 idempotency design):** the canonical authority is a 256-bit opaque token (base64url, QR-ready) and the 8-digit code is a short-lived org-scoped ALIAS — both stored ONLY as sha256 digests; issuance runs under `runIdempotent`, but the transaction RESULT carries no secret: the minted code/token travel through a closure captured only by the EXECUTING run, so the idempotency snapshot never holds a raw secret, a same-key replay returns `credentialIssued` metadata WITHOUT secrets (never minting a second credential, never superseding what the customer displays), and a customer who lost the code regenerates with a new key (supersede-and-mint). Concurrent different-key first issuances serialize on the one-live-per-target unique with ONE bounded service retry (proven: exactly one live credential; each response carries only its own secrets). Lock order: issuance takes the existing live credential row BEFORE the entitlement lock (`credential → entitlement`) — regeneration can never deadlock redemption. Alias collision: DB-backed live-scope uniqueness + a strict 5-try regeneration loop + a typed exhaustion failure, proven with deterministic injected generators. TTL 10 min (config); scheduled window −60/+60 min (config) enforced on PostgreSQL time; issuance eligibility: confirmed session-backed own Booking in-window and not yet attended · walk-in only on an owned, effectively-valid, walk-in-allowed, unexhausted entitlement (a reservation-required-only product CANNOT mint a walk-in credential); issuing/expiry/supersession consume nothing; `expires_at <= now()` is unusable with no sweeper.

**Provider authority (owner items 13–16, 25, 27):** `attendance.manage` ACTIVATED exactly per the ruling — owner · org_manager · branch_manager (branch-scoped) · front_desk (branch-scoped) · **coach only where `session.instructor_staff_id` proves assignment** (never walk-ins, never unassigned sessions — proven); `roster.view` stays RESERVED (registry + amended locks). Two provider-private routes under the capability: `POST {ORG}/check-in/preview` (PURE READ: org-scoped live-alias resolution; branch/coach scope; minimal front-desk context — participant first name, program title, target kind, occurrence/branch, finite `usesTotal/used/remaining`, validity; grants nothing, consumes nothing, and foreign-org/random/superseded codes are ONE generic not-found) and `POST {ORG}/check-in/redeem` (the atomic transaction: idempotency key → rate gate → credential FOR UPDATE → post-lock effective-liveness + org/branch/coach re-validation → target resolution (walk-in · reserved-use via the reservation subtype · plain session) → **entitlement FOR UPDATE LAST with a FRESH post-lock availability count** → credential `live → used` → ONE attendance row → audit `attendance.redeemed` + outbox once → `entitlement.exhausted` exactly when the final finite use was genuinely consumed). Preview output is never authority; Bookings are validated BY READ (no `credential → booking` lock edge exists anywhere). **Brute force:** durable PostgreSQL fixed windows (default 20 failures / 600 s, config) per staff principal + org — only lookup MISSES count, limits hold across horizontally scaled instances by construction, attempted codes are never stored, and the throttle was proven (miss-count → `rateLimited`) with per-principal isolation.

**Consumption truths (owner items 17–23):** finite `used` derives from consuming attendance rows (`uses_total − count`) with the trigger floor as the final structural backstop — never negative, never a mutable counter; unlimited memberships record every visit with NO count anywhere; plain session Bookings attend once per occurrence (re-issuance after check-in refuses `alreadyCheckedIn`; regenerated credentials cannot double-attend past the partial unique); reserved-use check-in records Booking + Entitlement + occurrence in one row — the commitment converts to consumption derivationally; V1 no-show consumes nothing (schema forces no consumption; policies stay deferred). Customer observation (item 29): `GET /customer/credentials/:credentialId` returns effective `live/expired/superseded/used` + expiry + `redeemedAt` — no secrets, cross-account not-found; the full Passes projection stays S6-3.

**Proofs (31 new across 3 suites):** `redemption-schema.test.ts` (9) — target/occurrence/used-facts CHECKs, shape triggers (org/branch/occurrence, consumed-credential, credential↔attendance identity, reservation agreement), finite-floor backstop, single-use unique, session-occurrence partial unique with regeneration, one-live + live-alias uniques + lifecycle freeze + alias-release-on-supersede, reservation lineage FKs/PK/append-only, grants + digest-only column hygiene. `redemption-attendance.test.ts` (21) — issuance (secrets-once replays, supersede-regeneration, window/unconfirmed/camp-fail-closed/foreign refusals, walk-in rules incl. the reservation-only bypass ban and expired/exhausted refusals, deterministic collision handling), preview (context + grants-nothing), all three redemption target forms, unlimited multi-visit, final-use exhaustion event once, sweeper-free expiry, branch/coach scope matrix, foreign-org privacy + durable throttling, secret hygiene across audit/outbox/idempotency stores, **the four multi-connection concurrency proofs** (two-staff single credential → exactly one attendance/consumption; regenerate-vs-redeem → never two live/never double attendance with used↔attendance agreement; two live targets racing ONE final finite use → entitlement-lock serialization to exactly one consuming attendance + `entitlementExhausted` for the loser; concurrent different-key issuance → one live credential), failure injection at both named points (credential still live, zero attendance, retry converges — never a consumed credential without attendance or attendance beside a redeemable credential), and the full HTTP journey + cross-principal/foreign-org/unauthenticated locks. `entitlement-migration-compat.test.ts` (+1) — the S6-2-native downgrade refusal (live credential through the real path → `0018` down refused by its preflight → schema at head, `db:verify` 18/0, credential untouched) beside the re-proven legacy-compatible full downgrade and the S6-1-data `0017` refusal.

**Lock amendments (owning-slice pattern, annotated in place):** customer route inventory (+3 credential routes) · provider surface + http-policy snapshot (+2 check-in routes) · reserved-capability lock (`attendance.manage` activated; `roster.view` + every other reserved capability still pinned; the reserved/active vocabularies stay disjoint and exhaustive) · forbidden-table lock (`attendance_record`/`redemption_credential` now exist because their owning slice shipped; everything else still forbidden) · structural-secret allowlist (+ the two digest columns and the credential FK, table-qualified) · catalogue decision-route lock (the `/check-in/preview` substring collision with /review/ annotated) · `db:verify` manifest (+12 objects). UNCHANGED and re-proven: both paid-confirmation seams HTTP-unreachable, no customer reservation route, `'entitlementReservation'` source lock (S6-3), no Admin attendance mutation, redemption touches no payment/commission path (no payment import exists in the redemption services; commission remains acquisition-only), deterministic provider production-impossible, `productionChargingPossible` literal false.

**Recorded S6-2 gaps/deferrals (truthful, not blockers):** CampWeek/EnrolmentCohort/recurring-programme check-in stays fail-closed pending S6-3 occurrence/calendar expansion (deliberate — no canonical occurrence identity exists and none was invented) · operational reservation creation, `availableToReserve`, the Passes projection, and the calendar read are S6-3 · attempt-window retention/cleanup is a future W6 operational sweep · QR rendering is later presentation over the already-QR-ready token authority · W2-13 (portal check-in UI) and RI-4 (customer check-in UX) remain owner-gated. **Next: owner review — S6-3, W2-13, RI-4, RI-5 do NOT start.**

**S6-2 owner-review correction (issuance/regeneration concurrency — this commit; `3638cc1` otherwise accepted; no other S6-2 behavior, migration, or frontend change):** issuance and explicit regeneration are now DISTINCT domain intents (owner rules A–H). **Initial issuance** (no `regenerateCredentialId`): a target with an effectively-live credential is NEVER superseded — the request returns `credentialAlreadyLive` (HTTP 200: credential id + expiry + `alreadyLive`, NO secrets); only the transaction that actually created the currently-live credential ever returns its secret material; a lapsed-but-unswept live row is terminalized (`expired`) under its lock and a fresh credential issues (rule F — sweeper-free, serialized against redemption at the boundary). **Explicit regeneration** (`regenerateCredentialId` on the same routes — one endpoint with an explicit precondition, the existing customer-route convention): the NAMED credential is locked, proven to be the caller's CURRENT live credential for that exact target, superseded with a CAS, and replaced by exactly one new credential whose secrets go only to the successful request; a stale/used/superseded id changes nothing (`credentialNotCurrent` → the certified `staleVersion` envelope) and a foreign id stays not-found-shaped — "supersede whatever is live now" no longer exists anywhere. Same-key replays still return metadata without secrets (raw secrets remain absent from idempotency/audit/outbox storage — re-proven); the recovery flow is replay-metadata (or `credentialAlreadyLive`) → explicit regeneration by id. New/updated multi-connection proofs: different-key INITIAL race → exactly `['credentialAlreadyLive','credentialIssued']`, one live row, only the creator holding secrets, loser superseding nothing; concurrent regenerations of C1 → exactly `['credentialIssued','credentialNotCurrent']`, one C2, no C3; stale-C1-after-C2 refusal leaving C2 live; the full lost-response recovery chain; regenerate-vs-redeem re-proven with the explicit id (one legal terminal outcome; never two live, never double attendance). Post-correction verification: backend Jest **1197/1197 across 96 suites** · tsc/ESLint clean · `db:verify` **18/0** (no migration change — the existing id/lifecycle fields sufficed) · migration proofs re-ran · Customer App **475/475** · Playwright **6/6** · portal **762/762** · admin **138/138**.


---

**W2-13 · Provider Fulfillment Configuration, Check-In & Attendance UX (2026-08-26 — this commit, awaiting owner review; S6-3, RI-4, RI-5 NOT started):** the S6-1/S6-2 backend authority is now USABLE by provider staff — the two provider surfaces the owner directed, plus the smallest backend companion S6-1 recorded W2-13 as owning.

**Pre-implementation reconciliation (owner items 1–3, reported):** fulfillment-configuration authorization = the EXISTING product capability **`listings.manage`** — the exact certified capability owning the parent price-option mutation (owner/org_manager/branch_manager/listings_editor); reads ride `catalogue.read`; **`attendance.manage` grants NOTHING on the fulfillment editor** (proven both directions). No new capability was invented and no STOP was required. Check-in rides the S6-2 role/scope map unchanged (owner/org_manager/branch_manager/front_desk/coach; coach = assigned-session only; branch scope enforced server-side).

**Backend companion (smallest honest slice — NO migration, none was needed):** `membership` joins `PRICE_OPTION_KINDS` as COMMERCIAL vocabulary only (D-S6-3; `ENTITLEMENT_OPTION_KINDS = package|membership`); entitlement kinds may price at **AED 0** (the S6-1 zero-price acquisition path — `optionShapeValid` per-kind floor; route schema `amountFils ≥ 0`; capacity kinds stay strictly positive). New `fulfillment-admin.ts`: `getFulfillmentConfig`/`setFulfillmentConfig` — **supersede-and-insert** under FOR UPDATE (never in-place edits; sold entitlements keep their historical `fulfillment_revision_id` — proven), per-kind `termsValid` (package ⇒ finite + NO usesTotal; finite membership ⇒ usesTotal ≥ 1; unlimited ⇒ validity ≠ none; ≥ 1 attendance method; validity pairings; scheduleTerms membership-only), branch ownership + active-branch scope, audit `fulfillment.revision.created/superseded` + outbox. Two provider routes: `GET/PUT {ORG}/programs/:programId/price-options/:optionId/fulfillment` (GET `catalogue.read`, PUT `listings.manage`); new typed outcome `invalidFulfillmentConfig` (422). **Recorded gap honored by the no-migration STOP rule:** `0008`'s `ck_program_revision_option_kind` predates `membership`, so review-gated (protected) listings cannot carry a membership option through the revision path — the service refuses with a TYPED `lifecycleConflict` instead of a CHECK 500; draft-path membership creation works fully; widening the CHECK is a one-line future migration awaiting owner authorization. Backend proofs: `fulfillment-admin.test.ts` (8) — immutable supersede/insert history, sold-entitlement binding, the terms-validation matrix, branch limits, the full authorization matrix (front_desk/coach 403 both verbs · listings_editor 200 · cross-org 404 · customer/unauthenticated refused), and draft-vs-review-gated membership creation.

**Portal — typed seams + live ports (W2-12 conventions exactly):** `checkin/contract.ts` (`CheckInPort` = `previewCheckIn`/`redeemCheckIn`; the typed `CheckInRefusalKind` vocabulary; QR/manual-mutation absence documented in the seam itself) and `catalogue/fulfillment-contract.ts` (`FulfillmentConfigPort` = `loadFulfillment`/`setFulfillment`; immutable-revision semantics documented). `live-checkin-port.ts`: real S6-2 routes; redeem accepts HTTP **201 ONLY** (a 200 with a body is NOT success); 1:1 refusal mapping with the S6-2 generic-not-found shaping preserved (never "improved" client-side); DTO validation fails closed to `unavailable`; structurally fixture-isolated. `live-fulfillment-port.ts`: GET/PUT mapping (`invalidFulfillmentConfig`/`invalidBranchScope`/`lifecycleConflict`), fail-closed DTOs. Transport `method` unions widened with `PUT`. Composition: both ports wired through `auth-runtime` (live/fixture/unconfigured — unconfigured stubs fail closed), `ports-context`, `app.tsx`, `render-portal`.

**Check-In UX (`/o/:organizationId/check-in`; nav item gated by `attendance.manage` — front desk lands directly; front-desk nav shows Check-In and NOT Listings/Team):** 8-digit NUMERIC-FIRST entry (inputMode numeric, digit filtering, no QR scanner anywhere — later presentation over the same opaque authority) → **Verify** (the pure-read preview: participant first name, program, walk-in/session/reserved visit, session time, branch, finite `remaining of total` or Unlimited, validity — grants nothing, decrements nothing) → **Confirm Check-In** (the atomic redeem; success renders ONLY from the server's recorded attendance, with remaining balance or the truthful last-visit/exhausted note) → next customer. Truthful race UX: a confirm-time `codeAlreadyUsed`/`codeExpired`/`codeNotFound`/`entitlementNotActive`/`entitlementExhausted` CLEARS the stale preview and states "This check-in wasn't recorded" — never a false success; retryable refusals (`unavailable`/`tooManyAttempts`/`notAuthorized`) keep the same intent (one idempotency key per verified intent). Rate-limit copy ("Too many invalid attempts. Try again shortly.") is PRESENTATION ONLY over the durable backend windows. Roles without the capability get the truthful no-access surface; server prose/codes are never rendered.

**Fulfillment editor (inside the certified listing editor, per active package/membership option):** load shows the ACTIVE terms or an honest "no terms yet — customers can't use this option at the door"; the form covers finite/unlimited (membership only — a package's total IS its sessions count, stated in place), visits, validity (days-from-purchase / fixed end date / no-time-limit where legal), walk-in/reservation methods (≥ 1 enforced with plain guidance), branch limitation from the real branch list, and optional membership attendance hours; the **"Changes apply to future purchases only"** note renders BEFORE saving and in the success message (the immutable historical-terms rule, owner item 24). Client checks mirror the server for earlier messages; the PUT outcome stays authoritative. `parseAedToFils` gains `allowZero` for the entitlement kinds only.

**Fixture parity (fixture-auth):** `attendance.manage` added to the fixture role registry (same five roles); semantic `fulfillmentPort` (in-memory supersede-and-insert with the same termsValid mirror) and `checkinPort` (stateful seeded credentials — finite walk-in `11112222`, scheduled session `33334444`, unlimited `77778888`, time-lapsed `55556666`; preview resolves LIVE credentials only so consumed/unknown codes are the SAME generic not-found; redeem marks used; miss counting → `tooManyAttempts`); a test-only `consumeCheckInCode` race seam (exposed on `window.__himmaPortalCheckInFixture` in fixture mode only); the fixture zero-amount rule matches the backend per-kind floor.

**Proofs:** portal Jest **815/815 across 63 suites** (+53 new: `live-checkin-port` 15 incl. the 201-only and DTO-fail-closed locks + fixture-isolation/no-scanner structural locks · `live-fulfillment-port` 12 · `check-in` page 11 incl. the consumed-between-preview-and-confirm race, rate-limit copy, role gating, nav truth, and the contract-operations/no-bypass locks · `check-in-a11y` 3 (jest-axe over entry/preview/success/refusal/no-access) · `fulfillment-editor` 6 incl. the future-purchases note, supersede display, membership-at-zero creation, and the validation mirrors; amended truthfully: navigation order/capability locks + the editor kind lock). Real-backend contract suite **76/76 across 7** (+`attendance-contract.test.ts` 6): fulfillment supersede-and-insert with BOTH DB rows asserted (rev 1 superseded, rev 2 active), capacity-unsupported + server-side terms refusals, the live authorization matrix (front_desk/coach forbidden both verbs; listings_editor allowed), and TWO check-in journeys through the LIVE ports over real HTTP — **finite walk-in** (real zero-price acquisition → real credential → preview grants nothing (0 attendance rows) → redeem 201 → exactly ONE attendance row → repeat = truthful `codeAlreadyUsed`; consumed code previews as generic not-found; sold entitlement keeps its historical revision after a live supersede) and **scheduled session** (real quote→hold→confirmed booking → session credential previews with session time → redeem 201 → one row; foreign-org staff get the ONE generic refusal). Fixture-mode Playwright **+15 (5 scenarios × 3 viewports)**: the front-desk happy path with evidence screenshots, nav gating, invalid/expired refusals, and the race refusal (preview cleared, no success frame ever rendered); full portal e2e **223 passed / 20 skipped**.

**Customer-side placement (bounded limitation, reported):** no portal browser E2E harness against the real backend exists (portal e2e is fixture-mode by design; the real-stack proof lives in `test-contract/` over `buildApp` + real PostgreSQL). The contract journeys drive the CUSTOMER side (acquisition, booking, credential issuance) through the backend's own S6-1/S6-2-certified service layer — those customer HTTP surfaces belong to RI-4 (Customer App), which stays untouched.

**Verification (full battery):** backend Jest **1205/1205 across 97 suites** (+8) · backend tsc/ESLint clean · `db:verify` **18/0** (head `0018` — NO new migration) · portal Jest **815/815 across 63** · portal tsc/ESLint clean · portal contract **76/76 across 7** · portal Playwright **223/223 (+20 skipped)** · Customer App Jest **475/475** + tsc/lint clean (zero app changes) · customer Playwright **6/6** · Admin Jest **138/138** · every production payment gate unchanged. **Deliberately absent (locks re-proven):** QR scanning, manual attendance/balance mutation, code-less bypass, provider-generated customer codes, reservation creation (S6-3), payment/commission UX, Customer App/Admin changes. **Next: owner review — S6-3, RI-4, RI-5 do NOT start.**

**W2-13 owner-review correction (membership ProgramRevision compatibility — this commit; `4dea0d4` otherwise accepted; no Check-In/attendance/fulfillment-design/portal-architecture change):** the one blocker — the already-approved `membership` commercial kind must survive the real moderated listing lifecycle.

**Reconciliation (owner item 1 — every price-kind enumeration audited before editing):** stale = exactly ONE structural location, `ck_program_revision_option_kind` (migration 0008, the program_revision store). Everything else already carries or tolerates `membership`: live `program_price_option` + `price_quote` CHECKs widened by 0017; `ck_booking_option_kind` (0013) is CORRECTLY capacity-only (Bookings never carry membership — acquisition is the purchase path); services type off the widened `PriceOptionKind`; revision snapshot creation (`createSensitiveRevision`) and approval/application (`approveRevision`) pass the kind through text/typed columns with no kind switch; moderation/review reads project `option_kind` verbatim; public catalogue projection + search document aggregate kinds kind-agnostically (`array_agg(kind)`, min-price) — `SEARCH_FORMATS` deliberately excludes `free` AND `membership` as a FACET vocabulary (a product-semantics fact, reported, not widened); Kysely codegen types the column `string`; the portal/admin contracts carry the widened vocabulary / plain `string`. The only behavioral stand-ins were the two W2-13 temporary typed `lifecycleConflict` guards in `price-option-management.ts` (add + patch).

**Migration `0019_membership_program_revision_kind` (owner-authorized; the smallest possible widening):** redefines `ck_program_revision_option_kind` in place (the 0017 naming convention) to admit `membership` beside the six existing kinds — nothing else. NO fulfillment semantics enter ProgramRevision (membership stays commercial vocabulary only; finite/unlimited/uses/validity/methods/branch/schedule remain exclusively on the immutable fulfillment-revision model); Booking/capacity/EntitlementPurchase/Entitlement/redemption/attendance/payment/commission untouched. **Down migration fails closed** (the 0017/0018 discipline): an explicit preflight — the FIRST down statement — refuses whenever any `program_revision` row carries `option_kind='membership'` (review history is never deleted, rewritten to another kind, or discarded); with no membership revision state the exact prior constraint is restored cleanly.

**Temporary refusal removed where now obsolete (owner item 7):** both W2-13 guards deleted — review-gated membership adds/patches route through the ORDINARY certified revision path; `lifecycleConflict` remains for genuinely illegal lifecycle states (locked listings, archived options, pending-revision blocks) — no conflict became success.

**Proofs (backend +5 → 1210/1210 across 98):** new `membership-revision.test.ts` (4, real PostgreSQL over the REAL admin review routes — never a state shortcut for the moderated truth): (1) the full moderated membership lifecycle — published listing → membership ADD = ordinary `revisionSubmitted` (row `option_kind='membership'`, nothing applied early) → moderation view + revision queue represent the change set → certified start-review/approve applies it → live membership option exists → the PUBLIC listing read represents the published membership vocabulary; (2) review-gated KIND PATCH to membership through the same lifecycle, with a capacity kind (`camp`) riding the identical path unchanged; (3) cross-org providers cannot introduce membership into another provider's revision (not-found-shaped, zero rows); (4) the F1→F2 immutability chain — customer purchases under fulfillment F1 (real `setFulfillmentConfig`) → provider creates F2 → an ordinary moderated price revision completes → the sold Entitlement still references F1 (F1 superseded but intact) while the next acquisition quote binds F2 at the applied price. `entitlement-migration-compat.test.ts` (+1, staged): 0019 SAFE downgrade with no membership revisions (down succeeds, the restored legacy CHECK genuinely refuses a membership insert, up re-applies) and the DATA-BEARING refusal (genuine membership ProgramRevision through the real provider path → down REFUSED by the preflight → schema at head, `db:verify` 19/0, the revision row byte-identical); the pre-existing 0017/0018 refusal + legacy-downgrade proofs re-staged across the three-migration window. `fulfillment-admin.test.ts` amended: the review-gated membership probe now asserts `revisionSubmitted` + the stored membership revision (was 409); the front_desk/coach/listings_editor/cross-org authorization matrix re-proven UNCHANGED (no editing authority broadened; `attendance.manage` still grants nothing). Portal contract +1 (77/77 across 7): the LIVE editor takes a review-gated membership ADD as an ordinary revision (the pre-0019 false limitation is gone) while `loadFulfillment` still returns the untouched active F1 — immutable fulfillment truth beside the listing revision lifecycle.

**Verification:** backend Jest **1210/1210 across 98** · backend tsc/ESLint clean · `db:verify` **19/0** (head `0019_membership_program_revision_kind`; dev DB migrated) · staged up/down/up + both-direction 0019 proofs green · W3 moderation/revision, Slice-4 catalogue/publication, S6-1 acquisition, and S6-2 attendance batteries all re-ran green inside the full run · portal Jest **815/815 across 63** · portal tsc/ESLint clean · portal contract **77/77 across 7** · portal Playwright **223 passed / 20 conditional skips** · Customer App **475/475** + tsc/lint clean · customer Playwright **6/6** · Admin **138/138** · payment gates unchanged. No S6-3/Customer App/Admin work. **Next: owner review — S6-3 does NOT start.**

**W2-13 · Provider Fulfillment Configuration, Check-In & Attendance UX — OWNER-APPROVED / CLOSED at `b111cbd` (2026-08-27; S6-2 closure bookkeeping `9e12f7e`; base W2-13 `4dea0d4`; membership-revision correction/final `b111cbd`). Closure verification (recorded at approval): backend Jest 1210/1210 across 98 suites · backend tsc/ESLint clean · `db:verify` 19/0 · Provider Portal Jest 815/815 across 63 · Provider contract 77/77 · Provider Playwright 223 passed / 20 conditional skips · Customer App 475/475 · Customer Playwright 6/6 · Admin 138/138 · migration head `0019_membership_program_revision_kind` · all payment/production gates unchanged · clean tree. S6-3 (Entitlement Reservations, Customer Fulfillment Reads, Occurrences & Calendar Authority / Slice 6 Closeout) is AUTHORIZED; RI-4, RI-5 do NOT start.**

---

**S6-3 · Entitlement Reservations, Customer Fulfillment Reads, Occurrences & Calendar Authority / Slice 6 Closeout (implementation `5858fce`, 2026-08-27 — owner-reviewed 2026-08-31: ACCEPTED as the migration-free base; SLICE 6 NOT CLOSED — D-S6-5/D-S6-6 ruled V1-required, correction plan in §25–§33 below; RI-4/RI-5 NOT started; NO migration created — head stays `0019_membership_program_revision_kind`):** the §7/§8/§12/§13 authority is now OPERATIONAL, with one newly-discovered stale-enumeration gap fail-closed typed and reported (below).

**Pre-implementation reconciliation (owner item 1, reported):** (1) `entitlement_reservation` = the 0018 structure exactly (PK `booking_id`; 3-column booking + 5-column entitlement ownership FKs; lineage trigger; append-only; NO status column — lifecycle derived). (2) Quote/hold support = fully structural since 0017: `ck_price_quote_shape` defines `entitlementReservation` (one unit · `option_kind IN (package, membership)` · `total_fils = 0` · `entitlement_id NOT NULL` · `fulfillment_revision_id NULL` — the PURCHASED revision binds transitively through the append-only entitlement's frozen `fulfillment_revision_id` via the five-column FK); composite hold↔quote unit FKs make the hold's unit ≡ the quote's; NO service could mint the shape (the source lock, amended this slice to the owning reservation service only); `claimHold` needed NO change; `confirmFreeBooking` needed the Correction-A4 shape guard (added — schema-tolerant so the staged pre-0017 compat seeding still runs; on the legacy schema the column's absence proves every quote is a capacity purchase). (3) Session = full canonical occurrence identity. (4) CampWeek = date span + daily times (canonical SPAN representation; no per-day durable identity). (5) Cohort = `enrolment_cohort_schedule ⇄ recurring_schedule` (weekdays/times/window/exceptions; timezone structurally pinned `Asia/Dubai`) — a canonical meeting pattern. (6) Camp/Cohort occurrences ARE derivable for CALENDAR display (stable derived keys); they are NOT sufficient for S6-2-grade CHECK-IN authority (no durable occurrence binding on `redemption_credential`/`attendance_record` — `session_id` is the only occurrence column — so a camp/cohort credential cannot durably name its day, per-day attendance uniqueness has no structural backstop, and capacity bookings have no entitlement serialization point): the recorded S6-2 fail-closed refusal STANDS, no fake Sessions were invented, and the smallest future design is reported below. (7) Existing customer APIs: bookings list/detail, program availability, quotes/holds/free-confirm/initiate, purchase + credential routes; no calendar existed.

**Reservation flow (items 2–6; §7 verbatim):** NEW `entitlement-reservation.ts` — `requestEntitlementReservationQuote` (Session occurrences; owned entitlement (cross-account NOT-FOUND-shaped) · effective validity · purchased-snapshot permission (`reservation_required` true — walk-in-only products refuse `reservationNotPermitted`) · Program/org lineage · purchased branch limitation · snapshotted schedule-pattern match (Dubai weekday/time against the immutable terms) · future occurrence inside validity · the certified S5-5 age gate against the entitlement's beneficiary (never caller-chosen — the wire carries only entitlementId+sessionId) · finite advisory `entitlementExhausted`/`entitlementFullyCommitted` (the D-10 advisory precedent); inserts the shape-checked zero-total quote + line + audit). Holds ride the UNCHANGED certified S5 machinery (10-min TTL, effective expiry, locking, idempotency, counters; acquisition quotes stay structurally unholdable). `confirmEntitlementReservation` (scope `entitlement.reservation.confirm`): accepts ONLY reservation-shaped quotes (proof 14; `confirmFreeBooking` refuses them — proof 15); ONE transaction: idempotency → duplicate-live-booking check → provisional Booking + the EXPORTED certified `confirmCore` (unit → hold → booking locks, hold consumption, counter movement, policy fail-close — never a second capacity system) → **entitlement FOR UPDATE LAST** → re-validation (active, reservation permitted) → **FRESH post-lock READ COMMITTED commitment recount** → `availableToReserve > 0` → the ONE `entitlement_reservation` insert under the held lock → audit/outbox once. Entitlement-level refusals throw a sentinel so the ENTIRE provisional transaction (booking, counters, hold consumption, idempotency row) rolls back atomically — the hold stays live. No PaymentIntent/Stripe/commission/attendance/consumption anywhere on the path.

**Finite commitment truth (items 7–10; §7 vocabulary):** one authoritative query — `used` = consuming attendance rows; `reservedUpcoming` = commitments on a confirmed Booking whose Session has not concluded with no fulfilling attendance; `remaining = usesTotal − used`; `availableToReserve = max(0, usesTotal − used − reservedUpcoming)`; no mutable counter anywhere (closeout-locked). The attendance INSERT converts commitment → consumption atomically by derivation; a concluded no-show consumes NOTHING and drops out derivationally (proven); no penalty/auto-debit/status column added; customer cancellation stays deferred (no certified path exists — none invented). Unlimited products expose NO counters (regex-locked on the wire). **The S6-2 walk-in redemption gained the owner-item-32 commitment gate:** a walk-in consumes only an UNCOMMITTED use (fresh statement under the entitlement lock — `used + activeCommitments ≥ usesTotal` refuses the new typed `entitlementFullyCommitted`); reserved-use redemption is covered by its own commitment and unchanged; the S6-2 walk-in-vs-reserved race proof amended accordingly (owning-slice amendment — exactly one attendance, now provably the RESERVED one).

**Concurrency proofs (items 9, 30–32; real multi-connection PostgreSQL):** **proof 11** — `usesTotal = 1`, two transactions reserving two DIFFERENT Sessions: each takes its own unit/hold/booking locks; at the shared Entitlement the winner recounts 1 → commits; the loser's fresh post-wait statement sees the committed reservation → `entitlementFullyCommitted` → its provisional Booking/counters roll back atomically (final: ONE confirmed Booking row total, ONE commitment, one committed seat, zero attendance, `used 0 / remaining 1 / reservedUpcoming 1 / availableToReserve 0`). **Item 32** — walk-in redemption vs reservation confirmation racing the final unconsumed/uncommitted use: both converge at entitlement FOR UPDATE; exactly one effect ever exists (attendance XOR commitment; the loser typed + rolled back). **Reservation after attendance consumed the final credit** → `entitlementExhausted` at the fresh recount. **Idempotency:** same-key replay returns the SAME reservation; a different key against the consumed hold finds `alreadyBooked`; one Booking/commitment/seat ever.

**Customer APIs (items 12–20, 26–27; §13):** NEW `entitlement-read.ts` + `reservation-customer-routes.ts` — `GET /customer/entitlements(/:id)` (Passes & Memberships: participant/program/provider/branch display identity, product label, derived status `active | exhausted | expired` (D-S6-4 — no activation state exists; expiry precedence over exhaustion), validity, walk-in/reservation flags, the four finite truths, the immutable purchased schedule summary, `nextReservedSessionAt`; page-scoped correlated aggregation + ONE grouped schedule-term read — no N+1; NO commission/economics/digest/staff/internal-revision-id material, regex-locked) · `GET /customer/entitlements/:id/attendance` (paged own history; no validator identifiers) · `GET /customer/entitlements/:id/reservable-sessions?from&to` (purchased terms ∩ real upcoming availability with the certified derived-availability bands; bounded ≤ 60 days ∩ validity; authoritative `availableToReserve` SEPARATE from seat availability) · `POST /customer/entitlements/:id/reservation-quote` · `POST /customer/entitlement-reservations/confirm` · `GET /customer/calendar?from&to`. Booking projection (items 26–27, owning-slice amendment): `coveredByEntitlement` + `entitlementId` on the customer Booking read — an entitlement-covered AED 0 Booking is never presentable as a provider's free product; upcoming/past categorization untouched; no internal leakage. Smuggled wire fields stay stripped by the app-wide Ajv — server authority proven.

**Calendar (items 17–20, 34–37, 44; §12):** a DERIVED bounded read (`from`/`to` REQUIRED; max 62 days — no prior window convention existed; recorded), combining confirmed Session Bookings (ONE event each — a reserved Session IS its Booking, annotated `reservedWithPass` with the covering entitlement; never duplicated from the subtype) · CampWeek Bookings as their canonical SPAN (per-day attendance occurrences NOT invented — item 22) · Cohort Bookings expanded ONLY from `enrolment_cohort_schedule → recurring_schedule` ∩ cohort window ∩ range minus `exception_dates` (item 23) · membership occurrences from the entitlement's IMMUTABLE schedule snapshot ∩ validity ∩ range (a later provider revision changes NOTHING — proven byte-identical) · unscheduled packages contribute ZERO dates. Stable derived event keys (`booking:<id>`, `booking:<id>:<date>:<time>`, `entitlement:<id>:<date>:<time>`); dedup: a real booked/reserved Session suppresses the derived membership occurrence for the same participant/program/civil day. All civil-date math in canonical Asia/Dubai (fixed +04:00, structurally pinned; DATE columns read as text — process-timezone-proof). Expansion in service memory after bounded reads; no per-day/per-entitlement queries. The endpoint mutates NOTHING (audit-count-invariant proven). Event shape: key/source/context/participant/program/provider/branch/start/end/span/bookingId/entitlementId — no capacity counters, schedule ids, economics, or staff ids (regex-locked).

**RECORDED S6-3 GAP (STOP-before-migration honored — the W2-13/0019 precedent exactly):** reconciliation + implementation surfaced ONE more stale kind enumeration: **0013's `ck_booking_option_kind`** (`dropIn|monthly|term|camp|package|free`) predates `membership`, so a membership-kind reservation Booking cannot be REPRESENTED (0017 widened the quote CHECK — the reservation QUOTE may carry membership — but its Booking insert would violate the 0013 CHECK). Rather than 500ing at confirmation or quietly creating `0020`, the reservation quote refuses membership-kind entitlements typed (`membershipReservationUnavailable`, 409; the gate deliberately ordered LAST so the refusal proves all other eligibility passed — tested). **Fully operational regardless:** package reservations (the primary V1 reservation product) and membership walk-in/attendance/passes/calendar. **Blocked until the owner authorizes the widening:** reservation confirmation for membership-kind entitlements (and therefore the schedule-matched positive reservation issuance, a membership-only product form). **Smallest proposed design:** `0020` redefining `ck_booking_option_kind` in place to add `membership` (the exact 0019 pattern) with the same fail-closed down preflight (refuse when any membership-kind Booking exists); nothing else touched; credential/attendance/calendar unaffected (they never read `booking.option_kind`).

**CampWeek/Cohort occurrence gate result (items 21–24):** existing durable data is SUFFICIENT for calendar representation (spans + pattern expansion — shipped, with deterministic civil-date tests) and INSUFFICIENT for check-in extension: the missing identity is a durable per-occurrence binding on `redemption_credential` + `attendance_record` (an `occurrence_date` beside the camp/cohort target, pairing CHECKs, and a partial one-attendance-per-`(booking_id, occurrence_date)` unique; a key derived from `occurred_at` is NOT canonical — the −60/+60 window legally crosses civil-date boundaries, and capacity bookings have no entitlement lock to serialize on). Camp/cohort credential issuance therefore STAYS the recorded S6-2 fail-closed refusal (`occurrenceUnsupported`); no scheduled-credential extension shipped (the item-24 conditional was not met); no fake per-day Sessions, no whole-term attendance workaround. An owner decision is needed only if camp/cohort check-in becomes a launch requirement.

**Proofs (+36 → backend 1246/1246 across 102):** `entitlement-reservation.test.ts` (17 — quote shape/row + advisory projection, cross-account shaping, walk-in-only + expired refusals, the purchased-terms occurrence matrix (foreign program/branch/schedule/past/beyond-validity + the last-gate membership proof), the age gate, the full confirm truth-set (booking+commitment+seat+outbox once+ZERO payment artifacts), proofs 14/15 both directions with the hold left live, the idempotency triple, PROOF 11, exhausted-after-attendance, journeys A+B (reserved preview context; attendance carrying Booking+Entitlement+occurrence+participant+provider; commitment→consumption derivation), journey C no-show, the ITEM-32 race, COMMISSION-ONCE (one PAID sale through the real deterministic-gateway pipeline; two reservations + reserved+walk-in attendances; intents/economics/transactions stay exactly 1/1/1), the full HTTP wire journey with the included-with-pass Booking truth, cross-principal/authored-field/unauthenticated security, acquisition-quote unholdability) · `entitlement-customer-read.test.ts` (8 — the four-truth finite detail + hygiene locks, unlimited counter-absence + schedule summary + exhausted/expired derivation, cross-account invisibility, attendance history + paging + staff-free shape, reservable filtering + seat-vs-credit separation, the recorded membership-gap probe (409 + the pass stays alive via walk-in), walk-in-only reservable refusal, My Bookings coveredByEntitlement) · `customer-calendar.test.ts` (5 — the item-44 deterministic proof incl. dedup + immutability + stable keys, the CampWeek span, Cohort expansion + exceptions + instant determinism, range bounds + read-only, the customer-safe shape + auth) · `slice6-closeout.test.ts` (6 — the §42 locks). Owning-slice amendments: customer route inventory (+7 routes) · the `'entitlementReservation'` source lock (owning service only) · the Booking field lock (+2 fields) · http-policy snapshot regenerated · the S6-2 final-credit race assertion (item-32 semantics).

**Slice-6 closeout audit (item 47):** Commercial — package/membership acquisition first-class; commission exactly once per sale (proven after reservations/attendance). Fulfillment — finite/unlimited/validity immutable; historical terms frozen (the W2-13 F1→F2 proofs + the calendar immutability proof). Reservation — capacity + Entitlement commitment cannot oversubscribe (proof 11 + item 32 + the structural backstops). Attendance — code redemption consumes exactly once (credential UNIQUE + entitlement serialization; append-only locked). Provider — W2-13 check-in/configuration operational (batteries re-ran green). Customer backend — Passes/attendance/reservation/calendar authority EXISTS for RI-4/RI-5. Security — cross-account/provider/branch boundaries re-proven; `confirmEntitlementReservation` reachable ONLY through the customer route (import + route-inventory locked). Payments — no new financial authority after acquisition (reservation/read modules payment-import-free by lock; both W5 paid seams still HTTP-unreachable; `productionChargingPossible` literal false; no calendar/occurrence persistence exists).

**Remaining launch/deferred gaps (explicit):** the 0013 `ck_booking_option_kind` membership widening (owner-gated `0020` — above) · camp/cohort check-in occurrence binding (owner-gated durable design — above) · customer Booking cancellation (no certified path; reservation release-by-cancellation is derivationally ready when one exists) · no-show policies/penalties (deferred with the owner) · attempt-window retention, W6 runners, QR presentation, VAT ruling, commission-term administration, real Stripe certification/live activation, production Cognito — all carried unchanged. **RI-4 and RI-5 remain owner-gated; no Customer App/Portal/Admin S6-3 surface exists (closeout-locked).**

**Verification:** backend Jest **1246/1246 across 102** (+36; the S5 concurrency, W5 payment, S6-1 acquisition, S6-2 credential/attendance, and W2-13 fulfillment/moderation batteries all inside the run) · backend tsc/ESLint clean · `db:verify` **19/0** (head `0019_membership_program_revision_kind` — NO migration created) · Provider Portal Jest **815/815 across 63** · portal tsc/ESLint clean · portal contract **77/77 across 7** · portal Playwright **223 passed / 20 conditional skips** · Customer App Jest **475/475** + tsc/ESLint clean (ZERO app changes) · customer Playwright **6/6** · Admin **138/138** (ZERO admin changes) · payment/production gates unchanged. **Next: owner review — RI-4/RI-5 do NOT start.**

---

## S6-3 owner-review correction plan (2026-08-31 — this commit; DOCUMENTATION/DESIGN ONLY)

**Owner review result recorded:** implementation `5858fce` is ACCEPTED as the migration-free S6-3 base; **Slice 6 is NOT CLOSED.** The two recorded S6-3 gaps are ruled V1-REQUIRED: **D-S6-5 RESOLVED** — reservation-enabled `membership` must support the normal EntitlementReservation → Booking path; **D-S6-6 RESOLVED** — sellable/bookable CampWeek and EnrolmentCohort products require canonical per-occurrence attendance/check-in and may not remain permanently `occurrenceUnsupported`. This section is the owner-directed correction DESIGN: migration `0020` is specified below but **NOT created**; no backend/portal/app/admin code changes in this commit; every certified S6-3 authority (reservation quote/hold/confirm, final-credit locking, Passes projections, attendance history, reservable-Session projection, derived Calendar, commission-once, all security/idempotency work) is preserved unreopened.

### §25. D-S6-5 reconciliation — why membership reservation needs schema widening, and why the CHECK alone suffices

**The exact blocking object (re-verified in place):** `confirmEntitlementReservation` creates the reservation Booking by copying the reservation quote's `option_kind` into the `booking` INSERT (`entitlement-reservation.ts` — the certified S5 insert shape). A membership-kind entitlement's reservation quote legally carries `option_kind = 'membership'` (`ck_price_quote_shape`, `entitlementReservation` branch, widened in `0017`), but `0013`'s **`ck_booking_option_kind`** still enumerates `('dropIn','monthly','term','camp','package','free')` — the Booking ROW is unrepresentable. Nothing else blocks it: `ck_booking_one_unit`, the three `fk_booking_hold_*` unit FKs, the live-booking partial uniques, `ck_booking_confirmation_facts`, and `enforce_booking_transition` are all kind-agnostic (the transition trigger freezes `option_kind` but never enumerates it), and `booking` has NO insert-time shape trigger. The widening is therefore exactly the `0019` situation: one stale kind enumeration in one CHECK.

**CHECK-only widening IS sufficient — the cross-trail proof, explicit:** admitting `'membership'` to `ck_booking_option_kind` opens no path that violates any commercial-trail invariant, because every trail boundary is owned by OTHER structures that already handle `membership` correctly:

1. **Ordinary `capacityPurchase` can never purchase membership** — `ck_price_quote_shape` restricts the `capacityPurchase` branch to `option_kind IN ('dropIn','monthly','term','camp','free')`. A membership capacity quote is row-level impossible; no hold, Booking, or PaymentIntent can ever descend from one.
2. **Membership acquisition remains EntitlementPurchase, never ordinary Booking** — the ONLY nonzero membership quote shape is `entitlementAcquisition` (unit-less by CHECK), which is structurally unholdable (`fk_capacity_hold_quote_*` composite FKs require the hold's unit to equal the quote's non-null unit) and therefore unbookable (`booking.hold_id NOT NULL` + the hold-unit FKs). The purchase trail's own trigger (`enforce_entitlement_purchase_shape`) accepts only `entitlementAcquisition` quotes — a reservation quote can never create a Purchase.
3. **No PaymentIntent is created for a reservation** — the reservation quote is `total_fils = 0` by CHECK, and `enforce_payment_intent_amount` refuses any intent on a zero-total quote at the row level. A membership reservation Booking can never acquire payment/economics/commission artifacts.
4. **The only entry points that create Bookings dispatch by quote shape** — `confirmFreeBooking` refuses `commercial_shape ≠ 'capacityPurchase'` (Correction A4, proof 15); `initiateBooking`/`startPaidCheckout` ride capacity quotes only; `confirmEntitlementReservation` accepts only `entitlementReservation` quotes (proof 14). After widening, a membership-kind Booking is reachable through exactly ONE authority: `confirmEntitlementReservation` over an owned, active, reservation-permitted membership entitlement.
5. **`booking.option_kind` carries no commercial authority** — money, economics, and commission all key off the immutable QUOTE (and the purchase trail off `entitlement_purchase`); `confirmCore` consults `option_kind` only for the cohort `monthly|term` Enrolment subtype, which a membership Session reservation never enters. The column is denormalized display/dispatch truth whose agreement with the quote is service-authored at the only two INSERT sites — exactly the standing (pre-`0017`) treatment of `'package'`, which `ck_booking_option_kind` has admitted since `0013` without a kind-agreement trigger. No NEW trigger or constraint is required, and none is added: widening the enum does not weaken commercial-trail separation because that separation never lived in this CHECK.

**Service-side completion (implementation slice, not migration):** delete the deliberately-last-ordered `membershipReservationUnavailable` gate in `requestEntitlementReservationQuote` (the W2-13 obsolete-guard precedent — no conflict becomes success; the typed outcome vocabulary entry and its HTTP mapping retire with it), and amend the two owning tests that pin the gate (`entitlement-reservation.test.ts` last-gate proof; `entitlement-customer-read.test.ts` recorded-gap probe) into the positive membership-reservation proofs of §29. Everything else in the reservation flow is already kind-correct (eligibility, purchased-snapshot schedule matching, the entitlement lock discipline, and the commitment recount never read `option_kind`).

### §26. D-S6-6 reconciliation — the exact durable CampWeek occurrence model

**Verified durable truth:** `camp_week` = `start_date`/`end_date` (civil dates, `end_date >= start_date`, both inclusive) + ONE daily window `daily_start_time`/`daily_end_time` (`daily_end_time > daily_start_time`) + branch/org/program lineage + capacity/state. There are NO exception/closure columns, NO per-day rows, and NO timezone column — the timezone authority is the platform's canonical **Asia/Dubai** (structurally pinned by `ck_recurring_schedule_timezone`; applied as the fixed +04:00 civil-date authority the S6-3 Calendar already uses).

**Canonical occurrence identity:** every civil date `d ∈ [start_date, end_date]` is exactly ONE attendance occurrence with scheduled window `dubaiInstant(d, daily_start_time) → dubaiInstant(d, daily_end_time)`. Multiple attendance occurrences on one date are **structurally impossible** (a single daily time pair exists per camp), so **date-only identity IS safe for CampWeek** — though §28 stamps the start time too, for one uniform key across both multi-occurrence kinds. **Recorded limitation (no semantics invented):** the durable model cannot express a non-running day inside the span (e.g. a camp closed on Fridays); the canonical occurrence set is EVERY span date. If real camps need closures, that is a future owner-ruled model change — the correction does not smuggle one in, and the down-migration/uniqueness design below is unaffected either way. The whole CampWeek is never one attendance; occurrence identity is never inferred from `occurred_at`.

### §27. D-S6-6 reconciliation — the exact canonical Cohort occurrence model, and why date-only identity is UNSAFE

**Verified durable truth:** `enrolment_cohort` (`effective_start`/`effective_end`, branch, capacity) meets per `enrolment_cohort_schedule` (≥ 1 association rows, program-pinned both sides, `active` flag) → `recurring_schedule` (`weekdays[]`, ONE `start_time`/`end_time` pair, `effective_start`/`effective_end`, `exception_dates[]`, timezone CHECK-pinned `'Asia/Dubai'`, state `active|ended`). The canonical meeting set — already the S6-3 Calendar's expansion authority — is: for each ACTIVE association's ACTIVE schedule, every civil Dubai date in (cohort window ∩ schedule window ∩ query range) whose weekday matches, minus `exception_dates`.

**Two meetings on one civil date ARE structurally possible:** a cohort may carry multiple schedules (the PK is `(cohort_id, schedule_id)`), and two schedules can share a weekday with different times (Mon 09:00 + Mon 17:00 — a perfectly legal twice-a-day program). Within ONE schedule at most one occurrence per date exists (one `start_time`; `weekdays` is a set). Therefore **option A (`occurrence_date` only) is INVALID for cohorts** — a per-day unique would make the second legitimate same-day meeting unattendable. **The canonical identity is option B: `occurrence_date + occurrence_start_time`** (civil Dubai date + the schedule term's start time at minute precision) — exactly the identity the derived Calendar already emits (`booking:<id>:<date>:<HH:MM>`), deterministic from durable canonical schedule truth, and self-contained once stamped (no join back through mutable rows). A schedule-ID-based identity was considered and REJECTED: `recurring_schedule` term columns (weekdays/times) are mutable while `active` (the immutability trigger freezes only identity/ownership/`created_at`), so `(schedule_id, date)` does not pin the time the customer was promised, and it diverges from the Calendar's identity. Two schedules degenerately colliding on the same `(date, start_time)` describe the SAME customer-visible occurrence (they already produce one deduplicating Calendar slot) — one attendance for it is correct. Provider edits to active schedules remain the standing §12 disruption-policy seam (deferred with the owner); the durable occurrence stamp on credential/attendance (§28) keeps issued credentials and recorded history stable regardless.

### §28. The one canonical occurrence target model (additive; Session unchanged)

**Session stays untouched:** `session_id` IS its canonical occurrence — issuance, redemption, uniqueness, and Calendar identity are complete and certified. The additive binding below applies ONLY to CampWeek/Cohort Booking targets.

**`redemption_credential` gains two nullable columns:** `occurrence_date date` + `occurrence_start_time time` — the durable, immutable statement of WHICH occurrence this credential admits, stamped at issuance from the canonical schedule (CampWeek: the span date + `daily_start_time`; Cohort: the matched schedule term's date + `start_time`). `ck_redemption_credential_occurrence` is REPLACED by the four-form pairing CHECK: walk-in entitlement target ⇒ `session_id`/`occurrence_date`/`occurrence_start_time` all NULL · Booking target ⇒ EXACTLY one of (`session_id`) XOR (`occurrence_date AND occurrence_start_time` both NOT NULL) · `occurrence_date` and `occurrence_start_time` always NULL or NOT NULL together. `enforce_redemption_credential_shape` extends per target kind: a camp Booking requires `occurrence_date BETWEEN start_date AND end_date` and `occurrence_start_time = daily_start_time`; a cohort Booking requires the `(date, time)` pair to be generated by the canonical model (an active association's active schedule: weekday match + windows + NOT an exception date); branch stamping generalizes from the session's branch to the unit's branch (`camp_week.branch_id`/`enrolment_cohort.branch_id`). `enforce_redemption_credential_transition` freezes the two new columns with the other identity columns. **The provider can never choose or change the occurrence during redeem — it is frozen on the credential before redemption; the customer can never mint an arbitrary date/time — the server derives/validates it from the owned confirmed Booking and the canonical schedule (trigger backstop + service authority); redemption never derives the occurrence from the clock.**

**One-live-per-target generalizes per occurrence:** `uq_redemption_credential_live_booking` is REPLACED by two partial uniques — `(booking_id) WHERE state='live' AND session_id IS NOT NULL` (session Bookings: unchanged semantics) and `(booking_id, occurrence_date, occurrence_start_time) WHERE state='live' AND occurrence_date IS NOT NULL` (one live credential per Booking per occurrence — necessary because adjacent cohort occurrences' ±60 windows can legally overlap, e.g. Mon 09:00 + Mon 10:30). The certified S6-2 issuance/regeneration rules A–H apply verbatim with "target" read as *Booking + occurrence*: `credentialAlreadyLive` returns the live credential FOR THAT OCCURRENCE only; explicit regeneration supersedes by id; secrets travel once; replays carry metadata only.

**`attendance_record` gains the same two nullable columns** + the pairing CHECK (`occurrence_date` NULL ⇔ `occurrence_start_time` NULL; occurrence columns NOT NULL ⇔ booking-backed AND `session_id IS NULL` — i.e. exactly the camp/cohort form) and `enforce_attendance_record_shape` extends its credential↔attendance identity equality to the two new columns — the attendance row's occurrence is provably the CREDENTIAL's, which was provably canonical at issuance. **Scheduled occurrence ≠ redemption timestamp, durably:** `occurred_at`/`used_at` remain the actual redemption instants; the occurrence binding is the scheduled truth. A customer redeemed Sunday 23:50 inside the −60 window of Monday 00:30 produces an AttendanceRecord bound to MONDAY's occurrence — which is exactly why the binding must be a durable column and can never be derived from `occurred_at` (§29 cross-midnight proof).

**Attendance uniqueness (exact):** Session — `uq_attendance_record_session_booking (booking_id) WHERE booking_id IS NOT NULL AND session_id IS NOT NULL` stands UNCHANGED. CampWeek/Cohort — new partial unique **`uq_attendance_record_occurrence_booking (booking_id, occurrence_date, occurrence_start_time) WHERE occurrence_date IS NOT NULL`** — one attendance per participant/Booking/canonical occurrence (one participant per Booking is certified D-2), each remaining occurrence separately attendable. No broad `UNIQUE(booking_id)` (multi-occurrence products attend many times) and no `UNIQUE(booking_id, occurrence_date)` (per §27, two legitimate cohort meetings can share a date; the time component is load-bearing — for camps it is constant and harmless). The `credential_id UNIQUE` single-use backstop, the finite floor trigger, and the reservation-entitlement agreement all stand unchanged.

**Calendar convergence (no second occurrence database):** the attendance occurrence identity `(booking, date, start_time)` corresponds 1:1 with the Calendar's derived event keys — cohort `booking:<id>:<date>:<HH:MM>` already; camp `booking:<id>:<date>:<HH:MM>` once §30 lands. Both derive from the SAME canonical schedule/date/time authority (no separate expansion rules); attendance history references the same occurrence the Calendar displays; recording an attendance changes no event key; nothing occurrence-shaped is persisted anywhere except the two durable stamp columns above.

### §29. Credential issuance API (explicit occurrence selection) + required proofs

**Route (repository-consistent):** the certified `POST /customer/bookings/:bookingId/credential` route gains an OPTIONAL `occurrence` body object `{ date: 'YYYY-MM-DD', startTime: 'HH:MM' }` (additionalProperties:false; the `regenerateCredentialId` explicit-precondition precedent — one endpoint, explicit input, rather than a new URL shape that would fight the route-inventory/URL-regex locks). Dispatch by the Booking's unit: session-backed ⇒ `occurrence` FORBIDDEN (`occurrenceNotApplicable` typed refusal — the Session is the occurrence); camp/cohort-backed ⇒ `occurrence` REQUIRED (`occurrenceRequired` when absent — **never inferred from "whichever scheduled date is closest to now"**, which is genuinely ambiguous under overlapping ±60 windows). The server proves, in order: Booking ownership (cross-account NOT-FOUND-shaped) · `confirmed` state · the requested occurrence belongs to that Booking and exists canonically (§26/§27 derivation — camp span membership + daily time equality; cohort pattern match minus exceptions; foreign/invented `(date,time)` ⇒ `occurrenceNotEligible`) · the occurrence's −60/+60 issuance window is open (server clock vs `dubaiInstant(date, startTime)`; `outsideCheckInWindow` otherwise) · that occurrence not already attended (`alreadyCheckedIn` keys on the occurrence unique, not the whole Booking) · exactly-one-current-credential per occurrence with the S6-2 secrets-once/regeneration semantics (§28). The S6-2 `occurrenceUnsupported` refusal RETIRES for camp/cohort at the implementation slice. Redemption (`/check-in/preview` + `/check-in/redeem`) needs NO wire change: the occurrence rides the resolved credential; preview additionally renders the occurrence date/time context; redeem's attendance INSERT copies the credential's occurrence stamp.

**Required proofs for the later implementation (the owner's matrix, pinned):**

| # | Proof |
|---|---|
| M1 | Membership reservation E2E: real membership acquisition → reservation-enabled Entitlement → Session reservation quote → hold → confirm → membership-kind confirmed Booking + EntitlementReservation → ZERO payment artifacts (no intent/attempt/transaction/economics rows) — and proof 11 (final credit) re-run green on a membership-kind entitlement |
| M2 | Membership acquisition still impossible as ordinary Booking: capacity quotes refuse `membership` at the CHECK; acquisition quotes stay unholdable; commission-once re-proven across acquisition + reservations + attendance |
| C1 | Camp occurrence: confirmed CampWeek Booking → select valid span date → credential inside window → provider redeem → ONE AttendanceRecord bound to that occurrence → same occurrence refuses a second attendance (unique + typed) → a DIFFERENT valid camp day attends separately |
| C2 | Cohort occurrence: confirmed Cohort Booking → valid recurring meeting → credential → redeem → one attendance for that exact meeting → the week's second occurrence remains separately attendable; two same-date meetings (Mon 09:00 + Mon 17:00) attend independently — the date-only-unique counterexample proven as the POSITIVE case |
| X1 | Cross-midnight: redemption within ±60 on the adjacent civil date → AttendanceRecord bound to the SCHEDULED occurrence date/time, never `occurred_at`'s date |
| X2 | Invalid occurrence: arbitrary `(date,time)` not generated by the canonical schedule (wrong weekday, wrong time, outside windows, foreign Booking) → issuance refused typed; nothing persists |
| X3 | Exception date: a cohort `exception_dates` entry → no credential issuable AND no Calendar occurrence emitted (one shared derivation proven at both surfaces) |
| R1 | Per-occurrence concurrency: two staff racing ONE occurrence credential → exactly one attendance; concurrent issuance for TWO occurrences of one Booking → both live simultaneously (per-occurrence uniques), each redeeming exactly once; regenerate-vs-redeem re-proven per occurrence |
| R2 | Occurrence immutability: credential/attendance occurrence columns frozen by trigger; a post-issuance provider schedule edit never changes an issued credential's occurrence or a recorded attendance |

### §30. Calendar corrections (CampWeek daily occurrences; Cohort preserved)

**CampWeek (recommendation adopted as the corrected model):** once canonical daily occurrences exist, the customer Calendar emits **individual daily activity occurrences** — `sourceType: 'campWeekOccurrence'`, key `booking:<id>:<date>:<HH:MM daily_start>`, one per span date, each carrying the existing `span` object as presentation metadata (Himma's calendar shows actual scheduled activity occurrences; a camp day is independently attended and independently real). The single whole-span event retires — the span survives as metadata on every daily occurrence, never as a REPLACEMENT for occurrence truth. This is a stable-key change for camp Bookings only, safe to make exactly now: RI-5 has not started, no frontend consumes the Calendar, and keys must not change later — which is itself a reason this correction precedes RI-5. Attendance history and Calendar then reference identical occurrence identities. **Cohort Calendar is preserved untouched:** the certified expansion (associations → schedules ∩ windows minus exceptions, Asia/Dubai) is ALREADY the canonical occurrence authority — §28's attendance identity reuses it rather than implementing any second expansion; exception dates stay excluded everywhere; historical stability rides the canonical model exactly as certified (schedule edits to active cohorts remain the §12 deferred disruption seam).

### §31. Provider authorization for multi-occurrence check-in (S6-2 map reconciled)

Owner/org_manager (org-wide) and branch_manager/front_desk (branch-scoped, enforced against the credential's stamped unit branch) handle CampWeek/Cohort check-ins in V1 — their S6-2 certification is occurrence-independent and extends without change. **Coach stays FAIL-CLOSED for CampWeek/Cohort:** the certified coach authority is proof-of-assignment via `session.instructor_staff_id` — occurrence-level, immutable-enough, and exact. No equivalent occurrence-level proof exists for the new kinds: `camp_week` has NO instructor column at all, and `recurring_schedule.instructor_staff_id` is schedule-level, OPTIONAL, and mutable while active — and the credential's canonical `(date,time)` identity cannot even unambiguously resolve back to one schedule row when terms collide. Inferring "assigned somewhere in the cohort ⇒ may check in every occurrence" is exactly the inference the ruling forbids. **Coach occurrence assignment for camp/cohort is recorded DEFERRED** (with `recurring_schedule.instructor_staff_id` noted as a possible future basis, unrelied-upon); no new staff-assignment schema is invented — none is genuinely needed for core check-in, which the four non-coach roles fully cover.

### §32. Proposed migration `0020_membership_booking_and_multi_occurrence_attendance` (ONE migration, both blockers; NOT created in this commit)

| # | Object |
|---|---|
| 1 | `ck_booking_option_kind` redefined in place (+`membership` — the exact `0019` pattern; §25) |
| 2 | `redemption_credential` + `occurrence_date date` + `occurrence_start_time time`; `ck_redemption_credential_occurrence` REPLACED by the four-form pairing CHECK (§28); `enforce_redemption_credential_shape` re-created with camp/cohort occurrence validation + unit-branch stamping; `enforce_redemption_credential_transition` re-created freezing the new columns |
| 3 | `uq_redemption_credential_live_booking` REPLACED by the session-scoped + occurrence-scoped live partial uniques (§28) |
| 4 | `attendance_record` + the same two columns; occurrence pairing CHECK; `enforce_attendance_record_shape` re-created with the occurrence-equality extension; new partial unique `uq_attendance_record_occurrence_booking (booking_id, occurrence_date, occurrence_start_time) WHERE occurrence_date IS NOT NULL` |
| 5 | NOTHING else: no new tables, no new indexes beyond the uniques themselves (they are the lookups), grants unchanged (attendance stays INSERT-only; no DELETE anywhere), `db:verify` manifest extended by the owning-slice pattern |

**Untouched, guaranteed:** `entitlement_purchase`, all payment/economics/commission objects, the capacity model and counters, `session` attendance semantics (the session partial unique and all S6-2 behavior are byte-identical; the credential/attendance changes are additive-nullable — every existing row satisfies every new CHECK with zero rewrites), `entitlement`, `entitlement_reservation`, and the Calendar's read-only nature. Existing-row compatibility: all existing credentials/attendances are session- or walk-in-shaped (camp/cohort issuance was fail-closed), so the new occurrence columns are NULL everywhere and every replaced CHECK/unique validates live rows unchanged.

**Rollback preflight (the `0017`/`0018`/`0019` discipline — the FIRST down statement, before any destructive DDL):** the down migration REFUSES, with counted messages, whenever the database is not representable by the pre-`0020` schema: any `booking.option_kind = 'membership'` row · any `redemption_credential.occurrence_date IS NOT NULL` row · any `attendance_record.occurrence_date IS NOT NULL` row. No commercial or attendance history is ever deleted or transformed automatically; with none of that state present, the exact prior CHECKs/uniques/triggers are restored and up re-applies cleanly (proven up/down/up + both data-bearing refusals, the staged compat pattern).

### §33. Owner decision state at this STOP

**RESOLVED this round (recorded, binding):** D-S6-5 (membership reservation → Booking path is V1-required; §25 CHECK-only widening) · D-S6-6 (canonical per-occurrence camp/cohort attendance is V1-required; §26–§29 model). **Remaining genuine owner decisions:** (1) approval of THIS correction plan, which authorizes the `0020` implementation slice (migration + service/route/test work of §25/§28/§29/§30 + the owning-lock amendments — nothing else); (2) ratification of the §30 CampWeek daily-occurrence Calendar recommendation (the correction is specified around it; a span-retaining alternative would keep attendance/Calendar identities divergent, which §8 of the owner directive argues against); (3) the §26 recorded CampWeek closure-day limitation — only if real camps need non-running days inside a span (no V1 blocker identified); (4) coach occurrence-assignment for camp/cohort — recorded deferred (§31). Deferred items carried unchanged: no-show policy, customer cancellation, disruption policy, attempt-window retention, QR presentation, W6 runners, and every production payment gate. **This commit is documentation only: no migration, no backend/portal/app/admin change, `0020` NOT created, RI-4/RI-5 NOT started.**