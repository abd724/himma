# 34 — W1-RI · Customer App Real-API Integration / Production-Like Customer Experience — Workstream Plan (W1-RI-0)

**Status: W1-RI-0 OWNER-APPROVED and CLOSED at `ea6cfbf` (2026-08-25). The §12 owner rulings are RESOLVED and recorded in §12.1 — headlines: D-RI-1 resolved in principle (unit-less purchases NEVER forced into the capacity Booking model or faked with capacity units; S6-0 owns the smallest parallel entitlement-purchase aggregate reusing/generalizing the certified W5 commercial/payment authority — never a second payment stack; any additive evolution of the structural `PaymentIntent → Booking` binding requires S6-0 to specify the exact invariant and STOP before any migration; W5 is NOT reopened during RI-1) · D-RI-2 resolved (V1 semantics: finite multi-use package · limited-use membership · unlimited time-based pass · validity snapshot · reservation-required · walk-in-allowed; S6-0 reconciles the canonical schema/model; pricing and fulfillment semantics stay separated where appropriate — not everything belongs in a price kind) · D-RI-3 resolved (V1 auth target: email/password + Sign in with Apple + Google; RI-1 makes the real email/password flow functional FIRST; Apple/Google reuse the certified identity architecture and stay FAIL-CLOSED with recorded operational prerequisites when genuine provider/app configuration is unavailable — social-login success is never faked) · D-RI-4 resolved (public guests may see customer-safe upcoming occurrence information: actual date/time, branch/location identity needed for selection, `available|fewLeft|full`, low `spotsLeft` where the certified projection genuinely provides it; NEVER held/booked counters, versions, locks, or internals; the bounded public projection lands with RI-2 where backend support is absent) · D-RI-5 resolved (customer payment copy follows AUTHORITATIVE status only: `awaitingPayment` after browser return = "We're checking your payment status…" · trusted-received/saga-unresolved = "Payment received — confirming your booking…" · `compensationPending` = "Your payment was received, but the spot was no longer available. Your payment is being returned." · `compensated` = "Your payment has been returned because the spot was no longer available."; browser return alone can NEVER select the payment-received wording). **RI-1 (Customer App Foundation, Authentication, Account & Participants) is OWNER-APPROVED and CLOSED at `8046a8a` (2026-08-25; W1-RI-0 closure bookkeeping `1816d2f`; closure record at the end of this document — verification at approval: backend Jest 1119/1119 across 90 suites · backend tsc/ESLint clean · `db:verify` 16/0 · Customer App Jest 444/444 across 28 suites · app tsc/lint clean · Playwright 1/1 real local-stack journey · portal contract 70/70 · admin contract 15/15 · no migration · clean tree). Binding completion distinction: real Himma `LoginSession`/account/participant persistence is INTEGRATED; the current email/password development credential acquisition uses the dev identity stand-in behind the certified ports; the real Cognito pool + Apple/Google configuration remain operational prerequisites and are NOT claimed complete. **RI-2 (Customer Discovery, Catalogue, Search & Public Availability Integration) is OWNER-APPROVED and CLOSED at `d851c61`** (2026-08-26; RI-1 closure bookkeeping `d012989`; closure record at the end of this document — backend Jest 1124/1124 across 91 suites · backend tsc/ESLint clean · `db:verify` 16/0 · Customer App Jest 460/460 across 30 suites · app tsc/lint clean · Playwright 3/3 · portal Jest 762/762 · admin Jest 138/138 · no migration · clean tree; preserved invariants: ladies-only = public discovery metadata only, public availability = effective unexpired-hold truth, real discovery IDs never cross into mock booking authority, public/provider media = a separate later authority where not already real). **RI-3 (Customer Booking, Checkout, Payment Status & My Bookings Real Integration) is OWNER-APPROVED and CLOSED at `f4ec0e6`** (2026-08-26; RI-2 closure bookkeeping `1c24026`; closure record at the end of this document — backend Jest 1124/1124 across 91 suites · backend tsc/ESLint clean · `db:verify` 16/0 · Customer App Jest 475/475 across 33 suites · app tsc/lint clean · Playwright 6/6 · portal Jest 762/762 · admin Jest 138/138 · no migration · clean tree; preserved non-blocking gaps: native `himma://` checkout-return/deep-link configuration → RI-6/native integration · bounded stale `holdAlreadyActive` copy after reload → later UX hardening unless it becomes blocking · native/mobile validation → RI-6). **S6-0 (Fulfillment, Entitlements & Attendance planning/canonical design — PLANNING ONLY, no migrations/backend/portal/app code) is AUTHORIZED; S6-1, RI-4, and W2-13 do NOT start.** *(Superseded by later closures:)* **RI-4 (Passes & Memberships, Entitlement Reservation & Customer Check-In) is OWNER-APPROVED and CLOSED at `b060dd4`** (2026-08-31; base implementation `7080b1c` + owner-directed occurrence-contract correction `b060dd4`; closure record at the end of this document — Customer App Jest 499/499 across 36 suites · app tsc/lint clean · Customer Playwright 10/10 · backend Jest 1259/1259 across 103 suites · backend tsc/ESLint clean · `db:verify` 20/0 · Provider Portal Jest 815/815 + contract 77/77 · Admin 138/138 · migration head `0020_membership_booking_and_multi_occurrence_attendance`, no RI-4 migration · clean tree; binding: `eventKey` opaque — occurrence authority is the explicit server `occurrence: {date, startTime}` DTO only). **RI-5 (Unified Himma Calendar) is AUTHORIZED; RI-6 does NOT start.**

Authority: owner direction 2026-08-25 (W1-RI activation; binding product direction: production-like Customer App on real authoritative APIs, minimum intervention, approved UX preserved; customer use of Himma is FREE — no subscription/service/platform fee, customer pays only the provider's listed price; launch marketplace commission 12% of the provider sale via the certified D-W5-7 provider-specific mechanism at `1200 bps` for normal launch providers — never customer-visible); docs/23 §3 (W1 = Customer application: "…then API integration and native hardening"; W4 = backend platform; each workstream plans on the docs/20–22 pattern as it activates), §16 (P2 "customer app integrates identity + catalogue slices behind existing contracts", P3 "W1 Payment & Confirmation milestone"); docs/24 (canonical domain: §1.2 Participant, §2.2/§2.6 ProgramPriceOption + Amendment A2, §3.4 Booking/Enrolment/PackageEntitlement, §3.5 AttendanceRecord, §13 slice table, §14.B5/B7); docs/32 (Slice-5 closure: certified booking/capacity authority + recorded package-purchase discrepancy + deferred inventory); docs/33 (certified W5 payment core + W5-5 customer contract); docs/04 (approved 32-screen IA), docs/08 (frontend engineering contract), docs/12 (native compatibility), docs/17–22 (closed mock milestones), docs/09 (open decisions incl. §21.7/§21.14/§6/§22).

---

## 1. Customer App as built (reconciled inventory, verified at `3c0bb45`)

**Stack:** Expo SDK 57 / React Native 0.86 / React 19, expo-router (file-based, typed routes), React Context + local state (no Redux/query lib), `StyleSheet` over central Orbit-Indigo tokens (`src/theme/`), Manrope, light-only, scheme `himma`. **Data fetching:** screens call directly-imported MOCK service singletons in `useEffect` — there is NO dependency-injection seam and NO HTTP client anywhere (grep-verified: zero `fetch`/axios/env-URL usage; all imagery is bundled assets; the only external URL is the `https://himma.app/...` share placeholder). **Tests:** 413 Jest cases across 24 files (pure logic + mock-service determinism + structural locks incl. the payment-submission-absence lock); no component-render tests, no Playwright suite. **Localization/RTL:** none (English literals; RTL-hostile physical properties) — consistent with the English-only stage ruling; W7 debt recorded, not W1-RI scope.

**Screens (14 of the approved 32 built):** Home (HMA-004) · Discover (005) · All categories (011) · Category (012) · Activity type (013) · Results (010) · Search (009) · Map (016) · Program details (015) · Provider storefront (014) · Booking selection (017) · Participant (018, eligibility inline = 019) · Summary (020) · Checkout (021, CTA deliberately inert). **Dock destinations `Bookings` / `Saved` / `Profile` exist but are inert (no routes).**

**Per-surface classification (owner §1 vocabulary):**

| Surface | Classification |
|---|---|
| Home, Discover, categories/activity/results, Search, Map, Program details, Provider storefront, Booking selection/participant/summary, Checkout page | **Mock/fixture-backed** (deterministic services behind typed contracts; UI production-approved) |
| Checkout SUBMIT, payment | **UI exists, backend authority missing on the wire by design** — `PaymentSubmitRequest/Result` declared, never invoked (test-locked); the certified real contract now exists server-side (W5-5) |
| Sign-in / onboarding (HMA-002/003) | **Missing required V1 surface** (two decorative "Sign in" affordances only) |
| Bookings tab (006), Booking detail (024), Payment status (022), Confirmation (023) | **Missing required V1 surface** |
| Passes & Memberships (026 "Package or membership detail") + check-in credential UX | **Missing required V1 surface** (only a mock `ActivePlan` card on Home) |
| Profile (008) + Participants (030) | **Missing required V1 surface** |
| Saved (007) | Partially present (in-memory favourites toggle works; no route, no persistence) |
| Credit strip, reviews count, rewards accents, gifts | **Obsolete/deferred UI → HIDE, do not connect** (decorative; credits/gifts/reviews/loyalty are deferred product areas) |
| Cancellation (025), Credits (027), Gifts (028), Referrals (029), Notifications (031), Settings (032 beyond a minimal shell) | **Deferred** (not V1 blockers; §8) |
| REAL API already integrated | **NONE — 0 surfaces.** |

**Mock → real authority matrix (all 23 contract methods are mock-only today):**

| App contract method | Real authority (certified route(s)) | Status |
|---|---|---|
| `catalogue.getAllCategories` | `GET /catalogue/categories` + `/catalogue/activity-types` | REAL EXISTS — adapter only |
| `catalogue.getCategoryPage` / `getActivityTypePage` | compose `/catalogue/*` + `GET /search` (+collections) | REAL EXISTS — client-side composition |
| `search.getSuggestions` / `search` / `getResults` / `countResults` | `GET /search` (closed vocabulary) | REAL EXISTS — adapter + mapping |
| `search.getPreSearchContent` / `addRecentSearch` / `clearRecentSearches` | client-local (recents on device) + taxonomy reads | no backend needed (deliberate) |
| `details.getProgramDetailPage` | `GET /listings/:programId` (+ occurrence display — §12 D-RI-4) | REAL EXISTS (occurrence display gap §4.5) |
| `details.getProviderStorefrontPage` | `GET /providers/:organizationId` + `/providers/:organizationId/listings` | REAL EXISTS — adapter only |
| `homeFeed.getHomeFeed` / `getAreas`; `discoverFeed.*`; `map.getMapView` | client-side composition over `/catalogue/*`, `/search`, own bookings/entitlements | REAL EXISTS for parts; no aggregate endpoint (deliberate — minimum intervention; add server aggregates only if measured performance demands) |
| `schedule.getUpcomingActivity` / `getWeekSchedule` / `getActivePlans` | `GET /customer/bookings` + **S6 entitlement/occurrence reads (§4)** | PARTIAL — entitlement/occurrence authority missing |
| `booking.getBookingOptions` | `GET /listings/:programId` + `GET /customer/programs/:programId/availability` | REAL EXISTS (availability is auth-gated — §12 D-RI-4) |
| `booking.getBookingSummary` | `POST /customer/quotes` | REAL EXISTS — adapter |
| `checkout.getCheckoutPage` | quote + policy snapshot presentation | REAL EXISTS (D-8 policy CONTENT remains a launch prerequisite — §11) |
| *(no method yet)* auth/session/account/participants | `POST /auth/session`, `GET /me`, refresh/logout/step-up | REAL EXISTS **except participant management (§4.1)** |
| *(no method yet)* hold/confirm/initiate/payment status/bookings | `POST /customer/holds`(+release/status), `confirm-free`, `initiate`, `GET /customer/bookings/:id/payment`, `GET /customer/bookings(/:id)` | REAL EXISTS (certified S5-5/W5-5) — new app contracts |
| *(no method yet)* entitlements / check-in / attendance / calendar | **DOES NOT EXIST — S6 prerequisite (§4)** | MISSING backend authority |

---

## 2. Certified backend available to W1-RI (verified route inventory)

Public: `/listings/:programId` · `/providers/:organizationId` (+`/listings`) · `/catalogue/{categories,activity-types,collections,areas}` · `/search` · `/internal/health` · `POST /payments/webhook/<provider>` (Stripe-authenticated). Auth flow: `POST /auth/session` (Cognito access token → Himma session; first login atomically creates user + account + `self` participant) · `GET /auth/csrf` · `POST /auth/refresh` · `POST /auth/password/reset-request`. Authenticated customer: `GET /me` · session management (`/auth/sessions`, logout, logout-all, step-up, identity linking) · the certified S5-5 booking surface (availability · quotes · holds claim/status/release · confirm-free · initiate · booking reads) · the certified W5-5 payment surface (real checkout initiation behind composed payment capability + the converged `GET /customer/bookings/:bookingId/payment` projection). Conventions W1-RI consumes as-is: bearer auth (access token, ID tokens refused), body-field `idempotencyKey` (8–128), not-found-shaped cross-account refusals, typed outcome codes (docs/22 §22.10-compatible), fils-integer money, `Asia/Dubai` server time authority, effective-hold-expiry projections. **No new backend endpoint is invented where these exist.**

---

## 3. The V1 fulfillment model (owner-confirmed) vs the canonical domain

Owner-confirmed V1 product shapes: **(a)** single scheduled session; **(b)** multi-use package (buy once, no upfront date selection, `purchased → used → remaining`); **(c)** limited-use membership (e.g. 8 visits / 30 days); **(d)** unlimited time-based membership/pass (e.g. one month unlimited); **(e)** recurring scheduled program (term/academy; attendance per occurrence; absence ≠ attended). Automatic recurring Stripe billing is NOT V1 (manual renewal; schema stays recurring-billing-compatible per docs/24 §14.B5 — untouched).

**Vocabulary reconciliation (before any new enum):** the certified `program_price_option.kind` set is `dropIn · monthly · term · camp · package · free`. Mapping: (a)=`dropIn` (+`free` for genuinely free offerings; `freeTrial`/`paidTrial` Offers remain provider discounts, never a Himma subscription); (b)=`package` (+`sessions_count` — already certified and publicly served); (e)=`monthly`/`term` cohort enrolment (certified units + D-10-adjacent flow); (c)/(d)= **no certified representation** — `membership` was explicitly reserved as an additive CHECK widening pending docs/24 §14.B7 / docs/09 §21.14 (current ruling "monthly remains the membership representation" predates this owner direction and is now ripe for revision — §12 D-RI-2). Validity windows (30/60/90 days/fixed date/none) and reservation-mode (`reservationRequired` vs `walkIn`) have **no representation anywhere** and are provider/product configuration, not global rules.

---

## 4. Backend V1 gap analysis (the bounded prerequisite inventory)

The certified backend is COMPLETE for auth/catalogue/search/storefront/availability/quote/hold/free-booking/paid-checkout/payment-status/booking-reads. Genuine gaps, each verified against schema + source + structural locks:

**4.1 Customer participant management — GAP (unshipped certified-plan scope).** docs/24 §13 slice "Participants" specified participant CRUD; the executed slices shipped only implicit `self` creation at first login. `participant` supports `kind='child'` + `date_of_birth` (quote eligibility already consumes DOB), but **no route creates/edits/archives a participant and `/me` returns only `self`**. Without it a parent cannot book for a child — a V1 blocker. Bounded addition: customer participant routes (create/edit/archive child; list), one-`self` invariant already structural. No migration expected.

**4.2 Entitlement purchase — GAP (recorded Slice-5 discrepancy, now ripe).** `package_entitlement` exists as structure only (booking-subtype PK, `sessions_total/used`, valueless `expiry_policy`, INSERT-only grants — `sessions_used` is deliberately un-incrementable); **no path reaches it**, because a package sale has no dated capacity unit and the certified commercial spine requires Booking = exactly-one unit + hold, with `payment_intent` composite-FK-bound to that Booking. Packages/passes therefore CANNOT ride the spine unchanged. This is THE S6-0 design decision (§12 D-RI-1): extend the commercial spine so a unit-less entitlement purchase can carry a PriceQuote + PaymentIntent + D-W5-7 economics + the certified W5 checkout/saga unchanged in semantics (options: unit-less Booking subtype vs parallel purchase aggregate anchored into `payment_intent` the way Booking is). The W5 invariants (one live intent per purchase, hold-TTL-as-backstop → replaced by a purchase-window backstop for unit-less sales, compensation on unconfirmable fulfillment) must be preserved, not redesigned.

**4.3 Entitlement/pass domain — GAP.** Required and absent: participant-bound entitlement records (finite `total/used/remaining` AND explicitly-modeled unlimited semantics — never a fake large count); snapshotted validity (days-from-purchase/fixed-date/none — later provider changes never rewrite sold entitlements, the certified quote/policy-snapshot precedent); reservation-mode semantics (`reservationRequired` products book units against the entitlement via the certified hold flow at zero incremental price; `walkIn` products redeem without a preselected occurrence); customer entitlement reads ("Passes & Memberships"). Participant ownership must be structural (composite FKs on the certified `participant(id, account_id)` spine — cross-account/cross-participant substitution impossible, the 0015 precedent).

**4.4 Redemption credential + attendance — GAP (tables structurally FORBIDDEN until their owning slice).** `attendance_record` and any `*redemption*` table (besides `trial_redemption`) are lock-forbidden; `attendance.manage`/`roster.view` provider capabilities are RESERVED, awaiting exactly this slice. Required per owner §8–13: short-lived one-time redemption credential (default TTL 10 min, server-configurable; numeric code first, representable as QR later without redesign — the credential is a domain row, the rendering is presentation); generation consumes nothing; regeneration after expiry; at most one live credential per redemption intent; single-use forever after successful validation; concurrent double-redeem consumes exactly one (PostgreSQL authority, the S5-2 locking discipline); scheduled check-in window (~start−60 min → start+60 min, configurable) vs flexible-entitlement generation while valid; provider validation routes gated by org/branch/capability scope (owner/org-manager/branch-manager/front-desk/coach — activating the reserved capabilities); atomic attendance + consumption; append-only attendance history; V1 auto-consumption ONLY via successful provider validation (no-show policies deferred). AttendanceRecord's canonical §3.5 shape is the starting point; the credential entity is NEW canonical vocabulary → docs/24 amendment at S6-0.

**4.5 Occurrence/calendar reads — GAP (read-side only).** `session` rows carry real instants; `camp_week` carries date ranges; **cohort meeting patterns are NOT materialized into occurrences** (pattern = `enrolment_cohort_schedule` → `recurring_schedule` weekdays/times/exceptions ∩ cohort range). Per owner §14 (prefer deriving from authoritative data): a customer calendar read derives occurrences from Booking→unit joins + cohort-pattern expansion at read time — NO duplicate calendar truth, no new occurrence tables. Unscheduled entitlements appear under Passes & Memberships, never as fake calendar entries. Also here: guest-visible occurrence display on program details (docs/32 recorded "public availability exposure" as deferred/open — §12 D-RI-4).

**4.6 Runnable server composition — GAP (operational, bounded).** `buildApp` is composed ONLY by tests: backend has no entrypoint/`start` script. W1-RI needs a dev-server composition (env-driven `buildApp` + deterministic-or-Cognito identity adapters + deterministic payment provider + checkout URLs + seeded dev data). Bounded, no schema, no new authority.

**Explicitly NOT gaps (already certified):** paid checkout + processing/compensation truth (W5); free/zero-price flow + D-10; availability projections; cross-account shaping; idempotency; commission economics (12% = an `organization_commission_term` row at `1200` bps per launch provider — operational data, no code change, customer-invisible).

---

## 5. Prerequisite backend workstream: W4 Backend Slice 6 (S6) — Fulfillment, Entitlements & Attendance

Per docs/23 §3, backend work activates as a W4 slice with its own plan on the docs/20–22 pattern. The executed convention (docs/26/27/28/32 = Slices 2–5) makes this **Backend Slice 6 (S6)** — no new workstream name invented. Gaps 4.2–4.5 (+4.1, see §7 sequencing) are S6 scope; S6-0 authors `docs/35` (spec + docs/24 amendment + owner rulings D-RI-1/2 and ratification of the §8–9 credential defaults) before any S6 code. S6 must preserve untouched: the frozen Slice-5 capacity/booking authority, the certified W5 payment core (checkout/saga/economics consumed, never duplicated), D-10, and every structural lock (amended only by the owning-slice pattern). Expected magnitude: one migration family (entitlement purchase anchor + entitlement + redemption credential + attendance_record + vocabulary widenings), domain services (purchase→entitlement creation through the certified checkout; credential issue/validate/redeem; attendance), provider routes (validation/roster under activated capabilities), customer routes (entitlements read, credential issue, calendar read), full RED→GREEN concurrency proofs (double-redeem storm, TTL races, finite-balance floor, credential single-use).

**Provider-side consequence (recorded, not planned here):** providers must configure validity/reservation-mode and staff must validate check-ins → one bounded W2 Provider Portal slice ("check-in & product-config", after S6) and later Admin oversight reads. The portal slice gates the end-to-end package/membership journey, not the customer-app build.

---

## 6. Customer account IA + calendar (minimum intervention)

The approved IA already contains the owner's structure: **Bookings (HMA-006)** = Upcoming / Calendar-agenda / Past + "package or membership status"; **Package/membership detail (HMA-026)** = the Passes & Memberships detail (+ check-in credential UX added as a bounded extension of this approved screen); **Profile (HMA-008)** + **Participants (HMA-030)** = account hub. No redesign — W1-RI builds the missing approved screens and wires the built ones. Calendar = the §4.5 derived read across providers (sessions at instants; camps as date-span entries; cohort enrolments expanded from patterns; scheduled entitlement uses as they are booked). Redemption UX truth (owner §16): after provider validation the app re-reads server truth (`Checked in`, updated remaining/attendance) — never optimistic decrement; the existing payment-status polling pattern (`processing` → `confirmed`) is the precedent.

---

## 7. Proposed implementation sequence (each slice: implement → certify → commit → STOP → owner review)

| # | Slice | Scope (bounded) | Gate / depends |
|---|---|---|---|
| **W1-RI-0** | THIS PLAN | owner approval + §12 rulings | — |
| **RI-1** | **Integration foundation + auth + account** | The service seam (contract-conformant real-API adapters behind an injection boundary — screens keep their contracts, mock stays for tests/QA); typed HTTP client (bearer/refresh/typed outcomes/idempotency); env config; **bounded backend companions (identified prerequisite gaps): dev-server composition (§4.6) + customer participant management routes (§4.1)**; sign-in/onboarding screens (HMA-002/003) on the certified `POST /auth/session` flow (email/password via Cognito — D-RI-3); Profile shell (HMA-008) + Participants (HMA-030); guest browsing preserved (auth only at participants/booking/account per owner §17) | plan approved |
| **RI-2** | **Discovery on real APIs** | catalogue/search/results/details/storefront/map/home/discover feeds onto the §1 matrix authorities; contract-parity tests (mock vs real adapter shape); hide-not-connect sweep (credit strip, review counts, gifts affordances); Saved: local persistence + route (no backend) | RI-1 |
| **RI-3** | **Booking + paid/free checkout on real APIs** | availability→quote→hold (visible authoritative 10-min countdown; truthful expiry, no silent re-reserve)→confirm-free / initiate→Stripe-hosted redirect (expo-web-browser + `himma://` return as navigation only)→payment-status polling (processing/confirmed/compensated vocabulary — copy for compensation states fulfils the recorded W1 requirement)→confirmation (HMA-023)→Bookings tab v1 (HMA-006 upcoming/past)+Booking detail (HMA-024)+Payment status (HMA-022) | RI-2 (runs against dev payment composition; D-W5-6 unaffected) |
| **S6-0** | **docs/35 — S6 spec + docs/24 amendment** | §4.2–4.5 model + rulings D-RI-1/2 + credential-semantics ratification | may start in parallel any time after W1-RI-0 |
| **S6-1…n** | **S6 implementation** (its own plan's slicing; expect 2–3 slices: schema+purchase→entitlement; redemption/attendance+provider routes; customer reads/calendar) | S6-0 |
| **W2-13** | **Provider Portal check-in + product-config slice** | staff validation UI on the S6 provider routes; validity/reservation-mode editing | S6 |
| **RI-4** | **Passes & Memberships + check-in UX** | package/membership purchase flows (certified checkout over the S6 anchor); entitlement list/detail (HMA-026); credential generation (TTL countdown display only — server truth); post-redemption truth refresh; attendance history | S6 (+W2-13 for E2E) |
| **RI-5** | **Calendar + My Himma completion** | unified calendar (§6); home real-schedule surfaces (upcoming/week/plans on real reads); Profile completion (minimal Settings/help/privacy shell — HMA-032 minimal) | RI-3 + S6 reads |
| **RI-6** | **Production-like E2E hardening/closeout** | Playwright journeys (§8 acceptance set) + mobile viewports; native validation pass (docs/12 three levels; iOS Simulator); cross-account security regression at the app layer; performance/error/empty-state sweep; closeout audit (S5-6/W5-6 pattern) | all above |

Rationale: RI-1→RI-3 deliver the complete certified-backend value (auth→discovery→paid booking) with zero dependence on S6; S6/W2-13 proceed as the only new backend/portal authority; RI-4/5 consume it; nothing is built twice. Larger vertical slices per owner efficiency guidance; every authority-bearing milestone keeps the commit→STOP→review gate.

## 8. V1 / deferred matrix + final acceptance

**V1:** everything in §7. **Deferred (hide, don't connect; owners recorded):** automatic recurring billing (B5) · self-service cancellation/refunds (§18 #14 + refunds slice) · automatic no-show charging (provider policy capability) · disruption execution (D-5) · waitlists (§21.4) · reviews/ratings · loyalty/credits/gifts/referrals · medical/allergy sharing (counsel; ParticipantLegalProfile) · gender eligibility (no server rule exists) · Stripe Connect/payouts · Admin commission mutations (D-W3-5) · native PaymentSheet (D-W5-2 fast-follow) · Arabic/RTL implementation (W7; no new RTL-hostile surfaces in new screens — logical properties required) · customer web (§18 #8, open).

**Final production-like acceptance (RI-6, Playwright + native pass):** (1) paid single-session journey: browse→sign-in→participant→quote→hold(countdown)→hosted checkout→processing→confirmed→My Bookings/detail; (2) finite package journey: purchase→Passes→generate code→provider validates (portal)→remaining decrements→attendance visible; (3) time-membership journey: purchase→active pass→recurring calendar occurrences→check-in→attendance history; (4) free/zero-price journey: atomic non-payment confirmation (D-10 intact); (5) cross-account/cross-participant security probes around every journey; (6) truthful failure states (hold expiry, payment processing, compensation vocabulary, offline/error). All app-side money displays are provider list prices only; commission/economics never on the wire (already server-pinned).

## 9. Testing strategy

Per slice: Jest for adapters/pure logic (existing 413-case suite preserved; contract-parity suites pin mock↔real shape equivalence); backend companions certified on real PostgreSQL with the standing conventions (full backend Jest + tsc + ESLint + `db:verify` + route/security locks); S6 concurrency proofs RED→GREEN on real PostgreSQL (double-redeem, credential single-use, balance floor, purchase storms) on the S5-2 harness pattern; Playwright journey suites (introduced at RI-3, completed RI-6) at 390×844-first plus tablet/web-preview viewports; docs/12 native validation levels reported per screen (native validated only with a real Simulator/device pass); security tests at both layers (backend certified suites re-run + app-level cross-account probes).

## 10. Explicitly NOT in W1-RI (carried gates — do not weaken)

Production payment launch prerequisites stay exactly as recorded at W5 closure: D-W5-6 real Stripe certification · live activation · UAE VAT/principal-agent/invoicing · production commission-term administration · W6 wind-down runner · gateway-fee decision. Production-like ≠ production-live payments: W1-RI proves the complete app against certified contracts with deterministic/test payment composition; real-Stripe smoke remains separately reported. Also carried: D-8 cancellation-policy template CONTENT (production confirmation fail-closed until owner-approved content exists) · production Cognito pool + real-pool smoke (docs/26 §14.E′) · content-safety/storage operational items (W3) · Apple/Google sign-in operational setup (structurally supported; not a blocker per owner §17).

## 11. Operational/launch prerequisites NEW to this direction (recorded)

Launch commission terms seeded at `1200 bps` per launch provider (operational data via the recorded commission-administration prerequisite — no code) · dev/staging Cognito pool or documented deterministic dev-auth composition (RI-1) · dev payment composition (deterministic provider + checkout URLs; later Stripe TEST keys per D-W5-6) · seeded realistic dev catalogue/schedule data (mock fixtures graduate to seeds — docs/24 §13 slice-1 intent) · production checkout success/cancel deep-link URLs (with RI-3 shape, configured at launch) · S6 credential TTL/check-in-window configuration defaults (10 min / ±60 min).

## 12. Owner decisions genuinely required (none resolved silently)

| ID | Question | Options / recommendation | Blocks |
|---|---|---|---|
| **D-RI-1** | **Entitlement purchase commercial anchor** (§4.2): how unit-less package/membership purchases join the certified quote→payment→saga spine | (a) unit-less Booking variant (relax exactly-one-unit for entitlement kinds; hold-backstop replaced by a purchase window); (b) parallel `entitlement purchase` aggregate anchored into `payment_intent` beside Booking (Booking stays untouched). **Recommend (b)** — preserves every frozen Slice-5 invariant verbatim; W5 orchestration gains one bounded alternative T1. Final ruling at S6-0 with the full schema design | S6-1+ |
| **D-RI-2** | **Product vocabulary widening** (§3; re-opens docs/09 §21.14 / §14.B7 under the new direction): represent limited/unlimited time passes | **Recommend:** additive `membership` price-option kind + product config columns (validity days/fixed-date/none · finite `uses_count?` · `reservation_mode`) snapshotted into sold entitlements; `monthly`/`term` stay the recurring-program kinds; no customer-visible vocabulary change | S6-0/S6-1; W2-13 editor |
| **D-RI-3** | **V1 sign-in methods** | **Recommend:** email/password via Cognito at RI-1 (adapter certified; reset flow exists); Apple/Google = structurally-ready operational fast-follow (entitlements/OAuth setup), never blocking integration | RI-1 |
| **D-RI-4** | **Guest-visible occurrence display** (docs/32 recorded "public availability exposure" open): may program details show real session times/availability bands pre-login? | (a) bounded PUBLIC occurrence/availability projection (times + coarse band, never counters) — **recommended**: choosing a class time is discovery, and the binding default is browse-without-login; (b) schedule orientation only until sign-in. | RI-2 (a→one bounded backend read) |
| **D-RI-5** | **Compensation/processing customer copy** (recorded W1 requirement from W5-5) | approve the RI-3 copy set for `processing`/`compensationPending`/`compensated` at the RI-3 review (machine states are certified; only words are open) | RI-3 closeout |

Adopted as planning defaults needing only S6-0 ratification (owner already directed): credential TTL 10 min · scheduled check-in window ±60 min · walk-in generation while valid · numeric-code-first/QR-ready · validation authority = provider staff per §4.4 scope · no-show consumption deferred.

### 12.1 Owner rulings (2026-08-25 — ruled at the W1-RI-0 closure `ea6cfbf`; BINDING)

| ID | RULING |
|---|---|
| **D-RI-1** | **RESOLVED IN PRINCIPLE.** Unit-less package/pass purchases must NOT be forced into the certified capacity `Booking` model or represented with fake capacity units. **S6-0 owns the exact design of the smallest parallel entitlement-purchase aggregate**, which must REUSE/GENERALIZE the certified W5 commercial/payment authority rather than cloning a second payment stack. If that aggregate requires an additive evolution of W5's structural `PaymentIntent → Booking` binding, S6-0 must specify the exact invariant and STOP for owner review BEFORE any migration. W5 is not reopened during RI-1. |
| **D-RI-2** | **RESOLVED.** V1 must support: finite multi-use package · limited-use membership · unlimited time-based pass · validity snapshot · reservation-required · walk-in-allowed. S6-0 reconciles the canonical schema/model. Fulfillment semantics do NOT automatically belong in Offer/price kinds — pricing and fulfillment stay separated where appropriate. |
| **D-RI-3** | **RESOLVED.** V1 customer authentication target: email/password + Sign in with Apple + Google sign-in. RI-1 makes the already-supported real email/password flow functional FIRST. Apple/Google reuse the certified identity architecture; where genuine provider/app configuration is unavailable the boundary stays fail-closed with the missing operational prerequisite recorded — social-login success is never faked. |
| **D-RI-4** | **RESOLVED.** Public guests may see customer-safe upcoming activity/session information: actual upcoming date/time · branch/location identity needed for selection · `available \| fewLeft \| full` · low `spotsLeft` where the certified projection genuinely provides it. NEVER exposed: `held_count` · `booked_count` · capacity versions · locks · internal counters. The bounded public projection lands with RI-2 where backend support is genuinely absent. |
| **D-RI-5** | **RESOLVED.** Customer payment copy follows authoritative status: `awaitingPayment` after browser return → "We're checking your payment status…" · trusted payment received / saga unresolved → "Payment received — confirming your booking…" · `compensationPending` → "Your payment was received, but the spot was no longer available. Your payment is being returned." · `compensated` → "Your payment has been returned because the spot was no longer available." Browser return alone can never select the payment-received wording. |

## 13. Definition of done — met by this planning task

Complete as-built inventory + classification (§1) · mock→real authority matrix (§1) · certified backend contract map (§2) · fulfillment-model reconciliation against canonical vocabulary (§3) · bounded backend gap analysis with structural-lock verification (§4) · entitlement/attendance/redemption/credential model reconciliation (§4.3–4.4) · calendar authority design (§4.5/§6) · account IA mapping with minimum intervention (§6) · sequenced slice plan with acceptance gates (§7–§8) · testing strategy (§9) · carried + new operational prerequisites (§10–§11) · genuine owner decisions only (§12) · **no application/schema/frontend code changed**. Next step after owner approval + rulings: **RI-1 (and S6-0 in parallel) — and not before.**

---

## W1-RI implementation record

**RI-1 · Customer App Foundation, Authentication, Account & Participants — OWNER-APPROVED / CLOSED at `8046a8a` (2026-08-25; implementation `8046a8a`; preceding W1-RI-0 closure bookkeeping `1816d2f`; owner-authorized at the W1-RI-0 closure `ea6cfbf` with rulings D-RI-1…5; no correction round required).** Final verification: backend Jest **1119/1119 across 90 suites** (all 1107 certified tests + 12 new; W3/W4/W5 locks intact) · backend tsc/ESLint clean · `db:verify` **16/0** · **NO backend migration** (participant archive lifecycle already existed — `status='archived'` + FK-referenced history; nothing destructive was added) · Customer App Jest **444/444 across 28 suites** (all 413 certified + 31 new) · app tsc clean · `expo lint` clean · Playwright E2E **1/1** (the full production-like journey against the real local backend + PostgreSQL) · portal contract **70/70** · admin contract **15/15** · clean tree. Implementation record follows.

**Pre-code reconciliation (reported):** certified auth = `POST /auth/session` (provider access token as the ONLY API bearer + optional ID-token identity evidence with issuer/subject pair binding; first login atomically creates user + identity + account + `self` participant; ONE Himma `login_session` per provider `origin_jti`, permanently retired on logout) · cookie/CSRF channel = the browser-portal continuity path, NOT the app path (the app is pure bearer + gateway refresh) · `/me` returns user/account/self-participant · provider evidence kinds `apple|google|email` are first-class · participant schema fully V1-capable (self/child, DOB feeding certified quote eligibility, archive lifecycle, one-`self` unique, composite account spine, version CAS) with routes as the only gap · app architecture = typed contracts + directly-imported mock singletons with NO seam and NO HTTP anywhere.

**Backend companions (the two identified bounded gaps — nothing else touched):** (1) **Customer participant management** (`participant-routes.ts` + `services/participants.ts` via the identity repository layer — the module's service/repository boundary lock upheld): `GET/POST /customer/participants`, `PATCH /customer/participants/:id`, `POST /customer/participants/:id/archive` — all `authenticatedCustomer`, account-scoped with not-found-shaped foreign ids, version-CAS mutations (`staleVersion`), structural DOB validation only (real calendar date, past, ≥1900 — NO invented age policy; eligibility stays with the certified engine), child requires DOB, `self` renameable but never archivable, archive is the history-safe lifecycle (row retained; edits to archived profiles refuse typed `participantArchived`), audit-evented mutations, reads emit nothing. New typed outcomes: `invalidParticipant` 422 · `participantArchived` 409 · `cannotArchiveSelf` 409. Route-inventory + policy-snapshot locks amended by the owning-slice pattern. (2) **Runnable dev composition** (`scripts/dev-server.ts`, `npm run dev:server`): composes the certified `buildApp` against real PostgreSQL with the certified origin-allowlisted CORS (never globally open), admin/provider surfaces off, payment UNCOMPOSED (the certified fail-closed customer refusal stands) — refuses to start in production. (3) **Dev identity stand-in (D-RI-3)** (`providers/dev/dev-password-identity.ts` + `/dev/identity/{signup,signin,refresh}` routes): a deterministic email/password provider implementing the SAME certified ports as Cognito (`AuthProviderAdapter` + `AccessTokenVerifier` + `ProviderTokenRefresher`) so the ENTIRE Himma identity core runs real; salted-scrypt in-process store; fresh `origin_jti` per sign-in, stable across refresh; **structurally impossible in production** (`buildApp` throws — test-pinned) and absent entirely when not composed (404). 12 new backend tests (`participant-routes.test.ts` 6 · `dev-identity.test.ts` 6): the full journey, enumeration-safe refusals, refresh continuity on one Himma session, logout finality (old AND refreshed bearers of the retired provider session dead; only fresh sign-in works), CAS/archive/cross-account matrices, production refusal.

**Customer App:** the **composition seam** (`services/composition.ts`) — every screen/state module now resolves services through it (21 import sites migrated mechanically; zero behavior change); discovery/booking/checkout stay bound to the deterministic mocks until RI-2/RI-3 swap each binding in one line; the identity family (`identityGateway`/`sessionApi`/`participantApi`/`authSession`) is bound to the REAL HTTP adapters and has NO mock implementation anywhere (source-locked). The **bounded HTTP client** (`services/http/`): env base URL (`EXPO_PUBLIC_API_URL`; dev default localhost; no secret in the bundle), bearer injection, typed `ApiError` envelopes + `NetworkError`, abort support, 401 notification; backend outcome codes stay diagnostic — customer copy maps separately (`error-copy.ts`). **Auth**: `AuthSession` controller (sign-in/sign-up → gateway tokens → certified session establishment → `/me`; launch restoration with expiry-aware gateway refresh; unreachable-backend ≠ signed-out — material retained, guest UI with retry; logout server-first but device-forgets-regardless) + secure token storage (expo-secure-store native / localStorage web preview) + `AuthProvider`/`ProfilesProvider` state. **Account derivation**: `ResolvedAccount` now derives from REAL truth (guest = empty; authenticated = real participants with self as "Me" and EMPTY schedule/plans — truthful no-history until RI-3/RI-5; `participantsReady` lets participant-dependent screens hold their skeleton), with the `?qa-scenario` fixture override preserved dev-only. **Screens (approved-IA additions, token-faithful, no redesign):** Sign in / Create account (HMA-003 bounded; Apple/Google presented honestly unavailable); Profile tab ACTIVATED (HMA-008 bounded: identity, participants entry, sign out; deferred items NOT rendered as dead UI); Participants list + add/edit/archive forms (HMA-030; DOB as text input — native date picker recorded as later polish); the booking participant screen's sign-in card and Home's action card + header avatar now route to the REAL sign-in flow with return context (the hardcoded fixture avatar "Sarah" was found by E2E and fixed — Home header now reflects real auth); Bookings/Saved dock tabs stay inert until their slices. 31 new app tests: the **composition/source lock** (no `services/mock` import outside composition; no mock may implement the identity contracts; composition must bind the HTTP adapters; no raw fetch outside the client; no secret-shaped material in the bundle) · AuthSession matrix · HTTP client conventions · account derivation. **Playwright introduced** (`playwright.config.ts` + `e2e/ri1-auth-participants.spec.ts`, self-booting both servers): guest → sign-up → real account visible → add child → **reload on the protected deep URL restores the session and the child from PostgreSQL** → server-refused invalid DOB adds nothing (no optimistic fiction) → logout → protected surface routes to sign-in. Native/mobile E2E remains the recorded RI-6 gate (docs/12 three-level reporting: these screens are native-safe-implemented, web-reviewed; native validation pending).

**Recorded RI-1 operational gaps (fail-closed, not blockers):** real Cognito pool (dev + production) with email/password app client — swaps the token-acquisition gateway in composition only · Apple Sign-in capability/services-id + Google OAuth client + pool federation (D-RI-3 fast-follow; UI already honest) · production `EXPO_PUBLIC_API_URL` configuration · root tsconfig gained the obviously-intended `admin`/`e2e` excludes (pre-existing wart: `admin/` had broken the app typecheck unnoticed since W3). **Adulthood is NOT structurally modeled** on the account holder (docs/24 §1.2 models participants, not account-holder age) — recorded as a product/account policy gap per owner §8; no frontend age check invented. **RI-1 closure — binding completion distinction (owner wording):** real Himma `LoginSession`/account/participant persistence is INTEGRATED (the certified identity core runs real end-to-end); the current email/password development credential acquisition uses the dev identity stand-in behind the certified ports; the real Cognito pool + Apple/Google configuration remain OPERATIONAL PREREQUISITES and are NOT claimed complete. Closure verification (recorded at approval): implementation `8046a8a` · backend Jest 1119/1119 across 90 suites · backend tsc/ESLint clean · `db:verify` 16/0 · Customer App Jest 444/444 across 28 suites · app tsc/lint clean · Playwright 1/1 real local-stack journey · Provider Portal contract 70/70 · Admin contract 15/15 · no migration · clean tree. **RI-2 (Customer Discovery, Catalogue, Search & Public Availability Integration) is AUTHORIZED; RI-3 and S6-0 do NOT start.**

**RI-2 · Customer Discovery, Catalogue, Search & Public Availability Integration — OWNER-APPROVED / CLOSED at `d851c61` (2026-08-26; implementation `d851c61`; preceding RI-1 closure bookkeeping `d012989`; owner-authorized at the RI-1 closure `8046a8a`; no correction round required).** Final verification: backend Jest **1124/1124 across 91 suites** (all 1119 + 5 new public-availability; W3/W4/W5 locks intact; Slice-4 shape locks amended by the owning-slice pattern) · backend tsc/ESLint clean · `db:verify` **16/0** · **NO backend migration** (head stays `0016`; the D-RI-4 projection is a pure read over certified state) · Customer App Jest **460/460 across 30 suites** (all 444 + 16 new) · app tsc clean · `expo lint` clean · Playwright **3/3** (RI-1 auth journey preserved + 2 new RI-2 public-discovery journeys, real local backend + PostgreSQL) · portal Jest **762/762** · admin Jest **138/138** · clean tree. Implementation record follows.

**Pre-code reconciliation (reported):** discovery screens = Home `(tabs)/index` · Discover `(tabs)/discover/index` · All categories `discover/categories` · Category `discover/category/[categoryId]` · Activity type `discover/activity/[activityTypeId]` · Results `discover/results` · Search `/search` · Map `/map` · Program details `/program/[programId]` · Provider storefront `/provider/[providerId]` — consuming 17 of the 23 mock contract methods (catalogue 3 · search 7 · details 2 · homeFeed 2 · discoverFeed 2 · map 1). Existing public routes: `/listings/:programId` · `/providers/:organizationId(/listings)` · `/catalogue/{categories,activity-types,collections,areas}` · `/search` (closed vocabulary; provider names text-matched via trigram on display name). Direct field mappings: titles/descriptions/setting/ages/gender/skill/eligibility notes · taxonomy refs · provider identity+verified · branches (label/address/areaLabel/geo/facilities) · priceOptions/fromPrice (fils→AED) · offer badges. Mocked fields with NO authoritative source (→ hidden, never faked): ratings & review counts · popularity/"Trending" · availableToday/today-time/weekend/after-school schedule flags · proximity ("Near me"/nearest) · listing-level schedule line · saved/favourites state · credits/gifts · client-side cancellation-policy presets · media serving (refs exist; no public media route — deterministic bundled presentation photography remains) · catalogue-wide supply counts. **D-RI-4 required a bounded backend companion: YES** — the certified availability projection existed only behind `authenticatedCustomer`.

**Backend companions (bounded, no migration):** (1) **D-RI-4 public availability** — `GET /listings/:programId/availability?kind=` (`booking-public-routes.ts`, explicitly `public`): REUSES `listAvailability` verbatim — ONE effective-truth calculation for both surfaces (capacity − booked − ACTIVE UNEXPIRED holds; the S5-5 stale-hold semantics), bands `available|fewLeft(spotsLeft)|full|closed` only, no counters/versions/locks on the wire, unpublished/nonexistent = byte-identical not-found; additive `branchId` on the projection (public branch identity, D-RI-4 selection data) amended into the authenticated schema by the owning-slice pattern. New suite `public-availability.test.ts` (5): guest bands + branch/time identity + counter-hygiene sweep, full stays visible, **lapsed-unswept-hold regression on the PUBLIC surface** (physically `active` expired hold → fewLeft, never full), same-not-found proof, privileged-bearer-changes-nothing + route-inventory lock (`+ GET /listings/:programId/availability → public`; http-policy snapshot regenerated). (2) **`areaLabels` on the public listing summary** (search results + storefront listings): the same public `branch.area_label` values the detail read already served — the smallest bounded extension for the approved cards' location line (reported per owner §8); Slice-4 shape locks amended (catalogue-public/catalogue-search). (3) **`scripts/dev-seed.ts` (`npm run dev:seed`)** — the §11 "mock fixtures graduate to seeds" prerequisite: idempotent FICTIONAL catalogue (7 areas, 14 activity types, 7 live published providers, 14 published programs with options/offers, 29 future sessions incl. genuine fewLeft capacities, camp weeks, cohorts, a free program, a no-upcoming-sessions listing) written through the certified schema/state machines, search documents rebuilt via the owning projection service; refuses production. (4) **Dev-identity boot-unique identifiers** — the dev provider's per-process serials collided with login_sessions PERSISTED across dev-server restarts (fresh sign-up → sessionExpired); identifiers now carry a boot-unique prefix (dev-only; production impossibility unchanged).

**Customer App:** all six discovery contracts now bind to REAL HTTP implementations through the RI-1 seam (`services/api/discovery-api.ts` wire + `discovery-mapping.ts` pure mapping + `real-discovery-services.ts` + `taxonomy-cache.ts` — catalogue METADATA cached ~5 min shared/deduped per owner §19; search/availability stay request-fresh, results paginate through the OPAQUE server cursor with accumulation per session; cached availability is never authority). **Truth rules implemented:** counts/totals render ONLY when exact (server page complete) — never estimated; the reference-area context ranks nothing (no proximity authority; only the explicit area filter narrows, server-side); a child browsing context becomes a server-side age filter over public metadata (guest-grade; authoritative booking eligibility stays with RI-3 — no "eligible" booking claims); canonical UUIDs end-to-end with truthful not-found recovery on deep links. **Search/filters = exactly the certified vocabulary:** keyword (incl. provider names) · category/activity type · area · age/audience · ladies-only · formats · setting · price band · free · trial · skill · collection (now resolved SERVER-side via the `collectionId` dimension) · sorts recommended/price/newest. Unsupported mock dimensions REMOVED from the UI (When/after-school/near-me/offers/top-rated chips+controls, rating/popularity/proximity sorts, filter-sheet footer count → "Show results", `countResults` and the unused sync `search()` dropped from the contract). **Hide-not-fake sweep:** ratings/review counts gone from every card/row/detail (render only if a real authority ever provides them) · "Trending"→"New on Himma" (published recency = the only real ranking), "Available today" section gone, "Popular providers"→"Providers" · saved/favourites hearts removed (deferred domain; no fake state) · policy sections render only when a real policy exists (RI-3 quote snapshot) · storefront program count no longer claims branch specificity (real summaries are provider-wide) · fixture areas/participant lists replaced by real context (areas load from `/catalogue/areas`; the structural `Everyone` entry is no longer fixture data) — source-locked: NO screen/state module may import `data/mock/catalogue` (bundled presentation imagery excepted, keyed deterministically by category slug). **Booking handoff (owner §18):** `BOOKING_INTEGRATION_PENDING` boundary — the flow entry keeps the real program context and truthfully states booking is not yet available (`booking-pending` state, real title, back-to-activity); real canonical ids NEVER produce a mock quote/hold/Booking (structurally locked). **Loading/empty/error:** every surface holds skeletons, refreshes on context change, and truthfully renders no-results / backend-unavailable / invalid-stale-id ("no longer offered") / no-upcoming-sessions / full (visible with the band). Domain types evolved additively (authority-less fields now OPTIONAL; fixture rows keep them required via `FixtureProgram`/`FixtureProvider` so the mock family + its 413-case determinism suite stand for isolated tests). 16 new app tests: mapping truth (8) · real search adapter (5: exact-totals-only, cursor accumulation, unsupported-dimensions-never-on-the-wire, child-age server filter, taxonomy suggestions/empty popularity) · composition locks (+3: real bindings pinned, no discovery mock in composition, pending-boundary existence, fixture-catalogue ban). **Playwright:** `ri2-public-discovery.spec.ts` — guest (NO auth): Home real rails → search → exact totals → program detail with REAL D-RI-4 occurrences (dates, branch, "2 places left" from a genuine capacity-2 session) → verified storefront with real listings → deep-link reload recovers from backend truth → booking CTA reaches the truthful pending boundary → no-results and no-upcoming states; + unknown-id not-found proof. RI-1 auth journey preserved.

**Objective mock→real matrix (owner §21, from the 23-method baseline):** after RI-1: 0 discovery methods real (23 mock-backed). **After RI-2: 15 methods REAL** (catalogue 3 · search 5 — of which recents remain deliberately client-device-local and popularity truthfully empty · details 2 · homeFeed 2 · discoverFeed 2 · map 1), **6 remain mock-backed** (schedule 3 · booking 2 · checkout 1 — the RI-3/RI-5 families, unreachable with real ids via the pending boundary), **2 removed as unsupported** (`countResults`, sync `search()`). Production/local composition contains NO discovery mock binding (source-locked).

**Security (owner §20):** unpublished/nonexistent program ids are byte-identical not-founds on every public surface (listing, availability, storefront) — proven at the wire; private provider fields stay absent (public projections only); guest routes expose only public data (no bearer attaches to public reads — `auth:false` structurally); customer authentication adds no provider/admin visibility (privileged-bearer-changes-nothing proof on the public availability route; standing W3/S5 locks re-ran green in the full battery); no capacity internals on any wire (counter-hygiene sweeps at both layers); no service credentials in the bundle (lock re-ran).

**Recorded RI-2 gaps (truthful, not blockers):** external map-provider integration (the schematic area map now renders real areas/providers/counts; geo pins/distance remain a later bounded item per owner §13) · public media serving (refs recorded; bundled presentation photography until the W3 storage operational items land) · recents persistence across restarts (in-memory device-session parity with the approved mock; storage-backed recents = later polish) · per-branch program filtering on storefronts (summaries are provider-wide; branch chips remain location info) · area-proximity ranking (no authority; "nearby" vocabulary dormant) · Saved tab remains inert (deferred domain). Native validation remains the RI-6 gate (docs/12: these surfaces are native-safe-implemented, web-reviewed). **RI-2 closure — owner-preserved invariants (binding):** `ladies-only` is public discovery metadata only — no participant-specific gender eligibility exists anywhere; public availability uses EFFECTIVE unexpired-hold truth (the certified projection, never a second calculation); real discovery IDs may NEVER cross into mock booking authority; public/provider media remains a separate later authority where not already real. Closure verification (recorded at approval): implementation `d851c61` · backend Jest 1124/1124 across 91 suites · backend tsc/ESLint clean · `db:verify` 16/0 · Customer App Jest 460/460 across 30 suites · app tsc/lint clean · Playwright 3/3 · Provider Portal Jest 762/762 · Admin Jest 138/138 · no migration · clean tree. **RI-3 (Customer Booking, Checkout, Payment Status & My Bookings Real Integration) is AUTHORIZED; S6-0 and RI-4 do NOT start.**

**RI-3 · Customer Booking, Checkout, Payment Status & My Bookings Real Integration — OWNER-APPROVED / CLOSED at `f4ec0e6` (2026-08-26; implementation `f4ec0e6`; preceding RI-2 closure bookkeeping `1c24026`; owner-authorized at the RI-2 closure `d851c61`; no correction round required).** Final verification: backend Jest **1124/1124 across 91 suites** (W3/W4 booking-capacity/W5 payment regressions + route/security locks all green; the S5-5 own-booking view lock amended by the owning-slice pattern) · backend tsc/ESLint clean · `db:verify` **16/0** · **NO migration** (head stays `0016_commission_economics`) · Customer App Jest **475/475 across 33 suites** (all 460 + 15 new) · app tsc clean · `expo lint` clean · Playwright **6/6** (RI-1 auth + RI-2 discovery preserved; 3 new RI-3 real-stack journeys) · portal Jest **762/762** · admin Jest **138/138** · clean tree. Implementation record follows.

**Pre-code reconciliation (reported):** remaining mock families = booking (getBookingOptions/getBookingSummary) · checkout (getCheckoutPage) · schedule (3 scenario-fixture methods). Affected screens: the four booking-flow steps (selection/participant/summary/checkout) + the missing HMA-022/023/006/024 surfaces (payment status, confirmation, Bookings tab, booking detail) + Home's schedule sections. Certified routes reconciled 1:1: `POST /customer/quotes` (identifiers only → server lines/total/TTL) · `POST /customer/holds` + `GET /customer/holds/:id` (effective truth) + `POST /customer/holds/:id/release` (idempotent) · `POST /customer/bookings/confirm-free` · `POST /customer/bookings/initiate` (→ {bookingId, redirectUrl, holdExpiresAt}) · `GET /customer/bookings/:id/payment` (the converged D-RI-5 vocabulary) · `GET /customer/bookings(/:id)`. Genuinely necessary backend companions: ONE additive projection amendment (the own-booking view lacked provider/branch display identity needed by confirmation/My Bookings) + dev-only payment composition; everything else consumed as-built — W4/W5 needed no change.

**Backend changes (bounded):** (1) **Own-booking projection (owning-slice amendment, additive):** `CustomerBookingView`/schema gains `provider {id, displayName}` + `branch {id, label}|null` — the same public display names discovery serves; no commercial internals; view-shape lock amended, suites green. (2) **Deterministic provider `hostedBaseUrl` (additive option):** defaults to the unreachable fictional origin the W5 suites pin; only the dev-server composition points it at its local hosted-page stand-in. (3) **Dev payment composition (`dev-server.ts`, owner §13):** the certified deterministic provider + server-authored checkout URLs + the certified webhook ingress — production impossible at BOTH layers (the script refuses production; `resolvePaymentProvider` refuses the deterministic provider there); `paymentCapabilityReport.productionChargingPossible` remains the literal `false`, no readiness flag exists, W5 production gates untouched (payment-closeout suite re-ran green). (4) **Dev hosted-checkout stand-in (`scripts/dev-hosted-checkout.ts`, dev-server only — never in `buildApp`):** plays the PROVIDER'S OWN hosted pages exactly as checkout.stripe.com — the browser presses Pay on the provider page; the PROVIDER then delivers its SIGNED webhooks through the certified trusted ingress (raw-body signature verification, two-phase with a delay: `checkout.completed` first — the saga inspects, sees pending, defers → real observable `processing`; then capture + `payment.captured` → the certified W5-4 saga confirms). The browser/app never calls a trusted payment-success endpoint. `GET /dev/payments/state` exposes provider-side counters (session/create counts) for test proofs. (5) **dev-seed:** active FICTIONAL cancellation-policy template (the certified test pattern; D-8 owner-content gate untouched) + D-W5-7 commission terms at the recorded launch `1200 bps` for the fictional providers (paid checkout is fail-closed without an agreed term).

**Customer App:** the RI-2 `BOOKING_INTEGRATION_PENDING` boundary is RETIRED and replaced by the real flow. **Contracts/composition:** new `commerce` contract (Quote/CapacityHold/ConfirmedBooking/CheckoutStart/CustomerPaymentStatus/CustomerBooking + the 9-method `CommerceApi`) bound to a real HTTP adapter; `bookingService` is REAL (options compose from the public listing + D-RI-4 availability; the summary displays the AUTHORITATIVE server quote verbatim — fils→display only, zero client arithmetic); checkout became a PURE presenter (`composeCheckoutPage`) over the flow's stored summary so ONE quote travels summary→hold→checkout and is never re-derived behind the customer. **Idempotency authority (owner §10, unit-proven):** `BookingCommerceController` owns stable keys — hold key per material intent signature (program|option|unit|participant|quote; created once, survives failed attempts — a timeout retry replays the SAME key), confirm key per holdId, checkout key per (holdId, quoteId); a MATERIAL draft change releases the old hold explicitly (idempotent release key; expiry stays authoritative if the release is lost) in the dispatch handler; nothing regenerates on render/press. **Paid flow:** the pending-checkout record (identifiers only) persists BEFORE initiation and updates after — a lost response/web reload recovers the SAME intent with the SAME key (the certified W5-5 replay returns the same hosted session while the Himma hold lives; a dead hold yields the typed refusal and the stale redirect is never re-presented — the `/bookings/return` landing implements exactly this). **Hold UX (owner §8/§9/§18):** the hold claims when the customer commits to enter checkout; the countdown renders the server `expiresAt` (presentation only — on zero the SERVER is asked, never the device clock for mutations); effective expiry shows the truthful card with Recheck availability (releases + returns to the REAL occurrence picker); STRONGER FINANCIAL TRUTH DOMINATES — if a checkout was initiated, the payment projection is read FIRST and processing/confirmed/compensation states route to the status screen instead of an expiry claim. **Quote TTL (owner §7):** `quoteExpired` at any commitment point returns to the summary which re-derives and SHOWS the fresh total before anything continues — stale money never proceeds, prices never change silently after the final press. **Free path (owner §11):** zero-total quotes use ONLY `confirm-free` — no PaymentIntent, no payment screen; the review screen precedes the final Confirm; D-10 trial semantics stay server-side (`trialAlreadyRedeemed` mapped to customer copy). **Payment status (owner §15/§16, D-RI-5 verbatim):** browser return is NAVIGATION ONLY; the status screen reads the certified projection and renders the exact owner-approved copy per machine state; polling cadence (recorded): immediate → 1s → 2s → 3s → 5s steady, `compensationPending` slows to 10s, transient failures back off to ≤15s and NEVER present as payment failure, a 3-minute budget pauses to a manual "Check again", terminal states stop; reload/foreground recovers via the pending record + a fresh read. **Confirmation (owner §19):** authoritative fields only (reference/participant/program/provider/branch/when/price) — no QR/check-in/attendance/credits/invoice/review prompt. **My Bookings v1 (owner §20):** certified own-booking list with truthful sections — Needs attention (`pending_payment` → status read), Upcoming, Past, and quiet Not-completed history; failed attempts never show as confirmed; no cancellation/refund UI (bounded support copy only, owner §22); Passes & Memberships never faked. **Home schedule (owner §21):** `getUpcomingActivity`/`getWeekSchedule` semantics move REAL — the account derivation now maps confirmed upcoming bookings into the Home schedule shape (re-read on confirmation signals; nothing optimistic); `getActivePlans` intentionally deferred (S6/RI-4); the scenario-based schedule mock remains ONLY the dev-QA fixture layer (the RI-5 unified calendar stays deferred — no duplicate schedule truth was created). **S6 boundary (owner §23):** package/pass price options compose as VISIBLE, honestly disabled "Coming soon" rows — never a fake Session purchase; proven in E2E. **Commission privacy (owner §24):** customers see provider list prices only; a new source lock pins that no frontend service/contract/state file can model commission/provider-share/economics fields, and no customer payment-success authority exists in the app (`confirmPaidBooking` remains HTTP-unreachable — backend-locked). 15 new app tests: idempotency controller matrix (6) · D-RI-5 copy verbatim + compensation-sequence-never-success + polling cadence (4) · real options/summary/checkout composition incl. the S6 boundary and no-client-arithmetic (4) · composition/commission locks (+2, one replaced).

**E2E (real local stack, 6/6):** FREE journey — discovery → real session → participant → server quote (Free) → hold+countdown → certified free confirm → confirmed (HM-reference) → My Bookings, with PROVIDER-SIDE proof (session/create counters unchanged — no payment machinery touched). PAID journey — server quote AED 90 → 10-minute hold countdown ("held for 09:xx") → card method → the deterministic HOSTED page (Pay pressed on the provider's page) → signed webhooks → certified saga → return + status revalidation (the D-RI-5 progress copy observed — awaitingPayment/processing) → confirmed → My Bookings. S6-boundary journey — the 10-class pack renders disabled "Coming soon" (aria-disabled proven). RI-1/RI-2 journeys preserved (the RI-2 spec's handoff assertion updated to the real flow entry — the deliberate boundary replacement). **Compensation (owner §32, bounded as permitted):** the backend race stays proven by the standing W5-4 suites (unweakened); the app side is proven at the unit/copy layer — the compensation status sequence can never render success and carries the exact owner copy; the wired status screen consumes the same projection the backend suites certify. Wire-level proofs during implementation additionally confirmed: same-key initiate replay returns the SAME redirect with ONE provider session; awaitingPayment→processing→confirmed observed over the certified pipeline.

**Production gates preserved (owner §33):** D-W5-6 real Stripe smoke · live activation · UAE VAT/principal-agent/invoicing · production commission-term administration · W6 timed `sweepLapsedPaidCheckouts` runner · gateway-fee allocation — all still open; `productionChargingPossible` structurally false (literal type, re-tested); the dev composition cannot exist in production at either layer. **Recorded RI-3 gaps (truthful, not blockers):** native hosted-checkout return deep-link config (web same-tab flow is the proven V1 path; the native branch opens the URL + shows the status screen — real `himma://` return URLs are launch configuration) · `holdAlreadyActive` after a mid-flow reload surfaces bounded copy (the stale hold self-expires ≤10 min) · recents/pending-checkout persistence beyond one device unchanged · native validation remains the RI-6 gate (docs/12: implemented native-safe, web-reviewed). **RI-3 closure — owner-preserved non-blocking gaps (binding):** native `himma://` checkout-return/deep-link configuration → RI-6/native integration; bounded stale `holdAlreadyActive` copy after a mid-flow reload → later UX hardening unless it becomes blocking; native/mobile validation → RI-6. Closure verification (recorded at approval): implementation `f4ec0e6` · RI-2 closure bookkeeping `1c24026` · backend Jest 1124/1124 across 91 suites · backend tsc/ESLint clean · `db:verify` 16/0 · Customer App Jest 475/475 across 33 suites · app tsc/lint clean · Playwright 6/6 · Provider Portal Jest 762/762 · Admin Jest 138/138 · no migration · clean tree. **S6-0 (Fulfillment, Entitlements & Attendance planning/canonical design — PLANNING ONLY) is AUTHORIZED; S6-1, RI-4, and W2-13 do NOT start.**


---

## RI-4 · Passes & Memberships, Entitlement Reservation & Customer Check-In — OWNER-APPROVED / CLOSED at `b060dd4` (2026-08-31; base implementation `7080b1c`; owner-directed occurrence-contract correction `b060dd4`; one correction round; no Provider Portal/Admin change; NO backend migration — head stays `0020_membership_booking_and_multi_occurrence_attendance`)

**Pre-implementation reconciliation (owner item 1, reported):** (1) Passes surfaces present before RI-4: NONE — My Bookings truthfully deferred them ("Passes & Memberships stay RI-4/S6"); the only S6 touchpoint was the RI-3 options composition's non-purchasable "Coming soon" package rows. (2) Old mock coverage: no plans/entitlements/check-in mock existed (the mock layer never implemented them); Home's `activePlans` remains the dev-QA fixture override only. (3) S6 APIs now available: the full certified customer surface — entitlement-purchases quote/confirm-free/initiate/:id(/payment) · entitlements list/detail/attendance/reservable-sessions/reservation-quote · entitlement-reservations/confirm · bookings/:id/credential (with the 0020 `occurrence` selection + `regenerateCredentialId`) · entitlements/:id/credential · credentials/:id · the bounded calendar read · `coveredByEntitlement`/`entitlementId` on the Booking projection. (4) Transport/session conventions: the ONE bounded bearer HTTP client + typed contracts + `services/composition` + error-copy mapping + stable-idempotency-key controllers + `pendingCheckoutStore` + the D-RI-5 polling cadence — all reused, none duplicated. (5) The RI-3 boundary: `purchasable:false` "Coming soon" rows — RETIRED this slice where real support exists.

**Real API integration (owner §2):** NEW `contracts/entitlements.ts` + `api/entitlements-api.ts` (list/detail/attendance/reservable-sessions/reservation-quote/reservation-confirm · acquisition quote/confirm-free/initiate/payment-status/purchase read · booking+walk-in credential issuance with explicit regeneration and the 0020 occurrence pair · credential status · the bounded occurrence read), composition-bound (`entitlementsApi`) beside the existing families; no mock exists for this family (composition lock). The S5 hold rides the UNCHANGED `commerceApi.claimHold`. The Booking contract gained `coveredByEntitlement`/`entitlementId` (server flag — never inferred from AED 0). Calendar API typed for the bounded occurrence read ONLY; no Calendar UI exists (source-locked; RI-5 owns it).

**Customer IA (owner §3):** the Bookings tab is now two segments — **Bookings** (scheduled activities) and **Passes** (Passes & Memberships; deep-linkable `?view=passes`) — never hidden in payment history. New root surfaces: `/passes/[entitlementId]` (HMA-026 detail) · `/passes/reserve/[entitlementId]` · `/passes/status/[purchaseId]` · `/checkin/[credentialId]` · `/bookings/check-in/[bookingId]` (canonical occurrence selection). No backend vocabulary reaches the UI (exhausted = "Used up"; no revision/purchase/commission/digest material anywhere — regex-locked).

**Passes list/detail + finite truth (owner §§4–7):** cards and detail render the SERVER projection verbatim — product/program/provider/participant/validity/status, finite headline `R of T visits remaining`, and the truthful commitment line when reservations hold credits (`2 visits reserved for upcoming sessions · 2 more can be booked` — remaining and available-to-reserve NEVER conflated; unit-locked with the owner's 5/1/2 worked example). Unlimited shows "Unlimited" and no counter anywhere. Status is server authority (`active|exhausted|expired`); inactive passes disable both fulfillment actions and point to ordinary repurchase (no renewal invented). Detail adds methods wording, purchased schedule summary, next reserved session, and the bounded visit history (scheduled occurrence time preferred; no staff identifiers).

**Acquisition (owner §§8–9):** the RI-3 "Coming soon" boundary is RETIRED — package/membership options compose as purchasable `entitlementAcquisition` rows riding the SAME flow screens (selection → participant → summary → checkout): the summary quotes through the S6 acquisition API (unit-less; server terms displayed as customer-safe lines; `Price · …`, never Total), checkout dispatches on the acquisition quote — free → `confirm-free` (one stable key per quote) → the Pass with "ready to use"; paid → the purchase-shaped pending record → W5 `initiate` → the SAME hosted boundary → return routes purchase records to `/passes/status/:purchaseId` polling the D-RI-5 vocabulary (locked copy reused with the pass noun corrected; `confirmed` resolves the granted Entitlement and lands on the Pass). A package is NEVER routed through the Session Booking flow (locks + journey proof).

**Reservation (owner §§10–13):** "Book a session" appears only where the purchased terms permit; the reserve screen consumes the authenticated Entitlement-specific projection (never filtered search results), displays seat availability separately from CREDIT availability (`N of T visits available to book`; the CTA disables at zero credits), and runs the real chain quote → certified S5 hold → `confirmEntitlementReservation` in one intent with stable keys (hold key bound to the quote; confirm key to the hold — a retry replays the same Booking). No payment page/language exists on the path; success reads "Included with your pass — no payment needed."; confirmation is immediate so no countdown UI exists (S5 expiry stays the server authority).

**Booking presentation + check-in (owner §§14–15, 22):** an entitlement-covered Booking reads **Included with pass** everywhere (list price cell + detail), from the SERVER flag only — provider-free sessions still read Free; covered bookings link "View your pass". Confirmed bookings expose **Check in**: session bookings issue directly; camp/cohort bookings route to canonical occurrence selection (multi-occurrence bookings stay check-in-able across their span). Refusals map to plain copy ("Check-in opens closer to your session time." for the ±60 window); device time is never authoritative.

**Credential screen + secrets (owner §§16–21):** the code renders large in the approved 4-4 grouping with the server expiry countdown (presentation only), participant/occurrence context, and lives ONLY in the in-memory display store — never persisted (no AsyncStorage/SecureStore/localStorage), never logged/analytics, never in a route param/URL (locks + unit tests). Initial issuance honors the S6-2 correction exactly: one stable pending key per target+occurrence intent (double-tap/lost-response replays; the key RETIRES once an outcome is observed, so the next check-in is a new intent); `credentialAlreadyLive` renders metadata + "Generate a new code" — the app never fabricates a code and never hammers initial issue. Explicit regeneration names the current credential id (fresh key per explicit request); `staleVersion` reloads truth ("This code was already replaced…") — nothing silently supersedes. States: live (+secret or metadata-only) · **Checked in** (bounded 3s poll while visible, stop on terminal/blur/12-min budget — the customer observes the SERVER-confirmed redemption; success never assumed) · expired · replaced. On `used` the pass/booking projections re-read fresh server truth — never an optimistic decrement (no manual consumption affordance exists anywhere; locked).

**Camp/Cohort occurrence selection (owner §§24–27):** the day list derives from the bounded server occurrence read — the exact canonical projection (0020) — with the pair EXTRACTED verbatim from the server's own event keys (`booking:<id>:<date>:<HH:MM>`; unit-locked); the app never reconstructs recurring rules, never builds dates from the span, and never picks nearest-to-now. Two same-day meetings list separately; exception dates never appear (the server omits them); one camp day's check-in leaves every other day independently check-in-able; the credential screen labels the SCHEDULED occurrence (cross-midnight: the scheduled day, never the redemption timestamp's).

**Backend companions (no migration):** dev-seed gains the RI-4 catalogue half S6-1 recorded as pending — ACTIVE fulfillment revisions for every seeded package/membership option, a free intro pack + free unlimited membership program (with a per-run in-window session for reserved check-in), and a paid open-day camp re-anchored each run to today's Dubai date with an in-window daily start (whole-minute times) — plus `scripts/dev-checkin-actor.ts`, the dev-only front-desk ACTOR (the dev-hosted-checkout pattern: scripts-only, dev-server-only, production-impossible) that drives the REAL S6-2 preview/redeem services under a real staff membership for the Playwright journeys. One genuine backend bugfix (bounded owning amendment): the customer read's pg DATE serialization used `toISOString()`, shifting camp/cohort civil dates a day back on any UTC+ host — dates now serialize from their local components (`civilDateString`); the S5-5/W5 batteries re-ran green.

**Proofs:** Customer App Jest **498/498 across 36 suites** (+23: the entitlements adapter paths/outcomes/not-found shaping · the finite vocabulary incl. the owner's worked example · occurrence extraction incl. the two-meetings-one-date distinctness · schedule/code display · the credential store/keys/href secret-safety + key retirement · the bounded credential poll cadence · the RI-4 composition/secret/no-client-balance/no-manual-consumption/no-Calendar-UI locks · the acquisition summary/checkout derivation) · app tsc/ESLint clean. Customer Playwright **10/10** over the REAL local stack: the certified RI-1/2/3 journeys (the S6-boundary spec updated to the RI-4 purchasable truth) + four new journeys — **A/C/D/E/F** free-pack lifecycle (acquisition → `5 of 5` → reservation "Included with your pass" → included-with-pass Booking → reserved check-in code redeemed by the REAL provider actor → server-observed "Checked in" → `4 of 5` exactly once → explicit regeneration: C1 dead at the desk (non-201), C2 redeems → `3 of 5`) · **B** paid pack through the real W5 hosted boundary → "Pass ready" → `10 of 10` · **G** unlimited membership: "Unlimited", two independent check-ins, never a counter · **H** CampWeek canonical occurrences: five explicit days, today's checks in via a real code + actor redemption, tomorrow refuses with the window copy (no fake code, camp never collapsed to one attendance). Backend Jest **1259/1259 across 103** (the Slice-6 closeout frontend lock amended by the owning-slice pattern: the Customer App consumes S6-3 ONLY via the composition-bound adapter; portal/admin stay locked out) · backend tsc/ESLint clean · `db:verify` 20/0 · portal Jest 815/815 + contract 77/77 + Playwright 223/20 · Admin 138/138 — zero Provider Portal/Admin source changes.

**RI-4 owner-review correction (occurrence-contract, this commit — `7080b1c` otherwise accepted; nothing else reopened):** the app no longer parses Calendar event keys for occurrence authority. The bounded occurrence read now returns an EXPLICIT `occurrence: {date, startTime}` DTO on camp/cohort Booking occurrence events — authored by the SAME canonical derivation (occurrence-authority.ts) that already produces the event key and validates credential issuance (one authority, two projections; Calendar key generation unchanged; no migration — head stays `0020`). The `CalendarOccurrence` contract documents `eventKey` as OPAQUE (React keys/dedup only); `occurrenceChoices` passes the explicit DTO fields VERBATIM to `POST /customer/bookings/:bookingId/credential` — no parsing, no timezone arithmetic, no reconstruction; `parseOccurrenceKey` is DELETED. New source lock: no app module may dissect an event key (split/match/regex on the encoding) or reference the parser — plus the pass-through assertion on the presentation layer. Proofs: backend calendar suite asserts the explicit DTO equals the canonical pair on every camp day and cohort meeting (exception dates produce no DTO because they produce no event); the multi-occurrence suite asserts two same-date cohort meetings arrive as two DISTINCT explicit DTOs and that the cross-midnight camp day reads `{tomorrow, 00:30}` when queried on the adjacent civil date; app unit tests run occurrenceChoices over deliberately OPAQUE keys (format irrelevant) incl. the same-day and cross-midnight cases. Verification: app Jest 499/499 across 36 + tsc/lint clean · customer Playwright 10/10 · backend 1259/1259 across 103 + tsc/lint clean + `db:verify` 20/0 · portal 815/815 + contract 77/77 · admin 138/138 · no Provider/Admin/RI-5 change.

**Deliberately deferred (truthful, not blockers):** RI-5 unified Calendar UI (source-locked absent) · native `himma://` hosted-payment return + native validation → RI-6 (unchanged RI-3 posture; acquisition reuses the same boundary) · walk-in branch selection UX (no branch-scoped walk-in product exists in seed; the server enforces branch truth regardless) · QR presentation (numeric-first V1 stands) · Home `activePlans` wiring to real passes (RI-5 scheduling family).

**RI-4 closure (OWNER-APPROVED 2026-08-31 at `b060dd4`; one correction round — the occurrence-contract correction `b060dd4` on top of the accepted base `7080b1c`; closure bookkeeping `cc8fdeb`).** Binding preserved invariants: `eventKey` is OPAQUE to the app (React keys/dedup only; occurrence authority travels ONLY as the explicit server `occurrence: {date, startTime}` DTO, passed verbatim to credential issuance — parser deleted, source-locked); reserved-vs-bookable finite truths never conflated; credential secrets in-memory only; no optimistic consumption anywhere. Closure verification (recorded at approval): Customer App Jest **499/499 across 36 suites** · app tsc/lint clean · Customer Playwright **10/10** (real local stack) · backend Jest **1259/1259 across 103 suites** · backend tsc/ESLint clean · `db:verify` **20/0** · Provider Portal Jest **815/815** + contract **77/77** · Admin **138/138** · migration head stays `0020_membership_booking_and_multi_occurrence_attendance` (no RI-4 migration) · zero Provider Portal/Admin changes · clean tree. **RI-5 (Unified Himma Calendar — real `/customer/calendar` integration, Calendar destination, Home scheduling truth) is AUTHORIZED; RI-6 does NOT start.**

---

## RI-5 · Unified Himma Calendar (2026-08-31 — this commit, awaiting owner review; RI-6 NOT started; no Provider Portal/Admin change; NO migration — head stays `0020_membership_booking_and_multi_occurrence_attendance`)

**Pre-implementation reconciliation (owner item 1, reported):** (1) Dormant Calendar route/screen: NONE — source-locked absent by the RI-4 lock; only the typed `CalendarOccurrence` contract + `entitlementsApi.listOccurrences` (the bounded server read) existed, consumed solely by check-in occurrence selection. (2) Home upcoming/week: `account-context` fetched confirmed bookings → `toScheduleEntries`; the `week` section was emitted ONLY by the mock feed (dead in production); `WeekStrip`/`UpcomingActivityCard` were inert with stale "ships later" comments. (3) `activePlans`: truthfully `[]` in real derivation (fixture-only via the `?qa-scenario` boundary); the composition's mock `scheduleService` binding had ZERO consumers. (4) Exact server DTO (`CalendarEventSchema`): `eventKey` (opaque) · `sourceType ∈ {sessionBooking, campWeekOccurrence, cohortOccurrence, membershipOccurrence}` · `context ∈ {booked, reservedWithPass, includedSchedule}` · participant/program/provider/branch display refs · `startAt`/`endAt` ISO instants · `span?` (camp metadata) · `occurrence? {date, startTime}` (camp/cohort) · `bookingId?`/`entitlementId?` — the app contract modeled a SUBSET; RI-5 widened it (no backend change needed). (5) Max range: **62 days** inclusive (`from`/`to` required `YYYY-MM-DD`; reversed/oversized → typed 400 `invalidRange`). (6) Source vocabulary: the four sourceTypes; an entitlement-reserved Session is ONE `sessionBooking` event flagged `reservedWithPass` + `entitlementId` (server dedup also suppresses a membership occurrence dominated by a real same-participant/program/civil-day Session). (7) Navigation targets: `bookingId` on every Booking-backed event → `/bookings/[bookingId]`; `entitlementId` on membership occurrences (and reserved sessions) → `/passes/[entitlementId]` — both routes existed from RI-3/RI-4. **No missing backend calendar truth was found — no STOP required, no schema invented.**

**Calendar API/composition (owner item 2):** `CalendarOccurrence` widened to the full certified projection (typed `CalendarSourceType`/`CalendarEventContext`; `eventKey` documented OPAQUE); the ONE composition-bound `entitlementsApi.listOccurrences` remains the only wire (no scattered fetch, no Calendar mock anywhere, no client-side merging of Bookings/Passes/recurrences — locked). **IA (item 3):** the approved HMA-006 view set is ACTIVATED — the Bookings tab now carries three segments **Bookings · Passes · Calendar** (deep-linkable `?view=calendar&date=YYYY-MM-DD`), the exact RI-4 segment pattern; never hidden in Profile/Pass detail, never renamed to a transaction term. **UX (item 4):** smallest polished V1 — week navigation (prev/next + Today), a 7-day Monday-start date strip with has-activity dots, and a chronological agenda for the selected day; read-only (no drag/edit/create). **Bounded loading (items 5/25):** `calendar-loader.ts` — window = visible weekStart −7d…+20d (28 days ≤ 62), ONE request per range; adjacent-week navigation reuses the prefetched range; out-of-window weeks trigger exactly one bounded reload; stale responses for superseded ranges are ignored; a re-read REPLACES held truth wholesale (never merged, never optimistic — items 8/21).

**Event rendering (items 6–11, 16–17, 23, 26):** server events render verbatim, keyed/deduplicated ONLY by the opaque `eventKey` (no competing business dedupe — two children's same-time activities stay distinct; participant context renders on every card: "For you"/"For {child}"). Day grouping + time labels use the EXPLICIT canonical `occurrence` pair where present (camp/cohort — the cross-midnight invariant holds by construction: Monday 00:30 renders under Monday whatever the device clock says) and otherwise the server instant itself; NO recurring-rule math, span-based date generation, or event-key parsing exists (locked). Customer wording only: `reservedWithPass` → "Included with pass" badge · `includedSchedule` → "Membership schedule" · camp span → supporting "Camp week · {dates}" line; no backend vocabulary, no fake ratings/credits/attendance inference (past events are never auto-labeled attended/missed). Empty/loading/error states are bounded and truthful ("Nothing scheduled this day" — never a no-bookings claim; network failure → retry). **Navigation (items 12–15):** tap → Booking detail via the explicit `bookingId` (reserved sessions keep the RI-4 check-in path reachable through Booking detail) or Pass detail via the explicit `entitlementId`; an event with neither renders read-only; Calendar performs NO mutation and duplicates no credential/reservation logic (locked).

**Home integration (item 22, smallest clean):** `account-context` now sources the schedule from the SAME real authority — one bounded `listOccurrences` (today→+13d) + `listEntitlements` per auth session, re-read on booking-change signals; `account-derivation` maps Calendar events → `scheduleEntries` (with explicit optional `bookingId`/`entitlementId`) and ACTIVE real entitlements → `activePlans` (finite headline / "Unlimited"; next-session line ONLY from the server's `nextReservedSessionAt` — no fabricated dates). The real home feed finally emits the `week` section (`buildScheduleWeek`, previously mock-only); `UpcomingActivityCard` opens its Booking/Pass, `WeekStrip` opens the Calendar, `PlanCard` opens its Pass (fixture entries stay inert). The orphaned mock `scheduleService` composition binding (zero consumers) is REMOVED — no mock-backed contract binding remains; the `?qa-scenario` fixture resolver stays the only certified fixture boundary.

**Source locks (item 27, amended by the owning-slice pattern):** the RI-4 "no Calendar UI" lock became the RI-5 consumer allowlist — `listOccurrences` is consumed ONLY by `features/calendar/calendar-view.tsx` · `features/checkin/occurrence-select-screen.tsx` · `state/account-context.tsx`; NEW RI-5 lock: the Calendar binds through composition, the calendar feature contains no credential/reservation/acquisition/commerce API references, no app module carries recurrence vocabulary (`rrule`/`byweekday`/exception-dates), no app module constructs an `eventKey:` literal (events cannot be fabricated client-side), and camp spans never drive date generation; the RI-4 opaque-key lock (no split/match/regex on keys; explicit DTO only) now covers the new files unchanged.

**Backend companions (dev-only, NO migration, no route in `buildApp`):** dev-seed adds the two calendar sources the RI-2/RI-4 seed lacked — the Padel Fundamentals cohort gains TWO active weekly `recurring_schedule` meeting patterns on the SAME weekday (09:00 + 17:00) with the first meeting date recorded as `exception_dates` on both, re-anchored EVERY run (the camp-week precedent: cohort runs Dubai-today+7 → +97, cutoff +10d, meetings weekly on the weekday of today+10 with today+10 the exception → first real occurrences land today+17); and a FREE schedule-bound membership option on Mat Pilates (`Evening membership — Mon & Wed`, 19:00–20:00 snapshot terms). NEW `scripts/dev-fulfillment-actor.ts` (the dev-checkin-actor pattern: scripts-only, dev-server-only, production-impossible) drives the REAL W2-13 `setFulfillmentConfig` supersede-and-insert service under a real owner staff membership — the Playwright schedule-change journey's provider stand-in.

**Proofs:** Customer App Jest **521/521 across 38 suites** (+22, 2 new suites: `calendar-presentation` — grouping once-per-event, same-day cohort distinctness, participant distinctness, cross-midnight civil grouping, span metadata, explicit-id navigation targets over deliberately garbage opaque keys, bounded-window math; `calendar-loader` — bounded reload on range change, in-window reuse, error/retry, wholesale replace (new reservation once / no duplicate after redemption / revision shows through), stale-response guards; `account-derivation` rewritten — events→entries with explicit ids, defensive key-only dedup, finished-event drop, plans mapping incl. unlimited/expired-filtered/no-fake-next; composition-lock amendments) · app tsc clean · `expo lint` clean. Customer Playwright **11/11**: the certified RI-1…RI-4 journeys unchanged + the RI-5 journey — real construction of ALL six sources (free Session Booking · pass-reserved Session · flexible unscheduled pass with NO reservation · paid CampWeek · paid Cohort with the exception pattern · the child's Mon/Wed membership), then the Calendar: 4-week chip/week sweep (bounded reloads at the window edge) proving normal Booking exactly once · reserved Session exactly once as "Included with pass" · five INDIVIDUAL camp days · zero fabricated dates for the unscheduled pass · child membership only on Mon/Wed with participant context · cohort exception date EMPTY while the next week's same-day 09:00/17:00 meetings render distinct · a truthful empty future day · Booking-event → Booking detail and membership-occurrence → Pass detail by explicit ids · a NEW RI-4 reservation appearing exactly once after server re-read · and the item-30 immutability proof: the REAL Tue/Thu provider revision (dev fulfillment actor → certified supersede service) changes NOTHING on the existing customer's Mon/Wed calendar. Backend Jest **1259/1259 across 103** (full battery incl. the S6-3 calendar/reservation/attendance suites; the slice6-closeout frontend-boundary lock re-verified) · backend tsc/ESLint clean · `db:verify` **20/0** (head `0020`) · Provider Portal Jest **815/815** + contract **77/77** · Admin Jest **138/138** — ZERO Provider Portal/Admin source changes.

**Deliberately deferred (truthful, not blockers):** RI-6 native hardening (native `himma://` payment return, physical-device validation, store preparation, push, production Cognito, real Stripe certification — untouched; Calendar is native-safe-implemented and web-reviewed; native validation stays the RI-6 gate) · calendar EXPORT to Apple/Google calendars (docs/09 §13 — W5-integration scope) · month-grid visualization (the approved agenda V1 stands) · per-participant Calendar filter (existing pattern reusable later; not required for truth). **Next: owner review — RI-6 does NOT start.**
