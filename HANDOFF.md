# Himma — Session Handoff

Read this after README.md, CLAUDE.md, and docs/01–23. It captures live project state as of 2026-08-05 so a fresh session can continue without re-deriving anything.

## Where we are: the Checkout milestone (docs/22) is CLOSED

Four customer milestones are complete and owner-approved:

**Milestone 2 — customer discovery (docs/17 + docs/18), closed at `ec79c7d`.** Home · Navigation shell/dock · Search · Results + filtering · Discover feed · All Categories · Category pages · Activity-type pages · Schematic Map. Details in git history and docs/17–19.

**Milestone 3 — evaluation surfaces (docs/20), closed at `38c5ecb chore(details-review)`.** Program Details · Provider Storefront · Program ↔ Provider cross-navigation with stack reuse · typed favourites · participant suitability with explicit recovery · Blue Wave multi-branch model · mock cancellation-policy presets · informational session lists · save/share. Details in git history and docs/20.

**Milestone 4 — booking flow (docs/21), closed at `454ee91 chore(booking-review)`:**

| Step | Commit | Status |
|---|---|---|
| 12 Booking foundation + session selection (HMA-017) | `c1e5191 feat(booking)` | ✅ approved (design + frontend) |
| 13 Participant selection + eligibility (HMA-018/019 inline) | `df06260 feat(booking)` | ✅ approved (design + frontend + accessibility review) |
| 14 Booking summary + flow connections (HMA-020) | `6d952d0 feat(booking)` | ✅ approved (design + frontend + flow connections + accessibility review) |
| deps | `01b6f73 chore(deps)` | SDK 57 registry re-alignment (docs/13 note) |
| 15 milestone close | `454ee91 chore(booking-review)` | review/polish/documentation pass |

**Delivered by this milestone:** the full pre-checkout booking flow behind the Program Details Book CTA — type-appropriate selection (single session, camp weeks, trial dual options, skip rule for dateless single-option programs) · deterministic availability states (few left, full-disabled-explained, no sessions, registration closed) · single-select participant step with inline eligibility, preselection rule, no-eligible recovery, and the guest sign-in contract · fully re-validated booking summary per program type with `Booking price` wording, informational offer lines, edit round-trips, and the **inert Continue-to-checkout contract** (docs/09 §21.8) · in-memory `BookingDraft` whose lifetime equals the flow's (docs/09 §21.13) · shared session derivation keeping details and booking availability identical (docs/09 §21.5).

**Milestone 5 — checkout (docs/22), closed at this commit:**

| Step | Commit | Status |
|---|---|---|
| 16 Checkout foundation + price review (HMA-021) | `31f11b9 feat(checkout)` | ✅ approved (design + frontend) |
| 17 Payment-method contract + checkout readiness | `a04df62 feat(checkout)` | ✅ approved (conditional approval resolved by the correction below) |
| 17 accessibility correction | `07b0865 fix(checkout)` | ✅ approved (unready CTA `aria-disabled`/native disabled semantics; pure `ctaPressAllowed` gate) |
| 18 Revalidation states + flow hardening | `00e50de feat(checkout)` | ✅ logic approved (visuals superseded by the correction below) |
| 18 visual correction — CheckoutIssueCard | `fc583d0 fix(checkout)` | ✅ approved: **interaction design approved · provisional Orbit Indigo visual implementation approved · final brand styling pending the design-system engagement** |
| 19 milestone close | `chore(checkout-review)` (this commit) | review/verification/documentation pass |

**Delivered by this milestone:** one-screen checkout at `/booking/[programId]/checkout` inside the booking stack over the re-derived, re-validated `BookingSummary` (no CheckoutDraft copy, no CheckoutSessionProvider) — order recap with guardian context (`Booked by you`) on child bookings and a single `Edit booking` action · price breakdown with structured reconciling amounts, `Booking price` label continuity end-to-end (no `Total`, no VAT, no fees, no discount arithmetic, `Free` never `AED 0`) · cancellation-policy summary displayed with an inert `Full policy` contract and **no acceptance claim of any kind** · the single generic **`Card payment`** contract method (radio semantics, no card details; Apple/Google Pay declared-only) · pure `checkoutReducer`/`checkoutReadiness` with the named blocker `Choose a payment method to continue` in a polite live region · free-vs-paid CTA behavior (`Confirm booking` / `Continue to payment`), production-styled, duplicate-press-guarded (700 ms, pure `ctaPressAllowed`), **inert** — the unready CTA exposes `aria-disabled` + native disabled semantics on web and `accessibilityState.disabled` on native · typed `CheckoutValidation` revalidation states on the dedicated **CheckoutIssueCard** (per-code icon/state label/headline/support, structured `CheckoutPriceComparison` old → new display, `No payment has been made.` reassurance row, one full-width recovery CTA; no checkout CTA and no payment controls while an issue is unresolved; re-derive for review-style codes, owning-step returns otherwise) · `?qa-revalidate` review reachability for all seven issue codes (captured once at flow entry) · `checkoutStepAccess` sharing the summary's validation for cold-link/invalid-draft/unknown-program recovery · a source-scan guard proving `PaymentSubmitRequest`/`PaymentSubmitResult` exist only as contract declarations with no submit path anywhere.

**Not implemented (deferred, scheduled per docs/23 §16 — do not start without owner approval):** payment submission of any kind (prohibited until every docs/23 §19 condition exists) · payment status/confirmation routes (HMA-022/023) · reservation/capacity holds · legal-acceptance UI (required before real submission per docs/09 §22.7) · Marketplace Credit application (§22.12) · promo codes · saved cards, card entry, tokenization, Apple/Google Pay execution · multi-participant booking (docs/09 §21.2) · waitlists (§21.4) · draft persistence (§21.13) · cart · written review content · favourites persistence · real provider policies · universal links · real maps · provider support actions · gifts · authentication · profile/participant management UI · Bookings calendar · Saved screen · Credits · Rewards · backend · Arabic/RTL · provider portal · admin portal. The checkout CTA, `Full policy` row, support row, guest Sign-in action, and Bookings/Saved/Profile dock items stay **inert with press feedback** (docs/09 §17.2, §22.11).

**Current work: Phase P0 of the production platform plan.** `docs/23_PRODUCTION_PLATFORM_REBASELINE.md` is **owner-approved and binding (2026-08-05)**, including Amendment A1 (`a315c36`) and Amendment A2 (`a8a4480`): Himma is the final production marketplace — seven workstreams, three connected frontends (customer app, provider portal, admin portal) before backend completion, canonical domain model, lifecycle state machines, authorization model, production architecture, correctness-gated scale tiers, and launch gates. Governance was synchronized across CLAUDE.md, README.md, and the stale strategic docs in the same commit as this note.

Live status:
- **Checkout milestone (docs/22): CLOSED at this commit** — all four commits plus the two owner-approved corrections (see the Milestone 5 table above). The checkout approval record: **interaction design approved · provisional Orbit Indigo visual implementation approved · final brand styling pending the professional branding/design-system engagement · native validation pending · real payment submission prohibited (docs/23 §19)**. The `?qa-revalidate` demo detail worth remembering: the code rides the flow's *entry* URL and is captured once at `BookingSessionProvider` mount (pushed routes drop search params); the priceChanged demo previous price is current − AED 10 from structured data (the payable amount everywhere remains the catalogue price). docs/22 §16's "rows 16–18" screenshot reference is a recorded doc slip — §15 assigns rows 12–13, and rows 17–19 were added for the extra reviewable states.
- **Next customer work is sequenced by docs/23 §16, not momentum:** the Payment & Confirmation milestone cannot be planned honestly until the W5 gateway selection and docs/23 §18 decisions exist. The Bookings/Saved/Profile mock milestone is the recommended next W1 item (P1, RTL-safe per docs/23 §3.1) — **do not start it without an owner-approved plan document.**
- **Phase: P0** (docs/23 §16). Open P0 items: **Batch-1 owner decisions open** (docs/23 §18.1–10 — rebaseline ratified; scale tiers, hosting region, gateway, counsel, brand, commercial model, customer web, language launch, device procurement still open) · **native-validation environment procurement open** (first full device pass pending it) · **provider design-partner recruitment open** (docs/23 §8.6) · **canonical-model specification FULLY APPROVED and binding** — `docs/24_CANONICAL_DOMAIN_SPECIFICATION.md` including Amendment A1 (`605508c`), owner-approved 2026-08-06 · **backend stack owner-approved 2026-08-06** (TypeScript on Node 24 LTS · Fastify 5 modular monolith · PostgreSQL · pg + Kysely, SQL-first, no ORM · node-pg-migrate SQL migrations · npm · Jest on real PostgreSQL; recorded in docs/25) · **W4 Backend Slice 1 (database & migration foundation) APPROVED at `d9b5fff`** (see the Backend Slice 1 section below) · **Slice 2 (identity & authorization) plan approved in structure with the D1–D4 owner rulings applied as docs/26 Amendment A1 (this commit): D1 = Amazon Cognito User Pools behind the `AuthProviderAdapter` (Himma PostgreSQL canonical for all identities/roles/authorization; no Himma refresh-token family — Cognito rotation/revocation with Himma `login_session` liveness keyed to issuer+subject+`origin_jti`; production region and identity DR explicitly OPEN under docs/23 §18.3, no production pool provisioned); D2 = email contracts stay in Slice 2 with captured dev/test `MailSender`, production sending disabled until region + email service approved; D3 = one-time audited self-sealing two-Access-Administrator bootstrap CLI, dev/test seeding separate; D4 = role exclusivities (access_admin ∦ finance, platform engineer ∦ business-admin, auditor ∦ mutating roles) enforced. docs/26 incl. Amendment A1 **fully approved and binding** at `fe80920` · **B2-1 and its correction FULLY APPROVED at `7565766` (2026-08-06)** · **B2-2 FULLY APPROVED at `559ad2c` + `e93c742` (2026-08-06)** · **B2-3 + spine proof FULLY APPROVED at `2e2e9a1` (2026-08-06)** · **B2-4 FULLY APPROVED at `b20de7f` (2026-08-06)** · **B2-5 + its event-integrity correction FULLY APPROVED at `99575a4` (2026-08-07)** · **B2-6A FULLY APPROVED at `0756456` (2026-08-07)** · **B2-6B (Cognito MFA mediation port + adapter, TOTP enrollment services, Himma recovery-code services, TOTP step-up services — provider-independent application layer over the B2-6A schema; no routes, no pool, no cloud resource) implemented — see the B2-6B section below; awaiting owner review. The first-login concurrency fix is **APPROVED at `dd17030`**; **B2-6B including the TOTP-semantics correction is FULLY APPROVED at `25dd742` + `49da921`** · **B2-6C (MFA/step-up HTTP routes, principal step-up assurance, administrative MFA enforcement, final production capability gate, Slice 2 closeout) implemented — see the B2-6C section below; awaiting owner review.** **B2-6C FULLY APPROVED at `f95b84a` (2026-08-07) — Backend Slice 2 (Identity & Authorization) is code-complete and CLOSED**; production Cognito/admin-MFA activation stays PENDING operational validation (docs/26 §15): the real-pool SOFTWARE_TOKEN_MFA smoke (§14.E′), production Cognito integration, and approved production configuration remain open, and the production admin surface stays fail-closed until every capability reports ready. · **Slice 3: `docs/27` including Amendment A1 FULLY APPROVED and binding at `56c17e0` (2026-08-07)** · **S3-1 (Organization + OrganizationPublicProfile + Branch schema foundation, migration 0005) FULLY APPROVED at `3b5d12f` (2026-08-07)** · **S3-2 (staff memberships, branch scopes, digest-stored staff invitations, migration 0006) FULLY APPROVED at `3bcb37d` + the structural-secret-guard correction `8784ce0` (2026-08-07)** · **S3-3 (provider principal resolution, capability registry, `provider`/`providerStepUp` policies, §13.2 provider-private management routes) FULLY APPROVED at `051d77c` (2026-08-07)** · **S3-4 (Himma-admin organization lifecycle + customer-public storefront read) FULLY APPROVED at `4ff461a` (2026-08-07)** · **S3-5 closeout FULLY APPROVED at `799804a` (2026-08-07) — Backend Slice 3 (Provider Organizations) is CLOSED** (commit record + RLS deferral + pending-dependency register: docs/27 §18) · **Slice 4 (Catalogue, Listings, Classification & Search): `docs/28` approved in structure at `6b60fb2`; the owner rulings D-S4-1..3 are applied in place as docs/28 Amendment A1 (this commit), with the D-S4-1 multi-option model carried into canon as docs/24 Amendment A2 — see the Slice-4 specification section below; awaiting owner review of the amendment. The amended docs/28 was **fully approved and binding at `b016c85` (2026-08-07)** · **S4-1 (taxonomy migration 0007 + catalogue migration 0008 — full schema foundation, no services/routes) CLOSED: fully approved at `d98eb69` + the gender-eligibility correction (five-value model, docs/24 Amendment A3, migration 0009) at `3ef57e7` (2026-08-07)** · **S4-2 (provider catalogue services, Slice-4 capability activation, §16.2 provider-private catalogue routes) CLOSED: fully approved at `94a692c` (2026-08-07)** · **internal catalogue moderation + ProgramRevision decisions CLOSED: fully approved at `fecb8d0` (2026-08-07)** · **internal taxonomy administration (§16.3 `/admin/taxonomy`) implemented this commit — see the Backend Slice 4 · taxonomy administration section below; awaiting owner review.** Remaining Slice-4 work (customer-public reads, search foundation, closeout) and every other workstream are NOT started and must not start without explicit owner approval. No AWS resource, production pool or region choice, email vendor, or Apple/Google configuration; the real-pool smoke remains outstanding under §14.E′** · B2-6 (MFA + slice closeout) is NOT started and must not start without explicit owner approval.**
- The deferral list below still holds item-by-item, but its items are scheduled workstreams under docs/23 §3/§16, not indefinite futures.
- **Real payment submission remains prohibited** platform-wide until every docs/23 §19 condition exists (legal acknowledgments, server-side validation, certified gateway, operational controls).

## Approval status per surface (docs/12 three levels)

| Surface | Design | Frontend | Native validation |
|---|---|---|---|
| Home (HMA-004, docs/18 rebuild) | ✅ | ✅ | ✅ 2026-08-06 four-target audit; defect A2 corrected same day (`fix(a11y)` commit) — accessibility trees on both Android targets + both iPhones now expose `Your week` with no navigation promise |
| Navigation shell + floating dock | ✅ | ✅ | ✅ 2026-08-06 four-target audit (simulator/emulator; physical devices pending) |
| Search (HMA-009) | ✅ | ✅ | ✅ 2026-08-06 four-target audit (software keyboard exercised on all four targets) |
| Results + filtering (HMA-010) | ✅ | ✅ | ✅ 2026-08-06 four-target audit; cosmetic A3 corrected same day (`fix(results)` commit) — whole scope-tab labels verified at normal and ~135% type on 360-dp Android, iPhone 17 and Himma-SE |
| Discover feed (HMA-005) | ✅ | ✅ | ✅ 2026-08-06 four-target audit |
| All Categories (HMA-011) | ✅ | ✅ | ⏳ pending (not in the 2026-08-06 audit scope) |
| Category (HMA-012) | ✅ | ✅ | ⏳ pending (not in the 2026-08-06 audit scope) |
| Activity type (HMA-013) | ✅ | ✅ | ⏳ pending (not in the 2026-08-06 audit scope) |
| Map (HMA-016) | ✅ | ✅ | ✅ 2026-08-06 four-target audit |
| Program Details (HMA-015) | ✅ | ✅ (incl. Commit 11 accessibility/consistency fixes) | ✅ 2026-08-06 four-target audit (StatusBarScrim verified scrolled + at rest on all four) |
| Provider Storefront (HMA-014) | ✅ | ✅ (incl. Commit 11 accessibility/consistency fixes) | ✅ 2026-08-06 four-target audit (StatusBarScrim verified) |
| Booking selection step (HMA-017) | ✅ (docs/21) | ✅ (Commit 12; Commit 14 edit-return wiring; Commit 15 web aria-disabled fix) | ✅ 2026-08-06 four-target audit; defect A1 corrected same day (`fix(booking)` commit) — hardware back at a cold-linked flow start now lands on Program Details on both AVDs; warm chains still pop one step with the draft preserved |
| Booking participant step (HMA-018/019 inline) | ✅ (docs/21) | ✅ (Commit 13; Commit 14 Continue wiring; Commit 15 web aria-disabled fix) | ✅ 2026-08-06 four-target audit; defect A1 corrected same day (skip-rule flow start verified on both AVDs; iPhone 17 swipe-back unregressed) |
| Booking summary (HMA-020) | ✅ (docs/21) | ✅ (Commit 14, incl. accessibility review) | ✅ 2026-08-06 four-target audit |
| Checkout (HMA-021, docs/22) | ✅ (docs/22 + owner-directed CheckoutIssueCard) | ✅ (Commits 16–18 + `07b0865`/`fc583d0` corrections — interaction design approved; **provisional Orbit Indigo visual implementation approved; final brand styling pending** the design-system engagement) | ✅ 2026-08-06 four-target audit (all seven revalidation states verified per target; inert-submit contract held on every target) |

**Native-validation status (docs/12 §1 level 3):** the first full docs/12 §10 four-target audit ran 2026-08-06 (see "First full native-validation audit" below). "✅" above means every audited row passed on iPhone 17, Himma-SE, Pixel 7 (412 dp) and 360-dp Android via simulator/emulator — the definition docs/12 §10 uses. Physical-device testing and live VoiceOver/TalkBack runtime passes (screen readers cannot run in the iOS Simulator; semantics were verified through the exposed accessibility trees instead) remain pending platform-wide and are named residuals, not silent gaps. Surfaces marked 🟡 have exactly the named open defect blocking the full upgrade.

## Native-validation environment (P0 procurement — status 2026-08-05)

**Android: environment complete; first emulator smoke pass done.** Machine: macOS 26.5.2, Apple M3 Pro, 18 GB RAM. Toolchain verified: Android Studio 2024.3 · SDK at `~/Library/Android/sdk` (platforms 33/35/36, build-tools 36.0.0, platform-tools/adb 36.0.0, emulator 35.4.9, cmdline-tools 19.0, NDK 27.1 auto-installed by the Expo template) · JDK Temurin 17 (`JAVA_HOME` now set in `~/.zshrc`) · all SDK licenses accepted. AVDs: `demo_phone` (Pixel 7, API 36, 412 dp, 2 GB) and `Himma_Small_360` (small_phone, API 36, 360×640 dp, 1 GB — the docs/12 §10 ~360-dp target). `npx expo run:android` (debug, prebuild-generated gitignored `android/`) built and ran on both AVDs on branch `native-validation`. The `com.himma.dev` application/bundle identifier is committed in `app.json` since `b5fbe49` as a **development-only placeholder** — the production identifiers remain an open owner decision (docs/23 §18).

**First Android smoke pass (risk discovery, NOT full validation — no screen upgraded):** Home, Discover, Search+suggestions, Results, Program Details, booking participant step (skip rule fired for the dateless package), summary, and checkout all rendered and operated; hardware back popped exactly one step everywhere with state intact; checkout inert-submit contract held natively (unready CTA inert with named blocker; ready CTA inert; method selection persisted); carousels scrolled correctly inside vertical scroll; zero FATAL/JS errors in logcat on both AVDs.

**Findings logged (not fixed — separate task):**
1. **Native defect — camp price unit lost:** cards render `AED 850 /` / `AED 990 /` on Android where web renders `AED 850/week` / `AED 990/week` (verified same-build web comparison). Font-metric/wrap divergence class (docs/12 §4).
2. **QA-affordance gap:** `?qa-revalidate` (and `?qa-scenario`) are guarded `window.location` reads — native deep links (`himma://booking/...?qa-revalidate=...`, warm and cold) cannot trigger the CheckoutIssueCard review states on device until the params move to router params.
3. **Environment notes:** emulator Gboard sits in floating-toolbar mode, so docked-keyboard avoidance is still unexercised (low risk — transactional flows have no text inputs); the small AVD needed a wifi toggle (`svc wifi disable/enable`) after first cold boot before Metro was reachable.

**iOS: environment complete; launch verified; interactive smoke pass pending one owner command.** Xcode 26.6 (17F113) installed and selected (`/Applications/Xcode.app/Contents/Developer`), first-launch components done, iOS 26.5 simulator runtime (23F77) installed via `xcodebuild -downloadPlatform iOS`. Simulators: the default iOS 26.5 set (incl. **iPhone 17**, primary/Dynamic-Island class) plus created **`Himma-SE`** (iPhone SE 3rd gen, 375×667, no notch — docs/12 small-iOS target). CocoaPods installed via Homebrew (Expo auto-installed it; `pod install` needs `LANG=en_US.UTF-8`). `npx expo run:ios` **Build Succeeded**; Himma launched and Home render-verified on **both** iPhone 17 and Himma-SE (evidence in `artifacts/native-smoke-ios/`). Notably iOS renders `Your week` and `/week` correctly — narrowing finding 1 above to an **Android-only loss of the trailing word "week"** (headings and price units). **Interactive iOS smoke pass (2026-08-05, after the owner's `xcode-select` fix): DONE on iPhone 17** — deep-link `Open in "Himma"?` acceptance routed correctly into the booking flow (skip rule fired on the cold link); participant step contract (disabled Continue inert with `Choose who is attending`, explained `Not suitable` rows, selection → `Booking for you`); summary intact; checkout unready CTA inert with named blocker; `Card payment` selection → ready CTA with price label; **ready CTA inert (no-submit contract holds on iOS)**; edge-swipe-back popped checkout → intact summary → participant → Home; Search live suggestions working with typed input. Evidence: `artifacts/native-smoke-ios/` (8 captures). Caveats: the iOS Simulator defaults to hardware-keyboard mode, so on-screen-keyboard avoidance is still unexercised on both platforms (device-pass item); `simctl openurl` custom-scheme links always raise the system open dialog (expected iOS behavior). **Himma-SE abbreviated interactive pass (2026-08-05): DONE** — driven via `idb` (facebook/fb Homebrew tap: `idb-companion` + `fb-idb` in a scratchpad venv) because the Claude Code simulator-panel device grant for Himma-SE never registered. Verified at 375×667: Home/Discover/Search/Results/Details render with no clipping (chip rows scroll horizontally by design; provider names ellipsize gracefully) · skip-rule cold link → participant → summary → checkout · disabled Continue and checkout CTAs inert with correct blockers (blocker wraps to three lines in full) · participant + `Card payment` selection updates readiness · ready CTA inert (no-submit) · swipe-back pops one step at a time with the draft preserved (participant stays selected) · back from the cold-linked flow start lands on Discover (the documented fallback) · search typing + grouped suggestions work · checkout bottom scroll padding is sufficient (payment card + support row clear the sticky footer fully) · sticky-footer price label wraps to two/three lines on SE-width — readable, nothing obscured (item 3 visual debt, now with SE evidence) · dock fits all five items with Home/Discover pill behavior intact · zero native errors for the app process (`log show --level error`: 0) and zero Metro JS errors. Evidence: `artifacts/native-smoke-ios/se-*.png` (16 captures).

**New native visual finding (both iOS devices, discovered in this pass): scrolled Program Details content passes under the transparent status bar with no scrim** — page text collides with the status-bar clock/carrier text on iPhone 17 and Himma-SE (evidence: `se-15-home-carousel.png` predecessor frame `se-14`-adjacent capture and `17pro-details-scrolled2.png`). Android likely shares the hero edge-to-edge design; verify during the fix. Detail screens only (Home's pinned opaque header masks correctly). Recorded as native visual debt — do not fix in the environment task.

Physical-device testing (both platforms) not started.

## Native correction task (2026-08-05, post-P0-smoke — owner-approved as `89deda0`–`b96d50e`, fast-forwarded into `main` 2026-08-06)

**A — status-bar collision (Program Details + Provider Storefront): FIXED on all platforms.** Root cause: both detail surfaces scroll content under the transparent status bar with no top mask. Fix: shared **[StatusBarScrim](src/components/domain/status-bar-scrim.tsx)** — a safe-area-height `Animated.View` (`pointerEvents="none"`, native-driver opacity) that fades in over the 56-pt band before the hero/cover bottom reaches `insets.top`; renders nothing when the top inset is 0 (web preview unchanged). Wired into [program-details-screen.tsx](src/features/details/program-details-screen.tsx) (`HERO_HEIGHT` 264) and [provider-storefront-screen.tsx](src/features/details/provider-storefront-screen.tsx) (`COVER_HEIGHT` 220 — owner-directed extension after the collision reproduced there; identical structure). Verified: no collision when scrolled and edge-to-edge hero intact at rest on Pixel 7, 360-dp Android, iPhone 17, and Himma-SE, both surfaces; long + short content; no jump/flicker/duplicate header; back/swipe navigation unaffected.

**B — Android trailing-word loss ("Your" / "AED 850 /"): FIXED, single root cause.** RN-Android (Fabric) lays a `Text` out at its measured width; fractional widths (Manrope advances, negative letterSpacing) get floored by Android's integer text layout, silently wrapping the last word onto a clipped second line. String-dependent — "Your week" broke while "My week" didn't. Three structural fixes, no per-string or per-platform copy: [section-header.tsx](src/components/ui/section-header.tsx) title `flexGrow: 1` (layout width > measured width; letterSpacing kept); [program-card.tsx](src/components/domain/program-card.tsx) amount+unit merged into one nested `Text` (single paragraph, true text baselines); [compact-program-row.tsx](src/components/domain/compact-program-row.tsx) unit `paddingRight: 1` (floor loss < 1pt; preserves the documented whole-unit wrap at 360). Verified: 412-dp + 360-dp Android (incl. font scale 1.3), iPhone 17, Himma-SE, web QA suites. **Watch for:** the same latent defect class exists for any tightly-measured standalone `Text` — if a trailing word vanishes on Android again, reach for these three patterns.

**C — QA params are now Expo Router params (dev-gated): DONE.** `?qa-scenario` ([account-context.tsx](src/state/account-context.tsx)) and `?qa-revalidate` ([booking-session-context.tsx](src/state/booking-session-context.tsx) + [booking/_layout.tsx](src/app/booking/[programId]/_layout.tsx)) no longer read `window.location`; they read `useGlobalSearchParams` (query params ride the *focused* route — a layout's `useLocalSearchParams` never sees them on native) behind `__DEV__` gates, so production builds compile the affordance out. Capture is a **first-valid-wins latch** (conditional setState during render): native navigation state hydrates a render *after* mount, so mount-time capture misses cold deep links; once latched, the value is fixed for the provider's lifetime — the same boundaries as before (account: session; revalidate: booking-flow lifetime via the `key={programId}` remount). All seven revalidation codes now render on native — verified priceChanged/sessionFull/participantIneligible on Android and iPhone 17 via `himma://booking/<id>?qa-revalidate=<code>` cold links. `qa-fail`/`qa-nocount` were already router-param reads. Pure validators `scenarioFromParam`/`qaRevalidateFromParam` replace the search-string parsers (unit tests updated, 408 total).

**Correction-task verification (final, incl. the storefront/Android scrim extension):** tsc clean · ESLint `--max-warnings=0` clean · Jest 408/408 (23 suites) · expo-doctor 20/20 · all nine Playwright QA suites 808/808 (checkout 210 exercising every revalidation state through the router-param path on web; Playwright baselines with sub-pixel/decode-noise-only diffs were reverted per the screenshot policy) · native builds and launches re-verified on demo_phone, Himma_Small_360, iPhone 17, Himma-SE · zero fatal native or JS errors on any target. Curated evidence: `artifacts/native-fix-review/` (per-fix, per-device), `artifacts/native-smoke-{android,ios}/` (representative smoke subset). No screen is upgraded to "native validated" — that requires the full docs/12 §10 checklist (Dynamic Type, VoiceOver/TalkBack, reduce motion, per-screen sweep), which is the next scheduled native-validation task.

## First full native-validation audit (2026-08-06, on `main` at `b96d50e` — full-certification verification tier)

**Scope:** every implemented customer surface (Home, Discover, Search + suggestions, Results, Map, Program Details, Provider Storefront, booking selection/participant/summary, checkout, all seven revalidation states, error/empty/unavailable/cold-link recovery) × four targets (iPhone 17, Himma-SE 375×667, Pixel 7 412 dp, 360-dp Android) × the docs/12 §10 rows (normal type, ~135 % type, screen-reader semantics, Reduced Motion, dark-system-setting confirmation, safe areas/system bars, scroll + nested horizontal gestures, swipe-back/hardware back, focus order/labels/radio/disabled state, touch targets, image + scroll performance, backgrounding, cold + warm deep links, software keyboard, long-text/narrow-width). Validation-only pass — no fixes were implemented. Curated evidence: `artifacts/native-validation-audit/` (38 files; per-target folders; seven-code revalidation sheets per target).

**Method notes (environment-honest):** iOS Dynamic Type via `simctl ui content_size extra-extra-extra-large` (~1.35); Android via `font_scale 1.35`. Reduced Motion via the Accessibility defaults key (iOS — the Settings toggle itself resists synthetic taps) and zeroed animation scales (Android), functionally exercised. iOS screen-reader semantics read from the accessibility tree via `idb ui describe-all`; Android via `uiautomator` (checkable/checked/enabled/content-desc) — VoiceOver cannot run in the Simulator, so live screen-reader runtime remains a physical-device item. Software keyboard: hardware-keyboard disconnect + device reboot (both iOS simulators), `show_ime_with_hard_keyboard` (Android; docked Gboard exercised on the 360 target — this closes the previously standing docked-IME gap for Search; demo_phone still runs the floating toolbar).

**Result: zero blockers, zero high defects.** Everything explicitly re-verified held on all four targets: StatusBarScrim on Details **and** Storefront (opaque under the status bar when scrolled, invisible at rest, no flicker) · complete `Your week` / `/week` / `per session` text (Android trailing-word fix B holding everywhere, incl. price-comparison rows at 1.35 on 360 dp) · all seven CheckoutIssueCard states per target with correct state label, headline, structured old → new comparison, `No payment has been made.` reassurance, single recovery CTA, no payment controls · issue cards under large type on every target (tightest case 360 dp × 1.35 passes; SE × xxxL passes) · sticky-footer clearance and full multi-line blocker wrap (never truncated) · **no payment submission** (ready CTA inert on paid and free checkouts on every target; zero route changes, zero dialogs) · no legal-acceptance or booking-confirmation claim anywhere · QA params remain `__DEV__`-gated router params (verified in source at `b96d50e`) · no accessibility label or live region claims payment/reservation/booking success (checked on the exposed trees). Spoken semantics verified: payment radio `checked/unchecked`, unready CTA `enabled:false` + named blocker, ineligible rows disabled-with-reason, issue-card reading order (state label → headline → support → paired price-comparison label → reassurance → recovery). Backgrounding/reopen preserved state everywhere tested; cold + warm `himma://` links routed correctly (cold checkout links redirect to the flow start; unknown ids render recovery; cold-link back control falls back per `booking-navigation`/detail rules). Dark system setting leaves the app intentionally light with readable status bars on all four targets. Reduced Motion left every exercised surface fully functional. Zero native FATALs, zero JS errors, zero Metro errors across the entire audit (checked per platform).

**Defects (owner review; recommended correction order):** — *A1–A3 corrected 2026-08-06 in the approved correction package (see "A1–A3 correction package" below); A4 stands as recorded environment debt.*
1. **A1 · medium** — Android hardware back at a **cold-deep-linked booking-flow start** finishes the activity (exits to launcher) while the on-screen back chip navigates to Program Details (`booking-selection-screen.tsx:85`/`booking-participant-screen.tsx` fallback). Divergence between the two back affordances mid-flow; normal in-app chains pop exactly one step with state preserved (verified). Recommend intercepting hardware back on the flow-start screen to mirror the chip fallback. (Back-at-Home-root exiting the app is standard Android and not part of this defect.)
2. **A2 · medium (accessibility)** — Home's inert `Your week` card exposes "Your week. Opens your full schedule" to screen readers while the card is inert until HMA-024 (docs/09 §17.2 contract covers visual press feedback, but the spoken label promises navigation that does not happen). Recommend dropping the promise from the accessible label until Bookings ships. Same-class check recommended for other inert items with aspirational hints.
3. **A3 · low (Dynamic Type cosmetic)** — Results scope-tab segment labels break mid-word at ≥1.35 ("Categorie/s" on iOS xxxL, "Program/s · Provider/s · Categori/es" on 360 dp × 1.35). Readable but ugly; size/wrap the segment for large type.
4. **A4 · low (dev-environment only)** — LogBox warning toasts during the audit: Android occurrences are "Cannot connect to Expo CLI" (dev client's DevTools socket check uses `10.0.2.2:8081` while the bundle rides `adb reverse`; environment artifact). One iPhone 17 toast right after enabling Reduce Motion (text not captured — no debugger attached; consistent with Reanimated's dev-only reduced-motion advisory). No production impact; confirm/silence during the correction task.

**Observations (not defects):** floating overlay chips overlap scrolled content headings on Details/Storefront — approved floating-chip pattern, revisit at rebrand if desired · sticky-footer price label ellipsizes at large type/narrow width — documented behavior (full label in the price block and the accessible CTA label; the readiness blocker wraps in full instead) · Map opened during an active search session shows query-scoped counts including zero-count areas — correct session behavior · dev cold starts run ~10–14 s over Metro (dev-only) · `Upcoming activity` card is deliberately inert until HMA-024 (source-documented) — its chevron affordance is part of approved design.

**Performance:** smooth vertical scrolling and carousel-in-scroll on all four targets; no gesture fights; images decode without visible pop-in after first paint; no stutter at simulator/emulator level. (Real-device image-decode performance remains a physical-device item.)

**Keyboard findings:** Search is the only input surface; the input is top-anchored so the keyboard can never cover it; suggestions update live above the keyboard on all four targets; `returnKeyType` search key present; typing works through both docked and floating IMEs.

**Verification tier used:** full certification (branch integration + first full device matrix — per the 2026-08-06 tiered-verification policy). tsc/ESLint/Jest/Playwright were not re-run this session: no `src/` code changed (validation-only), and the identical tree at `b96d50e` carries the Commit-19-era full green run (tsc clean · ESLint clean · Jest 408/408 · expo-doctor 20/20 · Playwright 808/808) recorded above.

## A1–A3 correction package (2026-08-06, after audit approval at `62a3f04`)

Three independent commits, one per concern; A4 untouched (recorded dev-environment debt — no production build has reproduced it).

1. **A1 — `fix(booking): align Android hardware-back fallback`.** New `createFlowStartBackHandler` policy + `useFlowStartHardwareBack` hook in `booking-navigation.ts` (unit-tested), registered focus-scoped and Android-only on the two flow-start screens. Handler declines the event whenever `router.canGoBack()` — the navigator still pops exactly one screen, so normal chains and the draft are untouched — and only the no-history flow-start case mirrors the chip's `replace(programHref)` fallback. Nothing outside the booking flow is intercepted; `replace` cannot create a back loop.
2. **A2 — `fix(a11y): remove promises from inert controls`.** `Your week` card label reduced to `Your week` (promise restored only when Bookings ships). Full same-class sweep of every implemented surface found no other inert control whose label, hint, role, or copy promises unavailable navigation/functionality — the remaining inert items carry the docs/09 §17.2/§21/§22 descriptive-only contracts, and components with optional `onPress` already gate their hints on it. Single confirmed instance.
3. **A3 — `fix(results): preserve tab labels at larger text`.** Scope-tab labels render whole on one line and shrink toward (never below) base size (`adjustsFontSizeToFit`, floor 0.75 of the scaled size — docs/12 §4 dense-control allowance) with 4 dp segment padding for separation. Normal-scale rendering unchanged; no Results redesign. (The package's recorded observation — `CompactProgramRow` titles breaking long words at 1.35 × 360 dp — was corrected separately on owner instruction; see the card-title correction below.)

**Card-title long-word correction (2026-08-06, owner-approved follow-up · `7d3b870 fix(results): wrap long card-title words whole at large text`):** at ~135 % scale on narrow widths a `CompactProgramRow` title whose longest word exceeded the ~148 dp title column split mid-word without a hyphen on Android (`Beginner C/alisthenics`) and tail-truncated inside the word on iOS (`Fundament…`). Root-caused empirically: Android's break strategy renders word overflow as a bare split (greedy instead tail-ellipsizes mid-word), auto-fit APIs accept Android's mid-word-wrapped layout as "fitting", and **`maxFontSizeMultiplier` is inert on this RN version's Android build** (prop applied at mount or updated — layout identical; worth knowing for future capping work; the dock's 1.3 cap and CTA 1.4 caps should be re-checked on a physical-device pass). Fix in `src/utils/text-fit.ts` + the row component: a unit-tested detector reads real `onTextLayout` lines and reports any word cut; on detection the title steps its rendered size toward — never below — base size via style (remounting per step, since Android fires `onTextLayout` only on mount), and iOS additionally uses `adjustsFontSizeToFit` (its fit definition is exactly the iOS truncation mode; iOS layout events report un-ellipsized lines, so the detector alone cannot see iOS truncation), floored at base size. At normal font scale the factor clamps to 1 — approved rendering structurally unchanged. Verified per the correction's tiered policy: whole-word titles at ~1.35 and unchanged normal-type rendering on `Himma_Small_360` and `Himma-SE` (screenshots in session evidence) · focused catalogue/results/utils Jest 73/73 (11 new text-fit tests) · full tsc clean · targeted ESLint clean · Results QA suite green, zero console errors · zero FATAL/JS errors on both targets. Full matrix not re-run (no shared regression surfaced). Catalogue data, card layout, and product behavior untouched.

**Verification (tiered, per the correction-task policy):** full tsc clean · full ESLint clean · full Jest 410/410 (2 new A1 policy tests) · Home, Results, booking, and checkout QA suites all green with zero console errors (Results re-run after the final styling tweak) · **A1 on both AVDs** (demo_phone + Himma_Small_360): dated and skip-rule cold links land on the flow start and hardware back replaces to the correct Program Details with the app foregrounded; second back exits from Details (standard root behavior, no loop); warm chain participant→selection→Details pops one step per press with the selected session preserved · **A2/A3 at normal and ~135 %** (`font_scale 1.35` / `content_size extra-extra-extra-large`) on 360-dp Android, iPhone 17, and Himma-SE: accessibility trees (uiautomator / `idb ui describe-all`) expose `Your week` with no promise; scope tabs show whole, separated labels with no mid-word breaks · **iOS swipe-back unregressed** (iPhone 17: participant→selection→Details edge swipes pop one step each, draft intact) · zero Android FATALs, zero ReactNativeJS errors, zero iOS app-process errors on all four targets. The full four-target certification matrix was not re-run: focused verification surfaced no shared regression (per the correction-task verification policy).

**Environment notes for the next session:** Android font-scale changes recreate the activity and re-deliver the last `VIEW` intent (re-navigate before capturing evidence); the SE run briefly tripped the RN element inspector via a synthetic tap — relaunching the app clears it.

**Repository diff for this audit:** `HANDOFF.md` (this section, approval-table upgrade, two stale-wording fixes) + `artifacts/native-validation-audit/` (38 curated evidence files). No `src/` changes.

## Canonical domain specification (2026-08-06 — docs/24, conditionally approved; Amendment A1 applied, awaiting final owner approval)

The docs/23 §16 P0 deliverable "§5–§7 canonical-model specification" is drafted as `docs/24_CANONICAL_DOMAIN_SPECIFICATION.md` — a documentation-and-architecture-only commit (no backend code, schema, or `src/` change). It expands docs/23 §5–§7 into the implementation-ready model all seven workstreams share: accounts/identities (identity spine, one-`self`-participant invariant, staff invitations, admin roles, break-glass) · catalogue/supply (taxonomy, programs/listings, schedules, sessions, camp weeks, the uniform capacity-unit interface, owner-final eligibility, quotes, policy-template refs) · booking/inventory (client-only drafts, server quotes, atomic capacity holds, bookings/enrolments/attendance, disruptions, both capacity invariants) · append-only payments/finance entities · twelve state machines with server-rejected invalid transitions · fourteen database invariants (fils, AED, isolation, capacity, idempotency, gateway-event uniqueness, append-only, dual control, ownership FKs, versioning, retention) · ten exact single-transaction boundaries (outbox rows inside; no transaction ever spans a gateway call) · the recoverable checkout/payment saga (explicitly not a distributed transaction) · outbox/inbox rules with the launch event set · the operational server-authorization matrix with PII tiers and child-data minimization · API contract principles (typed results, idempotency, versions/stale conflicts, the docs/09 §22.10 issue-code vocabulary as the platform standard) · a full mock-to-production migration map (**no approved screen requires redesign**; every mock-only field, vocabulary conflict — `Provider`→Organization, free-text categories, `dayOffset`/`MOCK_TODAY`, the dropped `freeTrial` price kind — is named) · seven first backend slices with acceptance criteria, negative-authorization tests, and transaction/concurrency tests · and the §14 owner-decision register (blocking-schema vs blocking-payment/commercial vs configurable values; VAT, commission, recurring billing, cancellation templates, waitlists, multi-participant scope, bundle identifiers, and the gateway are recorded, not decided). The two structural points originally flagged for owner confirmation were **ruled on 2026-08-06 and applied as docs/24 Amendment A1** (documentation-only follow-up commit): **(1)** the first-draft `enrolment_pool` (capacity on RecurringSchedule) was rejected — schedules stay purely temporal rules, and the new **EnrolmentCohort** entity (term/month/intake with program+branch, effective dates, schedule refs, capacity, confirmed/held counts, enrolment cutoff, lifecycle state, price/policy refs) is the canonical third inventory unit; `CapacityHold` targets exactly one of Session, CampWeek, or EnrolmentCohort, all under the identical capacity and capacity-floor invariants, with entity/state-machine/transaction/API/migration/slice-test sections updated accordingly; **(2)** system-initiated refunds are approved **only** where an authoritative event plus a deterministic policy calculation establish the obligation — the system creates the request and calculated amount, normal approval remains a separate Finance action (`initiated_by ≠ approved_by` preserved), automatic approval of ordinary customer/provider refunds is **not** authorized, and gateway voids/automatic same-amount saga reversals are explicitly append-only `PaymentTransaction` reversal postings (auto-executed with audit + reconciliation), never discretionary Refund records. **Stop line honored: no W4 slice planning or implementation starts until the owner gives final approval of docs/24 as amended.**

## Backend Slice 1 (2026-08-06 — database & migration foundation, awaiting owner review)

W4's first slice, implemented under docs/24 §13 row 1 as narrowed by the owner's Slice-1 instruction, on the owner-approved stack (rulings recorded in `docs/25_BACKEND_ENGINEERING_CONTRACT.md`, the new binding W4 engineering contract).

**What exists:** an independent `backend/` npm package (own package.json/lockfile/tsconfig/ESLint/Jest; Node 24 LTS pinned via `engines` + `.nvmrc` + `engine-strict` — the Expo app's Node 22 toolchain is untouched; root tsconfig/eslint gained `backend` exclusions so root tooling never sweeps the backend) · migration `0001_foundation.sql` (plain SQL via node-pg-migrate): `money_fils` + `currency_code` (AED-only) domains, `set_updated_at`/`bump_row_version`/`forbid_mutation` trigger functions, append-only `audit_event`, transactional `outbox_event` (per-aggregate unique sequence, unpublished partial index), `inbox_event` consumer dedup, `idempotency_key` (unique principal+scope+key; reference implementation of the updated_at/version conventions), `himma_app` NOLOGIN role with append-only-enforcing grants (no DELETE anywhere; outbox UPDATE restricted to publishing columns) · shared kernel: typed env config (production requires explicit `DATABASE_URL`, fails closed), layered test-DB safety guards (localhost-only + mandatory `himma_test` prefix, asserted in config, pool, migration runner, and test helper), UTC-pinned pg pool, Kysely over codegen'd types (`db:codegen` from the live schema — no handwritten schema model), `withTransaction` with full rollback + typed `DbError` translation, UUIDv7 `newId()`, `appendAuditEvent`, `appendOutboxEvent` (Trx-typed, advisory-lock per-aggregate sequencing), `markInboxProcessed` · migration tooling with **checksum-enforced applied-migration immutability** (`db:migrate`/`db:verify` fail closed on any edit to an applied file), ordered ordinal SQL files, reviewed Down sections, `db:down` dev/test-only + confirmation-gated, `db:new` template with the IRREVERSIBLE marker convention · Fastify 5 foundation (`buildApp` with TypeBox type provider, typed error envelope, `/internal/health`) — **no business routes** · relay worker deliberately deferred to the async-processing infrastructure (owner's narrowed scope: outbox/inbox as shared database foundation only).

**Verification (all green, 2026-08-06):** backend `tsc --noEmit` clean · backend ESLint `--max-warnings=0` clean · backend Jest **41/41 across 6 suites** on real PostgreSQL 18.4 (blank-DB migrate-from-zero; ordered/unique migration files; idempotent re-migrate; checksum-tamper fails closed; failing migration rolls back completely with nothing partial; down/up round-trip; production-down refusal; fils/AED domain rejections; UTC sessions; UUIDv7; trigger-maintained version+updated_at incl. CAS exactly-once and version-overwrite protection; append-only denials as owner and as `himma_app` per grant; outbox per-aggregate sequencing incl. 8-way concurrency; inbox dedup incl. concurrent single-winner; idempotency-key concurrent single-insert; transaction rollback/commit/translation; Fastify health) · `db:verify` passes against the dev database · root non-regression under the app's own Node 22: root tsc clean, root ESLint clean, root Jest **421/421 across 24 suites** · expo-doctor: 1 pre-existing failure — upstream SDK-57 registry drift (expo/expo-router →57.0.11, expo-symbols →57.0.2), root `package.json` untouched by this slice (git-verified); per docs/13 this is a separate owner-approved `chore(deps)` task, not part of Slice 1.

**Local environment facts:** Homebrew `postgresql@18` 18.4 running on localhost:5432 (`himma_backend_dev` created); Homebrew keg-only `node@24` (24.19.0) at `/opt/homebrew/opt/node@24/bin` for backend commands; no PostgreSQL-18-only features used (noted in the migration header). Gitignored/local artifacts: `backend/node_modules/` (root ignore), `backend/.env` (backend ignore; only `.env.example` committed).

**Slice 2 entry criteria (do not start without ALL of):** owner approval of Slice 1 (✅ granted at `d9b5fff`) · D1–D4 rulings (✅ issued 2026-08-06 and applied as docs/26 Amendment A1 — Cognito behind the adapter, email sequencing, sealed two-admin bootstrap, role exclusivity) · **final owner approval of the amended `docs/26_BACKEND_SLICE_2_IDENTITY_AUTHORIZATION_PLAN.md` (pending)**. Production Cognito region + identity DR remain open under docs/23 §18.3 and gate production enablement only, not implementation; B2-2's real-adapter verification additionally needs the §14.E′ non-production AWS dev pool.

## Backend Slice 2 · B2-1 (2026-08-06 — identity & authorization schema foundation, awaiting owner review)

Implemented per approved docs/26 §8 (Amendment A1) on the Slice-1 conventions. **Schema and tests only — no identity services, no Cognito integration or cloud resource, no routes, no middleware, no CLI.**

**Migration `0002_identity.sql`** (one transaction, reviewed Down, no PG-18-only features): `app_user` (status/lock semantics, mfa_enrolled mirror flag) · `auth_identity` (normalized **issuer + subject** uniqueness — never email; per-user composite unique as the session FK target; partial unique on **verified** lower(email) so duplicate emails never merge users; `is_private_relay` representable without special cases; **no password/credential columns**) · `customer_account` (one per user) · `participant` foundation (kind self|child, **one-self partial unique invariant**; no user/login linkage — participants are never principals; full child model + counsel fields deferred to Slice 5) · `login_session` (issuer/subject/`origin_jti` provider references only — **no token material**; composite FK ties the session's identity to the same user; one **live** session per `origin_jti`; device/inventory fields; retention-class comments, no invented periods) · `auth_challenge` bookkeeping (no codes/secrets) · `admin_role_assignment` (five roles, **no super-admin value**; `requested_by ≠ approved_by` CHECK; finance-capable active requires approver; per-(user,role) active partial unique; **transition-guard trigger** requested→active|denied, active→revoked|expired; **D4 exclusivity trigger** — access_admin ∦ finance, auditor ∦ mutating roles — serialized per user via advisory xact lock; approval modeled as fields on the assignment row per docs/26 §8.7; platform-engineer separation deliberately NOT fabricated in PostgreSQL) · `bootstrap_seal` (singleton PK + CHECK, `forbid_mutation` trigger — exactly one successful production bootstrap ever; CLI itself is B2-5) · himma_app grants (SELECT/INSERT/UPDATE on identity tables, **no DELETE anywhere**, SELECT-only on the seal). Kysely types regenerated from the live schema (12 tables; deterministic re-run proven). `db:verify` extended with per-migration object checks (`SCHEMA_CHECKS` map in [migrations.ts](backend/src/db/migrations.ts)).

**Verification (all green, 2026-08-06):** backend tsc clean · ESLint `--max-warnings=0` clean · backend Jest **70/70 across 8 suites** (29 new) on real PostgreSQL — migrate-from-zero incl. 0002; down/up round-trip (latest-migration policy); issuer+subject uniqueness incl. cross-issuer; unverified-duplicate emails coexist with **zero merging** while verified duplicates are refused; one account per user; one self participant per account; child-login structural impossibility; **structural secret-column scan** (no password/secret/token/totp/recovery/credential column in any public table; no refresh/token table exists); session ownership composite-FK denial, live-`origin_jti` uniqueness with post-revocation reuse, version-CAS revoke-once; dual-control CHECKs incl. concurrent approval race (exactly one winner via version CAS); invalid role transitions rejected; both exclusivity pairs both directions **incl. concurrent conflicting activation** (advisory-lock serialization, exactly one active); super-admin value rejected; bootstrap seal single-row + immutable even for the owner; himma_app permission matrix; audit+outbox atomic with identity mutations (commit and rollback both proven) · Slice-1 suites green (two tests updated to be migration-count-agnostic: down-test targets the latest migration; broken-migration fixture renamed `9999_broken.sql`) · codegen drift check clean · root non-regression **from the repo root under Node 22**: tsc clean, ESLint clean, frontend Jest **421/421**. Expo Doctor not re-run (no root config change this task; the previously recorded SDK-57 registry drift stands as a separate `chore(deps)` item).

### B2-1 correction (2026-08-06 — owner conditional approval of `8ac5065`; migration 0003 at `7565766`, **fully approved with B2-1 the same day**)

The owner conditionally approved B2-1 and directed one narrowly scoped schema correction covering exactly two guarantees. Applied as migration `0003_email_ownership_and_finance_approver_controls.sql`; no identity-model redesign, no other scope.

**Guarantee 1 — verified-email ownership is per USER, not per identity row.** The 0002 global partial unique index on verified `lower(email)` wrongly blocked one user from linking two identities (e.g. Apple + Google) that legitimately verify the same email. Replaced by an **exclusion constraint** `excl_auth_identity_verified_email_owner EXCLUDE USING gist ((lower(email)) WITH =, user_id WITH <>) WHERE (email_verified AND status='active')` on `btree_gist` (trusted core extension, adds no tables). Rows conflict only when the normalized email matches AND the owning user differs, so: same user may hold several active identities verifying one email · two users can never both actively verify one email · unverified/ended duplicates coexist and never merge anything · enforcement is the index machinery itself, so concurrent cross-user first-login/linking claims admit exactly one owner with no trigger or lock · issuer+subject matching (0002 constraints) untouched — email still never identifies an identity.

**Guarantee 2 — finance-capable activation requires two qualified Access Administrators.** New **deferred constraint trigger** `enforce_finance_activation_controls` (checked at commit, re-reading the row's final state): activation of `finance`/`access_admin` requires `requested_by ≠ approved_by` and BOTH holding a currently `active`, unexpired (`expires_at` checked against `now()`, not just the sweep) `access_admin` assignment that **predates the activating transaction** (`age(xmin) > 0` discipline) — so `requested`/`denied`/`revoked`/time-expired assignments never qualify and two conspirators cannot self-qualify by inserting a cross-referenced pair in one transaction. **Bootstrap preserved with no bypass branch for finance:** the single transaction that inserts the `bootstrap_seal` row may qualify same-transaction `access_admin` rows (the docs/26 §9.9 cross-witnessed pair validates itself at commit); the seal admits one row ever (singleton + `forbid_mutation`) and `himma_app` can only SELECT it, so the path exists once, for the elevated bootstrap CLI, and structurally cannot create a `finance` role. **Concurrency:** the check takes the 0002 per-user advisory xact-lock keys on requester and approver (deterministic order); new trigger `serialize_access_admin_release` takes the same lock whenever an `access_admin` row leaves `active`, so approval-vs-revocation resolves to exactly one serial order (an approval committing after a concurrent revocation re-reads the revoked state and fails). New trigger `enforce_admin_role_immutability` closes post-activation rewriting: `user_id`/`role`/`requested_by` immutable, witnesses (`approved_by`/`denied_by`/`revoked_by`) immutable once set, `expires_at` frozen after `requested` — the rule binds every writer including `himma_app`; no service-layer path can bypass it. `db:verify` SCHEMA_CHECKS extended with `constraint`/`extension` kinds and the 0003 objects.

**Verification (all green, 2026-08-06):** backend tsc clean · ESLint `--max-warnings=0` clean · backend Jest **86/86 across 9 suites** (16 new/updated) on real PostgreSQL, each suite migrating a blank database from zero through 0003 · new `finance-approver-controls` suite: conspiracy cross-pair without the seal refused with nothing committed · rogue bootstrap attempting `finance` refused with the seal unconsumed · approved bootstrap shape succeeds (fixture `bootstrapAccessAdmins`, the test-side §9.9 equivalent) · two valid distinct access_admins activate finance · unqualified requester refused · unqualified approver refused · `requested`/`denied` disqualified · `revoked` disqualified · time-expired-but-unswept disqualified · requester=approver still refused by the 0002 CHECK · `himma_app` itself is bound by the trigger · role-mutation bypass (active `operations` → `finance`) refused · **deterministic concurrent approval-vs-revocation**: an approval commit blocks on the in-flight revocation's advisory lock (observed blocked, then refused after the revocation commits; final state = revoked admin, request still `requested`) · witness/identity-column immutability · identity-schema additions: one user links two active identities sharing a verified email · concurrent cross-user claims of one verified email admit exactly one owner with both users intact (nothing merged) · unverified-duplicate and cross-user-verified-refusal tests from 0002 still green over the new constraint · admin-roles fixtures rebased on the bootstrap pair (seal tests now assert the fixture's singleton) · down/up round-trip on 0003 green (reviewed Down restores the 0002 index; documented to fail if same-user duplicates exist — dev/test only) · dev `db:migrate` applied 0003; `db:verify` passes · Kysely codegen re-run deterministic with **zero drift** (no column changes) · root untouched: no repository-level configuration changed, so root frontend and native certification are not re-run per the owner directive.

## Backend Slice 2 · B2-2 (2026-08-06 — provider-independent identity core & adapter foundations, this commit, awaiting owner review)

Implemented per docs/26 §13 B2-2 (§9.1–9.3 services, adapter port + fake + Cognito adapter, mail port with dev capture) on the approved B2-1/0003 schema. **No HTTP routes, no middleware, no login sessions (B2-3 seam), no admin-role services, no MFA, no cloud resource, no email vendor, no Apple/Google configuration.**

**Modules introduced (`backend/src/modules/identity/`):** `providers/evidence.ts` — `ProviderEvidence` (provider kind · normalized issuer · subject · email attribute · verified/private-relay flags · assurance · optional display name) with total structural validation (`validateProviderEvidence`) · `providers/adapter.ts` — `AuthProviderAdapter` port returning typed `{ok,evidence} | {ok:false, invalidProviderEvidence|providerUnavailable}`; network I/O documented as strictly outside DB transactions · `providers/fake/fake-adapter.ts` — deterministic fake (mint token ↔ evidence, malformed-material minting, outage injection); the adapter ALL automated tests use · `providers/cognito/config.ts` — config types + env parser (returns undefined when no pool is configured — the universal B2-2 state; https-only issuer, ≥1 client id, derived JWKS URI) · `providers/cognito/cognito-adapter.ts` — JWKS validation via `jose@5` (RS256-only, issuer + audience + expiry + `token_use:'id'`, kid-rotation-aware injectable key source, remote JWKS default), claim normalization (Cognito `identities` → apple/google/email, string-or-boolean `email_verified`, private-relay suffix, `amr` → assurance), error normalization (JOSE token errors → invalidProviderEvidence; JWKS transport/non-JOSE → providerUnavailable); **`cognito:groups`/custom role claims read by nothing** · `mail/mail-sender.ts` — `MailSender` port + `CaptureMailSender` (captures, never delivers) + `mailLogLine` (template + recipient digest; the only sanctioned log form — no body/subject/address) · `persistence/identity-repository.ts` — ALL identity-table access (users, identities incl. FOR-UPDATE active-method listing and CAS ending, verified-email owner lookup, account+self-participant creation as one unit, challenge bookkeeping CAS) taking the caller's `Trx` · `services/account-status.ts` — `classifyAccountState` (user security state first, then customer-account business state; unknown user = deleted-shaped) + `readAccountState` · `services/first-login.ts` · `services/link-identity.ts` · `services/challenges.ts`.

**Service semantics (docs/26 §3, §9.1–9.3):** `firstLogin` — one transaction: resolve by issuer+subject → `identityResolved` (touches `last_login_at`; creates a missing customer account + self participant for a user first signing in as a customer, per §1.3) · else verified-email ownership precheck → create User + AuthIdentity + CustomerAccount + one `Me` self participant + audit + outbox atomically → `newCustomerCreated`. Concurrency: identical concurrent first logins converge by a single retry on the convergent unique constraints (issuer+subject, one-account-per-user, one-self-participant); cross-user verified-email races translate the 0003 exclusion violation to `verifiedEmailConflict`; **unexpected DB failures propagate as failures, never mislabeled as conflicts**. `linkIdentity(ctx, evidence)` — target user ONLY from the `AuthenticatedContext` abstraction (B2-4 supplies it after step-up): idempotent `identityAlreadyLinked`, permanent-subject `identityLinkedToAnotherUser` (active OR ended elsewhere), own-ended-row reactivation, same-user shared verified email allowed, cross-user `verifiedEmailConflict`, retry-once convergence under races. `unlinkIdentity` — §9.3 row-locked usable-method count (FOR UPDATE on all active identities serializes concurrent unlinks; a user can never lose the final method), version-CAS ending → `staleVersion`, cross-user rows not-found-shaped. Challenges — bookkeeping only (codes are the provider's): requested + audit, open-only attempt counting, CAS single-use completion; concurrent completion admits one winner. Account state: locked/deleted user → `accountLocked`/`accountDeleted`; suspended/anonymized account → `accountSuspended`/`accountDeleted` at login; login-method management gates on user security state only.

**Typed outcomes:** `identityResolved` · `newCustomerCreated` · `identityLinked` · `identityAlreadyLinked` · `identityLinkedToAnotherUser` · `verifiedEmailConflict` · `identityEnded` (ended identity presented as login; maps to §10 `accountLinkConflict` guidance at route time) · `identityUnlinked` · `lastLoginMethod` · `identityNotFound` · `staleVersion` · `accountLocked` · `accountDeleted` · `accountSuspended` · `invalidProviderEvidence` · `providerUnavailable` (adapter) · `challengeInvalid`/`recorded`/`completed`. No raw constraint names or provider errors cross the service boundary.

**Audit/outbox events (same transaction as the causal mutation, ids-only payloads):** audit `auth.user_created` / `auth.account_created` / `auth.identity_linked` / `auth.identity_unlinked` / `auth.challenge_requested` / `auth.challenge_completed`; outbox `user.created` (aggregate app_user) / `account.created` (aggregate customer_account) / `identity.linked` / `identity.unlinked` (aggregate app_user).

**Verification (all green, 2026-08-06):** backend tsc clean · ESLint `--max-warnings=0` clean · backend Jest **163/163 across 16 suites** (77 new across 7 new suites) on real PostgreSQL, every DB suite migrating from zero through 0003 · first-login: single-transaction creation proven, audit+outbox atomicity, forced-failure rollback (participant-insert trigger) leaves zero rows/events, idempotent repeat, 4-way concurrent convergence to one user/account, issuer+subject-not-email matching, unverified duplicates independent, cross-user verified-email refusal incl. concurrent one-winner, locked/deleted/suspended typed, malformed evidence rejected without DB contact, missing-account backfill on first customer sign-in · linking: link/idempotent/other-user (active + ended)/reactivation/shared-verified-email/cross-user-conflict/concurrent one-winner subject race/concurrent idempotent convergence/locked/deleted/malformed · unlink: ended+events, last-method refusal, not-found shaping, staleVersion, concurrent unlinks cannot strand a user · challenges: audit'd request, attempt counting, CAS single-use, expiry, concurrent single winner · adapter contract suite over BOTH fake and Cognito (valid → normalized evidence; invalid/garbage → typed; outage → providerUnavailable) · Cognito specifics: forged key, wrong issuer, wrong audience, expired, `token_use` access rejected, missing sub, JWKS rotation (new kid validates after key-set refresh), groups/custom-claims ignored and absent from evidence, Apple federation + private relay, string `email_verified`, `amr` mfa, config parsing (undefined when unset · https-only · client-id required) · boundary guards: jose/AWS imports only inside `providers/cognito/`; no Cognito claim names in the core; services free of direct table access and of console/stdout logging; **full service flows executed AS `himma_app`** (role-GUC pool) proving grants sufficient (breadth denials remain covered by B2-1 suites); outbox payload values are UUIDs only; audit rows carry no emails and no digests · CaptureMailSender records full messages, delivers nothing; `mailLogLine` contains template + digest only · migrate-from-zero + `db:verify` green (3 migrations; no new migration in B2-2) · Kysely codegen re-run: zero drift · root untouched (no repository-level change outside `backend/`; root certification not re-run). New backend dependency: `jose@^5` (zero-dependency JWT/JWKS; v5 chosen because v6 is ESM-only and the backend is CJS).

**Recorded deviations/decisions (docs/26 reconciliation):** §9.1's `login_session` insert is deliberately absent — sessions are the B2-3 commit per §13 (owner-directed split; first-login returns the resolved principal for B2-3 to layer sessions onto) · the docs/26 §13 B2-2 stop-line's real-Cognito-adapter exercise against a live pool is NOT done — this task prohibited AWS access; JWKS mechanics are proven against locally generated keys, and the §14.E′ dev-pool smoke remains an explicit outstanding item · `identityEnded` extends the core vocabulary (route mapping to `accountLinkConflict` guidance recorded in code) · the mail port ships standalone; no flow sends mail yet (enumeration-safe flows are B2-4, per D2).

### B2-2 correction (2026-08-06 — owner conditional approval of `559ad2c`; this commit, awaiting owner review)

**Final display-name rule (binding, owner-directed):** a customer's display name is a validated provider-supplied name when present; otherwise the neutral placeholder `Customer` (the explicit profile-incomplete default pending W1 onboarding). **A person's name is never inferred from an email address** — the B2-2 email-local-part fallback is removed, and Apple private-relay addresses and ordinary addresses receive identical treatment. Blank/oversized provider names are dropped by evidence validation and fall to the neutral placeholder. The self participant remains `Me` (docs/24 §1.2, never email-derived). Identity matching, verified-email ownership, linking, and all other B2-2 behavior are unchanged — the only production edit is `defaultDisplayName` in [first-login.ts](backend/src/modules/identity/services/first-login.ts).

**Verification (all green, 2026-08-06):** 5 new focused tests (valid provider name retained · absent name → `Customer`, never the local part · private-relay local part never a name · blank and oversized names → `Customer` · repeat first login never rewrites the stored name) — the local-part cases observed failing before the fix · repeat/concurrent first-login suites unchanged and green · backend Jest **168/168 across 16 suites** · tsc clean · ESLint clean. No schema, migration, or dependency change.

## Backend Slice 2 · B2-3 (2026-08-06 — sessions and liveness, this commit, awaiting owner review)

Implemented per docs/26 §13 B2-3 (§4.4–4.7, §9.4–9.6, §9.8 revocation leg) on the approved B2-2 core. **No new migration** (the B2-1 `login_session` schema already carries every approved field). **No HTTP routes, no Fastify hooks** (docs/26's "liveness middleware" is delivered as the framework-neutral service B2-4's middleware will wrap — per the owner's B2-3 directive), no admin services, no MFA, no cloud resource, no region choice.

**Modules introduced:** `providers/access-token.ts` — the **separate access-token verification boundary**: `AccessTokenEvidence` (issuer · subject · `origin_jti` · optional `jti`/client id · scopes · assurance · expiry) with total structural validation; an ID token is never an API bearer token · `providers/fake/fake-access-token-verifier.ts` — deterministic fake used by all session tests · `providers/cognito/cognito-access-token-verifier.ts` — RS256-only JWKS verification (injectable kid-rotation-aware key source), trusted issuer, **`client_id` claim** (access tokens carry no `aud`), **`token_use='access'`** (ID tokens rejected by construction), exp/nbf with **bounded configurable clock tolerance** (config `clockToleranceSeconds` 0–300, default 30, parser-validated), normalized subject + `origin_jti` required, scopes/`amr` carried as transport metadata; groups/custom claims read by nothing · `providers/revocation.ts` — `ProviderSessionRevoker` port (`session`/`allSessions` scope, ephemeral client-supplied token material passed through but **never persisted or logged**) + typed post-commit `ProviderRevocationDelivery` for retry processing · `providers/fake/fake-provider-revoker.ts` (records presence of ephemeral material, never the material) · `persistence/session-repository.ts` — all `login_session` access (insert, by-origin_jti, FOR-UPDATE by id, live-by-user, touch, revoke-if-live, version-CAS revoke, revoke-all-live) · `services/sessions.ts` — `establishSession` + `listSessions` + `digestIp` (sha256; raw IPs never at rest) + the shared `resolveActivePrincipal` gate · `services/session-liveness.ts` — `checkSessionLiveness` returning the framework-neutral `AuthenticatedSessionPrincipal` (**no roles — provider claims never create Himma permissions**; read-only by default, `touch` opt-in) · `services/session-revocation.ts` — `logoutSession` (current device without version / selected device with CAS), `logoutAllSessions`, `forceRevokeUserSessions` (reasons account_locked/suspended/deleted/security, composable into future account-status flows), post-commit provider follow-up.

**Session rules (documented in code + proven):** a session exists only for verified evidence whose issuer+subject resolves to an active identity of an active user/account · **one provider session (origin_jti) maps to at most one Himma session EVER** — repeated/concurrent establishment converges on the live row (unique-index retry), and a revoked row permanently retires its origin_jti, so logout can never be undone by re-presenting an old token; a fresh provider login carries a fresh origin_jti · an origin_jti recorded for a different principal is refused as untrustworthy evidence · rows hold normalized metadata only — no token material anywhere (structural guards + no-refresh-token schema guard stay green) · liveness gates session → expiry → identity → user/account, denies revoked/expired/locked/suspended/deleted/ended, and once a revocation commits every subsequent check denies (in-flight checks that began earlier follow docs/26 §4.6 "refused on its next request") · local revocation is authoritative: reason + actor audit + version bump + outbox (`session.revoked_all` for set-wide) in one transaction; provider revocation runs strictly post-commit through the port, failure yields a typed status + `auth.provider_revocation_failed` audit and never reactivates the session · revoke-all is point-in-time (documented race rule): sessions committed at its snapshot are revoked; an establishment committing after survives as a new login · ownership enforced (another user's session is not-found-shaped) · concurrent logouts idempotent (`alreadyRevoked`); stale mutation refused (`staleVersion`).

**Typed outcomes added:** `sessionEstablished` · `sessionNotRegistered` · `sessionExpired` · `sessionRevoked` · `authenticated` (principal) · `invalidAccessToken` · `providerUnavailable` · `loggedOut` · `alreadyRevoked` · `sessionNotFound` · `loggedOutAll` · `sessionsRevoked` · `staleVersion` · plus the B2-2 account/identity states reused. Audit: `auth.session_established` · `auth.session_revoked` · `auth.provider_revocation_failed`. Outbox: `session.revoked_all` (aggregate app_user, ids-only/empty payload).

**Verification (all green, 2026-08-06):** backend tsc clean · ESLint `--max-warnings=0` clean · backend Jest **223/223 across 20 suites** (55 new in 4 suites) on real PostgreSQL — access-token contract over both verifiers (valid accepted; **ID-token-as-bearer rejected**; missing `token_use`, wrong issuer, wrong client id, expired, not-yet-valid, forged signature, unknown kid, missing origin_jti all rejected; bounded skew tolerance honored and capped; groups/custom claims no effect and absent from evidence; scopes/mfa/`jti` carried; config fails closed without issuer/client ids and rejects out-of-range tolerance) · establishment (normalized metadata + digested IP only, audit event, idempotent + 4-way concurrent convergence to one live session, unknown/ended identity + locked/suspended gates, foreign-origin_jti refusal, forced-failure rollback leaves zero rows/events) · liveness (role-free principal, not-registered/revoked/expired distinctions, all account states, read-only by default, revocation-commit denial incl. concurrent storm) · revocation (reason/actor/version audit, concurrent idempotency with exactly one audit row, ownership, CAS staleVersion, revoke-all scoped to the intended user with one outbox event, forced revocation blocks all sessions, **provider called strictly after commit** — observed committed state at call time — with ephemeral material passed through and never retained, provider failure/throw normalized to typed status with local denial intact, point-in-time race rule sequential + concurrent) · secrecy sweeps (no token-like material in rows, audit, or outbox) · migrate-from-zero + `db:verify` green (3 migrations, none new) · codegen zero drift · B2-1 + B2-2 suites untouched and green · root untouched. **The boundary guards caught and forced two fixes during implementation** (a service-level direct table read moved into the repository; a provider claim-name literal removed from the core port) — the scans are load-bearing.

**Recorded deviations/decisions:** docs/26 §13's "liveness middleware" delivered as the framework-neutral `checkSessionLiveness` service per the owner's B2-3 directive — B2-4's Fastify middleware wraps it · the real-pool smoke (§14.E′ non-production dev pool) remains outstanding; local-key JWKS tests are NOT claimed as live Cognito validation · a Cognito `ProviderSessionRevoker` implementation is deliberately NOT built (one-device revocation needs client-held refresh-token material and pool endpoints; the port + orchestration + typed delivery status are in place, the concrete revoker lands with the dev-pool prerequisite) · `establishSession` treats a revoked origin_jti as permanently retired (`sessionRevoked`) — the schema-level post-revocation key reuse proven in B2-1 remains possible for elevated tooling but the service path refuses resurrection.

### B2-3 identity-spine consistency review (2026-08-06 — owner conditional approval of `345659e`; proof, this commit, awaiting owner review)

**Finding: the binding rule is already structurally guaranteed — no production change was required; the deliverable is a proof suite.** Inspection record: (1) the B2-2 ID-token adapter verifies the token `iss` against the configured pool issuer and emits `evidence.issuer = config.issuer`, `evidence.subject = payload.sub` — in a Cognito user pool, `sub` is the pool-assigned user identifier, identical in ID and access tokens; the Apple/Google external subject exists only inside the `identities` claim, which the adapter reads solely to derive the `provider` provenance kind (apple/google/email), never as a key. (2) First login therefore persists `auth_identity` keyed by **(pool issuer, Cognito sub)** — the canonical Cognito identity; `provider` is metadata. (3) The B2-3 access-token verifier emits the same `(config.issuer, payload.sub)` pair, so establishment and liveness resolve through the identical key by construction — external identities are not merely "not required" for bearer authentication, they are unreachable by it (the verifier can only ever stamp the configured pool issuer). (4) `login_session`'s composite FK `(user_id, provider_issuer, provider_subject) → auth_identity(user_id, issuer, subject)` ties every session to that canonical row at the schema level. (5) Apple/Google/email flows all reduce to the same key; email and groups remain non-authoritative (B2-2/B2-3 guards).

**Proof suite [identity-spine-consistency.test.ts](backend/test/identity-spine-consistency.test.ts)** (9 tests; real Cognito adapters over local JWKS + real services on real PostgreSQL): Apple-federated ID token (external `identities.userId` present) persists pool issuer + Cognito sub and the external subject appears nowhere as a key · ID-token and access-token evidence carry the identical (issuer, subject) for one user · Apple-federated, Google-federated, and email/password logins each followed by access-token establishment resolve one user, with the session row FK-tied to the canonical identity and liveness authenticating the same principal · a deliberately linked external Apple-issuer identity coexists as an additional row without replacing or conflicting with the canonical identity, and post-link access tokens still resolve the canonical row · two Cognito users sharing an unverified email stay separate end-to-end · a second Cognito user claiming an owned verified email gets `verifiedEmailConflict` with no merge · concurrent first logins and session establishments converge on one user and one live session.

**Verification (all green, 2026-08-06):** backend Jest **232/232 across 21 suites** (9 new) · tsc clean · ESLint clean · no schema change (schema verification/codegen unchanged per the owner directive) · no production code modified.

## Backend Slice 2 · B2-4 (2026-08-06 — auth routes and principal context, this commit, awaiting owner review)

Implemented per docs/26 §13 B2-4 (§10 customer routes under A1.1, §6 policy layers, §11.2/11.8–11.10) on the approved B2-2/B2-3 core. **No admin routes/services, no MFA, no customer login UI, no cloud resource, no production email, no region choice.**

**Route inventory (snapshot-guarded — [http-policy-pipeline.test.ts.snap](backend/test/__snapshots__/http-policy-pipeline.test.ts.snap); a future unprotected route breaks the suite):** `POST /auth/session` (unauthenticatedAuthFlow) · `POST /auth/password/reset-request` (unauthenticatedAuthFlow) · `GET /me`, `GET /auth/sessions`, `POST /auth/logout`, `POST /auth/logout-all` (authenticatedCustomer) · `DELETE /auth/sessions/:sessionId`, `POST /auth/identities/link`, `DELETE /auth/identities/:identityId` (stepUpRequired) · `GET /internal/health` (explicitly public). All bodies/params/responses TypeBox-validated; auth routes carry a 16 KB body limit; responses are nosniff and non-public routes no-store.

**Modules:** [policies.ts](backend/src/modules/identity/http/policies.ts) — four policy categories (`public` · `unauthenticatedAuthFlow` · `authenticatedCustomer` · `stepUpRequired`; role policies are a later-slice registry extension), **structural deny-by-default**: an undeclared route throws AT REGISTRATION (proven — the app cannot even construct), and the inventory is decorated for the snapshot test · [rate-limiter.ts](backend/src/modules/identity/http/rate-limiter.ts) — store abstraction + deterministic in-memory impl; **`createRateLimiterStore('production')` FAILS CLOSED** (no approved distributed store exists; a per-instance limiter is never shipped as production-safe); keys are sha256 digests (email/IP/user), never raw values; defaults (docs/26 §14.C): session 10/min·IP, reset 5/min·email+IP, link 10/min·user, invalid-bearer 10/min·IP · [auth-plugin.ts](backend/src/modules/identity/http/auth-plugin.ts) — bearer pipeline: strict header shape/length → ACCESS-token verifier (ID tokens rejected by token-usage semantics; provider I/O outside transactions) → B2-3 `checkSessionLiveness` → minimum principal (`RequestPrincipal` = B2-3 session principal + issuer/subject + `stepUpAt` + typed EMPTY `adminRoles`/`orgScope` slots — nothing fabricated, no provider claim ever creates a permission); step-up = provider `auth_time` within the configurable max-age (default 300 s; new optional `authTime` on AccessTokenEvidence, mapped from `auth_time`/`iat`); repeated invalid bearers rate-limited per IP digest · [http-outcomes.ts](backend/src/modules/identity/http/http-outcomes.ts) — one stable `{code,message}` envelope; documented collapses: locked/suspended/deleted → `accountSuspended` (§3.8 single message), all bearer session denials → `sessionExpired`, login-flow failures → `invalidCredentials`; nothing leaks constraints/JWKS/stacks/existence · [identity-routes.ts](backend/src/modules/identity/http/identity-routes.ts) + services [profile.ts](backend/src/modules/identity/services/profile.ts) (approved `/me` snapshot: user id · account id/displayName/contactEmail · self participant · empty roles; no security metadata, no child legal fields) and [password-reset.ts](backend/src/modules/identity/services/password-reset.ts) (enumeration-safe: identical outward result; bookkeeping + captured mail only when an identity exists; mail carries NO code — codes are the provider's) · [build-app.ts](backend/src/app/build-app.ts) wires it all behind injected deps.

**`POST /auth/session` orchestration (documented failure boundary):** access token verified → optional ID token verified → **token-pair binding: both must carry the same issuer + subject, else 401 with nothing created** → B2-2 `firstLogin` (own transaction) → B2-3 `establishSession` (own transaction) — deliberately NOT one spanning transaction (the §13 commit split separates them); a canonical user may exist when establishment fails (proven via forced trigger → 500 internalError, user exists), and a retry converges with zero duplicate users/accounts/sessions (both services are idempotent/convergent). Unknown identities on the login path collapse into `invalidCredentials` (§5.5), while unlink keeps not-found shaping.

**Verification (all green, 2026-08-06):** backend tsc clean · ESLint clean · backend Jest **265/265 across 24 suites** (33 new in 3 HTTP suites) on real PostgreSQL — inventory snapshot + per-route policy assertion · undeclared route fails at registration · health public · missing/malformed/oversized bearer headers 401 without echo · fake-ID-token-as-bearer rejected · valid token + live session authenticates · not-registered/revoked → `sessionExpired`; locked/suspended/deleted → `accountSuspended` · provider outage → 503 · scopes/claims yield empty roles · 10-failure bearer limit → deterministic 429 + retry-after · stale `auth_time` → 403 `stepUpRequired` on step-up routes · production rate-store fail-closed · session route: establish, pair-mismatch rejection with zero rows, idempotent re-present, token-only for known identity, unknown → `invalidCredentials`, partial-failure 500 then convergent retry, per-IP 429, 422 without echoing values · `/me` exact approved keys (no `locked|ip_digest|origin_jti|version|status` text) · inventory scoped + `current` flag · selected logout: foreign 404, stale 409, ok 200, step-up enforced · logout idempotent + local denial after provider-revocation failure · logout-all revokes only the caller's sessions (count 2, bystander intact), repeat safely denied 401 · link: linked/alreadyLinked/409 for another user's subject · unlink: CAS + `lastLoginMethod` 409 · reset-request: byte-identical bodies/status/header-sets for existing vs unknown email, both ≥ the 60 ms test floor, captured mail only for the existing identity with no code-like content, schema 422, 429 after 5 per email digest · secrecy sweeps: no token/code material in audit, outbox, or any error payload · migrate-from-zero + `db:verify` green (3 migrations, none new) · codegen zero drift · B2-1/2/3 suites untouched and green · root untouched.

**Recorded deviations/decisions (docs/26 reconciliation):** §10's `login`/`oidc/apple`/`oidc/google` collapse into `POST /auth/session` under A1.1 (all providers converge on Cognito token presentation; nonce handling is provider-side) · `register`/`verify-email`/`login(email)` password flows need adapter CREDENTIAL mediation against a real pool and are deferred to the §14.E′ dev-pool prerequisite (recorded, not silently dropped) · `POST /auth/refresh` has no Himma route — rotation is Cognito-client-side (§9.4); Himma's leg is per-request liveness · `POST /auth/step-up` as a route is deferred with MFA (B2-6); step-up ENFORCEMENT ships now via provider `auth_time` recency (§3.10/§14.C) · reset-request is the one §10 recovery route shipped (challenge bookkeeping + notice; provider-side code dispatch joins with the dev pool) · ephemeral client-held refresh material passes through logout bodies to the provider-revocation port only — never persisted, logged, audited, or echoed.

**Still pending production dependencies:** §14.E′ non-production dev pool (real-pool smoke; Cognito revoker implementation; credential-mediation routes) · approved distributed rate-limit store (production limiter startup is refused until then) · production email delivery (D2) · production region/DR (docs/23 §18.3) · production Apple/Google client ids (docs/24 §14.B13).

## Backend Slice 2 · B2-5 (2026-08-06 — admin roles, dual control, bootstrap CLI, this commit, awaiting owner review)

Implemented per docs/26 §13 B2-5 (§7, §9.7, §9.9, §10 admin routes, §11.6) on the approved B2-1..B2-4 stack. **No provider staff/organization roles, no MFA implementation, no cloud resource.**

**Services ([admin-roles.ts](backend/src/modules/identity/services/admin-roles.ts) over [admin-role-repository.ts](backend/src/modules/identity/persistence/admin-role-repository.ts)):** `resolveAdminRoles` — **direct database-backed resolution on every request** (documented rule: no caching, no role material in tokens; grants/revocations/expiry take effect on the next request); only `active` AND unexpired assignments qualify — requested/denied/revoked/time-expired are ignored (proven) · `requestRoleAssignment` — only an active access_admin may request; finance-capable roles (`finance`, `access_admin`) enter `requested`; other roles activate immediately on the single access-admin request (§7.2) · `approveRoleAssignment` (distinct-approver pre-typed `selfApprovalForbidden`, CHECK-backed) · `denyRoleAssignment` · `revokeRoleAssignment` · `processExpiredAssignments` sweep (resolution already ignores overdue rows) · `listRoleAssignments`/`getRoleAssignment` (readers: access_admin + auditor). All mutations are single transactions with audit (`auth.admin_role_requested/approved/denied/revoked/expired`, `auth.bootstrap_completed`) + outbox (`admin_role.requested/approved/denied/revoked`, ids-only payloads); the B2-1/0003 constraints and triggers remain the final authority — the service pre-checks for typed outcomes and TRANSLATES database refusals (trigger-message classification stays internal; nothing DB-shaped reaches HTTP/CLI). Platform-engineer separation stays an infrastructure-IAM + grant-time rule, deliberately NOT misrepresented as database-enforced. Typed outcomes: `roleRequested/roleActivated/roleDenied/roleRevoked/requesterNotQualified/approverNotQualified/selfApprovalForbidden/targetNotFound/assignmentNotFound/assignmentAlreadyFinalized/roleConflict/staleVersion/forbidden`.

**Admin routes ([admin-routes.ts](backend/src/modules/identity/http/admin-routes.ts), snapshot-updated intentionally):** new explicit `admin` policy category — live session + **MFA assurance** (`mfaRequired` typed refusal otherwise) + ≥1 active Himma database role (`forbidden` otherwise); Cognito groups/claims still create nothing. `GET /admin/role-assignments` · `GET /admin/role-assignments/:id` · `POST /admin/role-requests` · `POST /admin/role-requests/:id/approve` · `POST /admin/role-requests/:id/deny` · `DELETE /admin/role-assignments/:id` — TypeBox schemas, stable envelopes (`dualControlViolation` 409 for self-approval, `roleConflict`/`assignmentAlreadyFinalized`/`staleVersion` 409, `forbidden`/`mfaRequired` 403, `notFound` 404). **Production fail-closed boundary (until B2-6): admin routes are NOT registered in production builds (absent → 404), and explicitly forcing `enableAdminRoutes` in production THROWS at startup** — admin business access cannot become production-enabled through B2-5; B2-6's admin MFA enforcement lifts the gate.

**Bootstrap ([bootstrap.ts](backend/src/modules/identity/admin/bootstrap.ts) + CLI `npm run bootstrap:admins`):** CLI-only (no HTTP path) — requires explicit production env, the reviewed manifest `{users:[a,b], secretDigest}`, the exact confirmation phrase, and the secret via `HIMMA_BOOTSTRAP_SECRET`/interactive input (never argv); manifest/users (exactly two distinct, active, verified-identity) validated BEFORE any transaction; sha256 digest binds the secret to the reviewed manifest (timing-safe compare); one transaction under an advisory lock: exactly two cross-witnessed access_admin assignments + audit + outbox + the immutable seal (manifest digest + operator ref) — the implementation has NO role parameter, so no other role is creatable; concurrent attempts admit exactly one success; sealed forever after; forced failure rolls back everything; himma_app cannot touch the seal (B2-1 guarantee, still green). Secret lifecycle documented in the module header (generate ≥32 chars into the store at review, digest into the manifest, inject once, destroy); never logged/persisted — proven by sweep.

**Seeding ([seed.ts](backend/src/modules/identity/admin/seed.ts) + CLI `npm run db:seed-admins`):** entirely separate mechanism — refuses production, refuses sealed/foreign-state databases, never writes a seal, never calls the bootstrap implementation; deterministic identities (fixed seed subjects, `.invalid` emails) and safely repeatable (converges). Because 0003 only admits a first cross-pair inside the seal transaction, seeding suspends exactly the one finance-approver constraint trigger inside its own elevated transaction and restores it — proven not to weaken the database (a sealless conspiracy pair is still refused after seeding).

**Verification (all green, 2026-08-06):** backend tsc clean · ESLint clean · backend Jest **291/291 across 27 suites** (26 new: 9 service, 10 bootstrap/seeding, 7 route) on real PostgreSQL — resolution ignores every non-qualifying state · immediate vs dual-control activation · self-approval refusal at service and CHECK level · unqualified requester/approver refusals · D4 conflicts via service AND trigger · concurrent double-approval single-activation · concurrent approval-vs-revocation safe in every interleaving · stale CAS · expiry pre-sweep and sweep · customer 403 on admin routes, non-MFA admin `mfaRequired`, no-bearer 401 · full route lifecycle incl. conflict/stale/deny/inspect (no emails in responses) · production build: admin routes absent + forced enablement throws · full bootstrap rehearsal on fresh scratch DBs: env/manifest/secret/phrase gates create nothing, forced seal failure rolls back all, concurrency single-winner, exactly two access_admins with audit+outbox+seal, permanently sealed, structurally-single-role, secret-free sweeps · seeding: production refusal, seal untouched, deterministic repeatability, post-seed constraint integrity · migrate-from-zero + `db:verify` green (3 migrations, none new) · codegen zero drift · B2-1..B2-4 suites green (policy snapshot updated intentionally with the six admin routes) · root untouched.

**Recorded deviations/decisions:** the bootstrap manifest binds its secret by sha256 digest (the mechanism docs/26 D3 left to implementation; no secret-store vendor selected) · ~~bootstrap outbox uses the approved `admin_role.approved` events (×2) with `auth.bootstrap_completed` as audit — no new outbox event type invented~~ *(superseded by the event-integrity correction below: a dedicated `admin.bootstrap.completed` outbox event now exists alongside them)* · ~~expiry finalization is audit-only (no approved outbox event)~~ *(superseded below: the sweep now emits `admin_role.expired` audit + outbox atomically)* · seeding's single-trigger suspension is the recorded consequence of the 0003 conspiracy protection.

### B2-5 event-integrity correction (2026-08-07 — owner conditional approval of `5c1c98f`; correction `99575a4`, **fully approved with B2-5 the same day**)

Narrowly scoped per the owner's conditional approval; all existing B2-5 behavior preserved (every prior event, gate, and typed outcome unchanged — the correction only ADDS the two missing event emissions).

**1 · Bootstrap completion event ([bootstrap.ts](backend/src/modules/identity/admin/bootstrap.ts)):** the production bootstrap now appends a dedicated transactional-outbox event `admin.bootstrap.completed` (aggregate `bootstrap_seal` / manifest digest) in the SAME transaction as the two Access Administrator assignments, their role-approval events, the immutable seal, and the existing `auth.bootstrap_completed` audit event. Payload is safe identifiers only: `manifestDigest` (the singleton seal's canonical identifier — the seal table has no surrogate id by design), the two `assignmentIds`, the two `userIds`, `sealedAt` (read back from the inserted seal row), and `executedBy` (the reviewed ticket/operator ref) — no secret, no manifest contents, no emails, no tokens. The existing advisory-lock single-winner path means concurrent attempts produce exactly one completion event; a failed bootstrap rolls the completion event back with everything else.

**2 · Expiry events ([admin-roles.ts](backend/src/modules/identity/services/admin-roles.ts) + [admin-role-repository.ts](backend/src/modules/identity/persistence/admin-role-repository.ts)):** the expiry sweep's canonical `active → expired` transition now emits the existing `auth.admin_role_expired` audit event AND a new `admin_role.expired` outbox event (aggregate `app_user`/target, payload `{assignmentId}` only) in the same transaction as the state change; `expireDueAssignments` returns `{id, user_id}` pairs to carry the aggregate id. Merely reading an overdue assignment emits nothing — only the sweep's transition does. Already-expired rows are untouched (no duplicates); concurrent sweeps converge via row locks (each assignment transitions and emits exactly once); principal resolution continues denying time-expired assignments immediately, before any sweep.

**Tests ([admin-event-integrity.test.ts](backend/test/admin-event-integrity.test.ts), 9 new, real PostgreSQL, TDD red→green):** successful bootstrap writes exactly one completion audit + one completion outbox event · payload carries exactly the safe identifiers (verified field-by-field against the seal and assignment rows) · no secret and no email address in any event payload (bootstrap + expiry) · forced failure at the completion-event write (trigger on `outbox_event`) rolls back assignments, seal, and every bootstrap event atomically · concurrent bootstrap attempts still produce exactly one completion event · forced failure at the expiry outbox write rolls the state transition back (atomicity), then the clean sweep transitions + emits both events · repeated sweeps are idempotent (no duplicate events) · two concurrent sweeps over three due assignments emit exactly one transition/event set each · pre-sweep resolution denial re-proven.

**Verification (all green, 2026-08-07):** backend tsc clean · ESLint `--max-warnings=0` clean · backend Jest **300/300 across 28 suites** (9 new) on real PostgreSQL · migrate-from-zero on a fresh scratch database (3 migrations, none new) · `db:verify` passed · codegen zero drift (12 tables, generated file byte-identical) · no frontend or native surface touched (no recertification required).

## Backend Slice 2 · B2-6A (2026-08-07 — MFA, recovery-code, and step-up schema foundation; **FULLY APPROVED at `0756456` 2026-08-07**)

Owner-directed sub-slice of B2-6: **schema only** (migration `0004_mfa_and_step_up_foundation.sql`, regenerated Kysely types, focused real-PostgreSQL tests). No Cognito calls, no application services, no HTTP routes, no admin-login activation, no cloud resource, no frontend. Cognito remains authoritative for TOTP secrets and provider MFA configuration (Amendment A1.1 supersedes the §5.8 pre-amendment secret-storage wording — read as an applied amendment, not a contradiction); Himma stores only enforcement/audit/recovery/assurance bookkeeping.

**Tables (all Slice-1 conventions: UUIDv7 ids, UTC timestamptz, created/updated/version + shared triggers, CHECK-backed states, FK ownership):**
- **`mfa_method`** — Cognito-managed TOTP enrollment mirror (docs/26 §8.8). `kind` CHECK `'totp'` only; `provider_managed` CHECK-forced true (a Himma-managed method would imply secret storage — structurally impossible). Lifecycle `pending → active → disabled | superseded`: pending requires `enrollment_expires_at` and no confirmation; active requires `confirmed_at`; terminal states require `ended_at` and are frozen by trigger. Partial uniques: one active AND one pending method per user. Trigger `enforce_mfa_method_transition` also refuses activation past the enrollment window and makes `confirmed_at` write-once, settable only by the pending→active transition (so B2-6B's activation is a single atomic UPDATE after Cognito verification succeeds).
- **`app_user.mfa_enrolled` mirror** — maintained BOTH ways by trigger: `maintain_mfa_enrolled_mirror` (AFTER on `mfa_method`) recomputes it from active methods; `enforce_mfa_enrolled_consistency` (BEFORE on `app_user`) refuses any direct write disagreeing with the active-method truth — the flag alone can never fabricate enrollment, and it is bookkeeping, never recent-MFA assurance (runtime concern, B2-6B/C).
- **`mfa_recovery_code_batch`** — one identifiable issuance: `code_count` (>0; count is §14.C configuration), `digest_scheme` (`hmac_sha256`), `pepper_version` (≥1; the pepper value lives in the secret store, never PostgreSQL), `issued_at`/`ended_at`, states `active → superseded | invalidated` (terminal frozen; metadata immutable). Partial unique: one active batch per user. `UNIQUE (id, user_id)` is the composite-FK target so codes can never cross users. Cascade trigger `cascade_recovery_batch_invalidation`: when a batch leaves active, every remaining unconsumed code is invalidated in the same transaction — service omission cannot leave stale usable codes.
- **`mfa_recovery_code`** — one versioned digest per single-use code: globally unique `code_hash` (docs/26 uq), `consumed_at`/`invalidated_at` both write-once (trigger) and mutually exclusive (CHECK) — consumption is CAS, concurrency admits exactly one winner, and no consumed/invalidated code can ever be restored or its digest rewritten. Raw codes exist only transiently in the future issuing service.
- **`mfa_challenge`** — challenge bookkeeping (§8.6 pattern extended for MFA; the 0002 `auth_challenge` stays untouched — it is identity-bound with email/reset kinds, while MFA challenges are user/session-bound, recorded as a DDL-review decision). `purpose` `login | step_up` (step_up requires a session binding; login precedes any Himma session on the `mfaRequired` continuation), composite FK `(login_session_id, user_id)` so a challenge can never bind another user's session, `expires_at` NOT NULL, monotonic `attempt_count`, states `pending → passed | failed | expired | invalidated` (terminal frozen; expired can never pass; `passed_at` ⇔ passed). No provider challenge material — Cognito's challenge session travels client↔adapter only.
- **`step_up_grant`** — §3.10 recent-reauthentication bookkeeping backing `PrincipalContext.stepUpAt`: bound by composite FK to the owning user + login session, `method` `totp | recovery_code | password | oidc`, bounded window CHECK (`expires_at > granted_at`; max-age is §14.C configuration), immutable except one-time `invalidated_at`. A grant row alone proves nothing: runtime assurance additionally requires the verified B2-6B flow, a live session, and an unexpired uninvalidated grant (documented on the table).
- **`login_session`** gains `UNIQUE (id, user_id)` (`uq_login_session_id_user`) as the composite-FK target.

**Prohibited-material posture:** no column anywhere can hold TOTP secrets, QR contents, raw recovery codes, Cognito session tokens, access/refresh tokens, challenge secrets, or encryption/hash keys — proven structurally (exact column-set locks on `mfa_method`/`mfa_recovery_code` + expanded prohibited-name scan over every public column; the B2-1 secret-column guard also still covers all new tables). Grants: `SELECT, INSERT, UPDATE` (never DELETE) on all five tables to `himma_app`; retention classes documented per table (challenges/grants short-lived operational records for the elevated sweep; methods/batches follow the account lifecycle, pseudonymization-ready).

**Verification (all green, 2026-08-07):** backend tsc clean · ESLint `--max-warnings=0` clean · backend Jest **324/324 across 29 suites** (24 new in [mfa-schema.test.ts](backend/test/mfa-schema.test.ts), TDD red→green: lifecycle/invalid-combination refusals · one-active-and-one-pending method per user · expired-enrollment activation refused · terminal immutability · mirror follows only active methods, direct writes refused at insert and update · one active batch per user · duplicate digests and cross-user codes refused · regeneration invalidates remaining-not-consumed codes · CAS single consumption · concurrent consumption single winner · no restore/rewrite of consumed/invalidated codes or digests · step-up challenge session binding + foreign-session refusal · expired/terminal challenges cannot pass/reuse · monotonic attempts · grant user+session binding, bounded window, write-once invalidation · structural secret-absence · himma_app grant matrix) · migrate-from-zero (4 migrations) · `db:verify` passed (0004 objects registered in SCHEMA_CHECKS) · **rollback verified**: `db:down` (with its confirmation gate) cleanly reverted 0004, re-apply restored, verify green — the migrations suite's down-and-restore test now exercises 0004 automatically · codegen: 5 new interfaces, byte-identical on re-run (zero drift) · all B2-1..B2-5 identity/session/HTTP/admin-role/bootstrap suites green unchanged.

**Recorded deviations/decisions (within §8's "proposed; final DDL reviewed" latitude):** lifecycle columns (`state`, `enrollment_expires_at`, `ended_at`) extend §8.8's minimal method shape to model pending-expiry and disabled/superseded as the owner's B2-6A directive requires · recovery codes gain the batch table (docs/26 names only the code rows; batches realize §5.9 regeneration-invalidates-old-set as a database guarantee) · `mfa_challenge` is a new table rather than widening 0002 `auth_challenge` (different binding: user/session vs identity; applied migrations are immutable) · `step_up_grant` persists §3.10/PrincipalContext `stepUpAt` (per-request DB resolution needs persistent backing; docs/26 left the mechanism to implementation) · no production pepper value chosen — `pepper_version` metadata only.

## Backend Slice 2 · B2-6B (2026-08-07 — Cognito MFA mediation, recovery-code and step-up services, this commit, awaiting owner review)

Owner-directed sub-slice: provider-independent MFA application services over the approved B2-6A schema. **No HTTP routes, no production admin activation, no frontend, no Cognito pool or cloud resource, no region/email decisions.** Authority boundary held throughout: Cognito owns TOTP secret generation, provider-side enrollment, and challenge verification; Himma PostgreSQL owns mirrored enrollment state, recovery batches/consumption, challenge bookkeeping, step-up grants, audit, outbox, and every business authorization decision.

**MFA provider port ([mfa.ts](backend/src/modules/identity/providers/mfa.ts)):** ~~`beginTotpEnrollment` / `verifyTotpEnrollment` / `verifyTotpChallenge`~~ *(superseded by the TOTP-semantics correction below: step-up now uses the distinct `beginTotpStepUpChallenge`/`respondToTotpStepUpChallenge` challenge-flow operations)*, normalized to `enrollmentStarted · verificationSucceeded · invalidCode · challengeExpired · providerUnavailable · invalidProviderState`. Called strictly OUTSIDE PostgreSQL transactions. [FakeMfaProvider](backend/src/modules/identity/providers/fake/fake-mfa-provider.ts) is the deterministic test double; [CognitoMfaAdapter](backend/src/modules/identity/providers/cognito/cognito-mfa-adapter.ts) mediates AssociateSoftwareToken/VerifySoftwareToken behind an INJECTED `CognitoMfaClient` interface (no AWS SDK dependency added; the SDK-backed client is instantiated only by the future §14.E′ real-pool smoke task) and collapses every provider error into single-field results by error NAME — no AWS exception object, message, or challenge state escapes. Provider-side MFA disable is NOT implemented: docs/26 assigns no standalone disable operation to this sub-slice (removal is a step-up-gated later concern; support-assisted reset is W3 tooling) — replacement happens only via re-enrollment supersession.

**TOTP enrollment ([mfa-enrollment.ts](backend/src/modules/identity/services/mfa-enrollment.ts)):** `beginTotpEnrollment` — eligibility (active user) → provider call outside any transaction → one transaction superseding any stale pending enrollment and inserting the pending `mfa_method` with the configured expiring window → audit `auth.mfa_enrollment_started`; the shared secret is returned ONLY as the ephemeral service result (for the future B2-6C route/QR) and never persisted/logged/audited. `completeTotpEnrollment` — pending validation (one normalized `enrollmentInvalid` for absent/foreign/non-pending/expired) → provider verification outside the transaction → only on success one activation transaction: supersede the previous active method (audit + outbox `mfa.method_superseded`), CAS-activate the pending method (expiry re-checked in-statement; B2-6A trigger beneath; any late failure aborts the WHOLE transaction), the DATABASE maintains `app_user.mfa_enrolled`, audit `auth.mfa_enrolled` + outbox `mfa.enrolled`. Provider failure leaves no falsely active method (failure audit `auth.mfa_enrollment_failed`); expired pending enrollments cannot activate even with stale valid provider material. Enrollment bookkeeping is the pending `mfa_method` row itself (B2-6A defines challenge purposes `login|step_up` only — recorded reading, not a contradiction).

**Recovery codes ([mfa-recovery-codes.ts](backend/src/modules/identity/services/mfa-recovery-codes.ts)):** generation uses `crypto.randomInt` (CSPRNG, unbiased) over a 30-symbol unambiguous alphabet (no 0/1/i/l/o/u), 28 symbols grouped `xxxx-…-xxxx` ≈ **137 bits ≥ 128**; entry is lenient (case/separators stripped, entropy untouched). The configured count (default 10 per §5.9; §14.C-configurable) is generated in memory, returned EXACTLY once; PostgreSQL receives only **HMAC-SHA-256 digests under the batch's pepper version**; regeneration supersedes the previous active batch in the same transaction (B2-6A cascade invalidates its remaining codes) with audit `auth.recovery_codes_generated` + outbox `mfa.recovery_codes_generated`. No operation anywhere can return an original code again. `consumeRecoveryCode` — digest computed with the BATCH's pepper version, CAS consumption (exactly one concurrent winner), wrong/consumed/unknown/superseded all collapse into ONE `recoveryCodeRejected` outcome (audited), and with a live session the approved step-up grant (`method 'recovery_code'`) is created in the SAME transaction with `auth.step_up_completed` + `mfa.step_up_completed`; a dead session refuses without burning the code.

**Pepper configuration ([mfa-config.ts](backend/src/modules/identity/services/mfa-config.ts)):** `HIMMA_MFA_RECOVERY_PEPPER` (+ `_VERSION`) through the docs/23 §10.3 secret/config boundary only; held in memory for HMAC, never persisted/logged. **Production fails closed**: missing, short (<32), or dev-valued peppers throw at config parse; development/test default to the deterministic `DEV_TEST_PEPPER` constant (clearly non-production; no production secret exists anywhere in the repo). All §14.C values configurable with safe defaults: recovery-code count 10 · enrollment window 900 s · challenge TTL 300 s · challenge attempt cap 5 · step-up max-age 900 s. A batch whose pepper version is not configured throws loudly (never a silent "invalid code").

**TOTP step-up ([mfa-step-up.ts](backend/src/modules/identity/services/mfa-step-up.ts)):** `beginStepUpChallenge` (live owned session + active method → pending session-bound B2-6A challenge with TTL, audited) · `completeStepUpWithTotp` — bookkeeping pre-check (binding/pending/expiry/attempt-cap, finalizing expired→`expired` and capped→`failed` with the normalized **`tooManyAttempts`** outcome) → provider verification outside any transaction → on failure monotonic attempt bump + `auth.mfa_challenge_failed` (cap finalizes) → on success ONE transaction: session re-checked live, challenge CAS `pending→passed` (single-use; **concurrent replay admits exactly one winner**), one `step_up_grant` bound to the canonical user + live session with `granted_at`/`expires_at` from one clock (exact configured window), audit `auth.mfa_challenge_passed`/`auth.step_up_completed` + outbox `mfa.step_up_completed`. `resolveStepUpAssurance` (for B2-6C principal resolution) returns assurance ONLY for a live session with an unexpired uninvalidated grant — a revoked/expired session makes every grant useless; provider verification alone never grants business permissions.

**Audit/outbox vocabulary added:** audit `auth.mfa_enrollment_started/mfa_enrolled/mfa_enrollment_failed/mfa_method_superseded/recovery_codes_generated/recovery_code_used/recovery_code_rejected/mfa_challenge_requested/mfa_challenge_failed/mfa_challenge_expired/mfa_challenge_passed/step_up_completed` · outbox `mfa.enrolled` (docs/26 §8.9) plus `mfa.method_superseded`, `mfa.recovery_codes_generated`, `mfa.recovery_code_used`, `mfa.step_up_completed`. Payloads are opaque ids + safe metadata only — no TOTP seed, OTP, raw code, digest, pepper, provider challenge/session string, or token (proven by whole-database sweeps).

**Secret-safety architecture:** [mfa-repository.ts](backend/src/modules/identity/persistence/mfa-repository.ts) input types structurally cannot accept secrets (ids, timestamps, and precomputed digests only; `insertRecoveryCodeDigests` is digest-named and digest-typed); services write no logs (B2-2 boundary guard covers the services directory); the adapter's normalized results are single-field objects (proven: provider errors carrying secret-bearing messages leave nothing behind); a whole-database sweep helper ([db-sweep.ts](backend/test/helpers/db-sweep.ts)) proves TOTP secrets, OTPs, raw/normalized codes, provider tokens, digests-in-events, and the pepper appear in NO row of ANY public table after full flows.

### B2-6B Cognito TOTP-semantics correction (2026-08-07 — owner conditional approval of `25dd742`; correction `49da921`, **fully approved with B2-6B the same day**)

Owner-directed review finding, confirmed: `25dd742`'s `verifyTotpChallenge` reused **VerifySoftwareToken — the Cognito ENROLLMENT/registration confirmation API — as a generic step-up verifier** against an access token. Cognito has no supported operation that verifies a bare TOTP code against an access token; an already-enrolled user's ordinary TOTP verification is the **SOFTWARE_TOKEN_MFA challenge flow**: a fresh reauthentication yields `ChallengeName: SOFTWARE_TOKEN_MFA` + a provider `Session`, answered via **RespondToAuthChallenge**. Corrected honestly, no invented capability:

- **Port** ([mfa.ts](backend/src/modules/identity/providers/mfa.ts)): `verifyTotpChallenge` REMOVED. Enrollment keeps `beginTotpEnrollment`/`verifyTotpEnrollment` (Associate/VerifySoftwareToken only). Step-up gains two distinct operations: `beginTotpStepUpChallenge(providerUserRef) → challengeIssued{TotpChallengeSession}` and `respondToTotpStepUpChallenge(providerUserRef, challenge, code)`. `TotpChallengeSession` is documented ephemeral SECRET material — never persisted, logged, audited, or eventable; it travels only through the bounded begin→complete flow (service result → client → completion input).
- **Cognito adapter** ([cognito-mfa-adapter.ts](backend/src/modules/identity/providers/cognito/cognito-mfa-adapter.ts)): injected `CognitoMfaClient` gains `initiateSoftwareTokenMfaChallenge` (the reauthentication call yielding the SOFTWARE_TOKEN_MFA challenge — WHICH Cognito auth flow produces it is pool configuration, deferred to §14.E′) and `respondToSoftwareTokenMfaChallenge` (RespondToAuthChallenge). The adapter refuses any challenge start that is not `SOFTWARE_TOKEN_MFA`-with-session; in the respond context `NotAuthorizedException` (invalid/expired/replayed Session) maps to `challengeExpired` — mapping flagged for confirmation in the §14.E′ real-pool smoke. No cloud provisioning; no AWS SDK added.
- **Service** ([mfa-step-up.ts](backend/src/modules/identity/services/mfa-step-up.ts)): `beginStepUpChallenge` now models the honest flow — eligibility → **fresh provider reauthentication challenge outside any transaction** (failure creates no Himma bookkeeping) → non-secret Himma `mfa_challenge` row → the provider session returned only in the ephemeral result. `completeStepUpWithTotp` takes `providerUserRef` + the ephemeral `providerChallenge` and answers the provider challenge outside any transaction; the `step_up_grant` is created only after Cognito confirms the actual MFA challenge; provider failures (invalid code, dead/replayed session, outage) create no local assurance. Himma `mfa_challenge` stays non-secret bookkeeping only; recovery-code step-up is untouched (no provider involvement).
- **Fake** ([fake-mfa-provider.ts](backend/src/modules/identity/providers/fake/fake-mfa-provider.ts)): mirrors the semantics — single-use, user-bound deterministic challenge sessions, plus a `callLog` so tests prove operation separation.

**Tests (rewritten provider-contract 7 + step-up 10; enrollment/recovery suites untouched):** enrollment uses ONLY Associate/VerifySoftwareToken and step-up NEVER calls the enrollment-verification operation (spy counters on the mock client AND the fake's call log through full service flows) · non-SOFTWARE_TOKEN_MFA or session-less challenge starts refused · invalid, expired, and REPLAYED provider sessions (and forged ones) create no grant · successful completion creates exactly one session-bound grant (Himma CAS still the deciding guard under concurrent replay with two valid provider sessions) · provider session material swept absent from every public table, audit, and outbox · normalized error objects carry nothing from provider exceptions (secret- and session-bearing messages) · recovery-code behavior unchanged. **Verification (2026-08-07): tsc clean · ESLint clean · full backend Jest 365/365 (33 suites) · no schema/codegen/frontend change, so no migration/codegen/native re-verification applies.**

**First-login race fix (`dd17030`, separate commit — APPROVED):** the B2-6B regression gate exposed a pre-existing B2-2-era defect — the spine concurrency test failed ~50% of runs at approved HEAD `0756456` (proven by 15-run loops before/after stash). Root cause, confirmed by captured divergence: under READ COMMITTED an identical concurrent first login committing between the loser's issuer+subject lookup and its verified-email pre-check (or its exclusion-constraint check) was misreported as `verifiedEmailConflict`, refusing a legitimate sign-in. Fix: both paths re-resolve the same-provider identity before declaring a conflict; genuine cross-user conflicts unchanged (49/49 conflict-suite tests green); spine suite 15/15 consecutive green after the fix. No merge behavior introduced.

**Verification (all green, 2026-08-07):** backend tsc clean · ESLint `--max-warnings=0` clean · backend Jest **360/360 across 37 suites, run twice** (36 new: provider contract 5 · enrollment 9 · recovery codes 11 · step-up 9 + hygiene assertions throughout; TDD red→green) · migrate-from-zero (4 migrations, none new) · `db:verify` green · codegen zero drift · all B2-1..B2-6A identity/session/HTTP/admin/bootstrap/MFA-schema suites green. **Live-Cognito items still pending under §14.E′:** real-pool smoke for AssociateSoftwareToken/VerifySoftwareToken mediation, the SDK-backed `CognitoMfaClient` instantiation, and any pool MFA configuration — all blocked on the separately-approved non-production pool; no live Cognito MFA validation is claimed.

## Backend Slice 2 · B2-6C (2026-08-07 — MFA routes, admin MFA enforcement, production capability gate, Slice 2 closeout; this commit, awaiting owner review)

**Routes ([mfa-routes.ts](backend/src/modules/identity/http/mfa-routes.ts), all TypeBox-schema'd, body-limited, rate-limited per user, `Cache-Control: no-store` via the B2-4 pipeline, stable sanitized envelopes, bearer never echoed):** `POST /auth/mfa/totp/enroll` (**stepUpRequired**) → `enrollmentStarted` with the ephemeral TOTP secret (schema-documented one-time; never stored/logged/audited/outboxed; no QR generation) · `POST /auth/mfa/totp/confirm` (**stepUpRequired**) → `mfaEnrolled` (+`supersededMethodId`); enrollmentInvalid/invalidCode/challengeExpired/invalidProviderState collapse into ONE `challengeInvalid` envelope · `POST /auth/mfa/recovery-codes/regenerate` (**stepUpRequired** — recent step-up enforced by policy) → raw codes exactly once with `presentation: 'one-time'`; no read path can ever return them · `POST /auth/step-up/totp/begin` (**authenticatedCustomer**) → non-secret Himma challenge bookkeeping + the ephemeral provider challenge session (49da921 semantics preserved: genuine SOFTWARE_TOKEN_MFA flow, never the enrollment verifier) · `POST /auth/step-up/totp/complete` → exactly one session-bound grant on provider-confirmed success; `tooManyAttempts → rateLimited`; failures grant nothing · `POST /auth/step-up/recovery-code` → Himma-owned atomic single-use consumption + same-transaction grant; wrong/consumed/superseded/never-existed byte-identical. These are the docs/26 §10 route set (the two-phase step-up is the begin/complete equivalent of `POST /auth/step-up`); no additional product flows invented. New outcome `challengeInvalid` (400) added to the envelope registry; new per-user rate-limit rules `mfaEnrollment`/`mfaChallenge`/`recoveryCode` (§14.C-configurable defaults).

**Principal assurance ([auth-plugin.ts](backend/src/modules/identity/http/auth-plugin.ts)):** `stepUpRequired` policy now accepts a fresh provider authentication (`auth_time` within the configured max-age) OR a live session-bound Himma `step_up_grant` (B2-6B `resolveSessionMfaAssurance`); the principal exposes safe derived `mfaEnrolled` and `stepUp {at, method}` only. Enrollment state alone is never recent assurance; grants are queried only on the already-liveness-checked session, so revoked/expired sessions void every grant (proven).

**Administrative enforcement (binding order, docs/23 §7):** live Himma session → **≥1 active Himma database role** (non-admins get plain `forbidden` and learn nothing about MFA) → **Himma MFA enrollment** (`mfaRequired` otherwise — a forged/asserted `amr: mfa` token cannot compensate) → **recent MFA-verified assurance**: an MFA login within the step-up window OR a live TOTP/recovery-code grant (`password`/`oidc` grants never satisfy admin MFA); MFA-verified-but-stale → `stepUpRequired`. Roles and assurance resolve fresh per request — role or session revocation bites regardless of any existing grant; Cognito groups/claims still grant nothing.

**Production capability gate ([build-app.ts](backend/src/app/build-app.ts)):** the temporary B2-5 placeholder is replaced by `AdminProductionReadiness` — production registers the admin surface ONLY when `cognitoConfigured` + `mfaProviderValidated` + `realPoolSmokeVerified` + `productionConfigApproved` ALL report true; otherwise absent (fail-closed 404), and explicitly forcing it in an unready production build refuses startup loudly. No default-true, no env shortcut, no test bypass — dev/test exercise the full behavior through deterministic adapters. Because the §14.E′ smoke has not run, **no truthful production configuration can report ready: Slice 2 implementation complete, production Cognito/admin-MFA activation pending operational validation** (recorded in docs/26 §15).

**Slice-2 hardening review (bounded closeout, all fifteen areas):** raw credential/token/secret persistence (schema guards + whole-DB sweeps) · token-type confusion (ID-token-as-bearer refused) · issuer/subject consistency (composite FKs + spine suite) · account merging (exclusion constraint; `dd17030` convergence) · session replay (one live session per `origin_jti`; §9.5 revocation) · logout/revocation (immediate liveness denial) · role escalation (DB-only roles; customer→admin `forbidden`) · dual control (CHECK + triggers) · bootstrap bypass (seal + structural single-role) · MFA bypass (two-way mirror triggers; enforcement order; fail-closed production gate) · recovery-code replay (CAS + write-once) · challenge replay (Himma CAS + single-use provider sessions) · missing route policy (structural onRoute guard + snapshot) · sensitive logging (zero logging in services/providers/MFA routes; throws carry key names/versions only) · audit/outbox leakage (payload sweeps). **No new confirmed Slice-2 security defect.** One behavioral alignment applied during implementation (admin-gate check order made exactly the binding order). Recorded future items, NOT expanded into scope: log-redaction policy for the future production server entrypoint · grant/challenge invalidation on MFA reset when the support-tooling disable flow lands (W3) · admin-portal `client_kind` sessions (Slice 3) · optional recovery-code remaining-count metadata read.

**Verification (full Slice-2 certification, 2026-08-07):** backend tsc clean · ESLint clean · backend Jest **390/390 across 34 suites** (25 new in [mfa-routes.test.ts](backend/test/mfa-routes.test.ts): policy/structure/schema-rejection · enrollment ephemeral material + provider-gated activation · recovery codes once-only + regeneration + recent-step-up requirement · TOTP step-up ephemeral session, single grant, invalid/replayed/expired refusals, attempt throttling · recovery-code step-up single-use + concurrency + indistinguishable failures · expired/invalidated-grant and revoked-session assurance boundaries · admin enforcement matrix incl. non-MFA-grant refusal and unenrolled-admin refusal · production gate closed/partial/forced/ready · whole-DB secret sweep) · migrate-from-zero (4 migrations, none new) · `db:verify` green · codegen zero drift · route-policy inventory snapshot updated intentionally (+6 MFA routes) · root customer app untouched: tsc clean · expo lint clean · Jest **421/421 across 24 suites**.

**Slice 3 entry criteria (do not start without ALL):** explicit owner approval of B2-6C and the Slice-2 closeout — **✅ granted 2026-08-07 (`f95b84a` approved, slice closed)** · an owner-approved Slice-3 specification — **drafted as docs/27 (this commit), awaiting owner review** · the docs/23 §16 phase gate satisfied · production identity activation items remain independent operational work (§14.E′ smoke, docs/23 §18.3 region/DR, production config) and do NOT block Slice-3 planning, but real-pool validation must precede any production admin enablement.

## Backend Slice 3 · S3-1 (2026-08-07 — organization/profile/branch schema foundation; **FULLY APPROVED at `3b5d12f` 2026-08-07**)

**Migration [`0005_provider_organizations.sql`](backend/migrations/0005_provider_organizations.sql)** (one transaction, reviewed Down, all Slice-1 conventions) — exactly the docs/27 §2–§4/§12 S3-1 scope; no staff, invitation, route, listing, or portal object exists.

- **`organization`** — canonical PRIVATE commercial counterparty: `legal_name` (never public) vs `trade_name`, `org_kind` CHECK `'provider'` (D-S3-4 seam), `origin` CHECK `'admin_created'` (D-S3-2 seam; self-signup widens additively), `verification_state` with the docs/24 §5.1 machine enforced by `enforce_organization_transition` (every edge verbatim incl. rejected→submitted resubmission and suspended⇄live; invalid skips refused at trigger level even if a future service forgets; `offboarded` terminal — the whole row freezes), state-tied `suspended_at`/`offboarded_at` CHECKs (reinstatement must clear the timestamp), `commercial_terms_ref` (structure only, §14.B4 open), identity/provenance immutability, partial index on `live`. Actor metadata is audit-trail data, not columns. NO bank/payout/licence-content/commission/listing field exists anywhere in the slice.
- **`organization_public_profile`** — the 1:1 customer-facing storefront record (PK = organization_id, re-pointing refused by trigger): display name, en/ar descriptions, logo/cover/gallery media refs, four explicit opt-in public contact fields, and `published` (A1). Structural separation: future customer reads select from THIS table + public branch columns only; no json/jsonb column exists on it (nothing can smuggle private data in — schema-tested). **Documented semantics: `published` is publication intent/readiness and MAY be set pre-live (drafting allowed, no restricting trigger per the A1 ruling); EFFECTIVE visibility is always `verification_state='live' AND published`, and suspension/offboarding removes availability via the lifecycle regardless of the flag** — the predicate itself is exercised in tests across draft→live→suspended.
- **`branch`** — organization-owned location: label, address/city, `area_id` (FK-less until the Slice-4 taxonomy migration; `area_label` serves reads meanwhile), `geo_point` (lng,lat)-ordered with range CHECKs, `opening_hours` jsonb, `facilities text[]`, `active` (deactivate-only; no DELETE grant), ownership immutable by trigger, `ix_branch_organization (organization_id, active)`, and the binding **`UNIQUE (id, organization_id)` composite ownership target**.
- **Future-listing compatibility proven structurally:** a scratch probe of the docs/24 §1.4 Program shape (`listing.organization_id` FK + a listing↔branch join composite-FK'd to `branch (id, organization_id)`) attaches with zero alteration to the three S3-1 tables, and a cross-organization branch association is refused by the composite FK; the probe is dropped in-test — S3-1 ships no listing/category/membership/package table (asserted).
- **Grants:** `SELECT, INSERT, UPDATE` on all three tables to `himma_app`, no DELETE (tested). Audit/outbox: no new tables — the migration header records the org.* / organization.* event vocabulary the later service commits emit through the Slice-1 machinery.

**Verification (all green, 2026-08-07):** backend tsc clean · ESLint clean · backend Jest **410/410 across 35 suites** (20 new in [provider-org-schema.test.ts](backend/test/provider-org-schema.test.ts): defaults · seam CHECKs (partner/self_signup refused today, widening additive) · full §5.1 walk · invalid-skip matrix · timestamp-state ties · offboarded terminality · version CAS · profile 1:1/orphan/re-point refusals · pre-live published drafting + effective-visibility predicate across live/suspended · public/private structural scans + organization column-set lock · branch ownership/immutability/deactivation/geo-bounds · future-listing probe · grants) · migrate-from-zero (5 migrations) · `db:verify` green (0005 objects in SCHEMA_CHECKS) · **rollback verified** (down cleanly removed all objects; re-apply + verify green; the migrations suite's down-test now exercises 0005 automatically) · codegen: 3 new interfaces (`Organization`, `OrganizationPublicProfile`, `Branch`), byte-identical on re-run (zero drift) · all Slice-1/Slice-2 suites green unchanged. No deviations from docs/27.

**S3-2 entry criteria (do not start without ALL):** explicit owner approval of S3-1 · scope per docs/27 §16 row S3-2 — staff_membership (+branch scope join) + staff_invitation tables with the §5.2 machine, last-owner guard, and invitation services (issue/accept/revoke/expire sweep) implementing the D-S3-1 verified-email acceptance rules with the token-digest pepper boundary and captured MailSender · acceptance = docs/27 §14.5–6 race/hygiene matrix · focused suites + migration battery green → stop for owner review before S3-3. *(Met: S3-1 approved at `3b5d12f`; S3-2 implemented at `3bcb37d` — section below.)*

## Backend Slice 3 · S3-2 (2026-08-07 — staff memberships, branch scopes, digest-stored invitations; **FULLY APPROVED at `3bcb37d` + guard correction `8784ce0` 2026-08-07**)

**Migration [`0006_staff_memberships_and_invitations.sql`](backend/migrations/0006_staff_memberships_and_invitations.sql)** — exactly the docs/27 §16 row S3-2 scope (Amendment A1/D-S3-1). No provider routes, principal middleware, capability registry, admin lifecycle, storefront read, or listing object exists.

- **`staff_membership`** on the canonical `app_user` (no second identity system): exact 7-role CHECK vocabulary (`owner org_manager branch_manager listings_editor coach front_desk finance`; code↔display mapping in [provider-roles.ts](backend/src/modules/provider/provider-roles.ts)), `active → revoked` terminal machine, append-only history (role/scope change = revoke + NEW row; identity/role/provenance immutable), one-ACTIVE-per-user+org partial unique (multi-org allowed), org-bound composite invitation-provenance FK, `SELECT/INSERT/UPDATE` only.
- **Branch scopes** — `staff_membership_branch` composite-FK'd on BOTH sides with `organization_id` (cross-org scope unrepresentable by direct SQL); org-wide-only roles CHECK-forced to `'all'`; scope rows refused on `'all'` memberships; a `'branches'` membership with an empty set is refused at COMMIT (deferred trigger) — empty can never mean org-wide; scope rows append-only (INSERT+SELECT grants; `forbid_mutation`); branch deactivation never rewrites/transfers scope rows.
- **Last-active-owner guard** — advisory-xact-lock-serialized trigger refuses revoking the final active Owner of a non-offboarded organization (concurrent revocations of the last two Owners leave exactly one, tested); `offboarded` is the sanctioned terminal exception; no owner-transfer flow invented.
- **`staff_invitation`** — `sent → accepted | revoked | expired`; normalized lowercased target email (CHECK); HMAC-SHA-256 `token_digest` + scheme/pepper-version metadata ONLY (B2-6B boundary — raw token exists in service memory + mail payload only, never returned by services, never in rows/logs/audit/outbox — hygiene-tested); overdue rows unacceptable by time alone (trigger) even before the sweep; one live invitation per org+address (partial unique; resend = revoke + new row); target/role/scope/digest/expiry immutable; terminal rows frozen.
- **Services** ([staff-invitations.ts](backend/src/modules/provider/services/staff-invitations.ts), approved S3-2 caller-context abstraction — no fabricated S3-3 middleware): **issue** (owner-membership authority resolved fresh from PostgreSQL, or `admin` context validated as an active `operations` role for the future §13.3 founding path; active-same-org branch validation; resend supersession; audit+outbox in-transaction; post-commit MailSender with `mailDelivery: delivered|failed` on the result — mail failure never rolls back) · **accept** (D-S3-1: verified normalized email match required — unverified/display-name/subject-only/private-relay refused; relay users link+verify via the real Slice-2 flow then succeed; FOR UPDATE + CAS single-use; membership + scope rows + events in ONE §7.10 transaction; all failures byte-identical `invitationInvalid`; suspended/offboarded orgs refuse safely, invitation stays pending) · **revoke** (idempotent, exactly-once events) · **expiry sweep** (B2-5 pattern; deterministic-order locking, concurrent sweeps idempotent, exactly-once transition+events). Events: audit `org.staff_invited/staff_joined/invitation_revoked/invitation_expired`, outbox `staff.invited/joined/invitation_revoked/invitation_expired` on the `organization` aggregate — ids only, email NEVER. Config [staff-invitation-config.ts](backend/src/modules/provider/staff-invitation-config.ts): `HIMMA_STAFF_INVITATION_*` (TTL default 7 days; pepper fails closed in production).
- **Recorded S3-2 notes:** memberships have no expiry state (approved §5 model — time-bounded validity is invitation-side) · membership-revocation service + `org.staff_revoked` emission belong to S3-3 routes (last-owner protection is DB-enforced now) · acceptance/issue refusal on suspended orgs is a service-level application of §7.5 ahead of the S3-3 route policy.

**Structural-secret-guard rule (owner correction, 2026-08-07 — applied after conditional approval of `3bcb37d`):** the S3-2 commit had exempted `*_digest` columns generally from the two Slice-2 structural secret sweeps; the owner replaced that with a **binding clarified rule**: exemption is ONLY by explicit table-qualified allowlist entry in [test/helpers/structural-secrets.ts](backend/test/helpers/structural-secrets.ts) (`SANCTIONED_VERIFIER_COLUMNS`: `audit_event.before_digest/after_digest`, `idempotency_key.request_digest`, `bootstrap_seal.manifest_digest`, `login_session.ip_digest`, `mfa_recovery_code.code_hash`, `staff_invitation.token_digest`). A `_digest`/`_hash` suffix by itself exempts NOTHING: some one-way verifier digests are deliberately stored, but Cognito-managed credentials/tokens/TOTP secrets must never exist in Himma PostgreSQL raw OR hashed — `refresh_token[_digest]`, `password[_hash|_digest]`, `totp_secret[_digest]`, `credential[_digest]`, and provider challenge/session material stay offenders wherever they appear, and an allowlisted column name on any other table stays flagged. [structural-secret-guard.test.ts](backend/test/structural-secret-guard.test.ts) proves: sanctioned columns pass · the four prohibited digest synthetics fail · table-qualification holds (`secret_guard_probe.token_digest` fails) · every allowlist entry must exist in the live schema, so removing/renaming an approved column fails loudly instead of leaving a dormant exemption. Future migrations adding a reviewed digest column must extend the allowlist explicitly (owner-reviewed), never the pattern.

**Verification:** S3-2 run (2026-08-07): tsc clean · ESLint clean · Jest **451/451 across 37 suites** (19 new [staff-schema](backend/test/staff-schema.test.ts) + 22 new [staff-invitations](backend/test/staff-invitations.test.ts): role vocabulary · membership invariants/immutability · cross-org scope refusals · scope unambiguity · last-owner incl. concurrency · invitation constraints/transitions · D-S3-1 matrix incl. private-relay link+verify · single-use/concurrent-acceptance races · idempotent revoke/sweep · atomicity rollbacks · post-commit mail · raw-token hygiene · public-data untouched · least-privilege) · migrate-from-zero (6 migrations) · 0006 rollback via the migrations down-test · `db:verify` (0006 SCHEMA_CHECKS) · codegen 3 new interfaces, zero drift. Guard-correction run: focused structural suites green · tsc clean · ESLint clean · full Jest **454/454 across 38 suites** · no schema change (no migration/codegen/frontend/native work).

**S3-3 entry criteria (do not start without ALL):** explicit owner approval of the guard correction · scope per docs/27 §16 row S3-3 — `provider` route-policy category with the D-S3-5 MFA baseline + higher-risk step-up composition, `orgScope` PostgreSQL resolution, typed capability registry, §13.2 management routes, typed outcomes, rate limits · acceptance = docs/27 §14.3–4 + §14.8 matrix incl. D-S3-5 rows + policy snapshot · full backend suite green → stop for owner review before S3-4. *(Met: S3-2 + correction approved; S3-3 is the section below.)*

## Backend Slice 3 · S3-3 (2026-08-07 — provider principal, capability registry, and provider-private management routes; **FULLY APPROVED at `051d77c` 2026-08-07**)

No migration — S3-3 is application/HTTP layer only over the approved S3-1/S3-2 schema (schema untouched; codegen zero drift).

- **Capability registry** ([provider-capabilities.ts](backend/src/modules/provider/provider-capabilities.ts)): ACTIVE Slice-3 vocabulary (`org.read` · `org.legal.view` · `commercial_terms.view` · `profile.edit` · `branch.create/edit/deactivate` · `staff.read` · `staff.manage` · `org.submit`) strictly disjoint from the RESERVED docs/27 §6 future vocabulary (listings/schedules/sessions/capacity/offers/media/bookings/reports/attendance/roster/statements/payouts/refund-reports/payout-bank-details + bulk import). Role→active map per §6 (owner = everything incl. staff + submit + commercial-terms sight; org_manager = profile + branches; branch_manager = assigned-branch edit; listings_editor/front_desk/finance = org read; coach = minimized org read). Reserved capabilities are typed separately, never resolved into an orgScope, and the route-registration guard REFUSES a route declaring one — a reserved capability structurally cannot become executable.
- **Provider principal / orgScope** ([provider-principal.ts](backend/src/modules/provider/services/provider-principal.ts)): `RequestPrincipal.orgScope` is now `{ organizationId, membershipId, role, capabilities, branchScope: 'all'|branchIds[], organizationState }`, resolved FRESH from PostgreSQL per request for the ONE addressed `/provider/organizations/:organizationId` (docs/27 §8; nothing org-shaped in tokens per D1/A1.1). No membership, unknown org, and offboarded org collapse into one not-found shape (enumeration posture); Cognito groups/claims-shaped token material is inert (tested); revocation/suspension/role-replacement bite on the next request. `branchScope` lists assigned ACTIVE branches only — deactivation removes reach, empty never falls back to org-wide. `GET /provider/me` lists the caller's memberships (org switcher source).
- **Policies** ([policies.ts](backend/src/modules/identity/http/policies.ts) + [auth-plugin.ts](backend/src/modules/identity/http/auth-plugin.ts)): new `provider` and `providerStepUp` categories in the B2-4 deny-by-default registry. Binding order: live session → fresh membership resolution (404-shaped) → **D-S3-5 MFA baseline** (Himma enrollment + MFA-verified session factor: MFA login or live TOTP/recovery-code grant; `mfaRequired` otherwise) → recent step-up on `providerStepUp` (Slice-2 window; grant or fresh MFA login; `stepUpRequired` when stale) → declared ACTIVE capability (`forbidden` = right org, insufficient role/scope) → suspended-org mutation refusal (`organizationSuspended`; reads still work; offboarded = terminal 404). Provider routes must declare `providerCapability` or registration aborts; a capability on a non-provider policy also aborts. Admin and provider stay disjoint authorities (tested both ways).
- **Routes** ([provider-routes.ts](backend/src/modules/provider/http/provider-routes.ts)) — exactly the docs/27 §13.2 inventory: `GET /provider/me` (authenticatedCustomer) · `GET /provider/organizations/:orgId` (org.read; capability-shaped view — owner sees legal+commercial, managers legal only, coach neither) · `PATCH .../profile` (profile.edit, CAS; publish/unpublish audit vocabulary) · `POST .../submit` (org.submit; draft|rejected→submitted with §10 completeness = display name + ≥1 active branch; typed `organizationIncomplete`/`lifecycleConflict`) · `POST .../branches` + `PATCH .../branches/:id` + `POST .../branches/:id/deactivate` (branch.create/edit/deactivate, branch-scope-checked, CAS, idempotent deactivate) · `GET .../staff` (staff.read; memberships incl. history + invitations) · `POST .../staff/invitations` + `POST .../staff/invitations/:id/revoke` + `POST .../staff/memberships/:id/revoke` (providerStepUp + staff.manage — the D-S3-5 higher-risk set; role/scope change = revoke + re-invite, no owner-transfer flow) · `POST /provider/invitations/accept` (authenticatedCustomer; S3-2 D-S3-1 service). Explicit TypeBox schemas everywhere; no generic all-columns endpoint; last-owner trigger refusals translate to typed `lastOwnerProtected` with no constraint/trigger names in any payload.
- **New typed outcomes:** `organizationSuspended` (403) · `lastOwnerProtected` (409) · `invitationInvalid` (400) · `organizationIncomplete` (409) · `lifecycleConflict` (409) · `invalidBranchScope` (422), joining the existing vocabulary (forbidden/notFound/staleVersion/mfaRequired/stepUpRequired/rateLimited).
- **Rate limits** (existing store abstraction; in-memory dev/test, production store still fail-closed): `staffInvitation` 10/min and `staffManagement` 20/min per issuer+organization digest, `invitationAccept` 10/min per user.
- **Production gate (no bypass):** the provider surface shares the identity MFA-activation capability gate — in production it registers ONLY when the B2-6C `AdminProductionReadiness` capabilities (Cognito integration, MFA provider validation, real-pool SOFTWARE_TOKEN_MFA smoke, approved production config) ALL report ready; forcing `enableProviderRoutes` while unready refuses startup loudly (tested). Dev/test exercise full behavior through deterministic adapters.

**Verification (all green, 2026-08-07):** backend tsc clean · ESLint clean · backend Jest **479/479 across 40 suites** (12 new [provider-policy](backend/test/provider-policy.test.ts): registry disjointness · registration guards (missing/reserved/misplaced capability) · PostgreSQL-only authority incl. inert Cognito claims + byte-identical 404s · immediate revocation/suspension effect · per-org multi-membership resolution · offboarded terminality · the full D-S3-5 assurance ladder (no-enrollment/single-factor → mfaRequired; stale → stepUpRequired on higher-risk; grant or fresh MFA login → pass) · session-revocation-overrides-grant + membership-revocation-overrides-assurance · admin/provider separation · production fail-closed gate; 13 new [provider-routes](backend/test/provider-routes.test.ts): exact 7-role × 8-surface capability matrix · capability-shaped projection · org isolation with no-oracle probes · cross-org branch ids refused · branch-scope matrix incl. deactivation-removes-reach · last-owner over HTTP with sanitized refusal · role/scope replacement preserving history · idempotent invitation revoke + dead-token accept · CAS/staleVersion + publish audit vocabulary · submission completeness/lifecycle · malformed-input 422s · no-store + raw-token-free responses · per-issuer rate limiting) · route-policy inventory snapshot intentionally updated (+16 provider entries) · migrate-from-zero + `db:verify` green · codegen zero drift · all S3-1/S3-2 and Slice-1/Slice-2 suites green unchanged.

**Recorded S3-3 notes:** the private org view realizes coach data-minimization as capability-shaped response fields (`org.legal.view`/`commercial_terms.view`) rather than a separate route — one §13.2 route, no field-switching endpoint · pending-invitation emails appear ONLY in the owner-gated staff list (org-own data; never in events) · the §13.2 offboarding-request route stays future work with its lifecycle owner (S3-4/W3) · repeated cross-org attempts share the global invalid-bearer limiter; no dedicated cross-org limiter was approved, so none was invented.

**S3-4 entry criteria (do not start without ALL):** explicit owner approval of S3-3 · scope per docs/27 §16 row S3-4 — §13.3 admin org-lifecycle routes on the existing `admin` policy (create org + founding Owner invitation; verification edges; go-live/suspend/reinstate/offboard) with the D-S3-3 `verificationEvidenceCapabilityReady` fail-closed production gate on verify/go-live, plus the §13.1 customer-public storefront read (`live` AND `published` visibility, structural projection lock) · acceptance = docs/27 §14.7 suspension matrix + §14.9/9b projection and safeguard tests · full backend suite green → stop for owner review before S3-5. *(Met: S3-3 approved at `051d77c`; S3-4 is the section below.)*

## Backend Slice 3 · S3-4 (2026-08-07 — Himma-admin organization lifecycle + customer-public storefront read; **FULLY APPROVED at `4ff461a` 2026-08-07**)

No migration — S3-4 is application/HTTP layer only over the approved S3-1/S3-2 schema (codegen zero drift). Two new surfaces, strictly separated from the S3-3 provider-private surface.

- **Admin creation** ([organization-admin.ts](backend/src/modules/provider/services/organization-admin.ts) + [organization-admin-routes.ts](backend/src/modules/provider/http/organization-admin-routes.ts)): `POST /admin/organizations` (existing `admin` policy — live session + MFA + recent factor — with the `operations` role re-resolved fresh inside the transaction; other admin roles, providers, and customers refused) creates draft organization + public-profile shell (display name defaults from trade name) + the FOUNDING Owner invitation in ONE transaction via the S3-2 invitation model (shared `prepareInvitationToken`/`persistInvitationInTrx`/`composeInvitationMail` helpers — one code path with S3-3 issuance). Raw token exists only in memory + the post-commit mail; forced failure rolls back org/profile/invitation/audit/outbox together and sends nothing; mail failure never rolls back and reports `mailDelivery: 'failed'`; no placeholder User is created for an unknown invitee.
- **Lifecycle administration**: `POST /admin/organizations/:orgId/verification/(start-review|verify|reject)` and `/(go-live|suspend|reinstate|offboard)` — exactly the §5.1 admin edges over the untouched S3-1 trigger (services pre-check into typed outcomes; the trigger stays the authority). Every transition: `operations`-only, CAS on version (`staleVersion`), wrong state → `lifecycleConflict`, unknown org → `notFound`, audit + outbox atomic with the change (`org.review_started/verified/rejected/went_live/suspended/reinstated/offboarded` ↔ `organization.*`), suspended_at/offboarded_at maintained per the state-tie CHECKs, optional bounded `reasonCode` slug (pattern-validated; outbox payload only — free text refused, private notes impossible). Offboarding rides the admin policy's step-up-recency baseline; downstream booking/refund/payout consequences are recorded as FUTURE consumers of the lifecycle events, not invented now.
- **D-S3-3 verification-evidence gate (binding, no bypass):** `verificationEvidenceCapabilityReady` defaults FALSE with no env shortcut; in production `verify` and `go-live` fail closed (typed 503 `verificationEvidenceUnavailable` + audited attempt `org.verification_gate_refused`, org state unmoved) — and because Slice 3 ships NO VerificationCase/document-review implementation, a production build claiming readiness REFUSES STARTUP (build-app throws; the flag becomes honestly settable only when the admin workstream lands the real capability). Dev/test exercise the full lifecycle deterministically; ungated edges (start-review, reject, suspend…) work in production; admin roles grant no bypass (tested with a fully-activated production admin surface).
- **Customer-public storefront** ([storefront-read.ts](backend/src/modules/provider/services/storefront-read.ts) + [storefront-routes.ts](backend/src/modules/provider/http/storefront-routes.ts)): `GET /providers/:organizationId` (explicit `public` policy; no auth of any kind) serving the §13.1 projection ONLY — display name, en/ar descriptions, media refs, derived `verified` badge (live ⇒ true, never stored), the four explicit public contacts, and ACTIVE branches (id/label/addressLine/areaLabel/geo/openingHours/facilities) in stable creation (UUIDv7 id) order. Effective visibility = `verification_state='live' AND published=true`; draft/submitted/in_review/verified-not-live/rejected/suspended/offboarded/unpublished/nonexistent are byte-identical 404s. **Structural projection lock (§14.9):** `PUBLIC_STOREFRONT_SOURCES` is a closed, test-asserted column list (organization contributes id + eligibility state only; `published`/`active` are predicates, never serialized); no `select *` exists; the test adds a scratch private column with a sentinel value and proves it cannot appear — widening the projection is an owner-visible diff. Listings/ratings deliberately ABSENT (no fake placeholder arrays): Slice 4 composes its listing read models WITH this contract. Conservative HTTP semantics: schema-rejected malformed ids, nosniff, no invented cache/CDN contract (storefront cache headers stay a docs/27 §17 configurable for later).
- **Wiring:** admin org routes register inside the existing admin-surface production capability gate; the public storefront registers with the identity surface everywhere (no MFA dependency). New typed outcome: `verificationEvidenceUnavailable` (503).

**Verification (all green, 2026-08-07):** backend tsc clean · ESLint clean · backend Jest **493/493 across 42 suites** (7 new [organization-admin](backend/test/organization-admin.test.ts): atomic creation + digest-only + no-placeholder-user + post-commit mail · operations-only authority incl. access_admin/provider/customer refusals · forced-failure full rollback with sanitized 500 · mail-failure persistence · full lifecycle walk with CAS/events/terminal refusal · invalid/stale/unknown typed outcomes · reason-code slug + free-text refusal + resubmission edge · suspension biting provider mutations + storefront with reinstatement restore and intact history · the D-S3-3 matrix (startup refusal on claimed readiness; production verify AND go-live 503 + audited attempt with state unmoved on a FULLY-activated admin surface; ungated edges still work); 7 new [storefront-public](backend/test/storefront-public.test.ts): live+published serves, all 8 ineligible shapes byte-identical 404 · no-auth + garbage-bearer-ignored + malformed-id 422 + conservative headers · exact projection key sets + active-branch filtering/ordering + no inactive leak · private/staff/invitation/security data absent under a forbidden-pattern scan · structural source-list lock + scratch-private-column leak proof) · two S3-3 route-filter assertions intentionally updated for the new public `/providers/…` surface (`/provider/` prefix) · route-policy snapshot intentionally updated (+10 entries: 8 admin org routes + public storefront GET/HEAD) · migrate-from-zero + `db:verify` green · codegen zero drift · all S3-1…S3-3 and Slice-1/Slice-2 suites green.

**Recorded S3-4 realization notes:** docs/27 requires an audited refused attempt for the evidence gate without naming the action — `org.verification_gate_refused` was chosen in the `org.*` namespace · `reasonCode` is a bounded slug in outbox payloads only (docs/27 fixes no reason model; free text is structurally refused so private notes cannot enter events) · the §13.1 branch contract excludes `city` (not in the approved field list) · production storefront registration is ungated by design (public read, no MFA dependency).

**S3-5 entry criteria (do not start without ALL):** explicit owner approval of S3-4 · scope per docs/27 §16 row S3-5 — bounded isolation/authz hardening review incl. the §7.8 RLS evaluation item, docs/27 §15-style status section, HANDOFF closeout · acceptance = full Slice-3 certification (backend tsc/lint/full jest, migrate-from-zero, `db:verify`, codegen drift, root non-regression) → milestone report, stop before Slice 4. *(Met: S3-4 approved at `4ff461a`; S3-5 is the section below.)*

## Backend Slice 4 · taxonomy administration (2026-08-07 — internal `/admin/taxonomy` management; this commit, awaiting owner review)

**Scope delivered:** production internal-admin administration of the four S4-1 taxonomy entities (`area`, `category`, `activity_type`, `collection`) per docs/28 §16.3 — DATA management only; the database stays authoritative, no schema change was needed (migrations 0007–0009 untouched; `db:verify` checksums green). New [taxonomy-admin.ts](backend/src/modules/catalogue/services/taxonomy-admin.ts) + [admin-taxonomy-routes.ts](backend/src/modules/catalogue/http/admin-taxonomy-routes.ts).

**Nine routes (all `admin` policy + fresh per-transaction `operations` role — same shared check as moderation; snapshot intentionally +10 lines incl. HEAD):** `GET /admin/taxonomy` (full administration view incl. inactive rows) · `POST|PATCH /admin/taxonomy/areas[/:areaId]` · `…/categories[/:categoryId]` · `…/activity-types[/:activityTypeId]` · `…/collections[/:collectionId]`. Support/finance/auditor/access-admin, provider bearers, customer bearers, Cognito claims, and anonymous callers are all refused; revocation bites immediately.

**Semantics (canon-exact, nothing invented):** slugs immutable (structurally inexpressible in PATCH bodies + the 0007 trigger; unique violations surface as typed `slugConflict`) · English labels required, Arabic always optional (D-S4-3) · deterministic ordering via `sort_hint` · **deactivation, never deletion** — proven reference safety: deactivating an in-use activity type/category/area leaves `program.activity_type_id`, `branch.area_id`, program state, and branch rows byte-untouched (no cascade, no rewrite, no unpublish), while S4-2 provider validation immediately refuses the deactivated type for NEW listings; reactivation is a plain `active=true` (canon's active/inactive flag is not a terminal machine) · activity-type creation requires a real parent category (typed `invalidTaxonomy`); **re-parenting is deliberately not an operation** (canon defines none) · collection editorial fields incl. the typed preset booleans and the `draft|published|archived` state value set (the 0007 CHECK; canon defines no transition edges, so none were invented) · CAS `expectedVersion` on every PATCH; refused mutations emit nothing. Audit + outbox in the same transaction: `taxonomy.area_changed|category_changed|activity_type_changed|collection_changed` (aggregate `taxonomy`; payload = entity id + bounded `created|updated` slug). Seed integrity proven: the 11 seeded categories + 2 entry-12 lens collections remain exact and duplicate-free under administration.

**Recorded, not invented:** the future search projection (docs/28 §13) will consume the `taxonomy.*_changed` outbox events for document refresh — no search infrastructure exists yet · area launch content remains admin-created data (no approved seed list; the S4-1 note stands) · **RLS reconsideration: assumptions unchanged** — taxonomy is admin-owned data with no per-organization isolation dimension (exactly the docs/28 §20 analysis), so the documented deferral and its triggers stand untouched.

**Certification:** tsc clean · ESLint clean · **Jest 51 suites / 619 tests green** (new [catalogue-taxonomy-admin.test.ts](backend/test/catalogue-taxonomy-admin.test.ts): 14 tests; two prior boundary assertions amended for the legitimately arrived `/admin/taxonomy` surface) · migrate-from-zero · `db:verify` · codegen zero drift · route inventory exact-matched.

**Remaining Slice-4 responsibilities (unstarted; owner approval required):** customer-public listing detail + storefront-listings + catalogue/taxonomy public reads (§16.1/§19) · search foundation (`program_search_document`, FTS/trigram, `SearchReadPort`, §11–§13) · S4-6 hardening/closeout.

## Backend Slice 4 · moderation (2026-08-07 — internal catalogue moderation + ProgramRevision decisions; **CLOSED: fully approved at `fecb8d0`**)

**Scope delivered:** the internal Himma review path `provider submit → review → approved | changes_requested → provider publication` and the admin decision path for provider-submitted `program_revision` records, on the closed S4-1 schema and S4-2 services. New [moderation.ts](backend/src/modules/catalogue/services/moderation.ts) + [admin-moderation-routes.ts](backend/src/modules/catalogue/http/admin-moderation-routes.ts); no new tables, states, capabilities, or role semantics — authority is the EXISTING `admin` policy (B2-6C: live session + MFA assurance + recent factor + ≥1 active DB admin role) with the `operations` role re-checked fresh inside every transaction (docs/24 §10.2; docs/28 §6/§16.3). Support/Finance/Access-Admin/Auditor, provider bearers, and customer bearers are all refused; revoked admin authority bites on the next request; Cognito claims stay inert.

**Nine named-action routes (docs/28 §16.3; all `admin` policy; route-policy snapshot intentionally +12 lines incl. HEAD):** `GET /admin/listings` (review queue by state, paginated) · `GET /admin/listings/:programId` (moderation projection: full ProgramDetailView + organization identity/state + FULL structured open-revision change set; explicit TypeBox contract shared with the provider surface) · `POST …/review/start|approve|request-changes` (submitted→in_review→approved|changes_requested; bounded `reasonCode` slug only, never free-text notes) · `GET /admin/revisions` (revision queue) · `POST …/revisions/:revisionId/review/start|approve|reject`. **D-S4-2 holds: approval RESTS at `approved`** — publication remains the provider `listings.publish` action; no writable state field exists anywhere.

**Revision decisions (docs/28 §7):** machine `submitted → in_review → approved | rejected`, decided rows frozen. Approval applies the structured change set ATOMICALLY in the decision transaction: eligibility/safety-copy fields onto the program, the option change onto its stable target (or a NEW option id for an add-intent), `sensitive_fields_version` bumped exactly once — with `listing_state` untouched (published stays published; approved stays approved). All typed-refusal prechecks run BEFORE any mutation (merged-eligibility and merged-option-shape validation, archived-listing and archived-target-option conflicts), so a refusal can never commit a partial application; the decision CAS is the last fallible step. Stale drift (e.g. an out-of-band-archived target option) refuses with `lifecycleConflict`, leaves the revision in_review for rejection, and archived options can never be edited or resurrected. Double decisions fail with typed conflicts (one event, one `sensitive_fields_version` bump — proven). Events (same transaction, ids/safe slugs only): `listing.review_started/approved/changes_requested` · `listing.revision_approved/rejected` · reconciliation note: `listing.revision_review_started` added as the smallest consistent namespace entry for the review-start state change docs/28 §15 names no event for. Second reconciliation note (schema semantics): a NULL revision change column means "unchanged" — clearing a sensitive field to NULL is not representable in the approved §9.10 structure (a future owner/schema decision if ever needed).

**Certification:** tsc clean · ESLint clean · **Jest 50 suites / 605 tests green** (new [catalogue-moderation.test.ts](backend/test/catalogue-moderation.test.ts): 18 tests covering the full §18-style matrix — role matrix, surface separation, review machine walks, approval-never-publishes, CAS races, double-decision, stale-option refusal, ownership immutability, payload hygiene, exact route inventory; two S4-2 boundary assertions amended because moderation legitimately arrived) · migrate-from-zero · `db:verify` · codegen zero drift (no schema change).

**Remaining Slice-4 responsibilities (unstarted; owner approval required):** internal taxonomy administration (§16.3 taxonomy CRUD) · customer-public listing detail + storefront-listings + catalogue reads (§16.1/§19) · search foundation (`program_search_document`, FTS/trigram, `SearchReadPort`, §11–§13) · S4-6 closeout.

## Backend Slice 4 · S4-2 (2026-08-07 — provider catalogue services, capability activation, and §16.2 provider-private routes; **CLOSED: fully approved at `94a692c`**)

**Capability activation (docs/28 §1.5/§6; D-S4-2):** the S3-3 registry gains four ACTIVE capabilities — `catalogue.read` (owner, org_manager, branch_manager, listings_editor) · `listings.manage` (same four; branch_manager service-enforced within branch scope; also carries offer + branch-association + price-option management — `offers.manage` deliberately stays RESERVED because docs/28 §1.5 activates exactly these four) · `listings.publish` (**Owner + Organization Manager ONLY**) · `media.manage` (the listings.manage set). Coach, Front Desk, and Finance gain nothing (Finance acquires no access from monetary catalogue metadata). Exact-matrix test: [catalogue-capabilities.test.ts](backend/test/catalogue-capabilities.test.ts).

**New `catalogue` module** ([services](backend/src/modules/catalogue/services/catalogue-shared.ts) + [routes](backend/src/modules/catalogue/http/catalogue-routes.ts)): program create/list/detail/edit + named lifecycle actions (submit/publish/pause/archive — publication is NEVER a state patch; PATCH bodies are additionalProperties-stripped so a smuggled `listingState` is inexpressible), price-option add/edit/reorder/archive (stable ids, archive-only, typed `invalidPriceOption` prechecks over the S4-1 CHECK ties), branch associations (active same-org branches only; reactivating a historical row preserves history), media references (metadata only), offers (structured; Offer ≠ ProgramPriceOption). **Sensitive-edit routing (docs/28 §7):** on approved/published/paused listings, eligibility/safety-copy patches and EVERY price-option change create a `program_revision` (submitted, one open per listing → `revisionPending`) without touching live fields; mixed patches apply non-sensitive fields and defer sensitive ones; submitted/in_review listings refuse provider edits; drafts edit directly. **Completeness** is structured (`programIncomplete` + `missing: title|activeTaxonomy|activeBranch|activePriceOption`) at submit AND publish; publish additionally requires org `live` (`organizationNotLive`). Branch-scoped staff mutate a listing only while every active association is in their scope, associate only their own branches, and list only reachable listings. All 19 routes declare `provider` policy + capability (MFA baseline; no step-up — catalogue editing is not in the D-S3-5 higher-risk set per docs/28 §16.2); the production surface shares the provider fail-closed MFA gate in [build-app.ts](backend/src/app/build-app.ts). Audit/outbox (same transaction, ids-only payloads): `listing.created/updated/submitted/published/paused/archived/revision_submitted/branch_association_changed/media_changed/price_option_changed/price_option_archived` + `offer.created/updated/ended`. New HTTP vocabulary: `organizationNotLive`, `revisionPending`, `invalidTaxonomy`, `invalidEligibility`, `invalidPriceOption`, `invalidOffer`, `programIncomplete` (with `missing`).

**Certification:** backend tsc clean · ESLint clean · **Jest 49 suites / 587 tests green** (suites: [catalogue-services.test.ts](backend/test/catalogue-services.test.ts), [catalogue-routes.test.ts](backend/test/catalogue-routes.test.ts) — role×route matrix, cross-org byte-identical 404s, Cognito-claims inert, revocation/suspension bite, no revision-decision/moderation/taxonomy-admin route exists, response-hygiene key lock) · migrate-from-zero · `db:verify` · codegen zero drift (no schema change) · route-policy snapshot intentionally updated (+19 routes / 21 lines incl. HEAD, all `provider`).

**Next Slice-4 task entry criteria (do not start without explicit owner approval):** remaining docs/28 responsibilities in order — internal taxonomy administration (§16.3 taxonomy CRUD, deferred from the original S4-1 row) · admin listing review + revision decisions (§16.3) · customer-public listing detail/storefront-listings/catalogue reads (§16.1/§19) · search foundation (`program_search_document`, FTS/trigram indexes, `SearchReadPort`, §11–§13) · S4-6 closeout. docs/28 header carries the same status mapping.

## Backend Slice 4 · S4-1 (2026-08-07 — catalogue/taxonomy + Program schema foundation; **S4-1 CLOSED: fully approved at `d98eb69` + gender-eligibility correction `3ef57e7`**)

**S4-1 delivered (owner-directed re-scope: the FULL database-schema foundation, zero services/routes):** migration [0007_taxonomy.sql](backend/migrations/0007_taxonomy.sql) — `area` (+ the additive `branch.area_id` FK; `area_label` stays denormalized pending reconciliation), `category`, `activity_type` (synonyms arrays), `collection` (typed preset booleans), immutable slugs, deterministic docs/15 §3 seed (11 categories in order + entry-12 lenses "Kids & Teens"/"Camps & seasonal" as published collections, fixed UUID literals, English-only) — and migration [0008_catalogue.sql](backend/migrations/0008_catalogue.sql) — `program` (docs/24 §5.3 machine trigger-enforced; D-S4-2 approval≠publication proven; archived terminal/frozen; ownership immutable; NO price/schedule/rating column), `program_price_option` (D-S4-1: stable opaque ids, kind CHECK `dropIn|monthly|term|camp|package|free`, integer-fils + AED domains, free/package ties, `(sort_hint,id)` ordering, archive-only frozen retirement, CAS), `program_branch` (composite FKs BOTH sides on the S3-1 spine — cross-org association impossible in raw SQL), `program_media` (references only), `offer` (structured kinds, paid-trial amount tie), `program_revision` (structured sensitive-field change sets incl. option change set with same-program FK, one open revision per program, decided rows frozen). Deliberately EXCLUDED per the task boundary: `program_search_document` + all search indexes, admin taxonomy services/routes (deferred from docs/28 §17's S4-1 row), every service/route/moderation/public-read/session/capacity/booking/payment object. Grants: SELECT/INSERT/UPDATE only, no DELETE, trigger-bypass denied (all tested). Suites: [taxonomy-schema.test.ts](backend/test/taxonomy-schema.test.ts) · [program-schema.test.ts](backend/test/program-schema.test.ts) · [price-option-schema.test.ts](backend/test/price-option-schema.test.ts); the S3-1 schema-inventory guard was narrowed to still-future entities (only pre-existing test touched). Certification at `d98eb69`: tsc/lint clean · Jest 46 suites/547 green · migrate-from-zero · two-step rollback (0008→0007) + reapply · `db:verify` · codegen zero drift.

**S4-1 owner correction (2026-08-07, this commit): five-value gender eligibility.** The shipped `men|ladies|mixed` CHECK is superseded — the binding product/filter model requires DISTINCT values **`women|men|girls|boys|mixed`** (`women` = canonical stored code; "Ladies only" is presentation wording; `girls`/`boys` are explicit provider-declared eligibility, NEVER inferred from `min_age`/`max_age`, which stay independent). Because 0008 is checksum-immutable, the fix is the corrective migration [0009_gender_eligibility_correction.sql](backend/migrations/0009_gender_eligibility_correction.sql): recodes any dev/test `ladies` rows to `women` (user triggers suspended for the mechanical recode only — a privilege `himma_app` provably lacks), then replaces the two CHECKs (`program.gender_eligibility`, `program_revision.gender_eligibility`); reviewed lossy Down (dev/test only: `women`→`ladies`, `girls|boys`→`mixed`). Real-PostgreSQL proofs added in program-schema.test.ts: all five values succeed on program AND revision; legacy `ladies` and arbitrary values rejected; girls/boys never age-inferred or rewritten on age edits; English-only creation, lifecycle, option, and cross-org suites unchanged. Canon aligned: docs/24 **Amendment A3** (§2.5, §12.1) + docs/28 §9.6/§10 corrected in place; the mock's three-value `GenderEligibility` stays a presentation-era conforming contract mapping `ladies→women` at graduation (customer app untouched — no frontend/native recertification required).

**S4-2 entry criteria (do not start without ALL):** explicit owner approval of this correction · scope = the remaining docs/28 §17 material re-scoped after S4-1: admin taxonomy services/routes, capability activation (`listings.manage/publish`, `media.manage`, `catalogue.read`), provider listing/price-option/media/offer/branch-association services + §16.2 routes with the §7 revision flow, `program_search_document` arriving only with its owning search commit · docs/23 §16 phase gates honored.

## Backend Slice 4 · specification (2026-08-07 — `docs/28` approved in structure at `6b60fb2`; Amendment A1 applied at `b016c85`, **fully approved and binding**)

### docs/28 Amendment A1 (2026-08-07 — the owner's D-S4-1…3 rulings, applied in place; docs/24 Amendment A2 in the same commit)

- **D-S4-1 DECIDED — multi-option listings:** one canonical Program may carry multiple commercial **ProgramPriceOption** children (Program = discoverable concept; option = commercial way to purchase it: `Adult Beginner Jiu-Jitsu` trial/monthly/3-months · `Gym Access` monthly/3-month/annual · `Swedish Massage` 60/90-min/5-session package). Binding modelling rule: options never collapse materially different experiences (adult vs children, private vs group, different activities/content/search intent = separate listings). Options are catalogue/commercial metadata ONLY — never capacity/booking/entitlement/payment/attendance/redemption records; PriceQuote/checkout untouched. No authoritative single `Program.price`; the exact derived-field boundary (`From AED X` = min over active options; single-option direct display; id/kind/amount/label summaries for future option selection BY stable ID) is docs/28 §14. Option changes on published listings are sensitive-field revisions. **docs/24 Amendment A2** rewrites §2.2 (ProgramPriceOption entity row), §2.6, and §12.1 so no single-price wording survives in canon; the mock's single price maps to one option per listing.
- **D-S4-2 DECIDED — publication:** auto-publish-on-approval default OFF; approval ≠ publication; publish/unpublish = Owner + Organization Manager only; Listings Editor submits but never publishes; Branch Manager/Finance/Coach/Front Desk hold zero publication authority; no moderation bypass; parent-org visibility stays authoritative.
- **D-S4-3 DECIDED — taxonomy seed:** docs/15 §3 twelve entries as database-managed seed content (stable ids/slugs, deterministic ordering, active/inactive, English launch, localization-ready; Arabic never blocks); taxonomy changes are migration/admin-data, never frontend constants; no commercial behavior from taxonomy names.
- **Clarification (binding):** search results are one per LISTING across providers (`jiu-jitsu` → listings from many organizations, never one per membership duration), each retaining `Listing → Organization → Public Provider Storefront`.

**`docs/28_BACKEND_SLICE_4_CATALOGUE_LISTINGS_AND_SEARCH_PLAN.md`** — the owner-review specification for Catalogue, Listings/Offerings, Classification, and Search Foundation, per the docs/26/27 planning pattern. Documentation only; approval starts nothing by itself. Key shapes: the binding marketplace model (search at the listing level across providers; `Organization → Storefront → Branch → Listing (Program)` spine attached via `program.organization_id` + same-org `program_branch` composite FKs — the docs/27 §12.8-proven shape) · one catalogue model for gyms/academies/spas/kids-providers/camps through `activity_type` + price kinds + offers, never per-vertical schemas · the docs/24 §5.3 listing machine applied verbatim with a structured `program_revision` sensitive-field flow (published version stays live during review; no approval bypass exists structurally, so no extra capability gate is needed — analyzed in §8) · taxonomy/area/collection as admin-owned seeded DATA (docs/15 §3 twelve entries; slugs immutable; the S3-1 `branch.area_id` FK finally lands) · eligibility levels kept distinct (listing ≠ option ≠ session ≠ booking-time revalidation) · compound public visibility (`published` listing AND live+published org AND ≥1 active branch) applied as a query-time predicate so search/projections can never expose a suspended provider even with a stale document · **search recommendation: staged, PostgreSQL-native at launch** (FTS + pg_trgm + taxonomy synonyms over a rebuildable `program_search_document`, behind a `SearchReadPort`; docs/23 §18.20 engine-tier decision left open) · storefront-listings served by a dedicated paginated public endpoint (S3-4 projection untouched) · capability ACTIVATION (`listings.manage/publish`, `media.manage`, `catalogue.read`) on the existing S3-3 registry · **RLS: continue deferring** (re-evaluated per the S3-5 trigger; §20 records the next triggers) · proposed migrations 0007 (taxonomy) + 0008 (catalogue) with full DDL-review detail · six-commit plan S4-1…S4-6, each stop-for-approval · mock→production mapping confirmed against the approved docs/24 §12 (no customer-page redesign; schedule-derived fields explicitly pending Slice 6; frontend NOT connected in Slice 4).

**§22 owner decisions — BLOCKING Slice-4 schema:** **D-S4-1** listing↔priced-option multiplicity (recommend: additive `program_price_option` child ruled now; canon-as-is deferral is the safe fallback — the schema seam keeps either additive) · **D-S4-2** publication act (recommend: auto-publish-on-approval default OFF; `listings.publish` = owner + org_manager) · **D-S4-3** ratify the docs/15 §3 taxonomy seed. Can-wait items recorded (membership/weekly kinds §14.B7, private/group + language metadata, online/branchless listings, engine tier, Arabic workflow, reviews, ranking, media upload infra).

## Backend Slice 3 · S3-5 (2026-08-07 — hardening review, RLS evaluation, and Slice-3 closeout; **FULLY APPROVED at `799804a` 2026-08-07**) — **SLICE 3 CLOSED**

**Hardening review outcome: ZERO confirmed defects.** A full audit of every Slice-3 query site (all 47 `selectFrom`/`insertInto`/`updateTable` sites in the provider module) confirmed: every by-id write happens only after an org-scoped, row-locked confirmation in the same transaction; the only deliberately global queries are the invitation token-digest lookup (the token is the secret) and the system expiry sweep; no repository query drops its organization predicate. Three previously untested paths gained negative proofs in [slice3-hardening.test.ts](backend/test/slice3-hardening.test.ts) (5 tests): foreign membership/invitation ids inside A's own step-up revoke addressing are byte-identical to ghost ids with B untouched · a fully step-up-assured Owner of A stays capability-refused in B (assurance composes with, never replaces, per-org authority) · `himma_app` cannot DELETE from any Slice-3 table or the audit/outbox history, cannot DISABLE/DROP any lifecycle trigger or ALTER any Slice-3 table (owner-only DDL), and its grant set is asserted verbatim (SELECT/INSERT/UPDATE; scope rows SELECT/INSERT only). All other closeout checklist rows (isolation matrix, projection lock, seven-role capability matrix, D-S3-5 MFA/step-up, invitation/staff races and hygiene, lifecycle + D-S3-3 gate, event atomicity, §12.8 forward-compatibility, English-only launch) were re-certified by the existing S3-1…S3-4 suites in the full run below — no redesign was needed anywhere.

**§7.8 RLS evaluation — DEFERRED (recorded in docs/27 §18 with full rationale).** One pooled `himma_app` principal ⇒ per-org policies would hinge on transaction-local context GUCs with pooling-bleed/forgotten-context failure modes; the intrinsic cross-org paths (sweep, admin lifecycle, acceptance-before-context, public reads, `/provider/me`) would need bypasses reducing the policies to decoration over an already quadruple-guarded slice (composite FKs · org-scoped predicates · per-request pipeline resolution · negative suites). No partial/decorative policies added. **Reconsideration triggers:** Slice-4 catalogue tables landing (natural point to introduce transaction-local org context) · any second DB principal or direct-SQL/reporting access · provider-portal production GA.

**Full Slice-3 certification (2026-08-07, all green):** backend tsc clean · backend ESLint clean · backend Jest **498/498 across 43 suites** · migrate-from-zero (every test DB) · explicit `db:down` of 0006 + verify-detects-pending + reapply + verify green · `db:verify` green · Kysely codegen zero drift · route-policy snapshot current (38 routes incl. HEAD) · **root customer-app non-regression: tsc clean · expo lint clean · Jest 421/421 across 24 suites** (Slice 3 touched no customer UI; no native recertification required).

**Slice-3 commit record:** S3-1 `3b5d12f` · S3-2 `3bcb37d` + guard correction `8784ce0` · S3-3 `051d77c` · S3-4 `4ff461a` · S3-5 this commit. docs/27 §18 carries the same record with the RLS decision and pending-dependency register.

**Operational dependencies still pending (none block Slice 4):** production Cognito pool/region + real-pool SOFTWARE_TOKEN_MFA smoke (docs/26 §15 — production admin AND provider surfaces stay fail-closed) · VerificationCase/document-review capability (admin workstream — production verify/go-live stay fail-closed behind `verificationEvidenceCapabilityReady=false`) · production email delivery (D2) · production distributed rate-limit store (docs/23 §10.7). The customer app still runs on mock providers/catalogue until its integration milestone; provider portal (W2) and admin portal (W3) frontends are NOT built; no listings/catalogue exist.

**Slice 4 entry criteria (do not start without ALL):** explicit owner approval of this closeout · an owner-approved Slice-4 catalogue/listings specification (docs/26/27 planning pattern) built on the §12.8-proven spine (`Program.organization_id`, same-org `program_branch` composite FKs) and the docs/24 §12 / docs/27 §15 mock→production mapping · the RLS reconsideration carried as a planning input · docs/23 §16 phase gates honored.

## Backend Slice 3 · specification (2026-08-07 — docs/27 approved in structure at `6b5b6a3`; Amendment A1 `56c17e0`, **fully approved and binding**)

### docs/27 Amendment A1 (2026-08-07 — the owner's D-S3-1…5 rulings, applied in place)

- **D-S3-1 DECIDED:** invitation acceptance requires a normalized VERIFIED-email match on the accepting user's active identities — unverified emails, display names, and Cognito subject alone never accept; Apple private-relay users link + verify the invited address (existing Slice-2 flow) before accepting; all failures collapse into one `invitationInvalid` revealing no organization information; tokens stay single-use/CAS-concurrency-safe. Acceptance-test matrix extended accordingly (docs/27 §14.6).
- **D-S3-2 DECIDED:** organization creation is admin-initiated in Slice 3, with a new `organization.origin` seam column (`admin_created` today) so future provider self-registration (W2 portal spec) is purely additive — Organization/PublicProfile/Branch/StaffMembership need no redesign.
- **D-S3-3 DECIDED with a production safeguard:** Slice 3 owns lifecycle states, transition rules, and the audited admin transition API; `VerificationCase`/document-review/portal workflow stay with the admin workstream; **production `in_review → verified` and `verified → live` transitions fail closed behind a new `verificationEvidenceCapabilityReady` capability flag (B2-6C readiness pattern, default false, no bypass)** until the authoritative evidence/review capability exists — no production path can mark a provider verified/live without a real evidence record; dev/test deterministic. Storefront discoverability now requires `live` AND a new `organization_public_profile.published` flag; suspension/offboarding removes availability via the lifecycle.
- **D-S3-4 DECIDED:** no separate canonical Partner entity — Organization is the common entity; the `org_kind` classification/relationship seam absorbs later `provider`/`partner`/other distinctions additively; no differing partner authorization behavior without a later owner-approved requirement.
- **D-S3-5 DECIDED (revises the draft):** production provider-private management requires **MFA at baseline** (live session + active membership + valid org/branch scope + Slice-2 MFA enrollment/assurance — the same machinery as the admin surface), with **recent step-up additionally required** for staff invite/remove, role/scope changes, offboarding, ownership-sensitive changes (and later payout-bank-detail changes). Public storefront reads stay public. The `provider` route policy ships this in Slice 3; commit plan, §14 test matrix, and §17 register updated.
- Binding clarification reaffirmed: `Organization → PublicProfile → Branches → future Listings/Offerings`; every future listing belongs to one organization and exposes a safe navigation path to its public storefront; all later offering types ride this spine unchanged.

**`docs/27_BACKEND_SLICE_3_PROVIDER_ORGANIZATIONS_PLAN.md`** — the owner-review specification for Provider Organizations, Public Provider Profiles, Branches, Staff Memberships, and the Provider Authorization Foundation. Documentation and technical design only; approval starts nothing by itself. Grounded in the approved canon (docs/24 §1.3 entities, §5.1/§5.2 state machines, §7.10 acceptance transaction, §10.2 role matrix, §12 mock mapping, §13 row 3; docs/23 §7 role vocabulary, §8 portal workstreams) on the closed Slice-2 foundation. Key shapes: the binding ownership spine `Organization → OrganizationPublicProfile → Branch → future Listing` with structural (separate-table) public/private separation · organizations per docs/24 with legal vs trading identity split and the §5.1 lifecycle DB-enforced · branches with composite-FK org binding and the `UNIQUE (id, organization_id)` target future listings join against · staff memberships on the canonical Himma User (no second auth system), append-only role history, last-owner guard, org-isolated branch scoping · single-use HMAC-digest invitations (B2-6B pepper-boundary pattern) through the captured MailSender · the seven approved provider roles as a typed capability registry (Slice-3-active vs reserved) · a new `provider` route-policy category with per-request PostgreSQL membership resolution (orgScope) and not-found-shaped cross-org isolation · three separated API surfaces (customer-public storefront read · provider-private management · Himma-admin lifecycle) · five-commit implementation plan (§16), each stop-for-approval. **Two recorded consistency notes:** description/media realized on the public-profile satellite (DDL-latitude, not a model change) · docs/24 §10.1's pre-Cognito "org-bound tokens" wording superseded by the later D1/A1.1 no-token-state ruling — org context binds per request. **Owner decisions raised (§17):** D-S3-1 invitation email binding (recommend verified-email match) · D-S3-2 admin-initiated org creation (recommend; self-signup waits for W2) · D-S3-3 verification tooling split (recommend state machine + admin transition API in-slice, VerificationCase to the admin workstream — narrows docs/24 §13 row 3's letter, needs explicit approval) · D-S3-4 no separate partner type (recommend `org_kind` seam) · D-S3-5 provider MFA baseline (step-up on staff management only). Nothing outside the slice was decided: commission/payout/taxonomy/membership structures remain with their owning slices.


## Route inventory (implemented)

| Route | Screen | Dock |
|---|---|---|
| `/` (`(tabs)/index`) | Home | visible |
| `/discover` | Discover feed | visible |
| `/discover/results` | Results (query/filter session) | visible |
| `/discover/categories` | All Categories | visible |
| `/discover/category/[categoryId]` | Category page | visible |
| `/discover/activity/[activityTypeId]` | Activity-type page | visible |
| `/search` (root push) | Full-screen Search | hidden structurally |
| `/map?origin=results\|discover\|category\|activity\|provider` (root push) | Schematic map | hidden structurally |
| `/program/[programId]` (root push) | Program Details | hidden structurally |
| `/provider/[providerId]` (root push) | Provider Storefront | hidden structurally |
| `/booking/[programId]` (root push, nested stack) | Booking selection step (skip rule may `<Redirect>` to participant) | hidden structurally |
| `/booking/[programId]/participant` | Booking participant step | hidden structurally |
| `/booking/[programId]/summary` | Booking summary | hidden structurally |
| `/booking/[programId]/checkout` | Checkout (HMA-021) | hidden structurally |

QA-only params: `?qa-scenario=guest|me-only|me-active|household` (Home/Discover/Booking account fixtures), `?qa-fail=1` (Discover/Results/Map/Program/Provider/Booking-step/Checkout error state), `?qa-nocount=1` (Map missing counts), `?qa-revalidate=<CheckoutIssueCode>` on the **booking flow's entry URL** (all seven codes; captured once at `BookingSessionProvider` mount because pushed routes drop search params — surfaces at checkout as the typed revalidation review state).

### Detail-route activation matrix (docs/20 §2.4, all verified in QA)

Program Details opens from: Home rails, Discover carousels, Results (All + Programs), Category page, Activity-type page, Provider Storefront program lists, and cold deep links. Provider Storefront opens from: Discover provider carousel, Search provider suggestions (direct, no search submit), Results (All + Providers), Category page, Activity-type page, Program Details provider row, and cold deep links. By design there are **no** program cards on Search/Map and **no** provider cards on Home (docs/20 §2.4 documents each).

### Booking-flow behavior (docs/21 §3, §10 — all verified in QA)

- Single entry: the Program Details Book CTA (`useBookingEntry`, 700 ms double-tap guard). The flow is a root-level nested stack; `BookingSessionProvider` mounts in its `_layout` keyed by `programId`, so the draft's lifetime equals the flow's lifetime — leaving, switching program, or re-entering always yields a fresh draft (abandoned-flow reset is structural).
- Skip rule (docs/09 §21.1): one dateless bookable option ⇒ the index route `<Redirect>`s (replace) to the participant step; back lands on Program Details in one step; the summary shows `Step 2 of 2` and no Change session action.
- Step access policies in `booking-navigation.ts` (pure, unit-tested): `participantStepAccess` and `summaryStepAccess` re-validate the draft at response time — missing option/session or full session → flow start; missing/ineligible participant or guest → participant step; unknown program → recovery state. Cold deep links with an empty draft redirect to the flow start. No silent draft repair anywhere.
- Edit actions: Change participant pops to the participant step; Change session uses `router.dismissTo` to the selection step (participant survives; option switches clear stale session ids in the reducer). The summary CTA price line is a polite live region so edit round-trips re-announce.
- The flow reads `ParticipantProvider` once for preselection and never writes it; `ResultsSessionProvider` and the Program Details instance stay mounted beneath the whole flow (exact-origin back chain Summary → Participant → Selection → Details → origin).

### Checkout behavior (docs/22 §3–§9 — all verified in QA)

- Checkout is the booking stack's fourth screen over the **re-derived** `BookingSummary` (docs/22 §2 model A): the summary's Continue-to-checkout CTA pushes it behind the 700 ms guard; back pops to the intact summary; `Edit booking` is just back (the summary owns Change participant/session); returning re-derives everything with clean checkout-local state.
- `checkoutStepAccess` = `summaryStepAccess` (shared deliberately): invalid/missing drafts, full sessions, guests, ineligible participants, and cold links all redirect to the owning step; unknown programs render the standard recovery. Checkout-local state is only `{ paymentMethodId? }` in a screen-level reducer — no provider.
- CTA readiness (`checkoutReadiness`): free → always ready; paid → the generic `Card payment` contract method must be selected and `contractOnly` — otherwise the CTA is disabled-styled with `aria-disabled`/native disabled semantics and the polite live region names `Choose a payment method to continue`. Every press path (click, Enter, Space, native, repeated) is inert through the Pressable-layer block plus the pure `ctaPressAllowed` gate.
- Revalidation: a not-ok `CheckoutValidation` replaces the content with the `CheckoutIssueCard` (no CTA bar, no payment controls); `checkoutIssueRecovery` maps all seven codes — priceChanged/offerExpired re-derive in place, sessionFull/branchUnavailable/invalidDraft return to the flow start, participantIneligible to the participant step, registrationClosed to Program Details. Deterministic mock issues are QA-param demonstrations only; `priceComparison` labels come from structured amounts (demo previous = current − AED 10; never rendered on any customer-reachable path).

### Stack behavior (docs/20 §2.2–2.3)

- Exact-origin back everywhere; root pushes leave the origin (and the Results session) mounted beneath.
- Cold deep link: back control falls back to `router.replace('/discover')`; unknown ids render recovery states with a `Browse activities` action.
- Program ↔ Provider round-trips resolve as `back` instead of stacking a third route (`detail-navigation.ts`: press-time root-state read via `useNavigationContainerRef` — **press-time, not render-time**, because react-native-screens freezes covered screens and a render-captured state goes stale; the helper also unwraps Expo's synthetic `__root` wrapper, incl. nested wrappers — all unit-tested).
- Double-tap guard (700 ms) prevents duplicate detail routes from one action; QA double-clicks to prove it.
- Storefront taxonomy chips `dismiss()` then push the category/activity route so browsing surfaces never stack on top of a root detail route (cold-link fallback: replace).

## Automated verification (Commit 19 final run, exact totals)

- TypeScript `tsc --noEmit`: clean · ESLint `--max-warnings=0`: clean · Jest: **407/407 across 23 suites** · expo-doctor: **20/20**
- Playwright QA (playwright-core + system Chrome, Expo web :8081): **808 checks, 0 failures** — home 79, discover 64, search 22, results 35, catalogue 36, map 52, details 127, booking 183, **checkout 210**. Every suite asserts zero console errors and no horizontal overflow (390 and 360). The checkout suite additionally asserts: `Booking price` continuity with no `Total`/VAT/fee/discount/`AED 0`/card-detail/acceptance copy · payment-method radiogroup semantics with explicit aria state · CTA readiness (unready `aria-disabled="true"` + native disabled semantics, named blocker, focus refusal, Enter/Space/forced-click inertness; ready enabled semantics with focus acceptance and inert presses; repeated activation swallowed) · method-selection persistence through ordinary interactions and clean state on re-entry/edit round-trips/abandonment · all revalidation states on the CheckoutIssueCard (per-state copy, structured old → new comparison, reassurance, no CTA/no payment controls, recovery navigation, re-derivation) · exact-origin back chains and cold-link/unknown/error recovery · the inert-submit contract (zero route changes, zero dialogs). A source-scan unit guard proves `PaymentSubmitRequest`/`PaymentSubmitResult` exist only as declarations in `services/contracts/checkout.ts` with no submit-like call site anywhere in `src/`.
- Note: earlier commit messages counted the QA banner line, inflating totals by one (e.g. "211" = 210 checks); this section uses exact `^PASS`-line counts.

## Automated verification (Commit 15 booking-milestone run, historical)

- TypeScript `tsc --noEmit`: clean · ESLint `--max-warnings=0`: clean · Jest: **325/325 across 19 suites** · expo-doctor: **20/20**
- Playwright QA (playwright-core + system Chrome, Expo web :8081): **597 checks, 0 failures** — home 79, discover 64, search 22, results 35, catalogue 36, map 52, details 127, **booking 182**. Every suite asserts zero console errors and no horizontal overflow (390 and 360); the booking suite additionally asserts the inert-checkout contract (no route change, zero dialogs), no reservation/Total/VAT/fee copy, and structural accessibility (radiogroup/radio semantics with explicit checked/disabled states, heading roles, summary reading order).
- Dependency note: `01b6f73 chore(deps)` re-aligned to the current SDK 57 registry (expo ~57.0.10 line; `react-native-gesture-handler` back to ~2.32.0 — the registry reversed its own Commit-11 prescription of 3.1.0; full history in docs/13). npm audit: same 11 known moderate advisories, all under the Expo-tooling `uuid` root — zero in shipped code.

## Screenshot inventory — `artifacts/checkout-review/` (38 captures, docs/22 §15 complete + extensions)

Every row at both 390 × 844 and 360 × 780 (`-390`/`-360` suffixes): `01`-single (readiness-blocker entry state) · `02`-monthly · `03`-camp · `04`-package · `05`-free (`Confirm booking`, no payment section) · `06`-free-trial · `07`-paid-trial · `08`-child (guardian context) · `09`-offer (informational line) · `10`-method-selected (= matrix row 11's "ready" frame — deliberate pairing) · `11`-readiness-blocker · `12`-revalidate-sessionfull · `13`-revalidate-pricechanged (structured old → new) · `14`-error · `15`-cold-link · `16`-sticky · extension rows `17`-revalidate-ineligible · `18`-revalidate-closed · `19`-revalidate-invalid (owner-directed CheckoutIssueCard review set). Deferred states (submitting, payment failure, saved cards) are **not captured** — deferred states are never faked for screenshots (docs/22 §15).

## Screenshot inventory — `artifacts/details-review/` (62 captures, all regenerated in Commit 11)

Every state row exists at both 390 × 844 and 360 × 780 (`-390`/`-360` suffixes) unless noted:

- **Program:** `01-program-adult-{top,mid,bottom,saved}` · `02-program-child-eligible` · `03-program-child-ineligible` · `04-program-ladies-only` · `05-program-offer-weak-availability` · `06-program-free` · `06-program-no-sessions` · `07-program-sticky-cta` (mid-scroll frame, distinct from 01) · `13-program-error` · `13-program-unknown`
- **Provider:** `08-storefront-default-{top,mid,bottom}` · `08-storefront-saved` · `09-storefront-multibranch` · `09-storefront-branch-switched` · `10-storefront-child-eligible` · `10-storefront-child-ineligible-open` · `10-storefront-child-none` · `11-storefront-weak-supply` (scrolled to the one-program list) · `12-storefront-monogram` (top frame) · `13-storefront-error` · `13-storefront-unknown`
- **Flows:** `14-storefront-from-program` (Program → Provider) · `14-program-from-storefront` (Provider → Program) · `16-results-after-program-back` (Results session preserved) · `17-discover-after-storefront-back` (search-suggestion flow return) · `18-crosslink-reuse` (Program A → Provider A → Program X → Provider A resolved by back/reuse)

Known-identical pairs (deliberate, not stale): `13-program-error` ≡ `13-storefront-error` per width — both surfaces render the same shared full-screen `ErrorStateCard`; they document two distinct states that are pixel-identical by design. One-branch (falcon) and no-offers states are demonstrated by the `08` default set (QA asserts no selector and no offers section). Earlier milestone folders (`home-review`, `discover-review`, `search-review`, `home-regression`) are unchanged.

## Screenshot inventory — `artifacts/booking-review/` (52 captures, docs/21 §17 complete)

Every matrix row 01–20 exists at both 390 × 844 and 360 × 780 (`-390`/`-360` suffixes): `01`-single-session · `02`-few-left · `03`-full-session · `04`-no-sessions · `05`-registration-closed · `06`-trial-{free,paid} · `07`-camp-weeks · `08`-participant-preselected · `09`-participant-ineligible · `10`-participant-none · `11`-participant-changed · `12`-summary-single (+ `-bottom` frames both widths) · `13`-summary-{recurring,membership} · `14`-summary-{camp,package,free,trial} · `15`-summary-after-edit · `16`-summary-sticky · `17`-error · `18`-unknown · `19`-cold-link · `20`-guest. Deliberate near-identical pairs: `12-…-bottom` ≡ `16-…-sticky` per width (one scrolled frame documents two matrix rows); the whole summary fits without scrolling at both widths, so top and bottom frames differ only where content height demands it.

## State coverage (Commit 11 audit)

**Program Details — implemented and screenshot-demonstrated:** default/adult, saved + unsaved, child eligible, child ineligible (banner + recovery chips), Ladies-only, offer/trial, free, no sessions, weak availability (`3 places left`), sticky CTA, offline/error + retry, unknown id.
**Provider Storefront — implemented and screenshot-demonstrated:** default, saved + unsaved, one branch, multi-branch + switched branch, adult (eligible-first ranking), child eligible grouping (+ expanded ineligible group), child with no eligible programs (recovery), weak supply, no offers, monogram fallback, offline/error + retry, unknown id.
**Implemented, not screenshot-demonstrated:** loading skeletons on both surfaces (300 ms mock delay — transient, exercised on every QA navigation); missing-image `AppImage` fallback (needs a forced decode failure, same standing limitation as Home); missing-instructor section collapse (data-driven conditional, covered by extras invariants, no dedicated capture).
**Contract-level only:** none — every declared docs/20 §9 state renders.
**Deferred:** favourites persistence across full reload (QA reloads reset saves — session-local by design until the account/backend milestone).

## State coverage — booking flow (Commit 15 audit, docs/21 §13)

**Implemented and QA/screenshot-demonstrated:** every §13 state — default booking per type (single, recurring, term-equivalent, camp incl. multi-week override, package, membership-style, free, free/paid trial, discounted-offer line) · preselected eligible participant · ineligible browsing participant (never preselected, explained) · no eligible participants recovery · few places left · full session (disabled + explained on both booking and details) · no sessions (both surfaces agree) · registration closed · changed participant · changed session (incl. option switch clearing stale sessions) · error + retry on all three steps · unknown program · cold deep links (all three routes) · abandoned-flow reset · guest sign-in contract.
**Implemented, not screenshot-demonstrated:** per-step loading skeletons (300 ms mock delay — transient, exercised on every QA navigation; same standing limitation as the details milestone).
**Copy audit (Commit 15):** QA asserts the rendered flow never contains reservation/hold/charged-today, `Total`, VAT/fee, auto-renewal, or package expiry/redemption wording; a source grep confirms no such customer copy exists in `src/features/booking/` or the booking service.

## Commit 15 review changes (`454ee91`, historical)

- **Accessibility (web parity):** the two disabled radio rows (full session, ineligible participant) now set explicit `aria-checked={false}` / `aria-disabled` — RN-web does not emit aria state from `accessibilityState` (the standing HANDOFF rule PressableFeedback already follows); native behavior is unchanged. Only code change in the commit.
- **QA:** `booking-review.mjs` extended with structural accessibility assertions (radiogroup roles, exactly-one-checked radio, disabled-state exposure, heading roles, summary reading order) and the three missing matrix captures (`12-…-single-bottom-360`, `13-…-membership-360`, `17-…-error-360`); header updated to the Commits 12–15 scope. Booking checks 172 → 182.
- **Docs:** docs/21 status header records the closed milestone and per-commit approvals; docs/13 records the SDK 57 registry drift and the `chore(deps)` alignment; the temporary COMMIT_14_REPORT.md session file was deleted (this HANDOFF is the permanent record).
- Earlier-milestone review notes (Commit 11 ESLint ignore, `AppImage` labels, `providerMonogram` consolidation, deterministic QA scrolling) live in git history.

No approved-screen redesigns; no new product features.

## State coverage — checkout (Commit 19 audit, docs/22 §8)

**Implemented and QA/screenshot-demonstrated:** every §8 in-scope state — paid one-off/monthly/term/camp/package/free/free-trial/paid-trial checkouts · offer informational-only · child booking with guardian context and no consent artifacts · policy displayed with no checkbox and no acceptance claim · generic `Card payment` selected / not-yet-selected with the named readiness blocker · registration-closed and ineligible entry redirects via the access policy · missing-draft/cold-link/unknown-program recovery · error/retry and the offline-equivalent deterministic failure · abandoned checkout and re-entry with clean state · duplicate-submit protection (real 700 ms mechanics, inert contract).
**Contract-only (QA-param-reachable):** all seven `CheckoutValidation` revalidation states on the CheckoutIssueCard (`?qa-revalidate` — sessionFull/priceChanged/offerExpired per the spec plus the four additional codes made review-reachable for the visual approval round).
**Deferred (never rendered, never faked):** submitting state, payment success/failure UI, Apple/Google Pay as usable methods, saved cards (docs/22 §8) — `PaymentSubmitRequest`/`PaymentSubmitResult` remain declarations only, enforced by the source-scan guard test.

## Visual-branding debt register (for the design-system engagement — do not re-polish provisional styling)

Owner decision (2026-08-05): the CheckoutIssueCard and checkout surfaces are approved as the **provisional Orbit Indigo implementation**; final typography, iconography, shadows, colors, spacing polish, and brand expression belong to the professional branding engagement (docs/23 §18.6). Register (append here instead of iterating on provisional styling):

1. CheckoutIssueCard: Ionicons glyph choices per issue code, icon-chip tile treatment, uppercase state-label letter-spacing, comparison-block `primarySoft` tint, struck-through previous-price treatment, reassurance shield glyph/`status.success` pairing.
2. Checkout screen: recap card `primarySoft` surface, price/policy/method block border treatment vs. the issue card's shadow treatment (two card languages on one screen — unify at rebrand), sticky-CTA bar shadow (`shadows.sheet`) and 132 px scroll-clearance literal, blocker-line multi-line wrap at 360.
3. Booking/checkout shared: `letterSpacing: -0.3` heading de-facto style, CTA button 52 px height literal vs. a token, `EmptyFeedCard` magnifier glyph reused for program-missing recovery states across surfaces.
4. The pre-existing 8b token-debt list below stands unchanged; all of it folds into the same brand-token pass.

## Known limitations / polish debt (recorded, deliberately NOT fixed in Commit 11)

- **Documented assumptions (details milestone):** branch selector is a wrapping chip row treated as a segmented control and assumes **max two branches per provider** (docs/20 §4.6's "sheet for more than 2" is unimplemented — no provider has >2); session lists render at most **6 upcoming occurrences** from the two-week window (scannability cap in `buildUpcomingSessions`); sessions are informational only on details (docs/09 §20.9 — the booking flow owns selection); provider name inside program cards stays non-interactive (docs/09 §20.2 — path is card → Program Details → provider row).
- **Documented assumptions (booking milestone):** the booking draft is in-memory only — reload, restart, or OS termination discards it (docs/09 §21.13); one participant per booking, contracts keep the multi-participant widening path (docs/09 §21.2); membership-style products book through the `monthly` mapping (docs/09 §21.14); the `freeTrial` price kind stays unused (trials derive from offers); at 360 width the sticky-CTA price line may ellipsize on long cadence labels (`AED 85 per …`) — the full label is in the price block and the accessible CTA label (the checkout readiness *blocker* deliberately wraps in full instead — the reason a CTA is unready is never truncated).
- **Documented assumptions (checkout milestone):** the unready checkout CTA carries the browser's native disabled semantics on web (RN-web structurally couples `aria-disabled` with the `disabled` attribute on button hosts), so it is not tab-focusable while unready — screen readers reach it in browse mode and the live region announces the blocker; `registrationClosed` recovery replaces checkout with a new Program Details instance on top of the stack (back from it lands on the summary — acceptable for this future-backend-only path, revisit if it ever becomes deterministic-reachable); the priceChanged headline is booking-generic (`Your booking price has changed`, owner decision 2026-08-05); `react-test-renderer` (shipped inside jest-expo) is used by the PressableFeedback contract test — no new dependency.
- **Token debt (8b visual audit)** stands unchanged: compact-card-title 15/20 de-facto style, two 18 px group-title variants, monogram sizes, off-scale micro-gaps, badge padding, dock literals, sheet-handle triplication, etc. The details screens add: `letterSpacing: -0.3` on section titles (both screens, consistent), price block 26/32 hero variant, CTA 96 px content-clearance literal. All fold into the brand-token pass at rebranding.
- **Copy register drift** (8b list) unchanged; details copy follows the same register. One copy pass belongs with final brand voice.
- **RN-web vs native rendering divergences (device-verify):** 8b list plus: hero/cover `AppImage` heights (264/220) under `contentFit: cover` on device; scrim-chip overlay contrast over bright photos; `shadows.sheet` on the sticky CTA bar vs Android elevation.
- **`?qa-scenario` guarded `window.location` read** (`src/state/account-context.tsx`) — unchanged; account scenarios unreachable on native until it moves to router params.
- **Search keyboard on Android / sheet translucency flags** — unchanged 8b device-verify items.
- **Assets:** demo-imagery reuse means some heroes/covers repeat across entities and don't always depict the specific program (e.g. the Ladies Aqua hero photo shows a male swimmer — flagged for pre-release imagery replacement in docs/ASSET_ATTRIBUTION.md, alongside the four residual photo trademarks and the placeholder app icon). `artifacts/` now holds ~14 MB of git-tracked QA screenshots (accepted as review evidence).
- **Unused template deps** unchanged (docs/13; no advisories).
- Sheet swipe-down-to-dismiss remains deferred — never claim it (docs/17 §10).

## Native validation backlog (device-only checks, docs/12 §10)

**2026-08-06 status:** the first full four-target audit (section above) covered the great majority of items 1–13 below on simulators/emulators. Still outstanding after the audit: physical-device passes (both platforms, incl. live VoiceOver/TalkBack runtime and real image-decode performance) · native `Share.share()` sheet exercise (not in the audit scope) · All Categories / Category / Activity-type pages (audited surfaces list didn't include them) · A4 (dev-environment LogBox debt; A1–A3 corrected 2026-08-06, see the correction-package section). The item list below is retained for the physical-device pass.

Items 1–6 from the 8b backlog stand (Dynamic Island/dock, SE fit, Dynamic Type ~135%, iOS swipe-back, Android hardware back, reduce-motion/press-feel/decode). The details milestone adds:

7. **Detail routes, iOS:** swipe-back from `/program/*` and `/provider/*` to every origin; swipe-back through a Program ↔ Provider chain honors the reuse policy; overlay back/save/share chips clear the Dynamic Island; sticky CTA sits above the home indicator (`insets.bottom` has never been non-zero in this environment); native `Share.share()` sheet for both entity kinds (web path uses Web Share/copy-link and never runs on native).
8. **Detail routes, Android:** hardware back pops exactly one detail route (no sheets exist on these screens); share intent carries the URL in `message`; dashed/shadow divergences on session rows and policy cards.
9. **Both:** hero/cover image decode performance on device; VoiceOver/TalkBack pass for the new labels (hero photo, save checkbox state, radio branch selector, expanded/collapsed ineligible group, live-region suitability updates, spoken price/age forms); Dynamic Type on the key-facts strip, session rows, and CTA bar.

The booking milestone adds:

10. **Booking flow, iOS:** swipe-back through Summary → Participant → Selection → Details; the skip-rule `<Redirect>` replace keeps one-step back to Details; sticky Continue bars sit above the home indicator on both steps; VoiceOver pass for the radio groups (session/option/participant), disabled-with-reason rows, polite live-region announcements (`Booking for Adam`, summary price after edits), and the spoken `Booking price` CTA label.
11. **Booking flow, Android:** hardware back pops exactly one step (no sheets exist in the flow); TalkBack equivalents of item 10; font-metric tolerance on session rows and the two-line CTA status text.

The checkout milestone adds:

12. **Checkout, iOS:** swipe-back from checkout to the intact summary; sticky CTA above the home indicator; VoiceOver pass for the payment-method radiogroup (explicit checked state), the disabled-CTA state with the live-region blocker announcement and its resolution on selection, the spoken CTA label (`Continue to payment, Booking price, …`), the CheckoutIssueCard reading order (state label → headline → support → price comparison with its paired spoken label → reassurance → recovery action), and guardian-context grouping on child bookings.
13. **Checkout, Android:** hardware back pops to the summary; TalkBack equivalents of item 12; Dynamic Type tolerance on the price-comparison rows, the multi-line readiness blocker, and the issue-card headline; `shadows.card` on the CheckoutIssueCard vs Android elevation.

## Architecture map (stable — see git history for details)

- Routes: `src/app/_layout.tsx` (root Stack + providers: Account → Participant, Area, Favourites, ResultsSession) → `(tabs)/_layout.tsx` (Tabs + `DockTabBar`) → Home, `discover/` stack (`unstable_settings anchor: 'index'` — keep!), root `search.tsx` + `map.tsx` + `program/[programId].tsx` + `provider/[providerId].tsx` + `booking/[programId]/` (nested Stack `_layout` mounting `BookingSessionProvider` keyed by `programId` — the draft-lifetime mechanism, don't lift it).
- Screens in `src/features/{home,discover,search,results,catalogue,map,details,booking}/`; pure navigation rules in `map-navigation.ts`, `search-navigation.ts`, `details/detail-navigation.ts`, and `booking/booking-navigation.ts` (step-access policies + double-tap-guarded entry — all unit-tested), with pure presentation helpers `booking/participant-rows.ts` and `booking/summary-presentation.ts`.
- Services: typed contracts in `services/contracts/`, deterministic mocks in `services/mock/`; screens never import raw catalogue arrays. `MockDetailsService` (300 ms delay) exposes pure `buildProgramDetailPage`/`buildProviderStorefrontPage` cores plus `buildUpcomingSessions` and `providerMonogram`. `MockBookingService` (300 ms delay) exposes pure `buildBookingOptions`/`buildBookingSummary` cores; both surfaces share one session derivation (`sessionSpots`-aware `buildUpcomingSessions`), so details and booking can never disagree about availability.
- Booking state: `src/state/booking-session-context.tsx` — `BookingDraft` + pure `draftReducer` (`selectOption` clears stale session ids, `selectSession`, `selectParticipant`, `preselectParticipant`, `reset`); in-memory only.
- Data: `src/data/mock/catalogue.ts` (**frozen: 36 programs, 11 providers — first 21 programs' order is Home-stability-critical, append only**); details extras live in separate keyed modules `program-details.ts`, `provider-details.ts`, `policies.ts`, and booking overrides in `booking-extras.ts` (structured `trialAmount`, `registrationClosed`, `sessionSpots`, `campWeeks` — sparse, keys ⊆ catalogue ids, enforced by data-invariant tests); `schedule.ts` pinned to `MOCK_TODAY` 2026-08-02 (Sunday).
- Eligibility (owner-final): children hard-check provider age ranges; adults never gender-filtered; Ladies-only is a badge/filter only. Presentation layer `participantSuitability`/`householdSuitability` in `src/utils/eligibility.ts`. Demo participants: Sarah (Me), Adam (8), Lina (12).
- Favourites: one typed store (`program:<id>` / `provider:<id>`), session-local, `src/state/favourites-context.tsx` with pure `toggledFavourites` core.
- Share: `share-entity.ts` (native `Share`) with `.web.ts` platform split (Web Share → clipboard fallback) — the only file pair allowed to touch browser APIs; URLs from `share-url.ts` (`https://himma.app/...`, never `himma://`).
- Search submit: `newSearch` → `router.dismiss()` → push/replace per `search-navigation.ts` — don't "simplify" away.
- Dock: floating capsule as Tabs custom tabBar; active pill never moves to inert Bookings/Saved/Profile.

## QA environment notes

- `playwright-core` + system Chrome (`channel:'chrome'`, headless) against Expo web on 8081 (`.claude/launch.json` → `expo-web`).
- **Locator rule:** react-native-screens keeps covered screens in the DOM — scope text/label locators with `.locator('visible=true')`; prefer `exact: true` where names nest inside card labels.
- **Scrolling rule (Commit 11):** never `mouse.wheel` (position-dependent) or `Element.scrollTo` (swallowed by RN-web) in QA scripts — use `details-review.mjs`'s `scrollToY` pattern (direct `scrollTop` assignment on the visible scroller).
- Expo dev overlay: `clearDevOverlay()` before dock clicks; shot helpers hide `.__expo_fast_refresh`.
- RN-web doesn't emit `aria-selected`/`aria-checked` from accessibilityState — `PressableFeedback` sets them explicitly; keep that.
- ESLint (react-hooks v6) forbids sync setState in effects — put setState in async callbacks/event handlers. Generated `.expo/` is lint-ignored.
- Home feed tests call the pure builder with hand-built inputs — never scenario ids.

## Owner workflow expectations

Stop and report after every commit with the exact lists the owner asks for; never start the next commit or a deferred surface without approval; report honestly (native pending, inert items, deviations); one focused commit per step with checks green; commit messages follow `feat(x):`/`fix(x):`/`chore(x):` with the Claude co-author line.
